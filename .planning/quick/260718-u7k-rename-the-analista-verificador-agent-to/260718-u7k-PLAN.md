---
phase: 260718-u7k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0018_rename_verificador.sql
  - apps/web/components/workspace/MessageBubble.tsx
  - apps/web/components/workspace/CreatorControls.tsx
autonomous: true
requirements: [QUICK-260718-u7k]
must_haves:
  truths:
    - "The analyst_default persona displays as 'Verificador' everywhere in the UI"
    - "The other persona 'Analista Científico' is unchanged"
    - "The persona id 'analyst_default' is unchanged — only the name/display strings change"
    - "New bot messages from analyst_default resolve their icon/badge correctly (no fallback)"
  artifacts:
    - path: "supabase/migrations/0018_rename_verificador.sql"
      provides: "UPDATE of personalities.name from 'Analista/Verificador' to 'Verificador'"
      contains: "UPDATE public.personalities"
    - path: "apps/web/components/workspace/MessageBubble.tsx"
      provides: "BOT_PERSONA_DISPLAY map keyed by 'Verificador'"
      contains: "'Verificador'"
    - path: "apps/web/components/workspace/CreatorControls.tsx"
      provides: "Verificador UI strings (title, aria-label, toast, caption)"
  key_links:
    - from: "supabase/migrations/0018_rename_verificador.sql"
      to: "apps/web/components/workspace/MessageBubble.tsx BOT_PERSONA_DISPLAY key"
      via: "personalities.name must exactly match the map key 'Verificador'"
      pattern: "'Verificador'"
---

<objective>
Rename the `analyst_default` bot persona's display name from "Analista/Verificador" to "Verificador" so it is visually distinct from the unrelated "Analista Científico" persona.

Purpose: Two analyst-type personas are hard to tell apart / tag in the UI. Shortening this one to "Verificador" disambiguates them.
Output: A new Supabase migration updating the DB `name`, plus updated hardcoded UI strings in two React components. The persona **id** `analyst_default` stays unchanged.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly, no exploration needed. -->

From apps/web/components/workspace/MessageBubble.tsx (lines 72-87):
The BOT_PERSONA_DISPLAY map is keyed by `message.display_name` (which is populated
from personalities.name for bot messages). Current entry to change:
```
'Analista/Verificador': { icon: SearchCheck, badgeLabel: 'Verificador', authorName: 'Analista/Verificador' },
```
getPersonaDisplay() falls through to a generic Bot fallback if the key does not
match display_name exactly — so the map key MUST equal the new DB name 'Verificador'.

Last existing migration: supabase/migrations/0017_system_message_role.sql
Next sequential migration number: 0018.

The stale/duplicate `phase2jgm/` directory at repo root is NOT active app code and
is EXCLUDED from this plan — do not edit anything under it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migration renaming personalities.name to 'Verificador'</name>
  <files>supabase/migrations/0018_rename_verificador.sql</files>
  <action>Create a NEW migration file (do NOT edit the already-applied 0014_personalities.sql). It must run an idempotent UPDATE setting `public.personalities.name = 'Verificador'` WHERE `id = 'analyst_default'`. Only the `name` column changes — the `id` stays `analyst_default`. Add a short header comment explaining the rename disambiguates this persona from 'Analista Científico'. Do NOT touch the `analista_cientifico` row.</action>
  <verify>
    <automated>grep -q "UPDATE public.personalities" supabase/migrations/0018_rename_verificador.sql &amp;&amp; grep -q "analyst_default" supabase/migrations/0018_rename_verificador.sql &amp;&amp; grep -q "'Verificador'" supabase/migrations/0018_rename_verificador.sql</automated>
  </verify>
  <done>Migration 0018 exists and updates only the analyst_default row's name to 'Verificador'; id unchanged; 0014 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update UI display strings in MessageBubble and CreatorControls</name>
  <files>apps/web/components/workspace/MessageBubble.tsx, apps/web/components/workspace/CreatorControls.tsx</files>
  <action>
In MessageBubble.tsx (line ~75): change the BOT_PERSONA_DISPLAY map entry key from `'Analista/Verificador'` to `'Verificador'` AND change its `authorName` value from `'Analista/Verificador'` to `'Verificador'`. Leave `badgeLabel: 'Verificador'` and `icon: SearchCheck` as-is. The key MUST match the new DB name exactly or persona icon/badge resolution silently falls through to the generic Bot fallback. Do NOT touch the `'Analista Científico'` or `Facilitador` entries.

In CreatorControls.tsx: replace every hardcoded "Analista/Verificador" display string with "Verificador": the toast error message (~line 374), the card title div (~line 500), both branches of the toggle aria-label ternary (~line 510), and the cooldown caption template string (~line 524). Also update the code comment referencing the old name (~line 482) for consistency.

Do NOT change any persona **id** (`analyst_default`), the `@analista` mention regex in workspace.tsx, or anything under `phase2jgm/`.
  </action>
  <verify>
    <automated>! grep -rn "Analista/Verificador" apps/web/components/workspace/MessageBubble.tsx apps/web/components/workspace/CreatorControls.tsx &amp;&amp; grep -q "'Verificador': { icon: SearchCheck" apps/web/components/workspace/MessageBubble.tsx</automated>
  </verify>
  <done>No occurrence of "Analista/Verificador" remains in either component; MessageBubble map key and authorName are 'Verificador'; analyst_default id and unrelated personas untouched.</done>
</task>

</tasks>

<verification>
- `grep -rn "Analista/Verificador" apps/web supabase/migrations/0018_rename_verificador.sql` returns nothing (excluding the historical 0014 migration and phase2jgm/).
- `grep -rn "analyst_default" apps/api/src/lib/trigger-engine.test.ts` still present and unchanged (persona id preserved).
- `grep -rn "Analista Científico\|analista_cientifico" apps/web` unchanged (unrelated persona untouched).
- Apps typecheck/build passes for the two edited components (no dangling references).
</verification>

<success_criteria>
- New migration 0018 renames only the analyst_default persona's `name` to 'Verificador'; id unchanged; 0014 not edited.
- MessageBubble BOT_PERSONA_DISPLAY is keyed by 'Verificador' with authorName 'Verificador'; badge/icon resolve without fallback for new messages.
- CreatorControls shows "Verificador" in title, aria-labels, toast, and caption.
- No changes to persona ids, the @analista regex, the Analista Científico persona, or phase2jgm/.
</success_criteria>

<output>
Create `.planning/quick/260718-u7k-rename-the-analista-verificador-agent-to/260718-u7k-SUMMARY.md` when done
</output>
