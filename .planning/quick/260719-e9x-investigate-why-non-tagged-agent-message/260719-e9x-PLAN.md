---
phase: quick-260719-e9x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260719-e9x-investigate-why-non-tagged-agent-message/260719-e9x-ROOT-CAUSE.md
autonomous: false
requirements: [DIAG-E9X]

must_haves:
  truths:
    - "The exact code path that gates whether an untagged human message reaches the graph is identified and cited (file:line)"
    - "The reason a 'decided not to respond' turn produces no Langfuse trace is confirmed against the real code, not assumed"
    - "The user is given a decision with concrete options + cost tradeoffs before any behavior-changing code is written"
  artifacts:
    - path: .planning/quick/260719-e9x-investigate-why-non-tagged-agent-message/260719-e9x-ROOT-CAUSE.md
      provides: "Confirmed root-cause writeup with file:line citations for all three paths and recommended options"
      min_lines: 40
  key_links:
    - from: apps/web/app/(protected)/sessions/[id]/workspace.tsx
      to: apps/api/src/routes/ai.ts
      via: "openAIStream → POST /invoke, gated by ANALISTA_PATTERN.test(content)"
      pattern: "ANALISTA_PATTERN"
---

<objective>
Confirm, against the real code, why typing a message WITHOUT tagging `@analista` produces zero Langfuse activity, then present the user with a decision (with cost tradeoffs) before changing any behavior.

Purpose: The user expects that even a "the agent evaluated my message and chose to stay silent" turn should leave a Langfuse entry. The likely reason is architectural (untagged messages never reach the graph at all), which is a product/cost decision on the creator's BYOK key — NOT a clear low-risk fix. Per the task constraints, this plan confirms the cause first and stops at findings + a recommended approach rather than attempting a risky refactor.

Output: `260719-e9x-ROOT-CAUSE.md` in the quick task directory, plus a decision checkpoint that lets the user choose the direction (accept as-is / add zero-LLM observability on the proactive silent path / larger reactive-evaluation change).
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Preliminary trace already performed during planning. The executor MUST re-open each
     cited location and confirm the line still matches before writing findings — do not
     copy these citations blind. -->

REACTIVE PATH (human message → graph):
- apps/web/app/(protected)/sessions/[id]/workspace.tsx:60
    const ANALISTA_PATTERN = /@analista/i
- apps/web/app/(protected)/sessions/[id]/workspace.tsx:264-273  (handleAfterSend)
    Only calls openAIStream(...) — which POSTs to /invoke — when ANALISTA_PATTERN.test(content)
    is true. Untagged messages fall through to a plain refreshMessages(); the graph is never
    invoked, so no CallbackHandler is ever constructed for that message.
- apps/api/src/routes/ai.ts:65 (POST /:id/invoke), :378 (per-request CallbackHandler),
    :411 (callbacks: [callbackHandler]) — the /invoke route ALWAYS builds a Langfuse handler
    and runs the graph. There is no "agent decides to stay silent" branch on the reactive path;
    once /invoke runs, the orchestrator → agent path responds (unless DOMAIN_DRIFT+ignored).

PROACTIVE PATH (timer scan → facilitation, the ONLY thing the user currently sees):
- apps/api/src/lib/trigger-engine.ts — timer-based silence scan (SCAN_INTERVAL_MS), NOT a
    per-message evaluator. scanBranch() early-returns BEFORE constructing the CallbackHandler
    when: gate not passed (:271 `if (!gateResult.passed) return`), cooldown active
    (:275-278), or arbitration winner !== 'coach' (:282). The per-request CallbackHandler is
    only built at :311-317 and passed to graph.invoke at :317/:342 — i.e. a trace exists ONLY
    when the Coach actually fires. Every "evaluated, chose silence" proactive tick returns with
    no trace.

GRAPH (for completeness — routeFromStart):
- apps/api/src/graph/graph.ts:113-135 — triggerType null ⇒ 'orchestrator' (human path always
    proceeds to a response); 'silence_gate' ⇒ 'facilitation'. No silent-evaluation node exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm root cause in code and write ROOT-CAUSE.md</name>
  <files>.planning/quick/260719-e9x-investigate-why-non-tagged-agent-message/260719-e9x-ROOT-CAUSE.md</files>
  <action>
  Re-open each location cited in the &lt;interfaces&gt; block above and confirm the line numbers/logic still match (they were traced during planning but MUST be verified, not trusted — the constraint forbids assuming). Specifically confirm:
  1. Reactive gate: workspace.tsx handleAfterSend only calls openAIStream (→ POST /invoke) when ANALISTA_PATTERN matches; untagged content only triggers refreshMessages. Grep for any OTHER caller of openAIStream / the /invoke endpoint (e.g. reactions via use-reactions.ts triggersAI) so the writeup is complete about EVERY way the graph gets invoked reactively.
  2. Reactive response is unconditional: ai.ts /invoke always constructs the CallbackHandler (:378) and runs the graph; there is no "agent evaluated and stayed silent" branch — confirm via routeFromStart (graph.ts) that triggerType null routes to orchestrator and proceeds.
  3. Proactive silence path: trigger-engine.ts scanBranch early-returns (gate-not-passed / cooldown / not-winner) BEFORE the CallbackHandler is constructed, so silent evaluations leave no trace; only an actual Coach fire produces the facilitation trace the user sees.
  Then write 260719-e9x-ROOT-CAUSE.md containing: (a) the user's report restated; (b) the three-path model (reactive-tagged, reactive-untagged, proactive-timer) each with confirmed file:line citations; (c) the core finding — untagged messages are NOT evaluated per-message by any agent on any path, so there is nothing to trace, and the user's mental model ("agent weighed my message and chose silence") does not match the current architecture; (d) why this is NOT a trivial low-risk fix — any per-untagged-message evaluation spends the creator's BYOK key (CLAUDE.md budget constraint: creator's API key is the cost surface) and/or adds trace volume, making it a product/cost decision; (e) a "Recommended Options" section enumerating the options presented in Task 2 with their cost/effort tradeoffs. Do NOT modify any application code in this task.
  </action>
  <verify>
    <automated>test -f ".planning/quick/260719-e9x-investigate-why-non-tagged-agent-message/260719-e9x-ROOT-CAUSE.md" &amp;&amp; grep -q "workspace.tsx" ".planning/quick/260719-e9x-investigate-why-non-tagged-agent-message/260719-e9x-ROOT-CAUSE.md" &amp;&amp; grep -q "trigger-engine.ts" ".planning/quick/260719-e9x-investigate-why-non-tagged-agent-message/260719-e9x-ROOT-CAUSE.md"</automated>
  </verify>
  <done>ROOT-CAUSE.md exists, cites all three paths with file:line, states the confirmed cause, and lists recommended options with cost tradeoffs. No application code changed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>How to address "untagged messages produce no Langfuse activity" now that the root cause is confirmed?</decision>
  <context>
  Confirmed cause: untagged human messages never reach the graph (frontend gates /invoke on the @analista regex), and the proactive timer-scan only produces a trace when the Coach actually fires — "evaluated, chose silence" ticks return before any Langfuse handler is built. There is no per-message agent evaluation for untagged messages anywhere, so today there is literally nothing to trace. Any change that creates such an entry is a product/cost decision on the creator's BYOK key (CLAUDE.md: "creator's API key is the cost surface"), which is why the executor is stopping here for your call rather than auto-implementing.
  </context>
  <options>
    <option id="accept">
      <name>Accept current behavior — document only</name>
      <pros>Zero cost, zero risk. ROOT-CAUSE.md already gives you the full explanation. Matches the intended design (agents are quiet by default; @tag or proactive triggers are the only invocation paths).</pros>
      <cons>Untagged messages still show no Langfuse activity — the visibility gap the user noticed remains.</cons>
    </option>
    <option id="proactive-trace">
      <name>Low-risk observability: trace proactive "chose silence" decisions (no extra LLM call)</name>
      <pros>Gives visibility that the system evaluated a branch and declined to speak (cooldown / not-winner / gate-not-passed), via a lightweight Langfuse event/span emitted in trigger-engine.ts scanBranch before the early returns. No new LLM spend on the BYOK key. Contained to one file.</pros>
      <cons>Still not tied to a specific untagged message — it's the timer scan, not per-message. Adds Langfuse trace/event volume. Does not fully match the user's literal "entry per message I type" expectation.</cons>
    </option>
    <option id="reactive-eval">
      <name>Larger change: evaluate every untagged message and trace the silent decision</name>
      <pros>Directly matches the user's expectation — every typed message leaves a Langfuse entry showing it was considered.</pros>
      <cons>Architectural + cost-heavy: requires invoking an evaluation (LLM or a new cheap classifier) on EVERY untagged message on the creator's BYOK key, plus a new "stay silent" decision node/path on the reactive graph. Out of scope for a quick task; would need its own planned phase and an explicit cost budget decision.</cons>
    </option>
  </options>
  <resume-signal>Select: accept, proactive-trace, or reactive-eval (reactive-eval will be handed back for a dedicated planned phase rather than implemented here).</resume-signal>
</task>

</tasks>

<verification>
- ROOT-CAUSE.md confirms the cause against real code (file:line) for all three invocation paths.
- No application code is modified in this plan; the only write is the findings doc.
- The user has an explicit decision with cost tradeoffs before any behavior change proceeds.
</verification>

<success_criteria>
- The "why no Langfuse for untagged messages" question is answered with confirmed citations, not the planning-brief guesswork.
- The user has chosen a direction (accept / proactive-trace / reactive-eval).
- If `proactive-trace` is chosen, a follow-up quick task/plan implements the zero-LLM observability span; if `reactive-eval`, it is escalated to a dedicated planned phase; if `accept`, the task closes with the findings doc as the deliverable.
</success_criteria>

<output>
Create `.planning/quick/260719-e9x-investigate-why-non-tagged-agent-message/quick-260719-e9x-01-SUMMARY.md` when done.
</output>
