---
phase: quick-260703-t00
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(protected)/sessions/[id]/workspace.tsx
  - apps/api/src/routes/ai.ts
autonomous: true
requirements:
  - WSL-02
  - OBS-03
must_haves:
  truths:
    - "User's own message appears in chat immediately after sending (no Realtime dependency)"
    - "Langfuse trace ID is logged to console after every successful graph run"
    - "Missing trace ID (null) logs a distinct warning so silent callback failures are visible"
  artifacts:
    - path: apps/web/app/(protected)/sessions/[id]/workspace.tsx
      provides: refreshMessages() helper called from both onMessagesRefresh and handleAfterSend
    - path: apps/api/src/routes/ai.ts
      provides: console.log of callbackHandler.last_trace_id after flushLangfuse()
  key_links:
    - from: handleAfterSend
      to: refreshMessages
      via: "direct call with 300ms delay"
    - from: onMessagesRefresh
      to: refreshMessages
      via: "direct call (no delay — SSE done already signals completion)"
---

<objective>
Two targeted fixes for WSL2 dev environment:

1. workspace.tsx — extract the messages re-fetch into a shared `refreshMessages()` function and
   call it from `handleAfterSend` (with a 300ms delay to let the POST commit) so the user's
   own message appears without Supabase Realtime.

2. ai.ts — after `await flushLangfuse()`, log `callbackHandler.last_trace_id` so we can
   distinguish "trace created but not exported" from "callbacks never fired".

Purpose: Close the WSL2 chat gap (user sees their own message immediately) and add the
diagnostic signal needed to diagnose the silent Langfuse export failure.

Output: Two modified files, no new files.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jgm/dev/projects/web-projects/panelito/.planning/STATE.md
@/home/jgm/dev/projects/web-projects/panelito/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract refreshMessages and call it from handleAfterSend in workspace.tsx</name>
  <files>apps/web/app/(protected)/sessions/[id]/workspace.tsx</files>
  <action>
    Extract the messages re-fetch logic that currently lives inside the `onMessagesRefresh` callback
    into a named local function `refreshMessages` declared inside the `Workspace` component body,
    above the `useAIStream` call.

    The function signature and body (no parameters, no return value):

      const refreshMessages = () => {
        apiFetch<Message[]>(
          `/api/sessions/${liveSession.id}/messages?branchId=${activeBranchId}`
        )
          .then((msgs) => useSessionStore.getState().setMessages(msgs))
          .catch(() => {})  // non-fatal — Realtime may have already delivered
      }

    Then pass `refreshMessages` as the `onMessagesRefresh` callback to `useAIStream` (replacing
    the inline arrow function that was there).

    Then update `handleAfterSend` so it ALWAYS schedules a refresh regardless of whether
    `@analista` was mentioned. Keep the existing `openAIStream` call for the `@analista` branch.
    Add a 300ms delay before the refresh so the POST to `/api/sessions/:id/messages` has time
    to commit before the re-fetch runs:

      const handleAfterSend = (content: string) => {
        if (ANALISTA_PATTERN.test(content)) {
          openAIStream(content, false, activeBranchId).catch((err) => {
            console.error('[Workspace] openAIStream failed:', err)
          })
        }
        // WSL-02: always refresh after send — closes Supabase Realtime gap in WSL2.
        // 300ms delay ensures the message POST has committed before re-fetch.
        setTimeout(refreshMessages, 300)
      }

    The comment block above `handleAfterSend` (lines 195-200) should be updated to mention the
    WSL-02 refresh. Keep all other code unchanged. No other modifications to this file.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    TypeScript reports no errors in workspace.tsx. `refreshMessages` is defined once and called
    from both `onMessagesRefresh` and `handleAfterSend`. `handleAfterSend` always calls
    `setTimeout(refreshMessages, 300)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Log Langfuse trace ID after flush in ai.ts</name>
  <files>apps/api/src/routes/ai.ts</files>
  <action>
    In `apps/api/src/routes/ai.ts`, locate the block after `await flushLangfuse()` (currently
    around line 448). Immediately after that line, add two diagnostic log statements:

      // OBS-03 (temporary diagnostic): log trace ID to confirm whether Langfuse callbacks fired.
      // If trace_id appears in console but NOT in Langfuse dashboard, the export is failing.
      // If trace_id is null/undefined, the CallbackHandler callbacks never fired.
      if (callbackHandler.last_trace_id) {
        console.log('[ai] Langfuse trace flushed:', callbackHandler.last_trace_id)
      } else {
        console.warn('[ai] Langfuse trace ID is null after flush — callbacks may not have fired')
      }

    Insert these lines directly after `await flushLangfuse()` and before the `phase_signal`
    emission block that follows. No other modifications to this file.

    The property name is `last_trace_id` as documented — it is set by `CallbackHandler` after
    each LangChain run completes. No type assertion needed; it is typed as `string | undefined`
    on the CallbackHandler class.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    TypeScript reports no errors in ai.ts. The file contains a `console.log('[ai] Langfuse trace
    flushed:'` line and a `console.warn('[ai] Langfuse trace ID is null'` line immediately after
    `await flushLangfuse()`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/sessions/:id/messages | GET re-fetch triggered client-side after send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t00-01 | Information | console.log of trace_id | accept | trace_id is an internal Langfuse identifier with no PII or secrets; server-side log only |
| T-t00-SC | Tampering | npm/pip/cargo installs | accept | No new packages added in this plan |
</threat_model>

<verification>
1. Send a message WITHOUT @analista — message appears in chat within ~300ms without any Supabase Realtime event.
2. Send a message WITH @analista — message appears immediately (300ms refresh), AI stream opens, AI response appears after stream completes (via onMessagesRefresh → refreshMessages).
3. After an /invoke call completes, check API server console — either `[ai] Langfuse trace flushed: <uuid>` or `[ai] Langfuse trace ID is null after flush` appears.
</verification>

<success_criteria>
- workspace.tsx compiles cleanly; `refreshMessages` extracted and called from both paths.
- ai.ts compiles cleanly; trace ID log appears after every flushLangfuse() call in the happy path.
- No regressions: existing @analista flow and onMessagesRefresh behavior unchanged.
</success_criteria>

<output>
Create `.planning/quick/260703-t00-wsl-chat-also-refresh-messages-after-use/260703-t00-SUMMARY.md` when done.
</output>
