---
phase: quick
plan: 260703-la6
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/lib/model-config.ts
  - apps/api/src/graph/nodes/orchestrator.ts
  - apps/api/src/graph/nodes/agent.ts
  - apps/api/src/graph/nodes/drift-reply.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "No graph node file contains a hardcoded model string literal"
    - "All LLM calls in graph nodes use TASK_MODELS[providerName] for their model string"
    - "TASK_MODELS covers all three task types: analysis, compression, categorization, and classification"
    - "Switching provider from anthropic to openai requires no changes to graph node files"
  artifacts:
    - path: apps/api/src/lib/model-config.ts
      provides: "Central TASK_MODELS registry with per-provider defaults for every task type"
      contains: "classification"
    - path: apps/api/src/graph/nodes/orchestrator.ts
      provides: "Domain guardrail classifier using TASK_MODELS[providerName].classification"
    - path: apps/api/src/graph/nodes/agent.ts
      provides: "Canvas mutation agent using TASK_MODELS[providerName].analysis"
    - path: apps/api/src/graph/nodes/drift-reply.ts
      provides: "Drift reply node using TASK_MODELS[providerName].analysis"
  key_links:
    - from: "apps/api/src/graph/nodes/*.ts"
      to: "apps/api/src/lib/model-config.ts"
      via: "TASK_MODELS[providerName] import"
      pattern: "TASK_MODELS\\[providerName\\]"
---

<objective>
Replace every hardcoded model string in the LangGraph nodes with provider-aware lookups from the central TASK_MODELS registry.

Purpose: The three graph nodes (orchestrator, agent, drift-reply) hard-code 'claude-sonnet-4-6'. When the session uses an OpenAI or Gemini API key, these nodes still attempt to call a Claude model — which fails. TASK_MODELS already exists and is the correct central place; the nodes simply need to consume it.

Output: All graph node model strings sourced from TASK_MODELS; a new 'classification' task type added for the orchestrator's lightweight classifier call.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jgm/dev/projects/web-projects/panelito/.planning/PROJECT.md
@/home/jgm/dev/projects/web-projects/panelito/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'classification' task type to TASK_MODELS</name>
  <files>apps/api/src/lib/model-config.ts</files>
  <action>
    The orchestrator node runs a lightweight single-token classification call — semantically distinct from 'categorization' (which is labeler/tag work). Add 'classification' as a fourth TaskType to make the distinction explicit.

    Update model-config.ts:
    - Add 'classification' to the TaskType union: `export type TaskType = 'analysis' | 'compression' | 'categorization' | 'classification'`
    - Add a 'classification' entry to each provider block in TASK_MODELS:
      - anthropic.classification: 'claude-haiku-4-5-20251001'  (lightweight, single token response)
      - openai.classification: 'gpt-5.4-mini'
      - gemini.classification: 'gemini-2.5-flash'
    - Update the file-level JSDoc comment to mention the new task type and that 'classification' is for low-token binary/enum classifier calls (like the orchestrator's DOMAIN_MATCH/BRIDGE/DRIFT decision).

    Do NOT rename or remove 'categorization' — it is used by labeler.ts and is a different call shape (returns a label from a set of tags).
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && grep -c "'classification'" apps/api/src/lib/model-config.ts</automated>
  </verify>
  <done>TaskType union includes 'classification'; all three providers have a 'classification' entry in TASK_MODELS; TypeScript compiles without error (npx tsc --noEmit from apps/api passes).</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded model strings in graph nodes with TASK_MODELS lookups</name>
  <files>
    apps/api/src/graph/nodes/orchestrator.ts,
    apps/api/src/graph/nodes/agent.ts,
    apps/api/src/graph/nodes/drift-reply.ts
  </files>
  <action>
    Each node already receives `providerName` from `config.configurable.providerName` as `ProviderName | undefined`. Add a TASK_MODELS import to each file and replace the hardcoded string.

    orchestrator.ts:
    - Add import: `import { TASK_MODELS } from '../../lib/model-config'`
    - In the adapter.stream() call (around line 106): replace `model: 'claude-sonnet-4-6'` with `model: TASK_MODELS[providerName ?? 'anthropic'].classification`
    - The fallback `?? 'anthropic'` is correct: if providerName is missing the adapter creation guard above already returns DOMAIN_BRIDGE, but TypeScript needs a valid key for the index access.

    agent.ts:
    - Add import: `import { TASK_MODELS } from '../../lib/model-config'`
    - In the adapter.stream() call (around line 141): replace `model: 'claude-sonnet-4-6'` with `model: TASK_MODELS[providerName ?? 'anthropic'].analysis`

    drift-reply.ts:
    - Add import: `import { TASK_MODELS } from '../../lib/model-config'`
    - In the adapter.stream() call (around line 53): replace `model: 'claude-sonnet-4-6'` with `model: TASK_MODELS[providerName ?? 'anthropic'].analysis`

    NOTE: anthropic.ts verifyApiKey hardcodes 'claude-haiku-4-5-20251001' — leave it alone. That function uses the Anthropic SDK directly (not the provider-agnostic adapter) and is only ever called for Anthropic key verification. This is legitimately provider-specific and not a bug.

    After editing each file, confirm no remaining `'claude-sonnet-4-6'` literal exists in the three files.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && grep -rn "claude-sonnet-4-6" apps/api/src/graph/nodes/ | wc -l</automated>
  </verify>
  <done>
    - grep returns 0 for 'claude-sonnet-4-6' across all three node files
    - All three files import TASK_MODELS and use TASK_MODELS[providerName ?? 'anthropic'].{classification|analysis}
    - TypeScript compiles: cd apps/api && npx tsc --noEmit exits 0
    - Existing graph tests pass: cd apps/api && npx vitest run src/graph
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.configurable → node | providerName is caller-supplied; a bad value would index into TASK_MODELS with an unknown key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-la6-01 | Tampering | TASK_MODELS[providerName] index | mitigate | `?? 'anthropic'` fallback prevents undefined index; ProviderName union + Record<ProviderName, ...> ensures all valid values are covered at compile time |
</threat_model>

<verification>
1. `grep -rn "claude-sonnet-4-6" apps/api/src/graph/nodes/` returns no matches
2. `grep -c "TASK_MODELS" apps/api/src/graph/nodes/orchestrator.ts` returns >= 1
3. `grep -c "TASK_MODELS" apps/api/src/graph/nodes/agent.ts` returns >= 1
4. `grep -c "TASK_MODELS" apps/api/src/graph/nodes/drift-reply.ts` returns >= 1
5. `cd apps/api && npx tsc --noEmit` exits 0
6. `cd apps/api && npx vitest run src/graph` all tests pass
</verification>

<success_criteria>
Switching a session's API key provider from anthropic to openai causes the graph nodes to automatically use gpt-5.4 for analysis calls and gpt-5.4-mini for classification calls — no code changes required. The TASK_MODELS registry in model-config.ts is the single authoritative source for all per-provider model defaults.
</success_criteria>

<output>
Create `.planning/quick/260703-la6-we-cannot-use-claude-sonnet-if-our-provi/260703-la6-SUMMARY.md` when done.
</output>
