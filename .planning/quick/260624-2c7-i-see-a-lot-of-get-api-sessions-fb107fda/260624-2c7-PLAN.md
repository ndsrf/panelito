---
phase: quick-260624-2c7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/workspace/MessageList.tsx
autonomous: true
requirements:
  - QUICK-260624-2c7
must_haves:
  truths:
    - "No GET /api/sessions/{id}/messages requests fire on a 2-second interval during an active session"
    - "New messages still arrive in real time via Supabase Realtime broadcast"
    - "Initial message history still loads on mount and on branch switch"
  artifacts:
    - path: "apps/web/components/workspace/MessageList.tsx"
      provides: "MessageList without polling useEffect"
      contains: "useSessionChannel"
  key_links:
    - from: "apps/web/components/workspace/MessageList.tsx"
      to: "apps/web/hooks/use-session-channel.ts"
      via: "useSessionChannel call (line 155)"
      pattern: "useSessionChannel"
---

<objective>
Remove the unconditional polling fallback from MessageList — a setInterval that calls GET /api/sessions/{id}/messages every 2 seconds regardless of Supabase Realtime health.

Purpose: Supabase Realtime (useSessionChannel) already delivers new_message broadcast events in real time. The polling loop runs in parallel, generating redundant HTTP requests on every active session at 2s cadence — in both dev and production. Removing it cuts unnecessary load with zero user-visible impact.

Output: MessageList.tsx with the "Polling fallback" useEffect block (lines 183-200) deleted.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jgm/dev/projects/web-projects/panelito/.planning/STATE.md

The file to edit lives in the monorepo worktree:
apps/web/components/workspace/MessageList.tsx (path relative to /home/jgm/dev/projects/web-projects/panelito-sessions-list)

Absolute path: /home/jgm/dev/projects/web-projects/panelito-sessions-list/apps/web/components/workspace/MessageList.tsx

The block to remove (lines 183-200 as of plan creation):

  // Polling fallback
  useEffect(() => {
    const poll = async () => {
      try {
        const msgs = await apiFetch<Message[]>(`/api/sessions/${sessionId}/messages`)
        const knownIds = new Set(useSessionStore.getState().messages.map((m) => m.id))
        const newMsgs = msgs.filter((m) => !knownIds.has(m.id))
        if (newMsgs.length > 0) {
          newMsgs.forEach(useSessionStore.getState().addMessage)
        }
      } catch {
        // ignore
      }
    }
    const id = setInterval(poll, 2000)
    return () => clearInterval(id)
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [sessionId])

What remains after removal (still in the file):
- useSessionChannel call on line 155 — Supabase Realtime subscription (keep)
- Initial load useEffect (lines 157-170) — one-time fetch on mount/sessionId change (keep)
- Panel sync useEffect (lines 172-181) — syncs canvas_snapshot_state to panel store (keep)
- Auto-scroll useEffect and ResizeObserver below line 202 (keep)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the polling fallback useEffect from MessageList</name>
  <files>/home/jgm/dev/projects/web-projects/panelito-sessions-list/apps/web/components/workspace/MessageList.tsx</files>
  <action>
    Delete the entire "Polling fallback" useEffect block from MessageList.tsx. This is the block starting with the comment `// Polling fallback` and ending with the closing `}, [sessionId])` for that effect — 18 lines total (approximately lines 183-200).

    Do NOT add any replacement logic. Do NOT add a condition-based fallback. Simply remove the block. The Supabase Realtime subscription via useSessionChannel already handles ongoing message delivery. The initial load useEffect above it handles the first fetch on mount.

    After deletion, confirm the next useEffect after the removed block is the CHAT-03 auto-scroll effect (the one calling scrollToBottom), with no blank lines gap larger than one line.
  </action>
  <verify>
    <automated>grep -n "setInterval" /home/jgm/dev/projects/web-projects/panelito-sessions-list/apps/web/components/workspace/MessageList.tsx | grep -v "cancelAnimationFrame" ; echo "Exit: $?" </automated>
  </verify>
  <done>
    The grep returns no matches for setInterval in MessageList.tsx. The file compiles without TypeScript errors. The useSessionChannel call and initial load useEffect remain intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation passes</name>
  <files>/home/jgm/dev/projects/web-projects/panelito-sessions-list/apps/web/components/workspace/MessageList.tsx</files>
  <action>
    Run tsc --noEmit scoped to the web app to confirm the file is valid after the deletion. If any import becomes unused as a result of the deletion (e.g., apiFetch is no longer imported if it was only used by the polling block — check the remaining useEffects first), remove that unused import. Do not remove apiFetch if it is still used by the initial load useEffect on line 159.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito-sessions-list && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    tsc --noEmit exits with no errors referencing MessageList.tsx. No unused import warnings for apiFetch (it remains in use by the initial load fetch).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | This change removes code; introduces no new data flows or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2c7-01 | Denial of Service | GET /api/sessions/{id}/messages | mitigate (by removal) | Removing the 2s polling loop eliminates the self-inflicted DoS against the messages endpoint |
</threat_model>

<verification>
After execution:
1. Open a session in the browser with DevTools Network tab open
2. Confirm no repeating GET /api/sessions/{id}/messages requests fire every 2 seconds
3. Send a message — confirm it appears via Realtime without a page refresh
4. Reload the page — confirm initial message history loads correctly
</verification>

<success_criteria>
- setInterval is absent from MessageList.tsx
- tsc --noEmit passes with zero errors in the web app
- No GET /api/sessions/{id}/messages polling visible in browser network tab during an active session
</success_criteria>

<output>
Create `.planning/quick/260624-2c7-i-see-a-lot-of-get-api-sessions-fb107fda/260624-2c7-SUMMARY.md` when done.
</output>
