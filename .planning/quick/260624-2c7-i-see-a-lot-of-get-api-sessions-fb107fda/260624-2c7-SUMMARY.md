---
phase: quick-260624-2c7
plan: "01"
subsystem: frontend/workspace
tags: [polling, realtime, performance, cleanup]
dependency_graph:
  requires: []
  provides: [no-polling-in-MessageList]
  affects: [apps/web/components/workspace/MessageList.tsx]
tech_stack:
  added: []
  patterns: [supabase-realtime-only, no-polling-fallback]
key_files:
  created: []
  modified:
    - apps/web/components/workspace/MessageList.tsx
decisions:
  - Removed the 2s polling fallback entirely with no replacement — Supabase Realtime via useSessionChannel is the sole live-update mechanism
metrics:
  duration: "~2 minutes"
  completed: "2026-06-24T00:01:32Z"
---

# Phase quick-260624-2c7 Plan 01: Remove Polling Fallback Summary

**One-liner:** Deleted the `setInterval`-based 2s polling loop from MessageList, leaving Supabase Realtime as the sole live message delivery mechanism.

## What Was Done

Removed the 18-line "Polling fallback" `useEffect` block from `MessageList.tsx` that was unconditionally firing `GET /api/sessions/{id}/messages` every 2 seconds during every active session. The block was redundant because `useSessionChannel` already delivers `new_message` broadcast events in real time via Supabase Realtime.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Delete the polling fallback useEffect from MessageList | 47b212d | apps/web/components/workspace/MessageList.tsx |
| 2 | Verify TypeScript compilation passes | (no code change) | — |

## Verification Results

- `grep -n "setInterval" MessageList.tsx` returns zero matches
- `tsc --noEmit -p apps/web/tsconfig.json` exits with zero errors (verified via panelito-sessions-list installed node_modules)
- `apiFetch` import retained — still used by initial load `useEffect` (line 159)
- `useSessionChannel` call on line 155 intact
- Initial load `useEffect` on line 157 intact
- Panel sync `useEffect` on lines 172-187 intact
- CHAT-03 auto-scroll `useEffect` follows immediately after the removed block

## Deviations from Plan

None — plan executed exactly as written.

Note: The same fix commit (`436749d`) already existed in the `panelito-sessions-list` worktree's git history (that worktree is ahead of the plan base `f0780c6`). This agent worktree was reset to `f0780c6` and applied the equivalent change, producing commit `47b212d`.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. This change removes a periodic HTTP caller — it reduces the attack surface by eliminating 2s-cadence self-inflicted load against the messages endpoint (T-2c7-01 mitigated by removal, as per threat model).

## Self-Check: PASSED

- [x] `apps/web/components/workspace/MessageList.tsx` modified — verified no `setInterval`
- [x] Commit `47b212d` exists: `git log --oneline | grep 47b212d` confirms
- [x] `tsc --noEmit` passes with zero errors (panelito-sessions-list environment)
