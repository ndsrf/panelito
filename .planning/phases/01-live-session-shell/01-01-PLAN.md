---
phase: 01-live-session-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .gitignore
  - .nvmrc
  - .env.example
  - README.md
  - tsconfig.base.json
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/postcss.config.mjs
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/components.json
  - apps/web/lib/utils.ts
  - apps/web/lib/supabase/client.ts
  - apps/web/lib/supabase/server.ts
  - apps/web/middleware.ts
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/index.ts
  - apps/api/src/lib/supabase.ts
  - apps/api/src/lib/env.ts
  - packages/types/package.json
  - packages/types/tsconfig.json
  - packages/types/src/index.ts
  - packages/types/src/session.ts
  - packages/types/src/message.ts
  - packages/types/src/branch.ts
  - packages/types/src/api-key.ts
  - supabase/config.toml
  - supabase/migrations/0001_initial_schema.sql
autonomous: false
requirements:
  - CHAT-04
must_haves:
  truths:
    - "D-10: Turborepo monorepo established with apps/web (Next.js 15), apps/api (Hono), packages/types (shared TypeScript types/schemas)"
    - "D-11: Shared types package @panelito/types is the single source of truth for Message, Session, Branch, ApiKey types — imported by both apps/web and apps/api via workspace:* dependency"
    - "D-12: Supabase local CLI initialized via supabase init; supabase start spins up local Postgres + Realtime + Auth via Docker; migrations tracked in /supabase/migrations/"
    - "Running `pnpm install` at repo root installs all workspace deps without error"
    - "Running `pnpm dev` starts Next.js (port 3000), Hono (port 8787), and Supabase local stack"
    - "Visiting http://localhost:3000 returns a 200 with the placeholder home page"
    - "Visiting http://localhost:8787/health returns 200 JSON `{ok:true}`"
    - "Shared types from @panelito/types are imported successfully by both apps/web and apps/api"
    - "Supabase migration 0001 creates sessions, messages, creator_settings tables with RLS"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace globs for apps/* and packages/*"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Turborepo pipeline (dev, build, lint, typecheck, test)"
      contains: "pipeline"
    - path: "apps/web/package.json"
      provides: "Next.js 15.5.19 web app with pinned deps"
      contains: "next"
    - path: "apps/api/package.json"
      provides: "Hono 4.12.x API app with pinned deps"
      contains: "hono"
    - path: "packages/types/src/index.ts"
      provides: "Barrel export of all shared types"
      exports: ["Session", "Message", "Branch", "ApiKeyVerifyRequest"]
    - path: "supabase/migrations/0001_initial_schema.sql"
      provides: "sessions + messages + creator_settings tables with RLS"
      contains: "create table public.sessions"
    - path: "apps/web/lib/supabase/client.ts"
      provides: "Browser-side Supabase client factory"
      exports: ["createClient"]
    - path: "apps/web/lib/supabase/server.ts"
      provides: "Server-side Supabase client factory (SSR cookies)"
      exports: ["createServerClient"]
    - path: "apps/api/src/lib/supabase.ts"
      provides: "Hono-side Supabase service-role client"
      exports: ["createServiceClient"]
  key_links:
    - from: "apps/web/package.json"
      to: "packages/types"
      via: "workspace:* dependency"
      pattern: "@panelito/types.*workspace"
    - from: "apps/api/package.json"
      to: "packages/types"
      via: "workspace:* dependency"
      pattern: "@panelito/types.*workspace"
---

<objective>
Establish the Walking Skeleton scaffold: Turborepo monorepo with `apps/web` (Next.js 15 App Router + Tailwind v4 + shadcn/ui), `apps/api` (Hono on Node), `packages/types` (shared TS types), Supabase local dev stack with the initial database schema and RLS policies. After this plan, `pnpm dev` brings up the full local stack and both apps respond to health probes. No user-visible features yet — this plan exists because every later slice depends on this foundation existing.

Purpose: Lock the architectural contract from SKELETON.md into code. Every subsequent plan (auth, sessions, chat, BYOK) imports `@panelito/types`, talks to the Supabase local stack on the standard ports, and follows the directory layout established here.

Output: A monorepo that boots end-to-end on `pnpm dev` with three tables (sessions, messages, creator_settings) live in local Postgres, RLS active, and shared types consumable by both apps.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-live-session-shell/01-CONTEXT.md
@.planning/phases/01-live-session-shell/01-RESEARCH.md
@.planning/phases/01-live-session-shell/01-UI-SPEC.md
@.planning/phases/01-live-session-shell/01-PATTERNS.md
@.planning/phases/01-live-session-shell/SKELETON.md
@CLAUDE.md

<interfaces>
<!-- These are the type contracts every downstream plan will import from @panelito/types. -->
<!-- This plan CREATES them; later plans CONSUME them. -->

Target exports from `packages/types/src/index.ts`:

- `Session` — { id: string; creator_id: string; short_code: string; title: string | null; mode: 'strategy' | 'debate' | 'red_team' | null; status: 'active' | 'frozen' | 'closed'; ai_response_count: number; ai_response_cap: number; created_at: string; updated_at: string }
- `SessionCreateInput` — { title?: string | null; mode?: 'strategy' | 'debate' | 'red_team' | null }
- `Message` — { id: string; session_id: string; author_id: string; display_name: string; parent_id: string | null; path_id: string; content: string; canvas_snapshot_state: unknown | null; created_at: string }
- `MessageInsertInput` — { session_id: string; parent_id?: string | null; path_id?: string; content: string }
- `Branch` — { id: string; label: 'Main'; color_hex: '#6366f1'; path_id: string } (Phase 1 placeholder — only Main exists)
- `ApiKeyVerifyRequest` — { key: string }
- `ApiKeyVerifyResponse` — { success: true } | { success: false; error: 'invalid_key' | 'network_error' | 'rate_limited' }
- `CreatorSettings` — { user_id: string; has_api_key: boolean; api_response_cap: number; updated_at: string } (note: `encrypted_api_key` is server-only, never returned to client)

Each type also has a Zod schema co-located in the same file (e.g. `SessionSchema`, `MessageSchema`) for runtime validation in Hono routes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Turborepo monorepo and shared types package</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, .gitignore, .nvmrc, .env.example, README.md, tsconfig.base.json, packages/types/package.json, packages/types/tsconfig.json, packages/types/src/index.ts, packages/types/src/session.ts, packages/types/src/message.ts, packages/types/src/branch.ts, packages/types/src/api-key.ts</files>
  <read_first>
    - .planning/phases/01-live-session-shell/01-CONTEXT.md (D-10, D-11 — monorepo + shared types decisions)
    - .planning/phases/01-live-session-shell/01-RESEARCH.md (Standard Stack — exact package versions; Architectural Responsibility Map)
    - .planning/phases/01-live-session-shell/SKELETON.md (Directory layout decision)
    - .planning/REQUIREMENTS.md (CHAT-04 — message schema fields are load-bearing)
    - CLAUDE.md (stack table, "What NOT to Use" — no Prisma, no tRPC)
  </read_first>
  <action>
    Initialize a pnpm workspace at repo root with `turbo@2.9.16` as the orchestrator (per D-10). Create `pnpm-workspace.yaml` with `packages: ["apps/*", "packages/*"]`. Create `turbo.json` with a `pipeline` defining `dev` (cache: false, persistent: true), `build`, `lint`, `typecheck`, `test`. Create root `package.json` with `name: panelito`, `private: true`, scripts: `dev` (turbo run dev), `build`, `lint`, `typecheck`, `test`, `format`. Pin Node via `.nvmrc` to `22`. `.gitignore` covers `node_modules`, `.next`, `.turbo`, `dist`, `.env.local`, `.env`, `supabase/.branches`, `supabase/.temp`. `.env.example` documents every env var any app uses: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `KEY_ENCRYPTION_SECRET` (32-byte hex), `API_PORT=8787`, `NEXT_PUBLIC_API_URL=http://localhost:8787`. `tsconfig.base.json` defines shared compiler options (`target: ES2022`, `module: ESNext`, `moduleResolution: bundler`, `strict: true`, `noUncheckedIndexedAccess: true`, `paths` left to each app). README.md gives the one-liner setup: `pnpm install`, `supabase start`, `pnpm dev`, plus the three URLs (web 3000, API 8787, Supabase Studio 54323).

    Create `packages/types` with `name: "@panelito/types"`, `version: "0.0.0"`, `private: true`, `main: ./src/index.ts`, `types: ./src/index.ts`. Dev-dep on `zod@4.4.3` and `typescript@5.6.x`. `tsconfig.json` extends `tsconfig.base.json`. Write the type files exactly as specified in the `<interfaces>` block above:
    - `session.ts` exports `Session`, `SessionCreateInput`, `SessionSchema`, `SessionCreateInputSchema`, `SessionStatus = 'active' | 'frozen' | 'closed'`, `SessionMode = 'strategy' | 'debate' | 'red_team'`. The `short_code` field is a 6-character base32 string (validated by Zod regex `/^[A-HJ-NP-Z2-9]{6}$/` — Crockford alphabet to avoid look-alikes).
    - `message.ts` exports `Message`, `MessageInsertInput`, `MessageSchema`, `MessageInsertInputSchema`. `canvas_snapshot_state` is typed `unknown | null` here; full schema lives in Phase 2. `path_id` defaults to `"main"` in Phase 1 (the Phase 3 branch engine replaces this).
    - `branch.ts` exports `Branch`, `BranchSchema`, plus a constant `MAIN_BRANCH: Branch = { id: 'main', label: 'Main', color_hex: '#6366f1', path_id: 'main' }` so the Phase 1 Branch Navigator can render without any DB read.
    - `api-key.ts` exports `ApiKeyVerifyRequest`, `ApiKeyVerifyResponse`, `CreatorSettings`, plus the Zod schemas. The `ApiKeyVerifyRequestSchema` enforces `key.startsWith('sk-ant-')` and `key.length >= 50` as a cheap client-side guard before the server handshake.
    - `index.ts` re-exports everything from the four files (barrel export).

    Do not install Prisma, tRPC, Redux, Socket.io, or D3 (CLAUDE.md "What NOT to Use"). Do not write any executable code yet — types only.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito &amp;&amp; pnpm install &amp;&amp; pnpm --filter @panelito/types exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` exists and contains `apps/*` and `packages/*`
    - `turbo.json` exists and contains a `pipeline` key with `dev`, `build`, `lint`, `typecheck`, `test` entries
    - Root `package.json` has `private: true` and `pnpm` as packageManager (e.g. `"packageManager": "pnpm@9.x"`)
    - `pnpm install` exits 0
    - `pnpm --filter @panelito/types exec tsc --noEmit` exits 0
    - `packages/types/src/index.ts` exports `Session`, `Message`, `Branch`, `MAIN_BRANCH`, `ApiKeyVerifyRequest`, `ApiKeyVerifyResponse`, `CreatorSettings`, `SessionSchema`, `MessageSchema` (verifiable: `grep -E 'export (type |\{|const )' packages/types/src/index.ts` lists each name)
    - `packages/types/src/message.ts` references all five required message fields: `session_id`, `author_id`, `parent_id`, `path_id`, `canvas_snapshot_state` (CHAT-04)
    - `.env.example` contains `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `KEY_ENCRYPTION_SECRET`, `API_PORT`, `NEXT_PUBLIC_API_URL`
    - `.gitignore` lists `node_modules`, `.next`, `.turbo`, `.env.local`
    - No occurrence of `prisma`, `trpc`, `redux`, `socket.io`, `d3` in any `package.json` (verifiable: `grep -RE '"(prisma|@trpc|redux|socket\.io|d3)"' --include=package.json .` returns no matches)
  </acceptance_criteria>
  <done>The monorepo skeleton installs cleanly, shared types compile, and all required env vars are documented in `.env.example`.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold apps/web (Next.js 15 + Tailwind v4 + shadcn) and apps/api (Hono)</name>
  <files>apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/postcss.config.mjs, apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/app/globals.css, apps/web/components.json, apps/web/lib/utils.ts, apps/web/lib/supabase/client.ts, apps/web/lib/supabase/server.ts, apps/web/middleware.ts, apps/api/package.json, apps/api/tsconfig.json, apps/api/src/index.ts, apps/api/src/lib/supabase.ts, apps/api/src/lib/env.ts</files>
  <read_first>
    - .planning/phases/01-live-session-shell/01-RESEARCH.md (Standard Stack, exact package versions; "Don't Hand-Roll" — use `@supabase/ssr`, not custom JWT; pin `next@15`; install `@tailwindcss/postcss` BEFORE `npx shadcn@latest init`)
    - .planning/phases/01-live-session-shell/01-PATTERNS.md (Supabase client import paths; `cn()` and `getAvatarColor()` helpers in `apps/web/lib/utils.ts`)
    - .planning/phases/01-live-session-shell/01-UI-SPEC.md (shadcn init — New York style, Zinc base, CSS variables yes; install button input card dialog sheet badge avatar separator skeleton toast)
    - packages/types/src/index.ts (workspace dep wiring)
    - .planning/phases/01-live-session-shell/SKELETON.md (mobile layout decision — forbidden `100vh`/`dvh`/`svh`, inline `--app-height` script in `<head>`)
    - CLAUDE.md (Hono streaming chosen over Express; Anthropic SDK pinned)
  </read_first>
  <action>
    Scaffold `apps/web` as Next.js 15 App Router with the exact pinned deps from RESEARCH.md: `next@15.5.19`, `react@19.x`, `react-dom@19.x`, `@supabase/supabase-js@2.108.0`, `@supabase/ssr@0.10.3`, `zustand@5.0.14`, `framer-motion@12.40.0`, `react-hook-form@7.78.0`, `zod@4.4.3`, `qrcode.react@4.2.0`, `recharts@3.8.1`, `lucide-react@1.17.0`, `class-variance-authority@0.7.1`, `clsx@2.1.1`, `tailwind-merge@3.6.0`, `@panelito/types: workspace:*`. Dev-deps: `tailwindcss@4.3.0`, `@tailwindcss/postcss@4.3.0`, `typescript@5.6.x`, `eslint`, `eslint-config-next`, `@types/node`, `@types/react`. `apps/web/package.json` scripts: `dev: next dev -p 3000`, `build: next build`, `start: next start -p 3000`, `lint: next lint`, `typecheck: tsc --noEmit`.

    `next.config.ts` enables `experimental: { typedRoutes: true }` and adds `transpilePackages: ['@panelito/types']`. `postcss.config.mjs` exports `{ plugins: { '@tailwindcss/postcss': {} } }` (Tailwind v4 form — install this BEFORE running shadcn init per the RESEARCH.md pitfall). `app/globals.css` imports `@import "tailwindcss";` and defines the `@theme` block with the Zinc dark palette from UI-SPEC.md (CSS vars `--background: #09090b`, `--card: #18181b`, `--muted: #27272a`, `--primary: #6366f1`, `--accent: #312e81`, `--foreground: #fafafa`, `--muted-foreground: #a1a1aa`, `--destructive: #ef4444`, `--border: #3f3f46`).

    Initialize shadcn (`npx shadcn@latest init` — New York style, Zinc base, CSS variables yes) producing `apps/web/components.json`. Install the 10 components from UI-SPEC.md: `npx shadcn@latest add button input card dialog sheet badge avatar separator skeleton toast`. Create `apps/web/lib/utils.ts` exporting `cn(...inputs: ClassValue[])` (clsx + tailwind-merge) and `getAvatarColor(authorId: string): string` returning one of `['#818cf8', '#34d399', '#fbbf24', '#fb7185', '#38bdf8', '#a78bfa']` (CHAT-02 deterministic hash — use a stable string hash like FNV-1a, not `Math.random()`).

    Create `app/layout.tsx` with: Inter via `next/font/google`, `<html lang="es" className="dark">`, an inline `<script>` in `<head>` that runs BEFORE first paint and sets `document.documentElement.style.setProperty('--app-height', window.innerHeight + 'px')` (pitfall: setting only in `useEffect` causes a flash on mobile). Body wraps `{children}` in a div with `className="bg-background text-foreground"`. `app/page.tsx` renders a minimal centered placeholder ("Panelito" wordmark + "v0.1.0 — Phase 1: Live Session Shell"). Use `<Toaster />` from shadcn at the layout root for later toast notifications.

    Create `apps/web/lib/supabase/client.ts` exporting `createClient()` from `@supabase/ssr` `createBrowserClient` using `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY`. Create `apps/web/lib/supabase/server.ts` exporting `createServerClient()` that wraps `@supabase/ssr` `createServerClient` with Next.js `cookies()` adapter (per the canonical Supabase SSR docs pattern — see `@supabase/ssr` README; do not hand-roll cookie parsing). Create `apps/web/middleware.ts` that calls the server-client cookie refresh on every request to keep the OAuth session alive across page reloads (this is the Next.js + Supabase SSR canonical pattern; Plan 02 will tighten the matcher to protected routes).

    Scaffold `apps/api` as a Hono server with pinned deps: `hono@4.12.24`, `@hono/node-server@2.0.4`, `@anthropic-ai/sdk@0.102.0`, `zod@4.4.3`, `@supabase/supabase-js@2.108.0`, `@panelito/types: workspace:*`. Dev-deps: `tsx@4.x`, `typescript@5.6.x`, `@types/node`. Scripts: `dev: tsx watch src/index.ts`, `build: tsc`, `start: node dist/index.js`, `typecheck: tsc --noEmit`. `tsconfig.json` extends `tsconfig.base.json`, `outDir: dist`. `apps/api/src/lib/env.ts` validates `process.env` with Zod and exports `env: { SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, KEY_ENCRYPTION_SECRET, API_PORT, ALLOWED_ORIGINS }` — throw on missing required vars at startup (fail loud). `apps/api/src/lib/supabase.ts` exports `createServiceClient()` using `@supabase/supabase-js` with the service role key (server-only client; bypasses RLS — never expose to browser). `apps/api/src/index.ts` creates a Hono app with: CORS middleware (origin `ALLOWED_ORIGINS` from env, comma-split; defaults `http://localhost:3000`), `GET /health → c.json({ ok: true, ts: new Date().toISOString() })`, and starts via `@hono/node-server` `serve({ fetch: app.fetch, port: env.API_PORT })`. Log the bound port on startup.

    Update root `turbo.json` `pipeline.dev` to `dependsOn: []`, `cache: false`, `persistent: true` so `pnpm dev` runs both apps in parallel. Update root `package.json` `scripts.dev` to `turbo run dev`.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito &amp;&amp; pnpm install &amp;&amp; pnpm --filter @panelito/web exec tsc --noEmit &amp;&amp; pnpm --filter @panelito/api exec tsc --noEmit &amp;&amp; pnpm --filter @panelito/web exec next build &amp;&amp; pnpm --filter @panelito/api exec tsc</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` pins `next` to `15.5.19` exactly (verifiable: `grep '"next":' apps/web/package.json` contains `15.5.19`)
    - `apps/web/package.json` declares `@panelito/types: workspace:*`
    - `apps/web/components.json` exists with `style: "new-york"` and `baseColor: "zinc"`
    - The 10 shadcn components exist as files: `apps/web/components/ui/{button,input,card,dialog,sheet,badge,avatar,separator,skeleton,toast}.tsx`
    - `apps/web/app/globals.css` defines `--background`, `--card`, `--muted`, `--primary`, `--accent`, `--foreground`, `--muted-foreground`, `--destructive`, `--border` (verifiable: `grep -c -- '--background:\|--card:\|--muted:\|--primary:\|--accent:\|--foreground:\|--muted-foreground:\|--destructive:\|--border:' apps/web/app/globals.css` returns at least 9)
    - `apps/web/app/layout.tsx` contains an inline `<script>` setting `--app-height` from `window.innerHeight`
    - `apps/web/app/layout.tsx` does NOT contain the substrings `100vh`, `100dvh`, `100svh` (forbidden per SKELETON.md)
    - `apps/web/lib/utils.ts` exports `cn` and `getAvatarColor` (verifiable: `grep -E 'export (function|const) (cn|getAvatarColor)' apps/web/lib/utils.ts` returns 2 lines)
    - `apps/web/lib/supabase/client.ts` imports from `@supabase/ssr`
    - `apps/web/middleware.ts` exists and refreshes the Supabase session
    - `apps/api/package.json` pins `hono@4.12.24` exactly
    - `apps/api/src/index.ts` registers `GET /health` (verifiable: `grep -E "app\.(get|GET).*['\"]\/health['\"]" apps/api/src/index.ts` returns at least one line)
    - `apps/api/src/lib/env.ts` validates env with Zod and throws on missing required vars
    - `pnpm --filter @panelito/web exec tsc --noEmit` exits 0
    - `pnpm --filter @panelito/api exec tsc --noEmit` exits 0
    - `pnpm --filter @panelito/web exec next build` exits 0
    - No occurrence of the string `dvh` or `svh` in `apps/web/app/globals.css`
  </acceptance_criteria>
  <done>Both apps build cleanly. `apps/web` serves the placeholder home page with the dark theme tokens applied. `apps/api` boots on port 8787 and serves a JSON health probe. All shared types are importable from both apps via `@panelito/types`.</done>
</task>

<task type="auto">
  <name>Task 3: Supabase local CLI + initial migration (sessions, messages, creator_settings) with RLS</name>
  <files>supabase/config.toml, supabase/migrations/0001_initial_schema.sql</files>
  <read_first>
    - .planning/phases/01-live-session-shell/01-CONTEXT.md (D-12 Supabase local CLI; D-11 message schema source of truth in REQUIREMENTS.md)
    - .planning/phases/01-live-session-shell/01-RESEARCH.md (Code Examples — DDL for sessions/messages/creator_settings; RLS rules)
    - .planning/REQUIREMENTS.md (CHAT-04, CHAT-05 — every message stores parent_id/path_id/session_id/author_id/content/canvas_snapshot_state; CHAT-05 immutability => no UPDATE/DELETE)
    - packages/types/src/session.ts, packages/types/src/message.ts (column names and types must match types exactly)
  </read_first>
  <action>
    Run `supabase init` to produce `supabase/config.toml` (D-12). Pin the local stack ports to defaults (54321 API, 54322 DB, 54323 Studio, 54324 inbucket); enable `realtime` and `auth` in `config.toml`. Configure `[auth] enable_signup = true`, `site_url = "http://localhost:3000"`, `additional_redirect_urls = ["http://localhost:3000/auth/callback"]`. In `[auth.external.google]` set `enabled = true` and reference env vars `GOTRUE_EXTERNAL_GOOGLE_CLIENT_ID` + `GOTRUE_EXTERNAL_GOOGLE_SECRET` (the developer pastes their real OAuth client into `.env.local`; Task 4 checkpoint covers this).

    Author `supabase/migrations/0001_initial_schema.sql` creating these tables with exact columns matching `packages/types`:

    `public.sessions` — `id uuid primary key default gen_random_uuid()`, `creator_id uuid not null references auth.users(id) on delete cascade`, `short_code text not null unique check (short_code ~ '^[A-HJ-NP-Z2-9]{6}$')`, `title text`, `mode text check (mode in ('strategy','debate','red_team'))`, `status text not null default 'active' check (status in ('active','frozen','closed'))`, `ai_response_count int not null default 0`, `ai_response_cap int not null default 150`, `created_at timestamptz not null default now()`, `updated_at timestamptz not null default now()`. Add a trigger to bump `updated_at` on UPDATE.

    `public.messages` — `id uuid primary key default gen_random_uuid()`, `session_id uuid not null references public.sessions(id) on delete cascade`, `author_id uuid not null` (no FK to auth.users because guests use anonymous tokens with a distinct UUID), `display_name text not null`, `parent_id uuid references public.messages(id) on delete set null`, `path_id text not null default 'main'`, `content text not null check (length(content) between 1 and 4000)`, `canvas_snapshot_state jsonb`, `created_at timestamptz not null default now()`. Index on `(session_id, created_at)` for chat history fetch, and `(session_id, path_id, created_at)` for Phase 3 branch reads.

    `public.creator_settings` — `user_id uuid primary key references auth.users(id) on delete cascade`, `encrypted_api_key text` (nullable until BYOK onboarding), `api_response_cap int not null default 150 check (api_response_cap > 0 and api_response_cap <= 10000)`, `updated_at timestamptz not null default now()`. Add the same `updated_at` trigger.

    Enable RLS on all three tables: `alter table public.sessions enable row level security;` (same for the others). Policies:
    - `sessions`: SELECT — anyone authenticated (including anon guests) where `status in ('active','frozen','closed')` (D-03 read-only access for closed sessions). INSERT — only `auth.uid() = creator_id`. UPDATE — only `auth.uid() = creator_id`. DELETE — denied (no policy).
    - `messages`: SELECT — anyone whose JWT can see the parent session (subquery on `sessions.id`). INSERT — only when `(select status from public.sessions where id = session_id) = 'active'` AND `auth.uid() = author_id` (CHAT-05 immutability is enforced by the absence of UPDATE/DELETE policies). UPDATE/DELETE — denied.
    - `creator_settings`: SELECT/INSERT/UPDATE — only `auth.uid() = user_id`. DELETE — denied. The `encrypted_api_key` column is NEVER returned by client queries — the Hono API (service role) is the only consumer. Plan 06 adds a SQL `column privileges` grant that revokes `encrypted_api_key` from the `authenticated` role to make this enforceable in the database layer (not just in queries).

    Add a Postgres function `public.generate_short_code() returns text` that produces a 6-char Crockford base32 string for the `sessions.short_code` default (called in Plan 04's session insert; defined here so the schema is self-contained).

    Run `supabase db reset` to apply locally. Document in README.md the new commands: `supabase start` / `supabase db reset` / `supabase status` for fetching the local anon key + service role key.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito &amp;&amp; supabase start &amp;&amp; supabase db reset --local &amp;&amp; supabase db diff --schema public 2>&amp;1 | tee /tmp/db-diff.txt &amp;&amp; psql "$(supabase status -o json | jq -r '.DB_URL')" -c "select tablename from pg_tables where schemaname='public' order by tablename" -t</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/config.toml` exists and `site_url` equals `http://localhost:3000`
    - `supabase/migrations/0001_initial_schema.sql` contains `create table public.sessions`, `create table public.messages`, `create table public.creator_settings` (verifiable: `grep -c 'create table public\.\(sessions\|messages\|creator_settings\)' supabase/migrations/0001_initial_schema.sql` returns 3)
    - The migration enables RLS on all three tables (verifiable: `grep -c 'enable row level security' supabase/migrations/0001_initial_schema.sql` returns at least 3)
    - The migration has NO `for update` or `for delete` policy on the `messages` table (CHAT-05 immutability — verifiable: `grep -E 'on public\.messages for (update|delete)' supabase/migrations/0001_initial_schema.sql` returns nothing)
    - The migration defines the five required message columns: `session_id`, `author_id`, `parent_id`, `path_id`, `canvas_snapshot_state` (CHAT-04 — verifiable: each column name appears in the messages table definition)
    - `messages.content` has a `check (length(content) between 1 and 4000)` constraint
    - `sessions.short_code` has a unique constraint
    - `sessions.ai_response_cap` defaults to `150` (D-06 / SESS-12)
    - After `supabase db reset --local`, the three tables exist in the local DB (verifiable via the psql command above)
    - `supabase db diff --schema public` reports no pending changes after the reset (clean migration)
  </acceptance_criteria>
  <done>The local Supabase stack boots, the migration applies idempotently, all three tables exist with RLS enabled, message immutability is enforced at the policy layer, and the short-code generator function is callable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — full local stack boots and developer supplies Supabase env</name>
  <files>n/a (human verification — no files modified by the checkpoint itself)</files>
  <read_first>
    - The acceptance criteria of the prior tasks in this plan (the checkpoint verifies they integrate correctly)
  </read_first>
  <action>

    Plan 01 wired the monorepo, both apps, and the Supabase local stack with the initial schema. This checkpoint is the only step Claude cannot fully automate: the developer must (a) install pnpm and the Supabase CLI on their machine if not already installed, (b) populate `.env.local` files in each app with values from `supabase status`, (c) create a Google OAuth client in Google Cloud Console and paste the client ID/secret into Supabase local config, and (d) confirm `pnpm dev` brings up the full stack with all three URLs responding.

    Why this checkpoint is human-required (not automatable):
    - Installing system-level CLI tools (pnpm, supabase) on the developer's machine
    - Creating a Google OAuth Client in Google Cloud Console (no public CLI for this; per the canonical Google Cloud documentation, OAuth client creation is an interactive console operation)
    - Pasting secret env values into local files
  
  </action>
  <how-to-verify>

    Prerequisites (one-time):
    1. Install `pnpm@9.x` (https://pnpm.io/installation) if `pnpm --version` is not 9+.
    2. Install the Supabase CLI (https://supabase.com/docs/guides/local-development) if `supabase --version` is not present.
    3. Ensure Docker Desktop / Docker Engine is running (Supabase local stack needs it).

    Boot the stack:
    4. Run `pnpm install` from repo root — should exit 0.
    5. Run `supabase start` — wait for "Started supabase local development setup". Capture the printed `anon key`, `service_role key`, `API URL`, `DB URL`, `Studio URL`.
    6. Create `apps/web/.env.local` containing:
       - `NEXT_PUBLIC_SUPABASE_URL=<API URL from step 5>`
       - `NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key from step 5>`
       - `NEXT_PUBLIC_API_URL=http://localhost:8787`
    7. Create `apps/api/.env` containing:
       - `SUPABASE_URL=<API URL from step 5>`
       - `SUPABASE_SERVICE_ROLE_KEY=<service_role key from step 5>`
       - `KEY_ENCRYPTION_SECRET=<run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">` (32-byte hex; required for Plan 06 BYOK encryption)
       - `API_PORT=8787`
       - `ALLOWED_ORIGINS=http://localhost:3000`
    8. Google OAuth setup (REQUIRED for Plan 02 — but you can do it now and never think about it again):
       - Visit https://console.cloud.google.com/apis/credentials
       - Create OAuth 2.0 Client ID, application type "Web application"
       - Authorized JavaScript origins: `http://localhost:3000`, `http://127.0.0.1:54321`
       - Authorized redirect URIs: `http://127.0.0.1:54321/auth/v1/callback`
       - Copy Client ID + Client Secret into root `.env` (or shell) as `GOTRUE_EXTERNAL_GOOGLE_CLIENT_ID` and `GOTRUE_EXTERNAL_GOOGLE_SECRET`; restart `supabase start` so it picks up the env.

    Run and verify:
    9. Run `pnpm dev` from repo root.
    10. Open http://localhost:3000 — should render the "Panelito v0.1.0 — Phase 1" placeholder on a dark background.
    11. Open http://localhost:8787/health in a browser or `curl http://localhost:8787/health` — should return `{"ok":true,"ts":"..."}`.
    12. Open http://localhost:54323 (Supabase Studio) — confirm tables `sessions`, `messages`, `creator_settings` are visible under the `public` schema with RLS enabled (lock icon next to table name).

    Confirm or report issues.
  
  </how-to-verify>
  <acceptance_criteria>
    - All steps in <how-to-verify> succeed
    - Developer types 'approved' as the resume signal, OR pastes specific failure notes for Claude to address
  </acceptance_criteria>
  <verify>
    <human-check>Developer manually walks through <how-to-verify> and types 'approved' on success or pastes failure notes</human-check>
  </verify>
  <resume-signal>Type "approved" once steps 4-12 all succeed. If any step fails, paste the error output and Claude will fix the underlying issue and ask for re-verification.</resume-signal>
  <done>Developer confirms all manual verification steps passed and resumes the workflow with 'approved'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Hono API | Untrusted client input crosses here; CORS enforced, all bodies validated with Zod, no service-role key ever leaves the API process |
| browser → Supabase | Direct from client; RLS policies are the only authorization layer; the anon key is public-by-design |
| Hono API → Anthropic | Server-only; API key is decrypted from `creator_settings.encrypted_api_key` and used in-memory; never logged |
| developer env → repo | `.env.local`, `.env` are gitignored; only `.env.example` is committed; no real secrets in git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `creator_settings.encrypted_api_key` exposed via Supabase client | mitigate | RLS plus column-level revoke from `authenticated` role (Plan 06 finalizes the revoke; this plan leaves the column out of any client-facing select pattern). Service-role client is the only reader. |
| T-01-02 | Tampering | `messages` table mutability (UPDATE/DELETE bypass) | mitigate | RLS policies define INSERT/SELECT only; no UPDATE/DELETE policies exist, so PostgREST denies by default (CHAT-05). |
| T-01-03 | Elevation of Privilege | Unauthorized session mutation (guest freezes a session) | mitigate | RLS UPDATE policy requires `auth.uid() = creator_id`; service-role mutations in Hono explicitly check `creator_id === current user`. |
| T-01-04 | Tampering | npm package supply chain (Next, Hono, Supabase SDK, Anthropic SDK, etc.) | mitigate | All packages flagged "Approved" in the RESEARCH.md Package Legitimacy Audit (verified against npm registry + official GitHub repos). Versions pinned exactly; `pnpm-lock.yaml` committed to lock transitive deps. |
| T-01-05 | Spoofing | Guest user ID forgery (one guest claims another's `author_id`) | mitigate | Plan 04 issues anonymous Supabase tokens whose `auth.uid()` is the guest's actual identity; RLS INSERT policy requires `auth.uid() = author_id`, so a guest cannot impersonate someone else's ID. |
| T-01-06 | Repudiation | Missing audit trail for who created which session | accept | `sessions.creator_id` + `created_at` are persisted; richer audit logging is post-MVP. |
| T-01-07 | Denial of Service | Message flood from a single guest | mitigate | `messages.content` length-capped at 4000 chars; rate-limit middleware added in Plan 04. SESS-12 AI cap mitigates AI cost runaway. |
| T-01-08 | Information Disclosure | Service role key leakage from API process | mitigate | `apps/api/src/lib/env.ts` validates at startup; the key is read once into memory; never logged; never returned in any response body. `.env` is gitignored. |
| T-01-SC | Tampering | npm install of `next`, `hono`, `@supabase/ssr`, `@anthropic-ai/sdk`, `qrcode.react`, etc. | mitigate | Package Legitimacy Audit in RESEARCH.md ran the gate: all packages Approved (verified vs registry + official repos). No `[ASSUMED]` or `[SUS]` flags. `pnpm-lock.yaml` pins resolved versions. |
</threat_model>

<verification>
- `pnpm install` clean install exits 0
- `pnpm --filter @panelito/types exec tsc --noEmit` exits 0
- `pnpm --filter @panelito/web exec tsc --noEmit` exits 0
- `pnpm --filter @panelito/api exec tsc --noEmit` exits 0
- `pnpm --filter @panelito/web exec next build` exits 0
- `supabase db reset --local` applies the migration without errors
- `pnpm dev` starts both apps and they bind to ports 3000 + 8787
- `curl http://localhost:8787/health` returns `{"ok":true,...}`
- `curl -s http://localhost:3000` returns HTML containing "Panelito"
</verification>

<success_criteria>
After this plan: the developer runs `pnpm dev` once, the full local stack (Next.js + Hono + Supabase Postgres + Realtime + Auth + Studio) comes up, and `localhost:3000` + `localhost:8787/health` + `localhost:54323` all respond. The database has `sessions`, `messages`, `creator_settings` with RLS, and `@panelito/types` is importable by both apps. No user-facing feature exists yet — that is the next plan.
</success_criteria>

<output>
Create `.planning/phases/01-live-session-shell/01-01-SUMMARY.md` when done.
</output>
