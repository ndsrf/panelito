---
phase: 05-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/web/package.json
  - apps/api/src/lib/env.ts
  - vercel.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "pnpm install succeeds with no ERESOLVE or peer dep errors"
    - "@langchain/langgraph, @langchain/langgraph-checkpoint-postgres, @langfuse/langchain, and ajv are listed in apps/api/package.json dependencies"
    - "@xyflow/react is listed in apps/web/package.json dependencies"
    - "SUPABASE_DIRECT_URL is validated in apps/api/src/lib/env.ts at startup"
    - "vercel.json exists at monorepo root with maxDuration: 60 for the Hono bridge route"
    - "pnpm --filter @panelito/api typecheck exits 0 after adding SUPABASE_DIRECT_URL"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel build config with maxDuration and buildCommand"
      contains: "maxDuration"
    - path: "apps/api/src/lib/env.ts"
      provides: "SUPABASE_DIRECT_URL env var validation"
      contains: "SUPABASE_DIRECT_URL"
  key_links:
    - from: "apps/api/src/lib/env.ts"
      to: "SUPABASE_DIRECT_URL env var"
      via: "z.string().refine() postgresql:// check"
      pattern: "SUPABASE_DIRECT_URL"
    - from: "vercel.json"
      to: "apps/web/app/api/[[...route]]/route.ts"
      via: "functions glob key"
      pattern: "maxDuration.*60"
---

<objective>
Install all five new NSAI packages into their correct workspaces, create vercel.json with maxDuration + buildCommand, and add SUPABASE_DIRECT_URL validation to env.ts.

Purpose: Every downstream plan in Phase 5 depends on packages being resolvable and env validation being in place. This plan has zero dependencies and unblocks the parallel Wave 2 plans.

Output: Updated package.json files with new dependencies, pnpm-lock.yaml updated, vercel.json at monorepo root, env.ts with SUPABASE_DIRECT_URL field.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-foundation/05-CONTEXT.md
@.planning/phases/05-foundation/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install NSAI packages in correct workspaces</name>
  <read_first>
    - apps/api/package.json — current API deps; confirms @langchain/* packages are absent
    - apps/web/package.json — current web deps; confirms @xyflow/react is absent
    - .planning/phases/05-foundation/05-RESEARCH.md §Standard Stack — exact package names and versions
  </read_first>
  <files>apps/api/package.json, apps/web/package.json, pnpm-lock.yaml</files>
  <action>
    Run the following two install commands from the monorepo root. Package names and versions are EXACT — do not deviate (see RESEARCH.md §Package Name Critical Notes for why):

    Command 1 — apps/api workspace:
    pnpm --filter @panelito/api add @langchain/langgraph@1.4.7 @langchain/langgraph-checkpoint-postgres@1.0.4 @langfuse/langchain@5.9.1 ajv@8.20.0

    Command 2 — apps/web workspace:
    pnpm --filter @panelito/web add @xyflow/react@12.11.1

    After both commands complete, run:
    pnpm list --filter @panelito/api @langchain/core

    Verify a single @langchain/core version is resolved (not two versions simultaneously — Pitfall 6 in RESEARCH.md). If two versions appear in the lockfile, investigate and resolve before proceeding.

    Critical: DO NOT install @langfuse/langchain as "langfuse-langchain" — this creates an irreconcilable peer dep conflict (RESEARCH.md Pitfall 1).
    Critical: DO NOT install @xyflow/react as "reactflow" — legacy package name.
  </action>
  <verify>
    <automated>pnpm --filter @panelito/api list @langchain/langgraph @langchain/langgraph-checkpoint-postgres @langfuse/langchain ajv && pnpm --filter @panelito/web list @xyflow/react</automated>
  </verify>
  <done>
    - apps/api/package.json dependencies contains: "@langchain/langgraph": "1.4.7", "@langchain/langgraph-checkpoint-postgres": "1.0.4", "@langfuse/langchain": "5.9.1", "ajv": "8.20.0"
    - apps/web/package.json dependencies contains: "@xyflow/react": "12.11.1"
    - pnpm install exits 0 with no ERESOLVE errors
    - pnpm list @langchain/core shows exactly one version (not duplicate)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create vercel.json + add SUPABASE_DIRECT_URL to env.ts</name>
  <read_first>
    - apps/web/app/api/[[...route]]/route.ts — confirms `export const runtime = "nodejs"` is already declared (so vercel.json only needs maxDuration, not runtime declaration)
    - apps/api/src/lib/env.ts — current EnvSchema; new field goes inside the z.object({}) alongside existing fields
    - .planning/phases/05-foundation/05-RESEARCH.md §Pattern 5 — recommended vercel.json structure
    - .planning/phases/05-foundation/05-RESEARCH.md §SUPABASE_DIRECT_URL Format — exact z.string().refine() pattern for env validation
    - .planning/phases/05-foundation/05-CONTEXT.md §D-11, D-12, D-13 — locked decisions on vercel.json config
  </read_first>
  <files>vercel.json, apps/api/src/lib/env.ts</files>
  <action>
    Step A — Create vercel.json at the monorepo root (not inside apps/web or apps/api).

    The file must contain:
    - "$schema": "https://openapi.vercel.sh/vercel.json"
    - "buildCommand": "turbo run build"
    - A "functions" object keyed by the glob path "apps/web/app/api/[[...route]]/route.ts" with value { "maxDuration": 60 }

    Per D-11: ALL /api/* routes are covered because the single Hono bridge file handles all routes. No granular per-route exceptions.
    Per D-12: maxDuration is 60 (conservative; Hobby tier allows up to 300).
    Per D-13: Single vercel.json at monorepo root for the unified Turborepo project.

    The route.ts already declares `export const runtime = "nodejs"` — do not add a "runtime" field to vercel.json (it is redundant and may conflict).

    Step B — Add SUPABASE_DIRECT_URL to apps/api/src/lib/env.ts.

    Add the following field to the EnvSchema z.object() alongside the existing fields:

    SUPABASE_DIRECT_URL: z
      .string()
      .url("SUPABASE_DIRECT_URL must be a valid URL")
      .refine(
        (v) => v.startsWith("postgres://") || v.startsWith("postgresql://"),
        "SUPABASE_DIRECT_URL must be a postgresql:// connection string, not a REST API URL"
      ),

    Also add SUPABASE_DIRECT_URL to the build-phase fallback object (the branch where NEXT_PHASE === 'phase-production-build') with value: process.env.SUPABASE_DIRECT_URL || ''

    The validated env object (result.data) will now expose env.SUPABASE_DIRECT_URL for use by PostgresSaver in Phase 6.
  </action>
  <verify>
    <automated>test -f /home/jgm/dev/projects/web-projects/panelito/vercel.json && grep -c "maxDuration" /home/jgm/dev/projects/web-projects/panelito/vercel.json && grep -c "SUPABASE_DIRECT_URL" /home/jgm/dev/projects/web-projects/panelito/apps/api/src/lib/env.ts</automated>
  </verify>
  <done>
    - vercel.json exists at monorepo root and contains "maxDuration": 60 under the functions key
    - vercel.json contains "buildCommand": "turbo run build"
    - apps/api/src/lib/env.ts EnvSchema contains SUPABASE_DIRECT_URL field with postgresql:// refine check
    - apps/api/src/lib/env.ts build-phase fallback object contains SUPABASE_DIRECT_URL key
    - pnpm --filter @panelito/api typecheck exits 0 (no TypeScript errors from env.ts changes)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → runtime | SUPABASE_DIRECT_URL contains Postgres credentials; must never be logged or returned in responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | SUPABASE_DIRECT_URL in env.ts | mitigate | Validated once at startup into `env` object; never logged (same pattern as SUPABASE_SERVICE_ROLE_KEY per T-01-08); never returned in API responses |
| T-05-02 | Tampering | vercel.json maxDuration | accept | Low risk; maxDuration is not a security boundary — it limits cost exposure, not attack surface |
| T-05-SC | Tampering | npm package installs | mitigate | All 5 packages verified via slopcheck [OK] in RESEARCH.md Package Legitimacy Audit; no blocking checkpoint needed |
</threat_model>

<verification>
All three install targets verified:
- `pnpm --filter @panelito/api list @langchain/langgraph` shows 1.4.7
- `pnpm --filter @panelito/api list @langfuse/langchain` shows 5.9.1 (NOT langfuse-langchain)
- `pnpm --filter @panelito/web list @xyflow/react` shows 12.11.1
- `cat vercel.json` shows maxDuration: 60 and buildCommand: "turbo run build"
- `grep SUPABASE_DIRECT_URL apps/api/src/lib/env.ts` returns at least 2 matches (declaration + fallback)
- `pnpm --filter @panelito/api typecheck` exits 0
</verification>

<success_criteria>
- All 5 packages installed at exact pinned versions in their correct workspaces
- No peer dependency conflicts; single @langchain/core version in lockfile
- vercel.json at monorepo root configures maxDuration: 60 for Hono bridge route
- SUPABASE_DIRECT_URL validated in EnvSchema with postgresql:// refine guard
- API typecheck passes with no new errors
</success_criteria>

<output>
Create `.planning/phases/05-foundation/05-01-SUMMARY.md` when done
</output>
