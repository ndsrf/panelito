---
phase: 05-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - apps/api/src/lib/blueprint-loader.ts
autonomous: true
requirements:
  - BLUE-02
  - BLUE-01

must_haves:
  truths:
    - "loadBlueprint('debate-strategy-v1') returns a valid Blueprint object without throwing"
    - "loadBlueprint with an invalid blueprintId throws with message containing 'Blueprint not found: <id>'"
    - "A Blueprint with a missing required field (e.g., no canvas_view_mode) throws with Ajv error details"
    - "The Ajv ValidateFunction for the Blueprint meta-schema is compiled ONCE at module load — not per call to loadBlueprint()"
    - "The validatorCache Map<string, ValidateFunction> returns the same cached function on second call for the same blueprintId"
    - "pnpm --filter @panelito/api typecheck exits 0"
  artifacts:
    - path: "apps/api/src/lib/blueprint-loader.ts"
      provides: "Module-level Ajv singleton + cached validators + loadBlueprint() async function"
      contains: "validatorCache"
      exports: ["loadBlueprint"]
  key_links:
    - from: "apps/api/src/lib/blueprint-loader.ts"
      to: "public.domain_blueprints.definition"
      via: "createServiceClient().from('domain_blueprints').select('definition')"
      pattern: "from.*domain_blueprints.*select.*definition"
    - from: "apps/api/src/lib/blueprint-loader.ts"
      to: "@panelito/types BlueprintSchema"
      via: "BlueprintSchema.parse(data.definition) after Ajv validates"
      pattern: "BlueprintSchema.parse"
---

<objective>
Create apps/api/src/lib/blueprint-loader.ts — a module-level Ajv singleton with cached ValidateFunction instances, and the loadBlueprint(blueprintId) async function that queries domain_blueprints, validates the JSON definition against the meta-schema, and returns a typed Blueprint object.

Purpose: The Blueprint loader is the runtime enforcement point for BLUE-02 (compiled validators cached per blueprintId, never per request). Phase 6 graph construction depends on calling loadBlueprint() to inject Blueprint ontology into LangGraph State before any agent node executes.

Output: apps/api/src/lib/blueprint-loader.ts fully implemented and typechecked.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-foundation/05-CONTEXT.md
@.planning/phases/05-foundation/05-RESEARCH.md
</context>

<interfaces>
<!-- Key interfaces the executor needs -->

From apps/api/src/lib/supabase.ts:
```typescript
export function createServiceClient(): SupabaseClient
// Usage: const supabase = createServiceClient()
// Returns service role client — bypasses RLS
```

From apps/api/src/lib/env.ts (after Plan 01):
```typescript
export const env: {
  SUPABASE_URL: string;
  SUPABASE_SERVICE_ROLE_KEY: string;
  KEY_ENCRYPTION_SECRET: string;
  API_PORT: number;
  ALLOWED_ORIGINS: string;
  SUPABASE_DIRECT_URL: string; // Added by Plan 01
}
```

From @panelito/types (after Plan 03):
```typescript
export { BlueprintSchema } from "./blueprint";
export type { Blueprint } from "./blueprint";
// BlueprintSchema.parse(data) → throws ZodError if invalid
// z.infer<typeof BlueprintSchema> = Blueprint
```

Ajv import pattern (ajv@8.20.0):
```typescript
import Ajv, { ValidateFunction } from "ajv";
// new Ajv({ allErrors: true }) — reports all errors, not just first
// ajv.compile(schema) → ValidateFunction
// validate(data) → boolean; validate.errors → AjvError[] | null
```

domain_blueprints table schema (from migration 0008):
- id: text PRIMARY KEY (e.g., 'debate-strategy-v1')
- name: text NOT NULL
- definition: jsonb NOT NULL (the full Blueprint JSON)
- created_at: timestamptz
Query: .from("domain_blueprints").select("definition").eq("id", blueprintId).single()
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement blueprint-loader.ts with Ajv module-level singleton</name>
  <read_first>
    - apps/api/src/lib/supabase.ts — createServiceClient() function signature + usage
    - apps/api/src/lib/env.ts — env object shape (after Plan 01 adds SUPABASE_DIRECT_URL)
    - .planning/phases/05-foundation/05-RESEARCH.md §Pattern 3 — complete blueprint-loader.ts implementation with Ajv singleton + validatorCache
    - .planning/phases/05-foundation/05-RESEARCH.md §Common Pitfalls — "Calling ajv.compile() per request: violates BLUE-02"
    - .planning/phases/05-foundation/05-CONTEXT.md §BLUE-02 — module-level singleton requirement
  </read_first>
  <files>apps/api/src/lib/blueprint-loader.ts</files>
  <behavior>
    - Module-level: `const ajv = new Ajv({ allErrors: true })` — single Ajv instance for the entire process
    - Module-level: `const validatorCache = new Map<string, ValidateFunction>()` — keys are blueprintId strings
    - Module-level: `const blueprintValidator: ValidateFunction = ajv.compile(BLUEPRINT_JSON_SCHEMA)` — the meta-schema ValidateFunction is compiled ONCE at module load, stored at module scope. This is the critical constraint from BLUE-02: NOT compiled inside loadBlueprint(), NOT compiled inside getValidator()
    - The validatorCache caches per blueprintId so that if the same blueprint is loaded multiple times, the same compiled function is returned without re-compilation
    - loadBlueprint(blueprintId: string): Promise<Blueprint> — async; queries domain_blueprints; runs Ajv validate; runs BlueprintSchema.parse; returns Blueprint
    - Invalid JSON structure → throw Error with message: `Blueprint ${blueprintId} failed Ajv validation: ${JSON.stringify(validate.errors)}`
    - Blueprint not found in DB → throw Error with message: `Blueprint not found: ${blueprintId}`
    - After Ajv validates (boolean true), still run BlueprintSchema.parse() to get the TypeScript-typed object — Ajv validates shape, Zod produces the typed value
  </behavior>
  <action>
    Create apps/api/src/lib/blueprint-loader.ts.

    The file structure must be (in this order):
    1. Imports: Ajv from "ajv", ValidateFunction from "ajv", Blueprint and BlueprintSchema from "@panelito/types", createServiceClient from "./supabase"
    2. BLUEPRINT_JSON_SCHEMA constant — the JSON Schema object (inline constant, not imported). Shape must cover all required fields from D-01 through D-05:
       - required: ["id", "name", "canvas_view_mode", "node_types", "edge_types", "phase_sequence", "active_persona_ids"]
       - node_types items: required ["id", "label", "color", "description"]
       - edge_types items: required ["id", "label", "color"] — NO "description" in required (D-03)
       - phase_sequence items: required ["id", "label", "llm_instructions", "allowed_node_types"]
       - additionalProperties: false at top level
    3. Module-level Ajv instance: const ajv = new Ajv({ allErrors: true })
    4. Module-level compiled validator: const blueprintValidator: ValidateFunction = ajv.compile(BLUEPRINT_JSON_SCHEMA)
    5. Module-level cache: const validatorCache = new Map<string, ValidateFunction>()
    6. Internal helper getValidator(blueprintId: string): ValidateFunction — checks cache, stores blueprintValidator in cache under blueprintId if not present, returns cached entry. This caches the already-compiled validator (not re-compiles it — compile already happened at step 4)
    7. Exported function: export async function loadBlueprint(blueprintId: string): Promise<Blueprint>

    The caching rationale: blueprintValidator is compiled once at module load. getValidator() stores it in the cache keyed by blueprintId so future calls return immediately from cache without even touching blueprintValidator again. This satisfies "compiled validators cached per blueprintId — never recompiled per request" (BLUE-02).

    In loadBlueprint():
    - Call createServiceClient() to get a service role client (bypasses RLS — needed to read domain_blueprints)
    - Query: .from("domain_blueprints").select("definition").eq("id", blueprintId).single()
    - If error or !data: throw new Error("Blueprint not found: " + blueprintId)
    - Call getValidator(blueprintId) to get the cached ValidateFunction
    - Call validate(data.definition) — if false, throw with validate.errors as JSON string
    - Call BlueprintSchema.parse(data.definition) and return the result

    No default exports. No re-export of ajv internals. Only loadBlueprint is public.
  </action>
  <verify>
    <automated>pnpm --filter @panelito/api typecheck</automated>
  </verify>
  <done>
    - apps/api/src/lib/blueprint-loader.ts exists
    - File contains module-level `const ajv = new Ajv` and `const blueprintValidator = ajv.compile(BLUEPRINT_JSON_SCHEMA)` at top scope (not inside any function)
    - File exports exactly one function: loadBlueprint(blueprintId: string): Promise<Blueprint>
    - validatorCache is declared at module scope as Map<string, ValidateFunction>
    - pnpm --filter @panelito/api typecheck exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Write and run integration verification script</name>
  <read_first>
    - apps/api/src/lib/blueprint-loader.ts — just written in Task 1; verify the implementation before writing the script
    - apps/api/src/lib/env.ts — confirm SUPABASE_DIRECT_URL is present (needed for env validation at script startup)
    - apps/api/.env — check what env vars are present; SUPABASE_DIRECT_URL must be added manually by developer if not present
  </read_first>
  <files>apps/api/src/scripts/verify-blueprint-loader.ts</files>
  <action>
    Create apps/api/src/scripts/verify-blueprint-loader.ts — a one-shot verification script that exercises loadBlueprint() against the real local Supabase instance.

    The script must:
    1. Import { loadBlueprint } from "../lib/blueprint-loader"
    2. Test A (happy path): Call loadBlueprint("debate-strategy-v1") and assert:
       - Result is not null/undefined
       - result.id === "debate-strategy-v1"
       - result.canvas_view_mode === "graph"
       - result.node_types.length === 4
       - result.edge_types.length === 4
       - result.phase_sequence.length === 3
       - result.active_persona_ids includes "analista_cientifico"
       Print "PASS: loadBlueprint('debate-strategy-v1') returned valid Blueprint" if all assertions pass
    3. Test B (not found): Call loadBlueprint("nonexistent-blueprint") and verify it throws an Error with message starting with "Blueprint not found:"
       Print "PASS: loadBlueprint('nonexistent-blueprint') threw expected error" on success
    4. Test C (caching): Call loadBlueprint("debate-strategy-v1") twice and verify both calls succeed (second is cache hit)
       Print "PASS: second loadBlueprint call succeeded (cache hit)"
    5. Print "ALL TESTS PASSED" at end if all three pass; process.exit(1) if any assertion fails

    Run the script with:
    cd apps/api && npx tsx --env-file .env src/scripts/verify-blueprint-loader.ts

    If SUPABASE_DIRECT_URL is not yet in apps/api/.env, the env validation will throw a startup error. Add a placeholder comment to the script: "Ensure SUPABASE_DIRECT_URL is set in apps/api/.env before running".

    If the local Supabase instance is not running, start it with: supabase start

    The script does NOT need to test invalid Blueprint structure (Ajv rejection) — the unit of trust is that the Debate Blueprint seed is valid JSON matching the schema, and the not-found error path is covered by Test B.

    This script is for developer verification only — it is not a test file and does not need to be part of the vitest suite.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito/apps/api && npx tsx --env-file .env src/scripts/verify-blueprint-loader.ts 2>&1 | grep -c "PASS"</automated>
  </verify>
  <done>
    - apps/api/src/scripts/verify-blueprint-loader.ts exists
    - Script runs with `npx tsx --env-file .env src/scripts/verify-blueprint-loader.ts` and prints "ALL TESTS PASSED"
    - Test A: loadBlueprint('debate-strategy-v1') returns Blueprint with id, canvas_view_mode, node_types.length=4, edge_types.length=4
    - Test B: loadBlueprint('nonexistent-blueprint') throws "Blueprint not found:" error
    - Test C: second call to loadBlueprint returns (no crash; cache hit)
    - grep "PASS" output shows 3 matches
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blueprintId parameter → DB query | blueprintId comes from API caller; .eq("id", blueprintId) + .single() is safe — Supabase client parameterizes queries; no SQL injection risk |
| DB definition jsonb → Blueprint type | Ajv validates shape before BlueprintSchema.parse; double-validation is intentional defense-in-depth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | Blueprint definition column in domain_blueprints | mitigate | RLS allows only authenticated SELECT; service role writes only; Ajv validates definition at load time before any Blueprint is used |
| T-05-10 | Tampering | Blueprint injection via blueprintId | mitigate | .eq("id", blueprintId).single() — parameterized query via Supabase JS client; FK constraint on sessions.blueprint_id ensures ID must reference an existing row |
| T-05-11 | Denial of Service | Ajv.compile() called per request | mitigate | Module-level compile (blueprintValidator = ajv.compile(...)) at module load; getValidator() only adds to cache, never re-compiles; BLUE-02 enforcement |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new installs; ajv@8.20.0 already installed by Plan 01 and verified via slopcheck [OK] |
</threat_model>

<verification>
Full end-to-end verification:
1. `pnpm --filter @panelito/api typecheck` exits 0 — no TS errors in blueprint-loader.ts
2. `npx tsx --env-file .env src/scripts/verify-blueprint-loader.ts` prints "ALL TESTS PASSED"
3. `grep "const blueprintValidator" apps/api/src/lib/blueprint-loader.ts` → 1 match at module scope (not inside a function)
4. `grep "validatorCache" apps/api/src/lib/blueprint-loader.ts | grep -v "^//"` → module-level Map declaration + usage in getValidator
5. `grep -v "^#\|^//" apps/api/src/lib/blueprint-loader.ts | grep -c "ajv.compile"` → exactly 1 match (module-level only)
</verification>

<success_criteria>
- blueprint-loader.ts implemented with module-level Ajv singleton + blueprintValidator compiled at module load
- validatorCache Map caches per blueprintId — never calls ajv.compile() more than once per process
- loadBlueprint('debate-strategy-v1') returns valid typed Blueprint from local Supabase
- loadBlueprint with nonexistent ID throws descriptive "Blueprint not found: X" error
- All three verification tests pass in the script
- API typecheck exits 0 — no TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/05-foundation/05-04-SUMMARY.md` when done
</output>
