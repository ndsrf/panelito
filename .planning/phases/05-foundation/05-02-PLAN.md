---
phase: 05-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - supabase/migrations/0008_nsai_foundation.sql
autonomous: true
requirements:
  - INFRA-02
  - BLUE-05

must_haves:
  truths:
    - "supabase migration list shows 0008 applied"
    - "SELECT * FROM public.domain_blueprints LIMIT 1 returns the Debate/Strategy Blueprint row"
    - "SELECT * FROM public.canvas_nodes LIMIT 0 succeeds (table exists)"
    - "SELECT * FROM public.canvas_edges LIMIT 0 succeeds (table exists)"
    - "SELECT * FROM information_schema.schemata WHERE schema_name = 'langgraph' returns 1 row"
    - "SELECT * FROM public.sessions WHERE blueprint_id IS NULL returns 0 rows"
    - "RLS on domain_blueprints: authenticated users can SELECT; no INSERT/UPDATE/DELETE policy exists (service role only)"
  artifacts:
    - path: "supabase/migrations/0008_nsai_foundation.sql"
      provides: "All NSAI tables, RLS policies, langgraph schema, session truncation, Blueprint seed"
      contains: "domain_blueprints"
  key_links:
    - from: "public.sessions"
      to: "public.domain_blueprints"
      via: "blueprint_id NOT NULL FK ON DELETE RESTRICT"
      pattern: "blueprint_id.*NOT NULL.*REFERENCES.*domain_blueprints"
    - from: "public.canvas_nodes"
      to: "public.sessions"
      via: "session_id FK ON DELETE CASCADE"
      pattern: "session_id.*REFERENCES.*sessions"
---

<objective>
Create Supabase migration 0008_nsai_foundation.sql covering: domain_blueprints table + RLS, canvas_nodes + canvas_edges tables + RLS, langgraph schema creation, sessions table modification (TRUNCATE CASCADE + blueprint_id NOT NULL FK), and Debate/Strategy Blueprint seed.

Purpose: All downstream database work in Phase 5 (Blueprint loader) and Phase 6 (LangGraph graph + checkpointer) depends on these tables existing. This plan runs in Wave 2 parallel with Plan 03 (types package).

Output: supabase/migrations/0008_nsai_foundation.sql applied to the local Supabase instance.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-foundation/05-CONTEXT.md
@.planning/phases/05-foundation/05-RESEARCH.md
</context>

<interfaces>
<!-- Key DB patterns from existing migrations -->

From supabase/migrations/0007_branches_table.sql:
- RLS pattern: ALTER TABLE ... ENABLE ROW LEVEL SECURITY; CREATE POLICY "name" ON table FOR SELECT USING (auth.uid() IS NOT NULL);
- FK cascade: REFERENCES public.sessions(id) ON DELETE CASCADE
- trigger function: on_session_created() — creates 'main' branch on session INSERT (must NOT be dropped or replaced)

From supabase/migrations/0005_reactions_personas.sql:
- Multi-table migration: CREATE TABLE → indexes → RLS → ALTER existing table
- No INSERT/UPDATE/DELETE policy = service role only (bypasses RLS)

Existing session-dependent FK chain (all cascade from sessions):
- public.messages.session_id → sessions(id) ON DELETE CASCADE
- public.branches.session_id → sessions(id) ON DELETE CASCADE
- public.reactions.session_id → sessions(id) ON DELETE CASCADE
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0008_nsai_foundation.sql</name>
  <read_first>
    - supabase/migrations/0007_branches_table.sql — RLS pattern, trigger (on_session_created must survive)
    - supabase/migrations/0005_reactions_personas.sql — multi-table structure, no-policy-means-service-role pattern
    - .planning/phases/05-foundation/05-CONTEXT.md §D-06, D-07, D-08, D-09, D-10 — locked schema decisions
    - .planning/phases/05-foundation/05-RESEARCH.md §Pattern 6 — TRUNCATE CASCADE + blueprint_id NOT NULL
    - .planning/phases/05-foundation/05-RESEARCH.md §Pattern 7 — langgraph schema + checkpointer tables
    - .planning/phases/05-foundation/05-RESEARCH.md §Debate Blueprint Seed Design — complete JSON seed
  </read_first>
  <files>supabase/migrations/0008_nsai_foundation.sql</files>
  <action>
    Create supabase/migrations/0008_nsai_foundation.sql with the following sections IN ORDER. Each section is separated by a descriptive comment block.

    SECTION 1 — domain_blueprints table:
    - Table: public.domain_blueprints
    - Columns: id text PRIMARY KEY, name text NOT NULL, definition jsonb NOT NULL, created_at timestamptz NOT NULL DEFAULT now()
    - No creator_id column (D-06: platform-global, no per-creator ownership)
    - Enable RLS
    - RLS policy "blueprints_select": FOR SELECT USING (auth.uid() IS NOT NULL) — any authenticated user can read
    - No INSERT/UPDATE/DELETE policy — service role bypasses RLS, so no policy = effectively service-role-only write (D-06)

    SECTION 2 — canvas_nodes table:
    - Table: public.canvas_nodes
    - Columns: id uuid PRIMARY KEY DEFAULT gen_random_uuid(), session_id uuid NOT NULL REFERENCES public.sessions(id) ON DELETE CASCADE, branch_id uuid NOT NULL REFERENCES public.branches(id) ON DELETE CASCADE, blueprint_id text NOT NULL REFERENCES public.domain_blueprints(id) ON DELETE RESTRICT, node_type_id text NOT NULL, label text NOT NULL, status text NOT NULL DEFAULT 'committed' CHECK (status IN ('committed', 'ghost', 'silent')), position_x float8, position_y float8, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()
    - Index: canvas_nodes_session_branch_idx ON public.canvas_nodes(session_id, branch_id)
    - Enable RLS
    - RLS policy "canvas_nodes_select": FOR SELECT USING (auth.uid() IS NOT NULL)
    - RLS policy "canvas_nodes_insert": FOR INSERT WITH CHECK (auth.uid() IS NOT NULL)
    - RLS policy "canvas_nodes_update": FOR UPDATE USING (auth.uid() IS NOT NULL)

    SECTION 3 — canvas_edges table:
    - Table: public.canvas_edges
    - Columns: id uuid PRIMARY KEY DEFAULT gen_random_uuid(), session_id uuid NOT NULL REFERENCES public.sessions(id) ON DELETE CASCADE, branch_id uuid NOT NULL REFERENCES public.branches(id) ON DELETE CASCADE, blueprint_id text NOT NULL REFERENCES public.domain_blueprints(id) ON DELETE RESTRICT, source_node_id uuid NOT NULL REFERENCES public.canvas_nodes(id) ON DELETE CASCADE, target_node_id uuid NOT NULL REFERENCES public.canvas_nodes(id) ON DELETE CASCADE, edge_type_id text NOT NULL, status text NOT NULL DEFAULT 'committed' CHECK (status IN ('committed', 'ghost', 'silent')), created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()
    - Index: canvas_edges_session_branch_idx ON public.canvas_edges(session_id, branch_id)
    - Enable RLS
    - RLS policy "canvas_edges_select": FOR SELECT USING (auth.uid() IS NOT NULL)
    - RLS policy "canvas_edges_insert": FOR INSERT WITH CHECK (auth.uid() IS NOT NULL)
    - RLS policy "canvas_edges_update": FOR UPDATE USING (auth.uid() IS NOT NULL)

    SECTION 4 — langgraph schema:
    - CREATE SCHEMA IF NOT EXISTS langgraph;
    - DO NOT create the checkpointer tables in SQL. Per RESEARCH.md Pitfall 4, creating them manually and then calling setup() creates a version-tracking conflict (checkpoint_migrations table). Instead, only create the schema here. The tables are created by checkpointer.setup() in Phase 6 server bootstrap — this satisfies D-14 (schema visible in dashboard) while avoiding the setup() conflict.
    - Add a comment explaining: "Tables (checkpoints, checkpoint_blobs, checkpoint_writes) are created by PostgresSaver.setup() on first server startup — idempotent after that."

    SECTION 5 — sessions TRUNCATE + blueprint_id column:
    - TRUNCATE TABLE public.sessions CASCADE; — this wipes all session data and all FK-dependent rows (messages, branches, reactions, canvas_nodes, canvas_edges) atomically (D-07: beta data, acceptable)
    - Then: ALTER TABLE public.sessions ADD COLUMN blueprint_id text NOT NULL REFERENCES public.domain_blueprints(id) ON DELETE RESTRICT;
    - This MUST come after SECTION 1 (domain_blueprints must exist before FK can reference it) and after TRUNCATE (cannot add NOT NULL to table with existing rows that have no blueprint_id)

    SECTION 6 — Debate/Strategy Blueprint seed:
    - INSERT INTO public.domain_blueprints (id, name, definition) VALUES ('debate-strategy-v1', 'Debate / Strategy', '{...}'::jsonb)
    - The definition JSON must exactly match the structure from RESEARCH.md §Debate Blueprint Seed Design:
      - id: "debate-strategy-v1"
      - name: "Debate / Strategy"
      - canvas_view_mode: "graph"
      - node_types: Hypothesis (#6366f1), Evidence (#22c55e), Counter-Argument (#ef4444), Action (#f59e0b) — each with id, label, color, description fields (D-02, D-08)
      - edge_types: SUPPORTS (#22c55e), CONTRADICTS (#ef4444), BUILDS_ON (#6366f1), REFUTES (#f97316) — each with id, label, color (D-03, D-09)
      - phase_sequence: opening (allowed: hypothesis, evidence), debate (allowed: all 4), synthesis (allowed: hypothesis, evidence, action) — with llm_instructions per phase (Claude's discretion, see RESEARCH.md §Debate Blueprint Seed Design for full text)
      - active_persona_ids: ["analista_cientifico"] (D-05; PERSONA_IDS[0] from packages/types/src/persona.ts)
    - The id column in domain_blueprints is text (not uuid), so 'debate-strategy-v1' is valid

    NOTE: SECTION 5 must appear AFTER SECTION 1 (domain_blueprints table) and AFTER SECTION 6 seed... actually SECTION 5's TRUNCATE comes before the NOT NULL FK add, but domain_blueprints must exist before the FK references it. Final order must be:
    1 → 2 → 3 → 4 → 6 (seed Blueprint) → 5 (TRUNCATE + ADD COLUMN FK)
    This ensures domain_blueprints has the Debate Blueprint before the FK constraint is added. Reorder SECTION numbering or add a comment explaining the ordering dependency.
  </action>
  <verify>
    <automated>test -f /home/jgm/dev/projects/web-projects/panelito/supabase/migrations/0008_nsai_foundation.sql && grep -c "domain_blueprints" /home/jgm/dev/projects/web-projects/panelito/supabase/migrations/0008_nsai_foundation.sql</automated>
  </verify>
  <done>
    - File supabase/migrations/0008_nsai_foundation.sql exists
    - File contains: CREATE TABLE public.domain_blueprints, CREATE TABLE public.canvas_nodes, CREATE TABLE public.canvas_edges, CREATE SCHEMA IF NOT EXISTS langgraph, TRUNCATE TABLE public.sessions CASCADE, ALTER TABLE public.sessions ADD COLUMN blueprint_id text NOT NULL, INSERT INTO public.domain_blueprints
    - domain_blueprints has no INSERT/UPDATE/DELETE RLS policy (service-role-only write per D-06)
    - The Debate Blueprint INSERT comes before the blueprint_id NOT NULL FK add
  </done>
</task>

<task type="auto">
  <name>Task 2: Push migration to local Supabase and verify</name>
  <read_first>
    - supabase/migrations/0008_nsai_foundation.sql — the file written in Task 1 (verify SQL is syntactically correct before pushing)
  </read_first>
  <files>supabase/migrations/0008_nsai_foundation.sql</files>
  <action>
    Push the migration to the local Supabase instance:

    Command: CI=true supabase db push

    If supabase db push is not available or the project uses migration-up workflow, try:
    supabase migration up

    After successful push, verify the migration applied correctly by running these queries against the local DB:

    Query 1 — Blueprint seed exists:
    supabase db execute --sql "SELECT id, name FROM public.domain_blueprints;"
    Expected: one row with id='debate-strategy-v1'

    Query 2 — sessions table has blueprint_id column:
    supabase db execute --sql "SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name='sessions' AND column_name='blueprint_id';"
    Expected: column_name=blueprint_id, is_nullable=NO

    Query 3 — canvas_nodes table exists:
    supabase db execute --sql "SELECT COUNT(*) FROM public.canvas_nodes;"
    Expected: count=0 (no rows, but table exists)

    Query 4 — langgraph schema exists:
    supabase db execute --sql "SELECT schema_name FROM information_schema.schemata WHERE schema_name='langgraph';"
    Expected: one row with schema_name=langgraph

    Query 5 — domain_blueprints has no INSERT policy:
    supabase db execute --sql "SELECT policyname, cmd FROM pg_policies WHERE tablename='domain_blueprints';"
    Expected: only one row with cmd=SELECT (no INSERT/UPDATE/DELETE policies)

    If any query returns unexpected results, diagnose from the migration SQL before proceeding. Do not mark this task done until all 5 queries return expected results.
  </action>
  <verify>
    <automated>supabase db execute --sql "SELECT id FROM public.domain_blueprints WHERE id='debate-strategy-v1';" 2>/dev/null | grep -c "debate-strategy-v1"</automated>
  </verify>
  <done>
    - supabase db push exits 0
    - domain_blueprints row with id='debate-strategy-v1' exists
    - sessions.blueprint_id column is NOT NULL
    - canvas_nodes and canvas_edges tables exist (0 rows each)
    - langgraph schema exists
    - domain_blueprints has SELECT RLS policy only (no INSERT/UPDATE/DELETE)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → domain_blueprints | SELECT allowed; INSERT/UPDATE/DELETE requires service role (bypasses RLS) |
| sessions.blueprint_id | NOT NULL FK ensures every session references a valid, existing Blueprint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | domain_blueprints INSERT/UPDATE | mitigate | No INSERT/UPDATE/DELETE RLS policy; only service role can write; auth users can only read |
| T-05-04 | Tampering | sessions.blueprint_id FK | mitigate | ON DELETE RESTRICT prevents orphaned sessions if a Blueprint is removed; NOT NULL ensures every session has an active Blueprint |
| T-05-05 | Tampering | canvas_nodes.status check constraint | mitigate | CHECK (status IN ('committed', 'ghost', 'silent')) prevents arbitrary status strings at DB level |
| T-05-06 | Elevation of Privilege | canvas_nodes/edges INSERT | mitigate | RLS WITH CHECK (auth.uid() IS NOT NULL) — must be authenticated to write canvas mutations |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; packages installed in Plan 01 |
</threat_model>

<verification>
Full migration verification sequence:
1. `supabase migration list` shows 0008 in Applied column
2. `SELECT id, name FROM public.domain_blueprints` → debate-strategy-v1
3. `\d public.sessions` → blueprint_id text NOT NULL visible
4. `SELECT schema_name FROM information_schema.schemata WHERE schema_name='langgraph'` → 1 row
5. `SELECT policyname, cmd FROM pg_policies WHERE tablename='domain_blueprints'` → SELECT only
6. `SELECT COUNT(*) FROM public.canvas_nodes` → 0 (table exists, empty)
7. `SELECT COUNT(*) FROM public.canvas_edges` → 0 (table exists, empty)
</verification>

<success_criteria>
- Migration 0008 pushed and applied to local Supabase with no errors
- domain_blueprints table exists with Debate/Strategy Blueprint seed row
- canvas_nodes and canvas_edges exist with correct status check constraints
- langgraph schema exists (tables will be created by setup() in Phase 6)
- sessions.blueprint_id is NOT NULL FK — no session can exist without a Blueprint
- All RLS policies are correct: domain_blueprints SELECT-only for auth users; canvas tables full CRUD for auth users
</success_criteria>

<output>
Create `.planning/phases/05-foundation/05-02-SUMMARY.md` when done
</output>
