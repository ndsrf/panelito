---
phase: 07-invoke-route-modification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/graph/state.ts
  - apps/api/src/graph/nodes/agent.ts
  - apps/api/src/graph/nodes/drift-reply.ts
  - apps/api/src/graph/nodes/orchestrator.ts
  - apps/api/src/lib/env.ts
  - .env.example
autonomous: true
requirements: [ORCH-01]
user_setup: []

must_haves:
  truths:
    - "AgentNode and DriftReplyNode forward each text_delta token to config.configurable.streamWriter"
    - "buildAgentSystemPrompt bans canvas meta-commentary and prefers silence"
    - "OrchestratorNode sets steeringTextEnabled on DOMAIN_BRIDGE via a drift_reply_probability roll"
    - "AgentNode emits polite steering text via streamWriter when steeringTextEnabled is true"
    - "LANGFUSE_TRACE_LEVEL is a validated env var defaulting to 'graph'"
  artifacts:
    - path: "apps/api/src/graph/state.ts"
      provides: "steeringTextEnabled state field"
      contains: "steeringTextEnabled"
    - path: "apps/api/src/graph/nodes/agent.ts"
      provides: "streamWriter seam + persona/prompt rules + steering text"
      contains: "streamWriter"
    - path: "apps/api/src/graph/nodes/drift-reply.ts"
      provides: "streamWriter seam"
      contains: "streamWriter"
    - path: "apps/api/src/lib/env.ts"
      provides: "LANGFUSE_TRACE_LEVEL union env var"
      contains: "LANGFUSE_TRACE_LEVEL"
  key_links:
    - from: "apps/api/src/graph/nodes/orchestrator.ts"
      to: "apps/api/src/graph/nodes/agent.ts"
      via: "state.steeringTextEnabled"
      pattern: "steeringTextEnabled"
    - from: "apps/api/src/graph/nodes/agent.ts"
      to: "route SSE queue"
      via: "config.configurable.streamWriter"
      pattern: "streamWriter"
---

<objective>
Prepare the Phase 6 graph nodes to stream real-time text through Hono SSE and add the DOMAIN_BRIDGE steering-text path — the contract the /invoke route (Plan 02) consumes. Also add the LANGFUSE_TRACE_LEVEL env var.

Purpose: The route in Plan 02 cannot stream tokens or trace-control unless the streamWriter seam, the steeringTextEnabled state field, the updated system prompt, and the env var exist first. This plan is the interface layer; Plan 02 is the wiring.
Output: Modified graph nodes (agent, drift-reply, orchestrator), state schema (steeringTextEnabled), env schema (LANGFUSE_TRACE_LEVEL), and .env.example.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-invoke-route-modification/07-CONTEXT.md
@.planning/phases/07-invoke-route-modification/07-RESEARCH.md
@.planning/phases/07-invoke-route-modification/07-PATTERNS.md

<interfaces>
<!-- Current state schema — identity-reducer pattern to copy for the new field. -->
From apps/api/src/graph/state.ts:
  guardrailResult: Annotation<'DOMAIN_MATCH' | 'DOMAIN_BRIDGE' | 'DOMAIN_DRIFT' | null>({ reducer: (_, v) => v, default: () => null })
  driftAction: Annotation<'replied' | 'ignored' | null>({ reducer: (_, v) => v, default: () => null })
  agentOutput: Annotation<CanvasOp | null>({ reducer: (_, v) => v, default: () => null })

<!-- Current AgentNode signature the route will call through. -->
From apps/api/src/graph/nodes/agent.ts:
  export function buildAgentSystemPrompt(blueprint: Blueprint, currentPhaseId: string): string
  export async function agentNode(state: GraphState, config?: any): Promise<Partial<GraphState>>
  // config.configurable reads: blueprint, providerName, plaintextKey, agentAdapter (test seam)
  // stream loop currently handles ONLY event.type === 'tool_use' (canvas_mutation); no text_delta branch

<!-- Blueprint type — drift_reply_probability defaults to 0.8. -->
From packages/types/src/blueprint.ts:
  drift_reply_probability: z.number().min(0).max(1).default(0.8)

<!-- Env schema pattern to extend. -->
From apps/api/src/lib/env.ts:
  const EnvSchema = z.object({ ... ALLOWED_ORIGINS: z.string().default("http://localhost:3000") })
  // single EnvSchema.safeParse(process.env) validates all fields automatically
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add steeringTextEnabled state field + streamWriter seam to graph nodes</name>
  <files>apps/api/src/graph/state.ts, apps/api/src/graph/nodes/drift-reply.ts</files>
  <read_first>
    - apps/api/src/graph/state.ts (copy the identity-reducer pattern from driftAction exactly)
    - apps/api/src/graph/nodes/drift-reply.ts (the text_delta branch is currently an empty comment block at lines 57-61)
    - .planning/phases/07-invoke-route-modification/07-PATTERNS.md (LangGraph State Field Addition + streamWriter seam in DriftReplyNode)
  </read_first>
  <action>
    In state.ts, add a new annotation field `steeringTextEnabled: Annotation<boolean | null>` after the `agentOutput` field, using the identity-reducer pattern `reducer: (_: boolean | null, v: boolean | null) => v` and `default: () => null`. Document it: null = not a DOMAIN_BRIDGE steering path; true/false = OrchestratorNode probability roll result (D-11).

    In drift-reply.ts, replace the empty `text_delta` comment block (lines 57-61) with a call to `config?.configurable?.streamWriter?.(event.text)` inside the existing `if (event.type === 'text_delta')` branch (D-05). Do not accumulate text in the node — accumulation happens in the route. No other changes to drift-reply.ts.
  </action>
  <verify>
    <automated>cd apps/api && grep -q "steeringTextEnabled" src/graph/state.ts && grep -q "streamWriter" src/graph/nodes/drift-reply.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^$' | head; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - src/graph/state.ts contains `steeringTextEnabled: Annotation<boolean | null>` with reducer `(_: boolean | null, v: boolean | null) => v` and `default: () => null`
    - src/graph/nodes/drift-reply.ts contains `config?.configurable?.streamWriter?.(event.text)` inside the `text_delta` branch
    - `grep -v '^ ' src/graph/nodes/drift-reply.ts | grep -c "for await"` is unchanged (single stream loop, no structural rewrite)
    - `npx tsc --noEmit` in apps/api exits 0 (no type errors introduced)
  </acceptance_criteria>
  <done>steeringTextEnabled field exists in state schema; DriftReplyNode forwards text_delta tokens to streamWriter; project typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AgentNode — streamWriter seam, persona/silence prompt rules, steering text</name>
  <files>apps/api/src/graph/nodes/agent.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/agent.ts (current buildAgentSystemPrompt at lines 25-61; stream loop at lines 94-117 with ONLY a tool_use branch)
    - apps/api/src/graph/state.ts (confirm steeringTextEnabled field from Task 1 — same plan, so read after Task 1)
    - .planning/phases/07-invoke-route-modification/07-PATTERNS.md (streamWriter seam in AgentNode, buildAgentSystemPrompt signature extension, activePersonas consumption, steeringTextEnabled consumption)
    - .planning/phases/07-invoke-route-modification/07-CONTEXT.md (D-05, D-08, D-09, D-10, D-11, D-12)
  </read_first>
  <action>
    Extend `buildAgentSystemPrompt` signature to `buildAgentSystemPrompt(blueprint: Blueprint, currentPhaseId: string, activePersonaInstructions?: string): string` (D-10). Append two new rules to the existing rules array: (1) D-08 — NEVER describe canvas operations in text; ban the phrases "I added a node", "I mapped this to", "I connected", "I've recorded", and any description of canvas operations. (2) D-09 — produce text ONLY when the insight cannot be fully represented in the canvas mutation; prefer silence; when text is produced, limit to 1-2 sentences of substantive insight. When `activePersonaInstructions` is provided, append it after the Blueprint phase instructions under a "Persona style:" header (D-10).

    In `agentNode`, read `activePersonas` from `config?.configurable?.activePersonas as string[] | undefined`, join into a persona instruction string, and pass it as the third arg to `buildAgentSystemPrompt`. Read `state.steeringTextEnabled` (D-11): when it is `true`, extend the system prompt with a steering-text instruction — a brief, polite, redirective sentence guiding the human toward the active phase objective, that MUST NOT use the words "blueprint", "domain", or "ontology" (D-12). Add a `text_delta` branch to the `adapter.stream()` loop BEFORE the existing `tool_use` branch that calls `config?.configurable?.streamWriter?.(event.text)` (D-05); do not accumulate text in the node. Keep the existing tool_use / CanvasOpSchema handling and fail-silent catch unchanged.
  </action>
  <verify>
    <automated>cd apps/api && grep -q "activePersonaInstructions" src/graph/nodes/agent.ts && grep -q "streamWriter" src/graph/nodes/agent.ts && grep -q "steeringTextEnabled" src/graph/nodes/agent.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | head; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - buildAgentSystemPrompt signature has a third parameter `activePersonaInstructions?: string`
    - agent.ts contains a `text_delta` branch calling `config?.configurable?.streamWriter?.(event.text)` positioned before the `tool_use` branch in the stream loop
    - agent.ts reads `config?.configurable?.activePersonas` and passes joined persona instructions into buildAgentSystemPrompt
    - agent.ts reads `state.steeringTextEnabled` and conditionally augments the system prompt
    - System prompt string contains the banned-phrase rule text (e.g. grep matches `I added a node`) and the silence-preference rule
    - `npx tsc --noEmit` in apps/api exits 0
    - Existing canvas_mutation tool_use handling and the fail-silent `catch` block remain present (grep matches `CanvasOpSchema.safeParse`)
  </acceptance_criteria>
  <done>AgentNode streams text tokens via streamWriter, injects persona instructions, bans canvas meta-commentary, prefers silence, and emits steering text when the orchestrator enables it — while preserving canvas mutation behavior.</done>
</task>

<task type="auto">
  <name>Task 3: OrchestratorNode DOMAIN_BRIDGE steering roll + LANGFUSE_TRACE_LEVEL env var</name>
  <files>apps/api/src/graph/nodes/orchestrator.ts, apps/api/src/lib/env.ts, .env.example</files>
  <read_first>
    - apps/api/src/graph/nodes/orchestrator.ts (existing DOMAIN_DRIFT roll block at lines 144-157; return object at lines 159-163)
    - apps/api/src/lib/env.ts (EnvSchema object; single safeParse call)
    - .env.example (Langfuse section — LANGFUSE_PUBLIC_KEY/SECRET_KEY/BASE_URL at lines 45-47)
    - .planning/phases/07-invoke-route-modification/07-PATTERNS.md (orchestrator DOMAIN_BRIDGE block, Zod Env Var Addition, .env.example addition)
    - .planning/phases/07-invoke-route-modification/07-CONTEXT.md (D-11, D-15)
  </read_first>
  <action>
    In orchestrator.ts, after the existing DOMAIN_DRIFT roll block, add a DOMAIN_BRIDGE steering roll (D-11): when `guardrailResult === 'DOMAIN_BRIDGE'`, compute `steeringProbability = blueprint.drift_reply_probability ?? 0.8`, roll `Math.random()`, and set `steeringTextEnabled = roll < steeringProbability`. Default `steeringTextEnabled` to `null` for all non-BRIDGE paths. Log with `console.info('[orchestrator] bridge steering event', { steering_probability, steering_roll, steering_enabled })` mirroring the existing drift-event log structure. Add `steeringTextEnabled` to the node's return object. Ensure the two early-return branches (missing blueprint at line 35; no-messages at line 78) also return `steeringTextEnabled: null` so the field is always defined.

    In env.ts, add `LANGFUSE_TRACE_LEVEL: z.enum(['graph', 'full']).default('graph')` to the EnvSchema object (D-15). No change to the safeParse/export block — the new field validates automatically.

    In .env.example, append after the LANGFUSE_BASE_URL line: a comment explaining 'graph' traces LangGraph execution only and 'full' also traces DB queries and route overhead, then `LANGFUSE_TRACE_LEVEL=graph`.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && grep -q "bridge steering event" apps/api/src/graph/nodes/orchestrator.ts && grep -q "steeringTextEnabled" apps/api/src/graph/nodes/orchestrator.ts && grep -q "LANGFUSE_TRACE_LEVEL" apps/api/src/lib/env.ts && grep -q "LANGFUSE_TRACE_LEVEL=graph" .env.example && cd apps/api && npx tsc --noEmit -p tsconfig.json 2>&1 | head; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - orchestrator.ts contains a DOMAIN_BRIDGE branch computing `steeringTextEnabled` from `blueprint.drift_reply_probability`
    - orchestrator.ts return object and both early-return branches include `steeringTextEnabled` (null on non-BRIDGE paths)
    - orchestrator.ts contains `console.info('[orchestrator] bridge steering event'` with steering_probability/steering_roll/steering_enabled keys
    - env.ts EnvSchema contains `LANGFUSE_TRACE_LEVEL: z.enum(['graph', 'full']).default('graph')`
    - .env.example contains `LANGFUSE_TRACE_LEVEL=graph`
    - `npx tsc --noEmit` in apps/api exits 0
  </acceptance_criteria>
  <done>OrchestratorNode sets steeringTextEnabled via a probability roll on DOMAIN_BRIDGE; LANGFUSE_TRACE_LEVEL is a validated env var defaulting to 'graph' and is documented in .env.example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → SSE stream | AgentNode/DriftReplyNode text tokens flow to the client via streamWriter; must not leak system prompt or key material |
| Env var → runtime config | LANGFUSE_TRACE_LEVEL is external input validated at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | AgentNode system prompt | mitigate | streamWriter forwards only `event.text` deltas from the model, never the system prompt string or plaintextKey; D-08 bans canvas meta-commentary in output |
| T-07-02 | Tampering | LANGFUSE_TRACE_LEVEL env var | mitigate | Zod `.enum(['graph','full'])` rejects any other value at startup; `.default('graph')` handles absence |
| T-07-03 | Information Disclosure | Steering text (D-12) | accept | Steering text is model-generated natural language; prompt instruction forbids technical terms but content is low-sensitivity facilitation text |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (D-07); no supply-chain surface added |
</threat_model>

<verification>
- `cd apps/api && npx tsc --noEmit` exits 0 after all three tasks
- `cd apps/api && pnpm test` — existing graph unit tests (graph.test.ts) still pass; the new steeringTextEnabled field and text_delta branches do not regress DOMAIN_MATCH/DRIFT/BRIDGE routing
- `grep -R "streamWriter" apps/api/src/graph/nodes/` returns matches in both agent.ts and drift-reply.ts
</verification>

<success_criteria>
- steeringTextEnabled state field exists and is set by OrchestratorNode on DOMAIN_BRIDGE
- AgentNode and DriftReplyNode both forward text_delta tokens to config.configurable.streamWriter
- buildAgentSystemPrompt bans canvas meta-commentary (D-08), prefers silence (D-09), and accepts persona instructions (D-10)
- AgentNode emits polite steering text (no "blueprint"/"domain"/"ontology") when steeringTextEnabled is true
- LANGFUSE_TRACE_LEVEL validates as a 'graph' | 'full' union defaulting to 'graph', documented in .env.example
- apps/api typechecks and existing graph tests pass
</success_criteria>

<output>
Create `.planning/phases/07-invoke-route-modification/07-01-SUMMARY.md` when done
</output>
