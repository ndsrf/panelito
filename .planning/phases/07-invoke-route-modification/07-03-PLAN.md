---
phase: 07-invoke-route-modification
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false
requirements: [ORCH-01]
user_setup: []

must_haves:
  truths:
    - "A real /invoke call to a Blueprint session produces a visible Langfuse trace with node spans"
    - "An existing v1-shaped session (no blueprint) is rejected with 400 no_blueprint in the running app"
    - "A live client disconnect terminates graph execution and is recorded in Langfuse"
  artifacts: []
  key_links:
    - from: "live /invoke call"
      to: "Langfuse dashboard"
      via: "per-request CallbackHandler on graph.astream()"
      pattern: "CallbackHandler"
---

<objective>
Verify Phase 7 success criterion SC-4 and the end-to-end behavior that cannot be asserted from unit tests: a real /invoke call to a Blueprint session must produce a visible Langfuse trace, and the abort + no_blueprint behaviors must hold against the running application.

Purpose: SC-4 ("A Langfuse trace is visible for every real /invoke call") requires inspecting the Langfuse dashboard — no automated assertion can confirm a dashboard trace. This checkpoint closes the phase.
Output: Human confirmation that the trace, the blueprint gate, and abort handling work against the live app.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-invoke-route-modification/07-CONTEXT.md
@.planning/phases/07-invoke-route-modification/07-01-SUMMARY.md
@.planning/phases/07-invoke-route-modification/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify live /invoke — Langfuse trace, blueprint gate, abort (SC-1..SC-4)</name>
  <files>none — verification only</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 7 success criteria — exact language for SC-1 through SC-4)
    - .planning/phases/07-invoke-route-modification/07-02-SUMMARY.md (what was wired)
  </read_first>
  <what-built>
    The /invoke route now runs every AI response through the LangGraph StateGraph (graph.astream()) with a PostgresSaver checkpointer, a per-request Langfuse CallbackHandler, abort-signal propagation, and the no_blueprint 400 gate. Automated tests (Plan 02) already cover SC-1 (no_blueprint), SC-2 (SSE text_delta+done), and SC-3 (abort signal passed to config). This checkpoint verifies the pieces that require a running app + live Langfuse: SC-4 (dashboard trace) and end-to-end behavior.
  </what-built>
  <action>
    Human-verify checkpoint (no code changes). Run the app locally or against a Vercel preview and confirm SC-1 through SC-4 against the live application by following the steps in <how-to-verify>. Do not proceed until all acceptance criteria are confirmed or issues are reported.
  </action>
  <how-to-verify>
    Prerequisites: valid LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY / LANGFUSE_BASE_URL and SUPABASE_DIRECT_URL in apps/api/.env; a session that has an active Blueprint (blueprint_id set) and at least one active persona; a valid creator API key stored.

    1. Start the API and web app locally (or use a Vercel preview). Confirm `LANGFUSE_TRACE_LEVEL` is unset or `graph`.
    2. SC-2 / SC-4 (Blueprint session + trace): Open a Blueprint session, send a message that clearly matches the Blueprint domain (e.g. a Hypothesis-shaped statement for the Debate Blueprint). Confirm the AI text streams into the chat token-by-token without hanging or timing out. Then open the Langfuse dashboard and confirm a trace exists for this call showing node spans (orchestrator, agent, mutationGate), the guardrail classification result, and token cost.
    3. SC-1 (no_blueprint gate): Using an existing v1-shaped session with no blueprint_id (or a test session with blueprint_id cleared), send an /invoke request. Confirm the response is HTTP 400 with body `{ "error": "no_blueprint" }` and that no Langfuse trace is created for this call (D-16 — early exits are not traced).
    4. SC-3 (abort): Send a message, then close the browser tab / abort the request mid-stream. Confirm server logs show `[ai] client_disconnected` and the Langfuse trace (if the graph had started) records the disconnect; confirm no further tokens are billed after disconnect.
    5. Optional (DOMAIN_BRIDGE steering, D-11/D-12): Send a tangential message and confirm that when steering text appears it is polite, redirective, and never mentions "blueprint", "domain", or "ontology".
  </how-to-verify>
  <verify>
    <human-check>All acceptance criteria confirmed against the running application</human-check>
  </verify>
  <acceptance_criteria>
    - A Langfuse trace for a real /invoke call is visible with orchestrator/agent/mutationGate node spans, classification result, and token cost (SC-4)
    - AI text streams token-by-token into the chat without hanging or timeout (SC-2)
    - An invoke on a blueprint-less session returns HTTP 400 `{ "error": "no_blueprint" }` and produces no trace (SC-1, D-16)
    - Client disconnect mid-stream logs `[ai] client_disconnected` and stops further token generation (SC-3)
    - No regression: the AI message appears in chat and persists on refresh
  </acceptance_criteria>
  <done>Human confirms SC-1 through SC-4 hold against the running application; no regressions in the AI chat + persistence experience.</done>
  <resume-signal>Type "approved" once all criteria are confirmed, or describe the observed issues (which criterion failed and what happened).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live client → running /invoke | Real request path exercised end-to-end; verifies the mitigations from Plans 01-02 hold at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Repudiation | Missing trace for real calls | mitigate | SC-4 checkpoint confirms every real /invoke produces a Langfuse trace; forceFlush (OBS-02) ensures traces are not lost on function exit |
| T-07-10 | Information Disclosure | Langfuse span attributes | mitigate | Verify span attributes contain only session/branch ids and model metadata — never plaintextKey or SUPABASE_DIRECT_URL (T-06-04/T-06-05 patterns) |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (D-07) |
</threat_model>

<verification>
- Human confirms all acceptance criteria against the running app
- If any criterion fails, capture the failing criterion and observed behavior for gap-closure planning
</verification>

<success_criteria>
- SC-4 confirmed: real /invoke calls produce visible Langfuse traces with node spans
- SC-1, SC-2, SC-3 confirmed against the running application (not just unit tests)
- No regression in the AI chat + persistence experience
</success_criteria>

<output>
Create `.planning/phases/07-invoke-route-modification/07-03-SUMMARY.md` when done
</output>
