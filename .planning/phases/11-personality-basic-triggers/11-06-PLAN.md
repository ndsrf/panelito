---
phase: 11-personality-basic-triggers
plan: 06
type: execute
wave: 4
depends_on: [11-02, 11-05]
files_modified:
  - apps/api/src/lib/bot-registration.ts
  - apps/api/src/lib/silence-scan.ts
  - apps/api/src/lib/silence-scan.test.ts
  - apps/api/src/server.ts
autonomous: false
requirements: [TRIGGER-01, PERSONA-01]
must_haves:
  truths:
    - "A minimal interim scan loop runs on the standalone Node server (setInterval), evaluating the silence gate per active branch (D-15)"
    - "The Coach fires at most once per cooldown window; it does not re-fire during the active cooldown (TRIGGER-01, success criterion 3)"
    - "The scan skips any session whose status is not 'active' (frozen sessions get no bot activity — Critical Failure Mode 6)"
    - "Every Coach fire passes the Phase 10 chain: checkSilenceGate → runArbitration → checkBotBudget, releasing the bot lock in a finally block"
    - "The Coach message is delivered by a direct insert into messages with a dedicated bot author identity, then broadcast fire-and-forget (D-16)"
  artifacts:
    - path: "apps/api/src/lib/silence-scan.ts"
      provides: "startSilenceScanLoop + runSilenceScan (async setInterval, awaited invoke)"
      exports: ["startSilenceScanLoop"]
    - path: "apps/api/src/lib/bot-registration.ts"
      provides: "registerBot('coach', ...) / registerBot('analyst', ...) affinity scorers"
      exports: ["registerBots"]
  key_links:
    - from: "apps/api/src/server.ts"
      to: "startSilenceScanLoop"
      via: "registration alongside startAutoFreezeTracker"
      pattern: "startSilenceScanLoop"
    - from: "apps/api/src/lib/silence-scan.ts"
      to: "messages table + graph.invoke(bot thread)"
      via: "direct insert + bot thread_id"
      pattern: "bot-|from..messages"
---

<objective>
Ship the first live trigger: a minimal single-trigger (silence-only) interim scan loop on the standalone Node server that, per active branch, runs the Phase 10 silence→arbitration→budget chain and — when all pass and the Coach bot is enabled — invokes the graph on the bot thread and inserts a content-aware Coach question directly into the messages table (D-15, D-16, TRIGGER-01).

Purpose: This is the phase's only user-observable proactive behavior — a real Coach message appearing after real silence. The full 6-trigger TriggerEngine is Phase 14; this interim loop is deliberately minimal (D-15) and expected to be generalized later.
Output: `bot-registration.ts`, `silence-scan.ts`, `silence-scan.test.ts`, and `server.ts` registration.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-personality-basic-triggers/11-CONTEXT.md
@.planning/phases/11-personality-basic-triggers/11-RESEARCH.md
@.planning/phases/11-personality-basic-triggers/11-PATTERNS.md
@.planning/phases/11-personality-basic-triggers/11-AI-SPEC.md

<interfaces>
Phase 10 primitives (reuse, do NOT rebuild — Don't Hand-Roll table):
  checkSilenceGate() — apps/api/src/lib/silence-gate.ts (two-signal gate BOT-03)
  registerBot(botId, scorer) / runArbitration() / releaseBotLock() — apps/api/src/lib/bot-arbitrator.ts (BOT-02; release in finally)
  checkBotBudget(supabase, branchId, tokensUsed) — apps/api/src/lib/bot-budget.ts (BOT-01, fail-closed)
From Plan 05: createGraph(checkpointer); routeFromStart routes triggerType 'silence_gate' -> facilitation.
From Plan 01: state.triggerType (carrier — NOT triggerMetadata); TriggerMetadataEntry { last_fired_at, cooldown_until }.
From Plan 02: sessions.bot_overrides jsonb; blueprint bot_defaults/role_personalities/bot_cooldowns; personalities table (coach_default).
Human thread_id = `${branchId}:human`; bot thread_id = `${branchId}:bot` (BOT-04 dual-thread separation, Pitfall 4).
Delivery template: apps/api/src/routes/ai.ts lines ~428-454 (insert into messages + channel.httpSend fire-and-forget).
Registration template: apps/api/src/lib/auto-freeze.ts startAutoFreezeTracker + server.ts call site.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coach/Analyst bot registration (affinity scorers)</name>
  <files>apps/api/src/lib/bot-registration.ts</files>
  <read_first>
    - apps/api/src/lib/bot-arbitrator.ts (registerBot signature, ScorerFn type, runArbitration, _registry, releaseBotLock contract)
    - .planning/phases/11-personality-basic-triggers/11-PATTERNS.md (Shared Patterns — registration at module load)
    - .planning/phases/11-personality-basic-triggers/11-RESEARCH.md (State of the Art — registry was empty in Phase 10; non-empty path now exercised)
  </read_first>
  <action>
    Create bot-registration.ts exporting registerBots() that calls registerBot('coach', coachScorer) and registerBot('analyst', analystScorer). Define affinity scorers (0-9) per BOT-02: coachScorer returns a higher affinity for silence-gate context (the only live trigger this phase), analystScorer a lower/zero affinity for silence context (Analyst has no live trigger in Phase 11 — TRIGGER-05 is Phase 12). Keep scorers pure functions of the arbitration context object that bot-arbitrator passes. Do NOT put registerBot calls inside the graph node modules (import-time side effects would interfere with graph.test.ts) — centralize them here, invoked explicitly from server.ts. Make registerBots idempotent (safe to call more than once). Document that Phase 14's TriggerEngine will generalize this.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit 2>&1 | head -20; grep -c "registerBots" src/lib/bot-registration.ts; grep -c "coach" src/lib/bot-registration.ts</automated>
  </verify>
  <acceptance_criteria>
    - bot-registration.ts exports registerBots() that registers both 'coach' and 'analyst' via registerBot
    - Scorers are pure functions returning a 0-9 affinity; coach scores higher than analyst for silence context
    - No registerBot call exists inside any graph node module (grep of src/graph/nodes returns 0)
    - pnpm exec tsc --noEmit passes
  </acceptance_criteria>
  <done>Coach and Analyst are registered with the Phase 10 arbitrator via a dedicated module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Silence-scan loop + Coach delivery</name>
  <files>apps/api/src/lib/silence-scan.ts, apps/api/src/lib/silence-scan.test.ts</files>
  <read_first>
    - apps/api/src/lib/auto-freeze.ts (startAutoFreezeTracker registration + env-var timer override pattern lines ~29-47/76-93)
    - apps/api/src/lib/silence-gate.ts, bot-arbitrator.ts, bot-budget.ts (the chain to call; release-in-finally contract)
    - apps/api/src/routes/ai.ts (message insert + httpSend broadcast, lines ~428-454 — delivery template)
    - apps/api/src/lib/sessions-helpers.ts (SYSTEM_AUTHOR_ID + system-message insert shape — for the bot-author decision)
    - apps/api/src/graph/graph.ts (createGraph + routeFromStart — from Plan 05)
    - apps/api/src/lib/bot-registration.ts (registerBots — from Task 1)
    - .planning/phases/11-personality-basic-triggers/11-AI-SPEC.md (Section 4b.2 async-setInterval-with-await; State Management bot thread_id invoke shape)
    - .planning/phases/11-personality-basic-triggers/11-RESEARCH.md (Security Domain — MUST check sessions.status==='active'; bot author_id decision A3; Pattern 4)
  </read_first>
  <behavior>
    - runSilenceScan skips a session whose status !== 'active' (no graph.invoke, no message insert) — frozen-session guard (Critical Failure Mode 6)
    - runSilenceScan skips a branch where checkSilenceGate returns passed:false (someone typing or time threshold unmet)
    - When the gate passes but the Coach lost arbitration or the budget circuit is open, no Coach message is inserted
    - When the gate passes, arbitration selects coach, and budget allows, exactly one message row is inserted with role 'assistant', display_name 'Facilitador', and the bot author identity; releaseBotLock is called in a finally block
    - The scan does not fire the Coach for a branch whose cooldown_until (triggerMetadata.silence_gate) is still in the future — at most once per cooldown window (TRIGGER-01)
    - The setInterval callback is async and awaits runSilenceScan (no overlapping ticks — Failure Mode 3)
  </behavior>
  <action>
    Create silence-scan.ts exporting startSilenceScanLoop(supabase, graph?) and runSilenceScan(supabase, graph). startSilenceScanLoop reads SCAN_INTERVAL_MS from env (default 15000, with a min-floor warn like auto-freeze), calls registerBots() once at loop start, and registers an async setInterval whose callback is `async () => { try { await runSilenceScan(...) } catch (err) { console.error('[silence-scan] uncaught error in scan tick', err) } }` — the callback MUST be async and MUST await (Failure Mode 3 / AI-SPEC 4b.2); never fire-and-forget.
    runSilenceScan: load active branches for sessions where status = 'active' (frozen-session guard — none of the Phase 10 primitives do this, RESEARCH Security Domain). Resolve Coach-enabled state per session as bot_overrides.coach ?? blueprint.bot_defaults.coach ?? false — skip if disabled (D-10). For each candidate branch: (1) checkSilenceGate() -> skip if not passed; (2) enforce per-cooldown-window using the bot thread checkpoint triggerMetadata.silence_gate.cooldown_until (skip if still in the future, TRIGGER-01); (3) runArbitration() -> proceed only if winner is 'coach'; (4) checkBotBudget() -> skip if not allowed; (5) graph.invoke with input { triggerType: 'silence_gate', messages, blueprintId, currentPhaseId, argGraph } and config.configurable { thread_id: `${branchId}:bot`, blueprint, providerName, plaintextKey, facilityPersonality: <coach_default resolved from personalities via blueprint.role_personalities.coach>, streamWriter: undefined } on the BOT thread (Pitfall 4). Wrap the fire in try/finally with releaseBotLock() in the finally.
    Delivery (D-16): take the Coach text produced by facilitationAgentNode (returned partial / accumulated text) and insert into messages (role 'assistant', display_name 'Facilitador', parent_id null, path_id/branch_id of the branch, content = text, canvas_snapshot_state null), then supabase.channel(`session:${sessionId}`).httpSend('new_message', row).catch(...) fire-and-forget. Bot author identity: DECIDE and document a dedicated bot-author strategy (Assumption A3) — reusing SYSTEM_AUTHOR_ID with role 'assistant' + display_name 'Facilitador' is acceptable; do NOT silently reuse session.creator_id. Record the cooldown by writing triggerMetadata.silence_gate.{last_fired_at, cooldown_until} into the bot thread checkpoint (via the node partial and/or graph.updateState) using the blueprint bot_cooldowns.coach window, so the next tick honors it.
    Write silence-scan.test.ts mocking SupabaseClient + a stub graph, asserting the six behaviors above (frozen-session skip, typing/gate-not-passed skip, arbitration-loss skip, budget-open skip, single-insert-on-success + release-in-finally, cooldown skip). Analog: auto-freeze.test.ts / bot-arbitrator.test.ts — mock, do not hit a real DB.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit 2>&1 | head -20; pnpm test -- silence-scan 2>&1 | tail -30; grep -c "active" src/lib/silence-scan.ts; grep -c ":bot" src/lib/silence-scan.ts; grep -c "releaseBotLock" src/lib/silence-scan.ts</automated>
  </verify>
  <acceptance_criteria>
    - silence-scan.ts exports startSilenceScanLoop and runSilenceScan; the setInterval callback is async and awaits runSilenceScan
    - runSilenceScan filters sessions by status = 'active' before any graph invoke (frozen-session guard)
    - graph.invoke uses thread_id `${branchId}:bot` (never the :human thread); releaseBotLock is called in a finally block
    - On the success path exactly one message row is inserted (role 'assistant', display_name 'Facilitador') with a bot author id that is NOT session.creator_id; a cooldown is recorded so the next tick honors TRIGGER-01
    - silence-scan.test.ts covers all six behaviors and passes; pnpm exec tsc --noEmit passes
  </acceptance_criteria>
  <done>The interim silence-scan loop runs the Phase 10 chain, invokes the bot thread, and delivers a cooldown-respecting Coach message.</done>
</task>

<task type="auto">
  <name>Task 3: Register the scan loop in server.ts</name>
  <files>apps/api/src/server.ts</files>
  <read_first>
    - apps/api/src/server.ts (full file — startAutoFreezeTracker registration call site inside serve(...) callback)
    - apps/api/src/lib/silence-scan.ts (startSilenceScanLoop — from Task 2)
    - .planning/phases/11-personality-basic-triggers/11-PATTERNS.md (server.ts section — exact sibling-registration shape)
  </read_first>
  <action>
    Add import { startSilenceScanLoop } from './lib/silence-scan' and a sibling call startSilenceScanLoop(createServiceClient()).catch((err) => console.error('[panelito/api] silence-scan startup error:', err)) inside the same serve(...) callback where startAutoFreezeTracker is registered — alongside, not replacing it. Pass a graph created via createGraph(getCheckpointer()) if startSilenceScanLoop requires one, matching how ai.ts constructs the graph+checkpointer; keep the server-boot code minimal.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit 2>&1 | head -20; grep -c "startSilenceScanLoop" src/server.ts; grep -c "startAutoFreezeTracker" src/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - server.ts imports and calls startSilenceScanLoop inside the serve(...) callback, alongside the still-present startAutoFreezeTracker call
    - The call is wrapped in .catch(...) mirroring the auto-freeze startup-error pattern
    - pnpm exec tsc --noEmit passes
  </acceptance_criteria>
  <done>The scan loop starts on server boot next to the auto-freeze tracker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live silence trigger — Coach fires once per cooldown</name>
  <files>apps/api/src/lib/silence-scan.ts</files>
  <read_first>
    - apps/api/src/lib/silence-scan.ts (the loop under verification — from Task 2)
    - .planning/phases/11-personality-basic-triggers/11-CONTEXT.md (D-13/D-14 content-aware silence message; D-16 delivery)
  </read_first>
  <what-built>The interim silence-scan loop: after N seconds of no human message with no participant typing, on an active session with the Coach enabled, a content-aware "Facilitador" question is inserted into the chat and appears via the SSE-fallback/polling path (Realtime is dead in WSL2 — D-16).</what-built>
  <action>
    Run a live session to confirm the proactive silence trigger end-to-end. Start the standalone API server (so the setInterval loop is running) and the web app, then exercise the silence and cooldown behaviors described in how-to-verify. This is a human-observed verification of behavior that cannot be fully asserted in unit tests (real timing, real Realtime/SSE-fallback delivery). Do not mark the phase complete until the observed behavior matches the acceptance criteria.
  </action>
  <how-to-verify>
    1. Start the standalone API server and the web app; open an active session using the Debate Blueprint with the Coach (Facilitador) enabled.
    2. Post one or two human messages, then stop typing and wait past the silence threshold (SCAN_INTERVAL_MS + gate threshold).
    3. Confirm a "Facilitador" message appears in the chat, ends with a question mark, and references specific recent conversation/canvas content (not a generic "¿alguien quiere agregar algo?").
    4. Keep waiting through the cooldown window — confirm the Coach does NOT fire again during the active cooldown (at most once per window, TRIGGER-01 / success criterion 3).
    5. Freeze the session (or set status != 'active') and wait — confirm NO Coach message fires on a frozen session (Critical Failure Mode 6).
    6. (Optional) Check server logs / Langfuse for the silence_gate -> facilitation route and the checkSilenceGate/runArbitration/checkBotBudget chain.
  </how-to-verify>
  <verify>
    <human-check>Coach fires once with a content-aware question after real silence, respects the cooldown window, and stays silent on a frozen session.</human-check>
  </verify>
  <acceptance_criteria>
    - A "Facilitador" message appears in chat after the silence threshold, ends with '?', and references specific recent content
    - The Coach does NOT re-fire during the active cooldown window (at most once per window)
    - No Coach message fires when the session status is not 'active'
  </acceptance_criteria>
  <done>The live silence trigger is confirmed: one content-aware Coach fire per cooldown, none on frozen sessions.</done>
  <resume-signal>Type "approved" if the Coach fired once with a content-aware question, respected the cooldown, and stayed silent on a frozen session — otherwise describe what happened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan loop → session state | Proactive fire must never act on a frozen/non-active session or bypass the budget guard |
| bot invocation → checkpointer | Bot thread writes must not corrupt the human thread checkpoint |
| bot message → chat | Bot-authored rows are inserted server-side and seen by all participants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Denial of Service | budget-guard bypass (BYOK cost) | mitigate | Every fire calls checkBotBudget() before graph.invoke; fail-closed on RPC error; asserted in silence-scan.test.ts |
| T-11-17 | Tampering | firing on a frozen session | mitigate | runSilenceScan filters sessions to status='active' before any invoke (Critical Failure Mode 6); none of the reused primitives do this, so the scan owns the check |
| T-11-18 | Tampering | checkpoint write-skew (human vs bot) | mitigate | graph.invoke uses `${branchId}:bot` thread only (BOT-04 dual-thread separation, Pitfall 4) |
| T-11-19 | Denial of Service | overlapping scan ticks re-firing during cooldown | mitigate | async setInterval with awaited runSilenceScan + cooldown_until check (Failure Mode 3); releaseBotLock in finally |
| T-11-20 | Repudiation | ambiguous bot message authorship | mitigate | Dedicated documented bot-author identity (not session.creator_id); role 'assistant' + display_name 'Facilitador' |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- pnpm test -- silence-scan passes; pnpm exec tsc --noEmit passes in apps/api.
- server.ts registers startSilenceScanLoop alongside startAutoFreezeTracker.
- Live checkpoint (Task 4) confirms one content-aware Coach fire per cooldown, no fire on frozen sessions.
</verification>

<success_criteria>
- Interim silence-scan loop fires a content-aware Coach question after real silence, at most once per cooldown, never on frozen sessions, always through the Phase 10 safety chain on the bot thread.
</success_criteria>

<output>
Create `.planning/phases/11-personality-basic-triggers/11-06-SUMMARY.md` when done.
</output>
