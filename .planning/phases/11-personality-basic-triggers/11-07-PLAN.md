---
phase: 11-personality-basic-triggers
plan: 07
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - apps/api/src/routes/bots.ts
  - apps/api/src/app.ts
  - apps/web/components/workspace/MessageBubble.tsx
  - apps/web/components/workspace/CreatorControls.tsx
autonomous: false
requirements: [PERSONA-01, PERSONA-02, PERSONA-03]
must_haves:
  truths:
    - "The chat renders Facilitador (Coach) and Analista/Verificador (Analyst) bubbles visually distinct from each other and from the existing Analista Científico (D-06)"
    - "The creator can toggle Facilitador and Analista/Verificador on/off per session from the existing 'Analistas activos' drawer (D-09)"
    - "Toggling a bot writes sessions.bot_overrides via a creator-only route (V4 access control)"
    - "The creator sees a read-only cooldown configuration line for both bots (D-12, success criterion 5)"
  artifacts:
    - path: "apps/api/src/routes/bots.ts"
      provides: "creator-only per-session bot on/off toggle writing sessions.bot_overrides"
      exports: ["botsRouter"]
    - path: "apps/web/components/workspace/MessageBubble.tsx"
      provides: "persona-keyed BOT_PERSONA_DISPLAY lookup replacing hardcoded Analista Científico values"
      contains: "BOT_PERSONA_DISPLAY"
  key_links:
    - from: "apps/web/components/workspace/CreatorControls.tsx"
      to: "/api/sessions/:id/bots"
      via: "optimistic toggle POST (mirrors handlePersonaToggle)"
      pattern: "bots|bot_overrides"
    - from: "apps/api/src/routes/bots.ts"
      to: "sessions.bot_overrides"
      via: "creator-guarded update"
      pattern: "bot_overrides|creator_id"
---

<objective>
Deliver the narrow UI surface of Phase 11: distinct chat bubbles for the two new bots, a creator-only per-session toggle for each (reusing the "Analistas activos" drawer), a read-only cooldown display (D-12), and the backing toggle route that writes sessions.bot_overrides. No personality creation/editing UI (deferred).

Purpose: These are the only two things a user actually sees from this phase (per 11-UI-SPEC.md) — the Coach/Analyst bubbles and the settings toggles + cooldown display that satisfy ROADMAP success criterion 5.
Output: `bots.ts` route + mount, generalized `MessageBubble.tsx`, extended `CreatorControls.tsx`.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-personality-basic-triggers/11-CONTEXT.md
@.planning/phases/11-personality-basic-triggers/11-UI-SPEC.md
@.planning/phases/11-personality-basic-triggers/11-PATTERNS.md
@.planning/phases/11-personality-basic-triggers/11-RESEARCH.md

<interfaces>
From Plan 02: sessions.bot_overrides jsonb; blueprint bot_defaults / bot_cooldowns (coach 3/15, analyst 2/15).
Existing route pattern: apps/api/src/routes/personas.ts (creator-only toggle; T-02-18 creator_id check — MUST copy verbatim).
Existing UI: apps/web/components/workspace/MessageBubble.tsx (isAI variant, hardcoded 'Analista Científico' / FlaskConical / 'Analista'); CreatorControls.tsx (personaCard, handlePersonaToggle optimistic+rollback, "Analistas activos" Sheet — both desktop + mobile variants).
Copy + icons (11-UI-SPEC.md): Facilitador -> MessageCircleQuestion, badge 'Facilitador'; Analista/Verificador -> SearchCheck, badge 'Verificador'; Analista Científico -> FlaskConical, badge 'Analista' (unchanged). Cooldown line: "Facilitador: máx. 3 mensajes cada 15 min · Analista/Verificador: máx. 2 mensajes cada 15 min". Font-weight: change touched author-name / card-title from font-medium (500) to font-semibold (600).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creator-only per-session bot toggle route</name>
  <files>apps/api/src/routes/bots.ts, apps/api/src/app.ts</files>
  <read_first>
    - apps/api/src/routes/personas.ts (full file — imports, requireAuth, zod body parse, creator_id 403 check, service-role update; the exact template)
    - apps/api/src/app.ts (how personasRouter is mounted — mount botsRouter the same way at the same session-scoped path)
    - .planning/phases/11-personality-basic-triggers/11-PATTERNS.md (personas.ts analog section; Creator-only guard shared pattern)
    - .planning/phases/11-personality-basic-triggers/11-RESEARCH.md (Security Domain — creator_id check is must-copy, not a design choice)
  </read_first>
  <action>
    Create apps/api/src/routes/bots.ts exporting botsRouter (Hono, requireAuth on '*'), mirroring personas.ts. POST handler body: { botId: z.enum(['coach','analyst']), active: z.boolean() } parsed via zod (400 on failure). Fetch the session (select id, creator_id, bot_overrides); 404 if missing. Copy the T-02-18 creator-only guard VERBATIM: if session.creator_id !== user.id return 403 forbidden. Compute the new bot_overrides jsonb by setting bot_overrides[botId] = active (preserving other keys), update sessions.bot_overrides via the service-role client, return 200 with the new overrides. Mount botsRouter in app.ts at the same session-scoped base path convention personasRouter uses (e.g. /api/sessions/:id/bots).
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit 2>&1 | head -20; grep -c "creator_id !== user.id\|forbidden" src/routes/bots.ts; grep -c "bot_overrides" src/routes/bots.ts; grep -c "botsRouter" src/app.ts</automated>
  </verify>
  <acceptance_criteria>
    - bots.ts exports botsRouter with requireAuth and a POST toggle handler validating { botId, active } via zod
    - The creator-only 403 guard (session.creator_id !== user.id) is present verbatim from personas.ts
    - The handler writes sessions.bot_overrides[botId] = active preserving other keys, via the service-role client
    - botsRouter is mounted in app.ts; pnpm exec tsc --noEmit passes
  </acceptance_criteria>
  <done>A creator-only route toggles per-session bot on/off state into sessions.bot_overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Persona-keyed MessageBubble lookup</name>
  <files>apps/web/components/workspace/MessageBubble.tsx</files>
  <read_first>
    - apps/web/components/workspace/MessageBubble.tsx (full isAI variant — hardcoded 'Analista Científico'/FlaskConical/'Analista'; streaming aria-label 'Analista está escribiendo...'; author-name font-medium span)
    - .planning/phases/11-personality-basic-triggers/11-UI-SPEC.md (Component Contract — Bot Persona Message Bubble table + fallback + weight-cap adjustment)
  </read_first>
  <action>
    Replace the hardcoded isAI values with a lookup BOT_PERSONA_DISPLAY: Record<string, { icon: LucideIcon; badgeLabel: string; authorName: string }> keyed by message.display_name, with three entries per the 11-UI-SPEC table: 'Analista Científico' -> FlaskConical / 'Analista' / 'Analista Científico' (unchanged content); 'Facilitador' -> MessageCircleQuestion / 'Facilitador' / 'Facilitador'; 'Analista/Verificador' -> SearchCheck / 'Verificador' / 'Analista/Verificador'. Confirm the message key field during implementation — key off message.display_name (the field the scan loop / nodes write). Fallback for an unknown display_name: Bot icon + badge label = display_name truncated to 12 chars; never crash, never empty badge. All three entries keep the exact same indigo accent styling (avatar ring, left border, badge bg/border/text) — only icon/label/name change. Generalize the streaming indicator aria-label to `${authorName} está escribiendo...`. Change the shared author-name span from font-medium to font-semibold (Typography weight-cap; single shared className, applies to all personas).
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&1 | head -20; grep -c "BOT_PERSONA_DISPLAY" components/workspace/MessageBubble.tsx; grep -c "MessageCircleQuestion\|SearchCheck" components/workspace/MessageBubble.tsx; grep -c "font-semibold" components/workspace/MessageBubble.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MessageBubble.tsx defines BOT_PERSONA_DISPLAY with the three specified entries plus a Bot fallback (display_name truncated to 12 chars)
    - Facilitador uses MessageCircleQuestion/'Facilitador'; Analista/Verificador uses SearchCheck/'Verificador'; Analista Científico content unchanged
    - The streaming aria-label is generalized to `${authorName} está escribiendo...`; the author-name span is font-semibold
    - No second accent hue introduced; pnpm exec tsc --noEmit passes
  </acceptance_criteria>
  <done>Chat bubbles render three distinct AI personas via a lookup with a safe fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Bot toggle cards + read-only cooldown display in CreatorControls</name>
  <files>apps/web/components/workspace/CreatorControls.tsx</files>
  <read_first>
    - apps/web/components/workspace/CreatorControls.tsx (personaCard JSX shape, handlePersonaToggle optimistic+rollback, the "Analistas activos" Sheet — BOTH desktop Sheet and mobile bottom-sheet variants where personaCard is duplicated)
    - apps/api/src/routes/bots.ts (the POST /bots contract from Task 1)
    - .planning/phases/11-personality-basic-triggers/11-UI-SPEC.md (Component Contract — Cooldown Configuration Display; Copywriting Contract copy + aria-labels)
  </read_first>
  <action>
    Add two new personaCard-shaped rows inside the "Analistas activos" Sheet (in BOTH the desktop and mobile variants), one for Facilitador (icon MessageCircleQuestion) and one for Analista/Verificador (icon SearchCheck), directly below the existing Analista Científico card. Same JSX shape (icon box + title + description + Switch), same indigo accent styling. Titles/descriptions from the 11-UI-SPEC Copywriting Contract: Facilitador "Guía la conversación con preguntas abiertas cuando el grupo queda en silencio."; Analista/Verificador "Cita mensajes concretos del grupo y señala afirmaciones que conviene verificar." Card titles use font-semibold (weight-cap) across all three cards. Wire each Switch to an optimistic toggle handler generalized from handlePersonaToggle (accepting botId 'coach'/'analyst' matching the bots.ts enum) that POSTs to /api/sessions/:id/bots with { botId, active }, reverts local state + toast.error on failure ("No se pudo cambiar el Facilitador. Inténtalo de nuevo." / "...el Analista/Verificador..."), aria-labels "Activar/Desactivar Facilitador" and "Activar/Desactivar Analista/Verificador". Initial checked state resolves from bot_overrides[role] ?? blueprint bot_defaults[role] ?? false (read from session/blueprint props already available). Below the three cards (and below the existing "Los cambios se aplican de inmediato..." caption) add ONE read-only text-[13px] text-muted-foreground line with the cooldown copy: "Facilitador: máx. 3 mensajes cada 15 min · Analista/Verificador: máx. 2 mensajes cada 15 min" (D-12) — plain text, no input, no edit affordance. Do not add any cooldown-editing control.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&1 | head -20; grep -c "Facilitador" components/workspace/CreatorControls.tsx; grep -c "Verificador" components/workspace/CreatorControls.tsx; grep -c "cada 15 min" components/workspace/CreatorControls.tsx; grep -c "/bots" components/workspace/CreatorControls.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Two new toggle cards (Facilitador, Analista/Verificador) exist in BOTH Sheet variants, below the Analista Científico card, with the specified icons/titles/descriptions and font-semibold titles
    - Each toggle POSTs to /api/sessions/:id/bots with optimistic update + toast.error revert; aria-labels and error copy match the Copywriting Contract
    - Initial checked state derives from bot_overrides[role] ?? bot_defaults[role] ?? false
    - One read-only cooldown caption line renders the exact D-12 copy; no cooldown-editing control exists
    - pnpm exec tsc --noEmit passes
  </acceptance_criteria>
  <done>The creator can toggle both bots and see their read-only cooldown config in the existing drawer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification — bubbles, toggles, cooldown display</name>
  <files>apps/web/components/workspace/MessageBubble.tsx, apps/web/components/workspace/CreatorControls.tsx</files>
  <read_first>
    - apps/web/components/workspace/MessageBubble.tsx (the persona lookup from Task 2)
    - apps/web/components/workspace/CreatorControls.tsx (the toggle cards + cooldown line from Task 3)
    - .planning/phases/11-personality-basic-triggers/11-UI-SPEC.md (the visual contract being verified)
  </read_first>
  <what-built>Three distinct AI persona bubbles in chat (Analista Científico / Facilitador / Analista/Verificador), two new creator toggles in the "Analistas activos" drawer, and a read-only cooldown caption — all reusing the existing indigo single-accent visual language.</what-built>
  <action>
    Run the web app and visually verify the Phase 11 UI surface end-to-end (bubbles, toggles, cooldown copy, creator-only enforcement) using the steps in how-to-verify. This human-observed check confirms visual distinctness and interaction behavior that static assertions cannot fully cover. Do not mark the plan complete until all six checks pass.
  </action>
  <how-to-verify>
    1. Open a session as the creator; open the "Analistas activos" drawer (both on desktop and on a mobile viewport / bottom sheet).
    2. Confirm three cards render: Analista Científico (FlaskConical), Facilitador (MessageCircleQuestion), Analista/Verificador (SearchCheck), each with the specified Spanish description and a Switch.
    3. Toggle Facilitador off then on — confirm optimistic update and no error; force a failure (offline) and confirm the revert + Spanish toast.
    4. Confirm the read-only cooldown line reads exactly: "Facilitador: máx. 3 mensajes cada 15 min · Analista/Verificador: máx. 2 mensajes cada 15 min" and has no edit affordance.
    5. In chat, confirm a Facilitador message (from Plan 06's live trigger, or a seeded test message with display_name 'Facilitador') renders with the MessageCircleQuestion icon + 'Facilitador' badge, visually distinct from an 'Analista Científico' bubble, using the same indigo accent (no new hue).
    6. Confirm a non-creator user does NOT see the toggles act (route returns 403) — bots stay unchanged.
  </how-to-verify>
  <verify>
    <human-check>Three personas render distinctly; toggles work with optimistic+revert; the cooldown line matches exactly; non-creators are blocked.</human-check>
  </verify>
  <acceptance_criteria>
    - All three persona bubbles render distinctly with the specified icons/badges and a single indigo accent
    - Both toggles perform optimistic updates with revert-on-failure and Spanish toasts
    - The cooldown caption matches the D-12 copy exactly and is read-only
    - A non-creator cannot change bot state (403)
  </acceptance_criteria>
  <done>The Phase 11 UI surface is visually confirmed: distinct bubbles, working creator-only toggles, exact read-only cooldown display.</done>
  <resume-signal>Type "approved" if all three personas render distinctly, toggles work with optimistic+revert, the cooldown line matches exactly, and non-creators are blocked — otherwise describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client toggle → bots route | Only the session creator may change per-session bot state |
| bot message content → rendering | display_name drives icon/label; unknown values must not crash the bubble |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-21 | Elevation of Privilege | bots toggle route | mitigate | requireAuth + verbatim creator_id !== user.id -> 403 guard copied from personas.ts (must-copy per RESEARCH Security Domain) |
| T-11-22 | Input Validation | toggle body | mitigate | zod parse of { botId: enum, active: boolean } with 400 on failure before any DB write |
| T-11-23 | Denial of Service | MessageBubble unknown display_name | mitigate | Fallback to Bot icon + 12-char-truncated badge; never crashes or renders empty |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs; all shadcn components already present (11-UI-SPEC Registry Safety) |
</threat_model>

<verification>
- pnpm exec tsc --noEmit passes in apps/api and apps/web.
- Creator-only guard present in bots.ts; botsRouter mounted.
- Live checkpoint (Task 4) confirms distinct bubbles, working toggles, exact cooldown copy, and creator-only enforcement.
</verification>

<success_criteria>
- Three distinct AI persona bubbles; two working creator toggles writing bot_overrides via a creator-only route; read-only cooldown display satisfying success criterion 5.
</success_criteria>

<output>
Create `.planning/phases/11-personality-basic-triggers/11-07-SUMMARY.md` when done.
</output>
