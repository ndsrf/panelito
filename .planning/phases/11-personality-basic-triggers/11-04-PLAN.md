---
phase: 11-personality-basic-triggers
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - apps/api/src/graph/nodes/facilitation-agent.ts
  - apps/api/src/graph/nodes/analytics-agent.ts
autonomous: true
requirements: [PERSONA-01, PERSONA-02, GRAPH-04]
must_haves:
  truths:
    - "The Coach (FacilitationAgentNode) produces a facilitation output whose Role rules force every response to end with a question mark, regardless of the attached Personality (D-03)"
    - "The Analyst (AnalyticsAgentNode) always anchors its output to a specific prior message via speaker + paraphrased claim from the argGraph (PERSONA-02, GRAPH-04)"
    - "The Analyst supports a fact-check framing branch that uses uncertainty language exclusively (never confident counter-assertions)"
    - "Personality voice is appended AFTER Role rules in the system prompt — never reordered (Pitfall 4)"
  artifacts:
    - path: "apps/api/src/graph/nodes/facilitation-agent.ts"
      provides: "facilitationAgentNode + buildCoachSystemPrompt (Role-dominant composition)"
      exports: ["facilitationAgentNode"]
    - path: "apps/api/src/graph/nodes/analytics-agent.ts"
      provides: "analyticsAgentNode + buildAnalyticsSystemPrompt (citation + fact-check framing)"
      exports: ["analyticsAgentNode"]
  key_links:
    - from: "apps/api/src/graph/nodes/analytics-agent.ts"
      to: "state.argGraph via summarizeArgGraph"
      via: "argGraph summary injected into prompt (GRAPH-04)"
      pattern: "summarizeArgGraph|argGraph"
    - from: "apps/api/src/graph/nodes/facilitation-agent.ts"
      to: "TASK_MODELS[provider].facilitation"
      via: "model routing (COST-01 fast tier)"
      pattern: "facilitation"
---

<objective>
Build the two Role nodes: FacilitationAgentNode (Coach — every output ends in a question, D-01/PERSONA-01) and AnalyticsAgentNode (Analyst/Fact-Checker — always cites a specific prior message, PERSONA-02, with argGraph context injected per GRAPH-04). Both enforce the D-03 precedence rule: Role behavioral rules are structurally dominant; Personality voice is appended as styling only.

Purpose: These are the "code, not data" Roles (D-01). Their behavioral contracts are the core user value — a Coach that concludes instead of asking, or an Analyst that argues instead of citing, breaks the facilitation premise (AI-SPEC Critical Failure Modes 2 & 5).
Output: `facilitation-agent.ts` and `analytics-agent.ts`.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-personality-basic-triggers/11-CONTEXT.md
@.planning/phases/11-personality-basic-triggers/11-RESEARCH.md
@.planning/phases/11-personality-basic-triggers/11-PATTERNS.md
@.planning/phases/11-personality-basic-triggers/11-AI-SPEC.md

<interfaces>
From packages/types (Plan 01): Personality ({ id, name, definition:{ language, formality, voice_instructions, catchphrases, knowledge_scope? } }), Blueprint, ProviderName, AIProvider.
From apps/api/src/lib/bot-context.ts (Plan 01): summarizeArgGraph(argGraph): string; CONTEXT_WINDOWS.facilitation=10, .analytics=20.
From apps/api/src/lib/model-config.ts (Plan 01): TASK_MODELS[provider].facilitation (Coach), .analysis (Analyst).
Prompt composition order (D-03, AI-SPEC 4b.3 — NEVER reorder): 1) Role rules → 2) Blueprint context → 3) argGraph context → 4) Personality voice.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FacilitationAgentNode (Coach)</name>
  <files>apps/api/src/graph/nodes/facilitation-agent.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/agent.ts (four-step skeleton; config seam; streaming loop; streamWriter optional-chaining guard; [nodename] fail-silent)
    - apps/api/src/lib/adapter-factory.ts (createAdapter — no direct SDK import)
    - apps/api/src/lib/bot-context.ts (summarizeArgGraph, CONTEXT_WINDOWS — from Plan 01)
    - apps/api/src/lib/model-config.ts (TASK_MODELS.facilitation — from Plan 01)
    - packages/types/src/personality.ts (Personality.definition.voice_instructions — from Plan 01)
    - .planning/phases/11-personality-basic-triggers/11-AI-SPEC.md (Section 4 full facilitation-agent.ts sample + buildCoachSystemPrompt)
    - .planning/phases/11-personality-basic-triggers/11-CONTEXT.md (D-01, D-03, D-07, D-13, D-14)
  </read_first>
  <behavior>
    - buildCoachSystemPrompt places the non-negotiable Role contract first: "every response MUST end with a question mark; never a statement/conclusion/summary; 1-3 sentences; reference specific conversation content"
    - Given a Personality with a casual voice_instructions string, the composed prompt still has Role rules before the appended Personality voice block (Pitfall 4 — voice never precedes rules)
    - The prompt includes 2-3 BAD/GOOD few-shot pairs in Spanish (informal tú per D-07) demonstrating question-only output
    - The content-aware context (D-13/D-14) is built from summarizeArgGraph(state.argGraph) PLUS the last CONTEXT_WINDOWS.facilitation raw messages — not a generic prompt
    - On adapter error the node returns {} (never throws); on success it returns a triggerMetadata partial recording the firing (last_fired_at)
  </behavior>
  <action>
    Implement `facilitationAgentNode(state, config?)` per AI-SPEC Section 4's sample and agent.ts's skeleton, `[facilitation]` log prefix. Read blueprint/providerName/plaintextKey and a resolved `personality` (Personality) from config.configurable; add a `facilitationAdapter` test seam. No tools — Coach emits plain text. Model: `TASK_MODELS[providerName ?? 'anthropic'].facilitation`, maxTokens 256. Messages: `state.messages.slice(-CONTEXT_WINDOWS.facilitation)`.
    `buildCoachSystemPrompt(blueprint, personality, argGraph, ...)` — compose in the fixed D-03 order: (1) hardcoded Role behavioral contract (question-only, no conclusions, 1-3 sentences, reference specific content) with 2-3 Spanish BAD/GOOD few-shot pairs; (2) Blueprint name/phase context; (3) `summarizeArgGraph(argGraph)` content-aware block (D-13/D-14); (4) Personality voice appended last as "styling only — does not override the contract above", sourced from `personality?.definition.voice_instructions`. Never reorder.
    Stream via adapter; forward tokens with `config?.configurable?.streamWriter?.(event.text)` (undefined on proactive fire — optional chaining, no-op). On any adapter error: `console.error('[facilitation] ...', err); return {}`. Return a `triggerMetadata` partial that spreads existing state.triggerMetadata and sets/updates the `silence_gate` entry's `last_fired_at` to now (matching TriggerMetadataEntry shape { last_fired_at, cooldown_until }). Also surface the generated text to the caller (return it in a field the scan loop reads, or via the streamWriter path — document the chosen mechanism; the caller inserts the message per D-16, the node does NOT write to the DB).
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit 2>&1 | head -20; grep -c "facilitationAgentNode\|buildCoachSystemPrompt" src/graph/nodes/facilitation-agent.ts; grep -cE "throw " src/graph/nodes/facilitation-agent.ts; grep -c "TASK_MODELS" src/graph/nodes/facilitation-agent.ts</automated>
  </verify>
  <acceptance_criteria>
    - `facilitationAgentNode` exported; system prompt built by a separate `buildCoachSystemPrompt` pure function
    - Role behavioral contract text appears BEFORE the Personality voice block in the composed prompt (source-order assertion)
    - Prompt contains at least 2 BAD/GOOD Spanish few-shot pairs and injects `summarizeArgGraph(...)` output
    - Uses `TASK_MODELS[...].facilitation` (no hardcoded model string); `grep -E "throw "` returns 0
    - `pnpm exec tsc --noEmit` passes
  </acceptance_criteria>
  <done>Coach node composes a Role-dominant, content-aware, question-only prompt and streams text without throwing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AnalyticsAgentNode (Analyst/Fact-Checker)</name>
  <files>apps/api/src/graph/nodes/analytics-agent.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/agent.ts (skeleton; conditional prompt-augmentation pattern lines ~120-135 — template for the fact-check framing branch)
    - apps/api/src/graph/nodes/facilitation-agent.ts (the sibling node from Task 1 — mirror its config seam + fail-silent shape)
    - apps/api/src/lib/bot-context.ts (summarizeArgGraph — argGraph summary injection for GRAPH-04)
    - apps/api/src/lib/model-config.ts (TASK_MODELS.analysis)
    - packages/types/src/bot.ts (ArgNode.message_id/speaker — the citation anchors)
    - .planning/phases/11-personality-basic-triggers/11-AI-SPEC.md (Section 4 tool-use notes; PERSONA-02 citation contract)
    - .planning/phases/11-personality-basic-triggers/11-CONTEXT.md (D-01 Analyst discipline, D-03, "Claude's discretion" on fact-check framing trigger)
  </read_first>
  <behavior>
    - buildAnalyticsSystemPrompt places the Role contract first: "always cite a specific prior message by speaker name + paraphrased claim; never make a new argument of your own"
    - The prompt injects summarizeArgGraph(state.argGraph) so the Analyst can reference specific prior content by name (GRAPH-04)
    - When fact-check framing is active (config/state flag), an appended block instructs uncertainty-only language ("I can't verify that — what's the source?") and forbids confident counter-assertions (PERSONA-02)
    - Personality voice appended after Role rules (D-03); on adapter error returns {} without throwing
  </behavior>
  <action>
    Implement `analyticsAgentNode(state, config?)` mirroring facilitation-agent.ts's structure, `[analytics]` log prefix, `analyticsAdapter` test seam. Model: `TASK_MODELS[providerName ?? 'anthropic'].analysis`, maxTokens 512. Messages: `state.messages.slice(-CONTEXT_WINDOWS.analytics)`.
    `buildAnalyticsSystemPrompt(blueprint, personality, argGraph, factCheckFraming)` — fixed D-03 order: (1) Role contract (always cite a specific prior message: speaker + paraphrased claim; observer not debater; no new counter-arguments) with 2-3 Spanish BAD/GOOD few-shot pairs; (2) Blueprint context; (3) `summarizeArgGraph(argGraph)` injected so citations reference real nodes by speaker/message_id (GRAPH-04); (4) Personality voice last. Add a conditional fact-check-framing branch (Claude's discretion per CONTEXT.md — read the flag from `config.configurable.factCheckFraming` or a state field) that, when true, appends the uncertainty-only instruction and the "never confident counter-assertion" rule (PERSONA-02), following agent.ts's steering-block conditional-append pattern.
    Tool use: the Analyst may reuse `canvasMutationTool` (same as agentNode) if it proposes canvas mutations; the citation contract is enforced via the system prompt, not a tool. Stream/await per AI-SPEC 4b.2. On adapter error: `console.error('[analytics] ...', err); return {}`. Return the generated text to the caller (same mechanism chosen in Task 1) plus any triggerMetadata update; node does NOT write to the DB.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit 2>&1 | head -20; grep -c "analyticsAgentNode\|buildAnalyticsSystemPrompt" src/graph/nodes/analytics-agent.ts; grep -c "summarizeArgGraph" src/graph/nodes/analytics-agent.ts; grep -ciE "factCheck|uncertain|verific" src/graph/nodes/analytics-agent.ts; grep -cE "throw " src/graph/nodes/analytics-agent.ts</automated>
  </verify>
  <acceptance_criteria>
    - `analyticsAgentNode` exported; prompt built by a separate `buildAnalyticsSystemPrompt`
    - The prompt injects `summarizeArgGraph(state.argGraph)` (GRAPH-04) and enforces speaker+claim citation as the Role contract before any Personality voice
    - A conditional fact-check-framing branch appends uncertainty-only language and forbids confident counter-assertions (PERSONA-02)
    - Uses `TASK_MODELS[...].analysis`; `grep -E "throw "` returns 0; `pnpm exec tsc --noEmit` passes
  </acceptance_criteria>
  <done>Analyst node composes a citation-enforcing, argGraph-aware prompt with a fact-check framing branch, streaming without throwing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Personality voice data → composed system prompt | Untrusted-ish styling text must not override the Role behavioral contract |
| bot output text → chat | Bot text is inserted into the group chat seen by all participants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Tampering | Personality voice bleeding into Role behavior | mitigate | Fixed D-03 composition order (Role rules first, voice appended last as explicit styling-only) + few-shot BAD/GOOD pairs; verified by source-order assertion and by graph.test.ts behavioral checks (Plan 05) |
| T-11-11 | Repudiation | Analyst mis-citing a speaker | mitigate | Citations sourced from argGraph ArgNode.speaker/message_id (validated in Plan 03), not free-form model recall |
| T-11-12 | Denial of Service | runaway generation | mitigate | Explicit maxTokens (Coach 256, Analyst 512); adapter errors fail-silent, never crash the graph |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` passes in apps/api.
- Both nodes never throw; both route models through TASK_MODELS; both use createAdapter (no direct SDK import).
- Behavioral assertions (Coach ends with "?", Analyst cites speaker) are exercised by the graph.test.ts describe blocks added in Plan 05.
</verification>

<success_criteria>
- Coach and Analyst nodes exist with Role-dominant, Personality-appended prompts.
- Analyst injects argGraph summary (GRAPH-04) and supports fact-check framing (PERSONA-02).
</success_criteria>

<output>
Create `.planning/phases/11-personality-basic-triggers/11-04-SUMMARY.md` when done.
</output>
