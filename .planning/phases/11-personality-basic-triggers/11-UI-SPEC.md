---
phase: 11
slug: personality-basic-triggers
status: draft
shadcn_initialized: true
preset: none
created: 2026-07-10
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Scope Note

This phase is primarily backend/AI (LangGraph routing, argGraph builder, silence-window trigger, cooldown enforcement — see `11-AI-SPEC.md` and `11-RESEARCH.md`). The UI surface is deliberately narrow. Per `11-CONTEXT.md` this phase explicitly does **not** build a personality creation/editing UI (deferred). The only two things a user actually *sees* as a result of this phase:

1. **Chat panel** — a new kind of AI-authored message bubble appears: the Coach ("Facilitador") firing a silence-break question, and (once the `analysis` routing path is exercised) the Analyst/Fact-Checker ("Analista/Verificador"). These must be visually distinguishable from each other and from the existing reactive "Analista Científico" persona (D-06 — untouched, old system).
2. **Session settings** — the Creator can see the two new bots (Facilitador, Analista/Verificador) toggle on/off, and see their cooldown configuration as a **read-only** display (D-12; ROADMAP success criterion 5). No editable cooldown UI in this phase.

Everything else in the standard design-contract template (empty states, destructive-action flows, new page layouts) is explicitly **not applicable** to this phase and is marked as such below rather than invented.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `apps/web/components.json` present, verified via `npx shadcn info`) |
| Preset | none (manually configured, not a `ui.shadcn.com/create` preset string) — style `new-york`, base `radix` |
| Component library | Radix primitives via shadcn (`Badge`, `Sheet`, `Switch`, `Button` — all already installed, no new components needed this phase) |
| Icon library | lucide-react |
| Font | Inter (`next/font/google`, `apps/web/app/layout.tsx`) — unchanged, not touched this phase |

No new shadcn components are required. No third-party registries are used — see Registry Safety below.

---

## Existing Visual Language (pre-populated from codebase — do not re-derive)

Source: `apps/web/app/globals.css`, `apps/web/components/workspace/MessageBubble.tsx`, `apps/web/components/workspace/CreatorControls.tsx` (read in full this session).

- **Theme:** Zinc dark ("war room aesthetic"). `--color-background` zinc-950, `--color-card` zinc-900, `--color-muted` zinc-800, `--color-border` zinc-700.
- **Single accent hue:** Indigo (`--color-primary` / `--color-accent`, `#6366f1` family) is the project's one reserved accent color, already used exclusively for: AI/bot message left-border + avatar ring, the AI persona badge, the "Advance Phase" primary button, and branch-navigator gradient start color.
- **Existing AI-message pattern (`MessageBubble.tsx`, `isAI` variant):** 32px circular avatar (Indigo-tinted `rgba(99,102,241,0.20)` bg, `1px solid #6366f1` border, `Bot` lucide icon in `#818cf8`), author name row + a small `Badge` (indigo-tinted, `rgba(99,102,241,0.12)` bg, `1px solid rgba(99,102,241,0.30)` border, `#a5b4fc` text) containing a persona icon + short label, bubble body with `bg-card`, `rounded-lg rounded-tl-none`, `2px solid #818cf8` left border, `max-w-[90%]`.
- **Existing settings-drawer pattern (`CreatorControls.tsx` "Analistas activos" Sheet, `personaCard`):** a `flex items-center justify-between p-4 rounded-lg border bg-card` row containing a 40×40px icon box (indigo-tinted bg/border, opacity 60% when off / 100% when on) + title (`text-[15px] font-medium`) + description (`text-[13px] text-muted-foreground line-clamp-2`) + a shadcn `Switch` on the right, wired to a POST toggle route with optimistic local state and a `toast.error` revert on failure.

**Phase 11 reuses both patterns verbatim** — it does not introduce a new visual system. The net-new work is (a) generalizing the hardcoded "Analista Científico" values in `MessageBubble.tsx` into a persona-keyed lookup, and (b) adding two more `personaCard`-shaped rows + a read-only cooldown caption to the existing Sheet.

---

## Spacing Scale

Declared values (must be multiples of 4) — unchanged from existing project convention, no new scale introduced:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps (badge internals, `gap-1`) |
| sm | 8px | Compact element spacing (`gap-2`, badge padding) |
| md | 16px | Default element spacing (`p-4` persona card, `gap-3` avatar-to-content) |
| lg | 24px | Section padding between Sheet content blocks |
| xl | 32px | Not used in this phase's surface |
| 2xl | 48px | Not used in this phase's surface |
| 3xl | 64px | Not used in this phase's surface |

Exceptions: none. Message bubble touch target keeps its existing `minHeight: 44px` (icon-only touch-target exception, already established in `MessageBubble.tsx` — do not change).

---

## Typography

Unchanged from existing project convention — no new sizes/weights introduced by this phase.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 15px | 400 (message content) / 500 (author name) | 1.5 |
| Label | 13px | 400 | 1.4 (badge text, timestamps, cooldown caption, card descriptions) |
| Heading | 16px (browser default, `SheetTitle` sets no explicit size) | 600 (`font-semibold`) | 1.2 ("Analistas activos" Sheet title — unchanged, not introduced by this phase) |
| Display | n/a | n/a | n/a — not used anywhere in this phase's UI surface |

---

## Color

Unchanged from existing project convention — no new hue introduced. Phase 11 distinguishes the three AI personas (Analista Científico / Facilitador / Analista-Verificador) through **icon + badge label only**, not through additional accent colors, to keep the accent budget at a single reserved hue project-wide.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `oklch(9.4% 0.005 286)` zinc-950 | App background |
| Secondary (30%) | `oklch(13.7% 0.005 286)` zinc-900 (card) / `oklch(18.7% 0.005 286)` zinc-800 (muted) | Message cards, Sheet drawer background, persona-card icon boxes |
| Accent (10%) | `#6366f1` / `#818cf8` (indigo, `--color-primary` / `--color-accent` family) | AI message avatar ring + left border + badge (ALL bot personas: Analista Científico, Facilitador, Analista/Verificador); the two new toggle-card icon boxes in the "Analistas activos" Sheet; existing "Advance Phase" button (unchanged) |
| Destructive | `oklch(62.7% 0.209 27)` red-500 (`--color-destructive`) | Not used by this phase's UI surface (no destructive actions introduced — see Copywriting Contract) |

Accent reserved for: the bot-message avatar/border/badge treatment (all three personas, applied identically) and the corresponding toggle-card icon boxes in session settings. Not extended to any other new element in this phase.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | No new button-style CTA. The interaction is a `Switch` toggle, mirroring the existing Analista Científico pattern: `aria-label="Activar Facilitador"` / `"Desactivar Facilitador"` and `aria-label="Activar Analista/Verificador"` / `"Desactivar Analista/Verificador"` |
| Empty state heading | Not applicable — this phase introduces no new empty-data view. The Coach's silence-break message is AI-generated content governed by `11-CONTEXT.md` D-13/D-14 (content-aware, not a static UI empty-state string) |
| Empty state body | Not applicable (see above) |
| Error state | Toggle failure (mirrors existing `handlePersonaToggle` catch block verbatim): `"No se pudo cambiar el Facilitador. Inténtalo de nuevo."` / `"No se pudo cambiar el Analista/Verificador. Inténtalo de nuevo."` — `toast.error`, local switch state reverts optimistically |
| Destructive confirmation | None. Toggling a bot on/off is non-destructive — no `AlertDialog` required, consistent with the existing Analista Científico toggle (which also has no confirmation step) |

**Additional copy needed for this phase (not covered by the generic template rows above):**

| Element | Copy | Source |
|---------|------|--------|
| Facilitador card title | `Facilitador` | D-08 (locked) |
| Facilitador card description | `Guía la conversación con preguntas abiertas cuando el grupo queda en silencio.` | Claude's discretion per D-08/CONTEXT.md — describes the Coach role in the same register as the existing "Analiza datos, detecta falacias..." description |
| Analista/Verificador card title | `Analista/Verificador` | D-08 (locked) |
| Analista/Verificador card description | `Cita mensajes concretos del grupo y señala afirmaciones que conviene verificar.` | Claude's discretion — describes AnalyticsAgentNode's citation + uncertainty-framing behavior (PERSONA-02) |
| Cooldown caption (read-only, D-12) | `Facilitador: máx. 3 mensajes cada 15 min · Analista/Verificador: máx. 2 mensajes cada 15 min` | Values from PERSONA-03 defaults (Coach 3/15min, Analyst 2/15min); render as a single `text-[13px] text-muted-foreground` line below the two toggle cards, same visual weight as the existing "Los cambios se aplican de inmediato..." caption |
| Bot message badge labels (chat panel) | `Facilitador` (Coach) / `Verificador` (Analyst/Fact-Checker) / `Analista` (existing, unchanged) | Short badge form of the full display name, matching the existing "Analista" badge precedent (full name "Analista Científico" → badge "Analista") |

---

## Component Contract — Bot Persona Message Bubble (`MessageBubble.tsx`)

**Problem being fixed:** `MessageBubble.tsx`'s `isAI` variant currently hardcodes the author name (`'Analista Científico'`), the badge icon (`FlaskConical`), and the badge label (`'Analista'`) regardless of which persona actually authored the message. This phase introduces two more AI-authored personas (Facilitador, Analista/Verificador) that must render distinctly. The executor must generalize this into a persona-keyed lookup, not add another hardcoded branch.

**Contract:** Add a small lookup (e.g. `BOT_PERSONA_DISPLAY: Record<string, { icon: LucideIcon; badgeLabel: string }>`), keyed by `message.display_name` (or a dedicated persona-id field if the backend adds one — confirm exact key during planning against whatever field `ArgGraphBuilderNode`/`facilitationAgentNode`/`analyticsAgentNode` actually write to the inserted message row). All three entries share the exact same indigo accent styling (avatar ring, left border, badge background/border/text colors) already defined in `MessageBubble.tsx` — only the icon and label change per persona.

| Persona (message.display_name) | Icon (lucide-react) | Badge label | Author name shown | Notes |
|---|---|---|---|---|
| `Analista Científico` | `FlaskConical` (unchanged) | `Analista` (unchanged) | `Analista Científico` | Existing reactive persona (D-06) — untouched, must keep rendering exactly as today |
| `Facilitador` | `MessageCircleQuestion` | `Facilitador` | `Facilitador` | New — Coach role (FacilitationAgentNode); icon chosen to echo the "always ends in a question" behavioral contract (PERSONA-01) |
| `Analista/Verificador` | `SearchCheck` | `Verificador` | `Analista/Verificador` | New — Analyst/Fact-Checker role (AnalyticsAgentNode); icon chosen to echo the citation + verification behavioral contract (PERSONA-02) |

Fallback: if `message.display_name` matches none of the above (future persona not yet in this table), default to the existing `Bot` icon and a badge label equal to `message.display_name` truncated to 12 characters — never crash, never render an empty badge.

**Streaming indicator (`isStreaming`, no `streamingText` yet):** the existing three-bounce-dot indicator's `aria-label` currently hardcodes `"Analista está escribiendo..."`. Generalize to `` `${authorName} está escribiendo...` `` using the same lookup's `authorName`.

**D-16 note:** proactive Coach messages (silence trigger) are inserted directly into the `messages` table with no active SSE stream — they will render with `isStreaming: false` and `message.content` already populated (never the streaming-dots path). No new streaming UI state is needed for the proactive path; this is a data-flow fact for the executor, not a new visual contract.

---

## Component Contract — Cooldown Configuration Display (`CreatorControls.tsx`)

**Location:** the existing "Analistas activos" `Sheet` (both desktop `Sheet side="right"` and mobile bottom-sheet variants — the `personaCard` block is already duplicated between the two, per existing code structure).

**Contract:**
1. Add two new `personaCard`-shaped rows, one for Facilitador and one for Analista/Verificador, directly below the existing Analista Científico `personaCard`. Same exact JSX shape (icon box + title + description + `Switch`), same indigo accent styling, only icon/title/description differ per the table above. Checked-state and toggle handler follow the exact same optimistic-update + revert-on-error pattern as `handlePersonaToggle`, generalized to accept a persona id (`facilitador` / `analista_verificador`) instead of being hardcoded to `analista_cientifico`.
2. Below the three toggle cards (and below the existing "Los cambios se aplican de inmediato a los mensajes siguientes." caption), add one new read-only caption line rendering the cooldown copy from the Copywriting Contract table above. This is plain text — `text-[13px] text-muted-foreground` — no input, no edit affordance, no icon. This satisfies D-12 and ROADMAP Phase 11 success criterion 5 ("a Creator can see the cooldown configuration in session settings") at the minimum UI cost the decision calls for.
3. Do not add any control that writes a new cooldown value anywhere in the UI this phase — the values displayed come from the Blueprint/session config read path (`bot_defaults`/`bot_cooldowns`-equivalent per `11-RESEARCH.md` Finding 5), not from a form.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `badge`, `sheet`, `switch`, `button` (all already installed in `apps/web/components/ui/`, no new install needed) | not required |
| third-party | none declared | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
