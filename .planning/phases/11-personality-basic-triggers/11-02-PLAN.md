---
phase: 11-personality-basic-triggers
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - supabase/migrations/0014_personalities.sql
autonomous: false
requirements: [PERSONA-01, PERSONA-02, PERSONA-03]
must_haves:
  truths:
    - "A personalities table exists in Supabase with RLS (authenticated SELECT, service-role-only writes)"
    - "Three personalities ship seeded: a default Coach voice (Facilitador), a default Analyst voice (Analista/Verificador), and the migrated Analista Científico voice (D-06 — data only, invocation path untouched)"
    - "The debate-strategy-v1 Blueprint carries explicit bot_defaults, role_personalities, and bot_cooldowns in its definition jsonb"
    - "Sessions can store a per-session bot on/off override distinct from active_personas (D-06)"
    - "The live database reflects all schema changes after a push (not just config-derived types)"
  artifacts:
    - path: "supabase/migrations/0014_personalities.sql"
      provides: "personalities table + RLS + 3 seed rows + blueprint definition UPDATE + sessions.bot_overrides column"
      contains: "create table public.personalities"
  key_links:
    - from: "supabase/migrations/0014_personalities.sql"
      to: "domain_blueprints.definition (debate-strategy-v1)"
      via: "UPDATE ... SET definition = definition || jsonb"
      pattern: "bot_defaults|role_personalities"
---

<objective>
Create migration 0014: the personalities table (D-04), the shipped personalities — default Coach voice, default Analyst voice, and the migrated Analista Científico voice (D-06) — the Blueprint definition extension (bot_defaults + role_personalities + bot_cooldowns), and a per-session bot-override column, then push the schema so the live database matches.

Purpose: D-04 requires personalities in a real table (not a hardcoded TS list) so a future management feature needs no later migration; D-06 requires the existing Analista Científico persona's voice DATA to move onto the new model now (its invocation path stays on the old system); D-10 requires bot on/off defaults to actually live in the Blueprint DB row, not a hardcoded fallback (Finding 5).
Output: `supabase/migrations/0014_personalities.sql` applied to the live database.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-personality-basic-triggers/11-CONTEXT.md
@.planning/phases/11-personality-basic-triggers/11-RESEARCH.md
@.planning/phases/11-personality-basic-triggers/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0014 — personalities table, seeds, blueprint + session extensions</name>
  <files>supabase/migrations/0014_personalities.sql</files>
  <read_first>
    - supabase/migrations/0008_nsai_foundation.sql (domain_blueprints table shape lines ~33-46; debate-strategy-v1 seed INSERT line ~222 — the closest analog per D-04)
    - supabase/migrations/0012_bot_infrastructure.sql (header-comment + trust-boundary convention, COMMENT ON TABLE habit, service-role-only RLS)
    - supabase/migrations/0005_reactions_personas.sql (sessions.active_personas text[] column — the OLD system; the new bot override must be DISTINCT per D-06)
    - packages/types/src/persona.ts (PERSONA_LIBRARY 'analista_cientifico' entry — its displayName/description/systemPromptAddition are the source voice DATA to migrate per D-06)
    - packages/types/src/personality.ts (Personality.definition jsonb shape from Plan 01 — seed rows must match it)
    - packages/types/src/blueprint.ts (bot_defaults / role_personalities / bot_cooldowns field shapes from Plan 01 — the UPDATE must match)
  </read_first>
  <action>
    IMMEDIATELY re-verify migration numbering with `ls supabase/migrations/ | sort | tail -3` — use the next integer after the highest on disk (expected 0014 per Finding 3, but re-confirm; an intervening merge can change it).
    Write the migration with a header comment block (purpose, design rationale, trust boundary) matching 0012's convention. Contents:
    (a) `create table public.personalities ( id text primary key, name text not null, definition jsonb not null, created_at timestamptz not null default now() )` — exact shape of domain_blueprints per D-04. Add `comment on table public.personalities is '...'`. Enable RLS; add a single `personalities_select` policy `for select using (auth.uid() is not null)`; NO insert/update/delete policy (writes are service-role-only, matching Phase 11's no-editor-UI scope).
    (b) Seed THREE rows via `insert into public.personalities (id, name, definition) values (...)`: (1) id `coach_default` name `Facilitador`; (2) id `analyst_default` name `Analista/Verificador`; (3) id `analista_cientifico` name `Analista Científico` — the D-06 migration of the EXISTING reactive persona's VOICE DATA (source its voice_instructions/knowledge_scope from packages/types/src/persona.ts PERSONA_LIBRARY 'analista_cientifico' displayName/description/systemPromptAddition; move only the voice representation — do NOT touch its invocation path). Each definition jsonb must match packages/types Personality.definition shape with `language: 'es'`, `formality: 'informal'` (tú, per D-07), a Spanish `voice_instructions` string (styling only, NO behavioral rules per D-02/D-03), `catchphrases: []`, and an optional `knowledge_scope`. Coach/Analyst copy is Claude's discretion per D-08 but must be Spanish/informal; the analista_cientifico row must faithfully carry the existing persona's voice.
    (c) Extend the seeded Blueprint: `update public.domain_blueprints set definition = definition || '{"bot_defaults": {"coach": true, "analyst": true}, "role_personalities": {"coach": "coach_default", "analyst": "analyst_default"}, "bot_cooldowns": {"coach": {"max": 3, "window_minutes": 15}, "analyst": {"max": 2, "window_minutes": 15}}}'::jsonb where id = 'debate-strategy-v1';` (PERSONA-03 defaults; D-05/D-10/D-11). Values must match the Blueprint schema fields added in Plan 01.
    (d) Add a per-session bot-override column DISTINCT from active_personas (D-06): `alter table public.sessions add column if not exists bot_overrides jsonb not null default '{}'::jsonb;` with a comment noting resolution rule: effective on/off = bot_overrides[role] if present, else blueprint.bot_defaults[role], else false.
  </action>
  <verify>
    <automated>ls supabase/migrations/ | sort | tail -3; grep -c "create table public.personalities" supabase/migrations/0014_personalities.sql; grep -c "coach_default\|analyst_default\|analista_cientifico" supabase/migrations/0014_personalities.sql; grep -c "bot_defaults\|role_personalities\|bot_cooldowns\|bot_overrides" supabase/migrations/0014_personalities.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration filename is the next integer after the highest existing migration (re-verified at execution time)
    - File contains `create table public.personalities` with jsonb `definition`, RLS enabled, a SELECT-only policy, and NO write policy
    - THREE seed rows exist: `coach_default`/`Facilitador`, `analyst_default`/`Analista/Verificador`, and `analista_cientifico`/`Analista Científico` (the D-06 voice-data migration), all Spanish informal (tú) with no behavioral-rule keys in definition
    - An `update ... domain_blueprints ... where id = 'debate-strategy-v1'` sets bot_defaults, role_personalities, and bot_cooldowns
    - `alter table public.sessions add column ... bot_overrides jsonb` present (distinct from active_personas)
  </acceptance_criteria>
  <done>Migration 0014 written with table, RLS, 3 seeds (incl. D-06 migrated voice), blueprint UPDATE, and session override column.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push schema to the live database</name>
  <files>supabase/migrations/0014_personalities.sql</files>
  <read_first>
    - supabase/migrations/0014_personalities.sql (the migration from Task 1 — confirm it is the highest-numbered file before pushing)
  </read_first>
  <what-built>Migration 0014 (personalities table + RLS + 3 seed personalities incl. the D-06 migrated Analista Científico voice + debate-strategy-v1 bot_defaults/role_personalities/bot_cooldowns + sessions.bot_overrides).</what-built>
  <action>
    Run the schema push AFTER Task 1's migration file is complete and BEFORE any downstream verification. Execute `supabase db push`; for non-TTY execution set `SUPABASE_ACCESS_TOKEN` in env. If the push requires an interactive confirmation that cannot be suppressed, flag for manual intervention. Do not proceed past this task until the push has applied migration 0014 to the live database.
  </action>
  <how-to-verify>
    1. Run `supabase db push` (non-TTY: ensure `SUPABASE_ACCESS_TOKEN` is set in env). If push requires an interactive confirmation that cannot be suppressed, run it manually.
    2. Confirm the table exists: query `select id, name from public.personalities;` → returns exactly the three seed rows (coach_default, analyst_default, analista_cientifico).
    3. Confirm the blueprint carries the new keys: `select definition->'bot_defaults', definition->'role_personalities', definition->'bot_cooldowns' from public.domain_blueprints where id = 'debate-strategy-v1';` → all three non-null.
    4. Confirm `select column_name from information_schema.columns where table_name = 'sessions' and column_name = 'bot_overrides';` returns one row.
    5. Confirm RLS: `select policyname from pg_policies where tablename = 'personalities';` shows only the SELECT policy.
  </how-to-verify>
  <verify>
    <automated>supabase migration list 2>/dev/null | tail -5 || echo "run supabase db push and confirm via the queries in how-to-verify"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` applied migration 0014 without error
    - `select count(*) from public.personalities;` returns 3
    - The debate-strategy-v1 blueprint definition has non-null bot_defaults, role_personalities, and bot_cooldowns
    - `sessions.bot_overrides` column exists; personalities has only a SELECT RLS policy
  </acceptance_criteria>
  <done>Migration 0014 is applied to the live database and all five verification queries return expected results.</done>
  <resume-signal>Type "approved" once the push succeeded and all queries return the expected results, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → personalities table | Any authenticated user may SELECT personalities; only service role may write |
| session creator → sessions.bot_overrides | Per-session bot toggle written only via the creator-guarded route (Plan 07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Elevation of Privilege | personalities table writes | mitigate | RLS enabled with SELECT-only policy for authenticated users; no INSERT/UPDATE/DELETE policy → writes require service role (matches domain_blueprints pattern) |
| T-11-05 | Tampering | sessions.bot_overrides | mitigate | Column is data-only; the write path (Plan 07 route) enforces `session.creator_id === user.id` before writing |
| T-11-06 | Information Disclosure | personalities.definition | accept | Voice/tone strings only — no secrets or PII; authenticated SELECT is acceptable |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `supabase db push` succeeds and the five verification queries in Task 2 return expected results.
- `select count(*) from public.personalities;` = 3 (incl. the D-06 migrated analista_cientifico row).
- No pre-existing migration file was renumbered or edited; 0014 is purely additive; the Analista Científico invocation path is untouched (only its voice data is copied into personalities).
</verification>

<success_criteria>
- personalities table live with 3 seeds (incl. D-06 voice migration) and correct RLS.
- debate-strategy-v1 carries bot_defaults/role_personalities/bot_cooldowns.
- sessions.bot_overrides column live.
</success_criteria>

<output>
Create `.planning/phases/11-personality-basic-triggers/11-02-SUMMARY.md` when done.
</output>
