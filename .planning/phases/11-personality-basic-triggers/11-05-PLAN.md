---
phase: 11-personality-basic-triggers
plan: 05
type: execute
wave: 3
depends_on: [11-03, 11-04]
files_modified:
  - apps/api/src/graph/graph.ts
  - apps/api/src/graph/graph.test.ts
autonomous: true
requirements: [PERSONA-01, PERSONA-02]
must_haves:
  truths:
    - "The conditional START edge routes silence_gate triggers to FacilitationAgentNode, analysis triggers through ArgGraphBuilderNode into AnalyticsAgentNode, and human messages (no triggerType) to the orchestrator"
    - "A defined-but-unrecognized triggerType does NOT silently default — it emits a distinguishable console.error (ROADMAP success criterion 2)"
    - "The fixed START→orchestrator edge is removed so the conditional START edge is actually exercised (Pitfall 2)"
    - "Graph-level tests cover all three new START paths plus the unrecognized-trigger error path, using MemorySaver + mock adapters"
  artifacts:
    - path: "apps/api/src/graph/graph.ts"
      provides: "routeFromStart + conditional START edge + new node registration + edges"
      contains: "routeFromStart"
    - path: "apps/api/src/graph/graph.test.ts"
      provides: "describe blocks for facilitation / analysis+argGraphBuilder / orchestrator-fallback / unrecognized-trigger paths"
      contains: "routeFromStart"
  key_links:
    - from: "apps/api/src/graph/graph.ts"
      to: "state.triggerType"
      via: "routeFromStart reads triggerType"
      pattern: "triggerType"
    - from: "apps/api/src/graph/graph.ts"
      to: "argGraphBuilder → analysis edge"
      via: "addEdge('argGraphBuilder','analysis')"
      pattern: "argGraphBuilder"
---

<objective>
Wire the highest-risk topology change of the phase: the conditional START edge that routes between FacilitationAgentNode, ArgGraphBuilderNode→AnalyticsAgentNode, and the existing orchestrator pipeline — with explicit error handling for unrecognized trigger types (ROADMAP success criterion 2) — then prove all paths with graph-level tests. A short spike de-risks LangGraph's conditional-edge error-propagation behavior before committing the strategy.

Purpose: STATE.md flags the conditional START edge as the single highest-risk change in v3.0; it is isolated here so Phase 12+ triggers build on a proven topology. Pitfall 2/3 and Finding 6 make the failure modes concrete: a leftover fixed edge silently wins, and an unmapped return value silently drops the invocation.
Output: extended `graph.ts` and `graph.test.ts`.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-personality-basic-triggers/11-RESEARCH.md
@.planning/phases/11-personality-basic-triggers/11-PATTERNS.md
@.planning/phases/11-personality-basic-triggers/11-AI-SPEC.md

<interfaces>
From Plan 03: argGraphBuilderNode. From Plan 04: facilitationAgentNode, analyticsAgentNode.
From Plan 01: state.triggerType (string | null; null = human path).
Existing graph.ts: routeAfterOrchestrator, createGraph(checkpointer), fixed `.addEdge(START, 'orchestrator')` (line ~59 — MUST be removed).
Existing graph.test.ts: makeConfig helper (lines ~146-164), MemorySaver + mock adapter convention (no per-node test files exist — Pattern 3).
Accepted triggerType values: 'silence_gate' → facilitation; 'analysis_request' → analysis (via argGraphBuilder); null/undefined → orchestrator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike — LangGraph conditional-edge router error propagation</name>
  <files>apps/api/src/graph/graph.test.ts</files>
  <read_first>
    - apps/api/src/graph/graph.test.ts (existing test harness — the spike lives as a temporary describe block here, MemorySaver pattern)
    - .planning/phases/11-personality-basic-triggers/11-RESEARCH.md (Open Question 3, Assumption A2 — router-function error behavior is unverified)
    - .planning/phases/11-personality-basic-triggers/11-AI-SPEC.md (Pitfall 1/5 — nodes don't get automatic try/catch; router functions are architecturally different)
  </read_first>
  <action>
    Write a small throwaway spike (a `describe('SPIKE: router error propagation')` block) that builds a minimal StateGraph whose conditional START edge router either (a) throws, or (b) returns a string absent from the pathsMap, and asserts what `graph.invoke()` actually does in each case (throws to caller vs. silent drop vs. resolves with partial state). Record the observed behavior in the test as comments and in the plan SUMMARY. This determines the error-handling strategy for Task 2: if a thrown router error surfaces cleanly to the caller, `routeFromStart` may throw on unrecognized trigger types; if it is swallowed, `routeFromStart` must instead `console.error` AND route to a distinguishable terminal/error path. Keep the spike block or convert it into a permanent regression test asserting the chosen strategy — do not leave dead code.
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- graph.test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The spike runs and its assertions document, in-code, whether a router-function throw surfaces to `graph.invoke()` or is swallowed
    - The SUMMARY records the observed behavior and the chosen error-handling strategy for `routeFromStart`
    - No dead/commented-out spike code remains — it is either removed or promoted to a real regression test
  </acceptance_criteria>
  <done>Router error-propagation behavior is empirically known; the routeFromStart strategy is chosen on evidence, not assumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conditional START edge + routeFromStart + node/edge wiring</name>
  <files>apps/api/src/graph/graph.ts</files>
  <read_first>
    - apps/api/src/graph/graph.ts (routeAfterOrchestrator + createGraph factory; the fixed `.addEdge(START, 'orchestrator')` to REMOVE)
    - apps/api/src/graph/state.ts (state.triggerType — from Plan 01)
    - apps/api/src/graph/nodes/arg-graph-builder.ts, facilitation-agent.ts, analytics-agent.ts (the three new node exports)
    - .planning/phases/11-personality-basic-triggers/11-PATTERNS.md (graph.ts section — exact extend-in-place template)
    - .planning/phases/11-personality-basic-triggers/11-RESEARCH.md (Finding 6 — distinguish undefined trigger from unrecognized-but-defined)
  </read_first>
  <behavior>
    - routeFromStart(state with triggerType 'silence_gate') returns the facilitation key
    - routeFromStart(state with triggerType 'analysis_request') returns the analysis key (which maps to argGraphBuilder)
    - routeFromStart(state with triggerType null/undefined) returns the orchestrator key with an info log (valid human path)
    - routeFromStart(state with triggerType 'bogus_value') emits a console.error (NOT console.info) and follows the chosen error strategy from Task 1 — never silently returns orchestrator as if it were a human message
    - A compiled graph invoked with a silence_gate trigger reaches facilitation; with analysis_request reaches argGraphBuilder then analysis; both terminate at END
  </behavior>
  <action>
    Add `routeFromStart(state): '<facilitation>' | '<analysis>' | '<orchestrator>' | '<error>'` reading `state.triggerType`. Map 'silence_gate'→facilitation (console.info), 'analysis_request'→analysis (console.info), `null`/`undefined`→orchestrator (console.info, valid human path). For a defined-but-unrecognized value, `console.error('[graph] unrecognized triggerType: ...')` and apply the Task-1-chosen strategy (throw, or route to a distinguishable dead-end key present in the pathsMap) — do NOT fall through to orchestrator (Finding 6 / success criterion 2).
    In `createGraph`, register the three new nodes: `.addNode('argGraphBuilder', argGraphBuilderNode).addNode('facilitation', facilitationAgentNode).addNode('analysis', analyticsAgentNode)`. REMOVE `.addEdge(START, 'orchestrator')` and replace with `.addConditionalEdges(START, routeFromStart, { facilitation: 'facilitation', analysis: 'argGraphBuilder', orchestrator: 'orchestrator', <error-key>: <error-target> })` — every possible return value MUST be a pathsMap key (Pitfall 3, no runtime exhaustiveness check). Add edges `.addEdge('argGraphBuilder', 'analysis').addEdge('facilitation', END).addEdge('analysis', END)`. Leave all existing post-orchestrator edges unchanged.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit 2>&1 | head -20; grep -c "routeFromStart\|addConditionalEdges(START" src/graph/graph.ts; grep -c "addEdge(START, 'orchestrator')" src/graph/graph.ts; grep -c "argGraphBuilder\|facilitation\|analysis" src/graph/graph.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "addEdge(START, 'orchestrator')" graph.ts` returns 0 (fixed edge removed, Pitfall 2)
    - `addConditionalEdges(START, routeFromStart, {...})` present with every routeFromStart return value as a pathsMap key
    - routeFromStart emits `console.error` (not console.info) for a defined-but-unrecognized triggerType and does not return the orchestrator key for it
    - The three new nodes registered; edges argGraphBuilder→analysis, facilitation→END, analysis→END present
    - `pnpm exec tsc --noEmit` passes
  </acceptance_criteria>
  <done>Conditional START edge routes all three paths, errors loudly on bad trigger types, fixed edge removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend graph.test.ts with the new START-path describe blocks</name>
  <files>apps/api/src/graph/graph.test.ts</files>
  <read_first>
    - apps/api/src/graph/graph.test.ts (makeConfig helper + existing describe structure — extend in place; do NOT create per-node test files, Pattern 3)
    - apps/api/src/graph/graph.ts (routeFromStart + compiled graph from Task 2)
    - apps/api/src/graph/nodes/facilitation-agent.ts, analytics-agent.ts, arg-graph-builder.ts (adapter seam names to mock: facilitationAdapter, analyticsAdapter, argGraphAdapter)
  </read_first>
  <behavior>
    - A facilitation-path test: invoke with triggerType 'silence_gate' + mock facilitationAdapter returning a question-ended string → assert the Coach output ends with '?' and no canvas/orchestrator side effects
    - An analysis-path test: invoke with triggerType 'analysis_request' + mock argGraphAdapter (returns extractable claims) + mock analyticsAdapter → assert argGraph is populated before analysis and the Analyst output references a speaker from the argGraph
    - An orchestrator-fallback test: invoke with triggerType null + existing mock classifier/agent adapters → assert the human path still routes through orchestrator unchanged (regression)
    - An unrecognized-trigger test: invoke with triggerType 'bogus' → assert the chosen error behavior (thrown error caught, or routed to the error/dead-end path) and that a console.error was emitted
  </behavior>
  <action>
    Extend `makeConfig` to accept `facilitationAdapter`/`analyticsAdapter`/`argGraphAdapter` options and a `triggerType` initial-state field. Add four `describe`/`it` blocks per the behaviors above, using `createGraph(new MemorySaver())` and deterministic mock adapters (mirror the existing classifier/agent mock convention). Assert observable state: Coach output trailing '?', argGraph.nodes non-empty on the analysis path before analysis runs, orchestrator regression intact, and the unrecognized-trigger error path. Spy on `console.error` for the bad-trigger assertion.
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- graph.test 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - New describe blocks exist for facilitation, analysis+argGraphBuilder, orchestrator-fallback, and unrecognized-trigger paths
    - The facilitation test asserts the Coach output ends with `?`; the analysis test asserts argGraph is populated before AnalyticsAgentNode runs and the Analyst cites a speaker
    - The unrecognized-trigger test asserts a `console.error` was emitted and the invocation did not take the human orchestrator path
    - `pnpm test -- graph.test` passes (all blocks green)
  </acceptance_criteria>
  <done>All four START paths are covered by green graph-level tests using MemorySaver + mocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| triggerType (from state/caller) → START router | A malformed/corrupted trigger value must not silently misroute or drop the invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Tampering / DoS | routeFromStart unrecognized triggerType | mitigate | Explicit console.error + distinguishable non-orchestrator handling (Finding 6); every return value enumerated in the pathsMap (Pitfall 3); covered by the unrecognized-trigger regression test |
| T-11-14 | Tampering | leftover fixed START edge | mitigate | Fixed `.addEdge(START,'orchestrator')` removed (Pitfall 2); grep gate asserts 0 occurrences |
| T-11-15 | Spoofing | proactive vs human thread routing | mitigate | Human path (triggerType null) preserved unchanged; bot paths only reachable when triggerType is explicitly set by the scan loop (Plan 06) on the bot thread |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `pnpm test -- graph.test` passes including the four new blocks.
- `pnpm exec tsc --noEmit` passes in apps/api.
- `grep "addEdge(START, 'orchestrator')" graph.ts` returns 0; conditional START edge present with a complete pathsMap.
</verification>

<success_criteria>
- Conditional START edge routes facilitation / analysis / orchestrator correctly and errors loudly on bad triggers.
- Router error propagation is empirically verified; all START paths tested green.
</success_criteria>

<output>
Create `.planning/phases/11-personality-basic-triggers/11-05-SUMMARY.md` when done.
</output>
