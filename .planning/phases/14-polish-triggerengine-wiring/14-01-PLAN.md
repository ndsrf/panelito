---
phase: 14-polish-triggerengine-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/ai.ts
  - packages/types/src/speech-artifacts.ts
  - packages/types/src/speech-artifacts.test.ts
  - packages/types/src/blueprint.ts
  - packages/types/src/index.ts
autonomous: true
requirements: [COST-03, SPEECH-01, SPEECH-02, SPEECH-03, TRIGGER-07]

must_haves:
  truths:
    - "The AIStreamEvent union has a `usage` member so adapters can surface real token counts"
    - "A single shared blocklist + substring matcher exists in @panelito/types and both apps can import it"
    - "A Blueprint can opt into silence↔phase-readiness coupling; Blueprints without the field still parse"
  artifacts:
    - path: "packages/types/src/ai.ts"
      provides: "AIStreamEvent usage variant"
      contains: "type: 'usage'"
    - path: "packages/types/src/speech-artifacts.ts"
      provides: "SPEECH_ARTIFACT_BLOCKLIST + containsSpeechArtifact()"
      exports: ["SPEECH_ARTIFACT_BLOCKLIST", "containsSpeechArtifact"]
    - path: "packages/types/src/blueprint.ts"
      provides: "D-04 silence↔phase-readiness Blueprint toggle"
      contains: "z.boolean().default(false)"
  key_links:
    - from: "packages/types/src/index.ts"
      to: "speech-artifacts.ts"
      via: "barrel re-export"
      pattern: "speech-artifacts"
---

<objective>
Establish the three shared type/contract additions the rest of Phase 14 builds on: (1) a `usage` member on the adapter-side `AIStreamEvent` union so all three provider adapters can surface real token counts for COST-03, (2) a single shared speech-artifact blocklist + substring matcher in `@panelito/types` consumed by both the frontend filter (SPEECH-02) and any prompt/backend reference (SPEECH-01/03), and (3) the D-04 Blueprint toggle that opts a Blueprint into silence↔phase-readiness coupling (TRIGGER-07).

Purpose: These are the interface contracts downstream plans (03 adapters, 04 TriggerEngine, 05 frontend, 06 nodes) import against. Defining them first prevents scavenger-hunt exploration.
Output: Extended `ai.ts` union, new `speech-artifacts.ts` module + test, extended `blueprint.ts`, updated barrel.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md
@.planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md
@.planning/phases/14-polish-triggerengine-wiring/14-PATTERNS.md

<interfaces>
Current AIStreamEvent union (packages/types/src/ai.ts:65-68):
  text_delta | tool_use | done  — no usage/token data today.

Current blueprint precedent (packages/types/src/blueprint.ts:84):
  drift_detection_enabled: z.boolean().default(true)  — the exact "optional in DB, Zod supplies default, seeded Blueprints still parse" shape to mirror.

Barrel precedent (packages/types/src/index.ts:73-75):
  TriggerMetadata/TriggerMetadataSchema are re-exported here — mirror for speech-artifacts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `usage` member to the AIStreamEvent union</name>
  <files>packages/types/src/ai.ts</files>
  <read_first>
    - packages/types/src/ai.ts (see the AIStreamEvent union at lines 61-68 and the AIProvider.stream() return type at line 86)
  </read_first>
  <action>
    Add a fourth member to the `AIStreamEvent` union: an object literal with `type: 'usage'`, `inputTokens: number`, `outputTokens: number`. Do not remove or reorder the existing `text_delta`, `tool_use`, `done` members. The union is the contract all three adapters (Plan 03) will emit and the Generation helper (Plan 03) + Role nodes (Plan 06) will consume; keep the field names exactly `inputTokens` and `outputTokens` (camelCase, provider-neutral) so provider-specific names like `input_tokens`/`usageMetadata` never leak past the adapter boundary per CLAUDE.md's AI-coupling constraint. Update the doc-comment at line 10 that enumerates the union members to include `usage`.
  </action>
  <verify>
    <automated>cd packages/types &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "type: 'usage'" packages/types/src/ai.ts` returns a line inside the AIStreamEvent union with `inputTokens: number` and `outputTokens: number`
    - The doc-comment enumerating union members (line ~10) lists `usage`
    - `cd packages/types &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AIStreamEvent has a `usage` member with numeric inputTokens/outputTokens; types compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared speech-artifact blocklist + substring matcher</name>
  <files>packages/types/src/speech-artifacts.ts, packages/types/src/speech-artifacts.test.ts, packages/types/src/index.ts</files>
  <read_first>
    - packages/types/src/index.ts (barrel; see the TriggerMetadata re-export at lines 73-75 for the export convention to mirror)
    - .planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md (Pitfall 3 — the matcher MUST be substring `.includes`, never whole-message equality)
  </read_first>
  <behavior>
    - containsSpeechArtifact('Great, [canvas updated] — done') === true (substring match mid-message, per D-08/Pitfall 3)
    - containsSpeechArtifact('[graph modified]') === true
    - containsSpeechArtifact('[node added]') === true
    - containsSpeechArtifact('Let us keep discussing the options') === false
    - containsSpeechArtifact('') === false
    - SPEECH_ARTIFACT_BLOCKLIST contains at least the three ROADMAP-named strings
  </behavior>
  <action>
    Create `packages/types/src/speech-artifacts.ts` exporting a `readonly string[]` const `SPEECH_ARTIFACT_BLOCKLIST` seeded with the three ROADMAP-named artifact strings `[canvas updated]`, `[graph modified]`, `[node added]` (D-08 curated exact-string list; add any others discovered during implementation — Claude's discretion), and a function `containsSpeechArtifact(content: string): boolean` implemented as `SPEECH_ARTIFACT_BLOCKLIST.some((pattern) => content.includes(pattern))`. This is a substring check, NOT whole-message equality (Pitfall 3 — a naive `content === pattern` misses artifacts embedded mid-sentence). Re-export both symbols from the `packages/types/src/index.ts` barrel following the same pattern the TriggerMetadata re-export uses at lines 73-75. Write the accompanying `speech-artifacts.test.ts` covering the behavior cases above using vitest (the package's `test` script is `vitest run`).
  </action>
  <verify>
    <automated>cd packages/types &amp;&amp; npx vitest run speech-artifacts</automated>
  </verify>
  <acceptance_criteria>
    - `containsSpeechArtifact` uses `.includes(` (substring), confirmed by `grep -n "includes" packages/types/src/speech-artifacts.ts`
    - `grep -n "speech-artifacts" packages/types/src/index.ts` shows both `SPEECH_ARTIFACT_BLOCKLIST` and `containsSpeechArtifact` re-exported
    - `cd packages/types &amp;&amp; npx vitest run speech-artifacts` passes all behavior cases
  </acceptance_criteria>
  <done>Shared blocklist module exists, substring matcher tested green, both symbols exported from the barrel.</done>
</task>

<task type="auto">
  <name>Task 3: Add D-04 silence↔phase-readiness Blueprint toggle</name>
  <files>packages/types/src/blueprint.ts</files>
  <read_first>
    - packages/types/src/blueprint.ts (see `drift_detection_enabled: z.boolean().default(true)` at line 84 — the exact precedent; and the `phase_readiness_gate` nested shape at lines 49-54 to confirm this new toggle is Blueprint-global, NOT phase-nested)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-04 — Blueprint-level, "Blueprints that opt in", default off)
  </read_first>
  <action>
    Add a new sibling boolean field to `BlueprintSchema` at the same top level as `drift_detection_enabled` (line 84), named `silence_phase_readiness_coupling_enabled`, typed `z.boolean().default(false)`. Default is `false` (D-04: coupling is opt-in, not universal — unlike drift which defaults true). Follow the exact "optional in DB, Zod supplies the default so existing seeded Blueprints that lack this field still parse without a migration" convention documented in the `drift_detection_enabled` comment. Place it Blueprint-global (top level of BlueprintSchema), NOT inside `PhaseSequenceSchema` — D-04 scopes this per-Blueprint, not per-phase. Add a short comment referencing D-04 and the drift_detection_enabled precedent.
  </action>
  <verify>
    <automated>cd packages/types &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "silence_phase_readiness_coupling_enabled" packages/types/src/blueprint.ts` shows `z.boolean().default(false)`
    - A `BlueprintSchema.parse(...)` call on a blueprint object that omits the field yields `silence_phase_readiness_coupling_enabled === false` (add or extend a blueprint test asserting the default, or confirm via existing blueprint-loader test path)
    - `cd packages/types &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Blueprint schema exposes the opt-in coupling toggle defaulting to false; existing seeded Blueprints still parse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → chat render | Untrusted model-generated text; artifact strings must be detectable regardless of position |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | containsSpeechArtifact matcher | mitigate | Substring `.includes()` matching (not whole-message equality) so an artifact embedded mid-sentence cannot evade the filter (RESEARCH Pitfall 3); no regex means no ReDoS surface (ASVS V5, D-08 exact-string choice) |
| T-14-SC | Tampering | npm installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none); all symbols use existing `@panelito/types` + `zod` already locked |
</threat_model>

<verification>
- `cd packages/types &amp;&amp; npx vitest run` passes (includes new speech-artifacts test)
- `cd packages/types &amp;&amp; npx tsc --noEmit` compiles clean
- Downstream imports resolve: `import { containsSpeechArtifact } from '@panelito/types'` and the `usage` AIStreamEvent member are both available to consuming packages
</verification>

<success_criteria>
- AIStreamEvent union has a `usage` member (COST-03 enabler)
- Shared blocklist + substring matcher exist and are barrel-exported (SPEECH-01/02/03 enabler)
- Blueprint opt-in coupling toggle exists, default false, backward-compatible (TRIGGER-07/D-04 enabler)
</success_criteria>

<output>
Create `.planning/phases/14-polish-triggerengine-wiring/14-01-SUMMARY.md` when done
</output>
