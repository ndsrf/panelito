---
phase: 14-polish-triggerengine-wiring
plan: 06
type: execute
wave: 3
depends_on: ["14-01", "14-03"]
files_modified:
  - apps/api/src/graph/state.ts
  - apps/api/src/graph/nodes/facilitation-agent.ts
  - apps/api/src/graph/nodes/facilitation-agent.test.ts
  - apps/api/src/graph/nodes/analytics-agent.ts
  - apps/api/src/graph/nodes/analytics-agent.test.ts
autonomous: true
requirements: [PERSONA-04, SPEECH-01, COST-03]

must_haves:
  truths:
    - "Each Role node tracks a per-role, per-branch invocation count persisted in PostgresSaver thread state"
    - "Every 15th invocation appends an emphasized persona re-anchor reminder near the generation point (no extra LLM call)"
    - "Each Role node's adapter.stream() is wrapped in a Langfuse Generation observation carrying trigger, tier, and real usageDetails"
    - "Each Role node's system prompt instructs the model never to emit system artifact strings (SPEECH-01)"
  artifacts:
    - path: "apps/api/src/graph/state.ts"
      provides: "roleInvocationCounts Annotation (BOT-05 persisted)"
      contains: "roleInvocationCounts"
    - path: "apps/api/src/graph/nodes/facilitation-agent.ts"
      provides: "coach counter + re-anchor + Generation wrap + SPEECH-01 prompt line"
      contains: "roleInvocationCounts"
    - path: "apps/api/src/graph/nodes/analytics-agent.ts"
      provides: "analyst counter + re-anchor + Generation wrap + SPEECH-01 prompt line"
      contains: "roleInvocationCounts"
  key_links:
    - from: "apps/api/src/graph/nodes/facilitation-agent.ts"
      to: "langfuse-generation helper"
      via: "wraps adapter.stream()"
      pattern: "langfuse-generation|startObservation|Generation"
    - from: "apps/api/src/graph/nodes/facilitation-agent.ts"
      to: "state.roleInvocationCounts.coach"
      via: "increment + partial state return"
      pattern: "roleInvocationCounts"
---

<objective>
Harden the two Role nodes with three coupled changes: (1) PERSONA-04's periodic re-anchor — a per-role, per-branch invocation counter persisted in PostgresSaver thread state that, every 15th invocation, appends an extra-emphasized reminder of the Role's discipline rules near the generation point (D-06, prompt-only, no self-check LLM call); (2) COST-03's node-level instrumentation — wrap each node's `adapter.stream()` call with the Plan 03 Generation helper so per-generation cost (trigger + tier + real usageDetails) populates in Langfuse; and (3) SPEECH-01's prompt discipline — a system-prompt instruction never to emit system artifact strings.

Purpose: Completes PERSONA-04 (re-anchor), the node/generation half of COST-03, and SPEECH-01 (prompt discipline). Consumes Plan 01's `usage` type + Plan 03's Generation helper and adapter usage events.
Output: New `roleInvocationCounts` Annotation in `state.ts`; counter + re-anchor + Generation wrap + SPEECH-01 line in both Role nodes.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md
@.planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md
@.planning/phases/14-polish-triggerengine-wiring/14-PATTERNS.md

<interfaces>
state.ts triggerMetadata Annotation (lines 99-105) — overwrite-style, Record-shaped, default {} —
  the exact precedent for the new roleInvocationCounts field.

facilitation-agent.ts: buildCoachSystemPrompt() (lines 46-100) composes in FIXED order
  Role rules → Blueprint → argGraph → Skill guidance → Personality voice (step 4, appended LAST) —
  "NEVER reorder" per the file's own Pitfall 4 comment; add a step 5, do not reorder 1-4.
  adapter.stream() loop + partial-state return (lines 168-196) returns triggerMetadata overwrite-style.
  Fail-silent convention (lines 120-127, 180-183): return {} on error, '[facilitation]' log prefix.

analytics-agent.ts: sibling node (Analyst), same shape, ANALYST_SKILLS mirroring COACH_SKILLS.

langfuse-generation helper (Plan 03, apps/api/src/lib/langfuse-generation.ts): wraps an
  AsyncIterable<AIStreamEvent>, forwards text_delta to streamWriter, captures usage → usageDetails,
  sets metadata { trigger, tier }, never throws.

Branch scoping note (RESEARCH): branch scoping already comes from the bot thread_id `${branchId}:bot`,
  so roleInvocationCounts only needs role-keying (coach/analyst) within state (D-07).

escapeUntrustedText (bot-context.ts:44) — for any dynamic/untrusted content; re-anchor text itself is static.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add roleInvocationCounts Annotation to graph state (BOT-05 persisted)</name>
  <files>apps/api/src/graph/state.ts</files>
  <read_first>
    - apps/api/src/graph/state.ts (the `triggerMetadata` Annotation at lines 99-105 — copy its overwrite-style reducer + `default: () => ({})` shape; the "Bot infrastructure fields (Phase 10 — BOT-05)" section comment at lines 87-105)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-07: per role, per branch; inherited constraint — MUST persist in PostgresSaver thread state, never JS process memory)
  </read_first>
  <action>
    Add a new Annotation field `roleInvocationCounts` typed `Annotation<Record<string, number>>` with an overwrite-style reducer `(_, v) => v` and `default: () => ({})`, placed in the "Bot infrastructure fields (Phase 10 — BOT-05)" section alongside `triggerMetadata` (lines 87-105). This checkpoints automatically via `PostgresSaver` when a node returns it as partial state — it must NOT be JS process memory (RESEARCH Pitfall 4: `auto-freeze.ts`'s in-memory trackerMap is explicitly NOT an acceptable precedent for PERSONA-04 given BOT-05). Add a comment noting it is keyed by role id (coach/analyst) and that branch scoping comes from the bot thread_id, so only role-keying is needed here (D-07).
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; grep -n "roleInvocationCounts" src/graph/state.ts ; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roleInvocationCounts" apps/api/src/graph/state.ts` shows `Annotation<Record<string, number>>` with `default: () => ({})`
    - The field sits in the BOT-05 section next to `triggerMetadata`
    - `cd apps/api &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>roleInvocationCounts Annotation exists, overwrite-style, PostgresSaver-persisted, role-keyed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coach re-anchor counter + Generation wrap + SPEECH-01 prompt (facilitation-agent.ts)</name>
  <files>apps/api/src/graph/nodes/facilitation-agent.ts, apps/api/src/graph/nodes/facilitation-agent.test.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/facilitation-agent.ts (buildCoachSystemPrompt lines 46-100 with the fixed composition order + "NEVER reorder" comment; the adapter.stream loop + partial-state return lines 168-196; fail-silent convention lines 120-127, 180-183)
    - apps/api/src/lib/langfuse-generation.ts (the Plan 03 helper this node wraps adapter.stream() with)
    - apps/api/src/lib/model-config.ts (TASK_MODELS[provider].facilitation → the model string; facilitation tier is 'fast')
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-06 re-anchor cheap version only, no LLM call; D-07 per-role counter; D-08 blocklist strings)
    - .planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md (Pitfall 4 counter code; Pattern 3 Generation wrap; Security: re-anchor text is static, escapeUntrustedText for any dynamic content)
  </read_first>
  <behavior>
    - Given state.roleInvocationCounts.coach === 14, this invocation computes currentCount 15 and shouldReanchor === true; the returned partial state sets roleInvocationCounts.coach = 15
    - Given coach === 15, next invocation → 16, shouldReanchor === false
    - When shouldReanchor, the built system prompt contains the emphasized re-anchor reminder text as its final segment (after the Personality voice step)
    - The system prompt always contains a SPEECH-01 discipline instruction naming the artifact strings the model must never emit
    - The adapter.stream() call is routed through the langfuse-generation helper (Generation observation constructed with trigger + tier:'fast')
  </behavior>
  <action>
    Three additions to the Coach node, preserving the existing fail-silent (`return {}`) convention and `[facilitation]` log prefix:
    1. Re-anchor counter (D-06/D-07): read `state.roleInvocationCounts?.coach ?? 0`, add 1 → `currentCount`; define `REANCHOR_EVERY_N_INVOCATIONS = 15`; `shouldReanchor = currentCount % REANCHOR_EVERY_N_INVOCATIONS === 0`. When `shouldReanchor`, append an extra-emphasized reminder of the Coach's discipline rules (e.g. "always end with a question") as a NEW final step 5 of the composed system prompt — appended AFTER the Personality voice step (step 4), close to the generation point for the "recency boost" against lost-in-the-middle (D-06). Do NOT reorder steps 1-4 (the file's "NEVER reorder" invariant). No self-check/regenerate LLM call (D-06 explicitly rejects it). The re-anchor text is static/hardcoded; if it ever references dynamic content, route that through `escapeUntrustedText`. Return the updated count in the node's partial-state return object: `roleInvocationCounts: { ...state.roleInvocationCounts, coach: currentCount }`, alongside the existing `triggerMetadata` return (mirror the overwrite-style idiom).
    2. Generation wrap (COST-03): route the existing `adapter.stream(...)` iteration through the Plan 03 `langfuse-generation` helper so a Generation observation is constructed with `model` = `TASK_MODELS[providerName ?? 'anthropic'].facilitation`, `metadata: { trigger: state.triggerType ?? state.firingSkillId ?? 'human-reactive', tier: 'fast' }`, and `usageDetails` from the adapter's `usage` event. The helper forwards `text_delta` to the existing `config?.configurable?.streamWriter`. A Langfuse failure must not abort the turn (helper already never-throws; keep the node's fail-silent path intact).
    3. SPEECH-01 prompt discipline: add an instruction to the composed system prompt that the model must never emit system artifact strings ("[canvas updated]", "[graph modified]", "[node added]", or similar) — all output must be conversational and natural. This is prompt discipline only (D-09 layer 1); the frontend filter (Plan 05) is layer 2.
    Extend `facilitation-agent.test.ts` covering the behavior cases (counter 14→15 sets shouldReanchor + re-anchor text present; count returned in partial state; SPEECH-01 instruction present; the Generation helper is invoked). Mock the adapter/helper as the existing tests do.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; npx vitest run src/graph/nodes/facilitation-agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roleInvocationCounts" apps/api/src/graph/nodes/facilitation-agent.ts` shows read of `.coach` and return of the incremented count
    - `grep -n "REANCHOR_EVERY_N_INVOCATIONS\|15" apps/api/src/graph/nodes/facilitation-agent.ts` shows the 15-invocation cadence
    - The adapter.stream() call is wrapped by the langfuse-generation helper with `tier: 'fast'` and a trigger metadata value
    - The system prompt contains a SPEECH-01 discipline instruction naming the artifact strings
    - Re-anchor text is appended AFTER the Personality voice step (steps 1-4 not reordered)
    - `cd apps/api &amp;&amp; npx vitest run src/graph/nodes/facilitation-agent.test.ts` passes the behavior cases
  </acceptance_criteria>
  <done>Coach node has persisted per-role counter, 15-invocation re-anchor, Generation-wrapped stream (fast tier), and SPEECH-01 prompt discipline; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Analyst re-anchor counter + Generation wrap + SPEECH-01 prompt (analytics-agent.ts)</name>
  <files>apps/api/src/graph/nodes/analytics-agent.ts, apps/api/src/graph/nodes/analytics-agent.test.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/analytics-agent.ts (sibling of facilitation-agent.ts; same imports shape, fail-silent convention, ANALYST_SKILLS lookup mirroring COACH_SKILLS; its buildAnalystSystemPrompt composition order)
    - apps/api/src/graph/nodes/facilitation-agent.ts (the pattern established in Task 2 to mirror for the analyst, keyed 'analyst')
    - apps/api/src/lib/model-config.ts (analysis tier is 'capable' — sonnet/gpt-5.4/gemini)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-07 independent per-role counters; SPEECH-01)
    - ROADMAP §Phase 14 Success Criterion 2 (Analyst output must cite a specific prior message)
  </read_first>
  <behavior>
    - Given state.roleInvocationCounts.analyst === 14, this invocation → 15, shouldReanchor === true; returned partial state sets roleInvocationCounts.analyst = 15
    - The analyst counter is independent of the coach counter within the same branch (D-07)
    - When shouldReanchor, the system prompt contains the emphasized Analyst discipline reminder (including "cite a specific prior message") as its final segment
    - The system prompt always contains a SPEECH-01 discipline instruction
    - The adapter.stream() is Generation-wrapped with tier:'capable'
  </behavior>
  <action>
    Apply the exact same three additions as Task 2 to the Analyst node, keyed `analyst` instead of `coach`: (1) re-anchor counter reading `state.roleInvocationCounts?.analyst ?? 0`, `REANCHOR_EVERY_N_INVOCATIONS = 15`, appending an emphasized Analyst-discipline reminder as a new final prompt step (the Analyst discipline is "cite a specific prior message" per Success Criterion 2), returning `roleInvocationCounts: { ...state.roleInvocationCounts, analyst: currentCount }` — independent of the coach count within the same branch-scoped field (D-07); (2) Generation wrap via the Plan 03 helper with `model = TASK_MODELS[providerName ?? 'anthropic'].analysis`, `metadata: { trigger: ..., tier: 'capable' }`, `usageDetails` from the usage event; (3) SPEECH-01 prompt discipline instruction. Preserve the Analyst node's fail-silent convention and `[analytics]`/`[analysis]` log prefix. Extend `analytics-agent.test.ts` for the behavior cases (counter, independence from coach, re-anchor text, SPEECH-01, Generation wrap with capable tier).
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; npx vitest run src/graph/nodes/analytics-agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roleInvocationCounts" apps/api/src/graph/nodes/analytics-agent.ts` shows read of `.analyst` and return of the incremented count
    - The adapter.stream() call is Generation-wrapped with `tier: 'capable'`
    - The system prompt contains both the "cite a specific prior message" re-anchor discipline and a SPEECH-01 instruction
    - Test asserts the analyst counter increments independently of the coach counter
    - `cd apps/api &amp;&amp; npx vitest run src/graph/nodes/analytics-agent.test.ts` passes
  </acceptance_criteria>
  <done>Analyst node has independent per-role counter, 15-invocation re-anchor, Generation-wrapped stream (capable tier), and SPEECH-01 prompt discipline; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participant/message content → re-anchor prompt text | Any dynamic content spliced near the generation point is untrusted |
| Role node LLM call → Langfuse | Generation metadata must be non-sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06a | Tampering | re-anchor prompt injection | mitigate | Re-anchor reminder text is static/hardcoded (D-06 prompt-reinforcement only, no dynamic user content interpolated); any dynamic content it references routes through the existing `escapeUntrustedText()` helper (bot-context.ts) already applied elsewhere |
| T-14-06b | Denial of Service | Langfuse Generation calls in the hot LLM path | mitigate | The Plan 03 helper never throws; the node's fail-silent (`return {}`) convention is preserved so a tracing failure cannot abort a Coach/Analyst turn |
| T-14-06c | Information Disclosure | Generation metadata | mitigate | trigger/tier are non-sensitive enum strings; usageDetails are token counts, not content (T-06-05) |
| T-14-06d | Tampering | counter integrity across restarts | mitigate | Counter persisted in PostgresSaver thread state (BOT-05), never JS process memory, so re-anchor cadence survives redeploys (RESEARCH Pitfall 4) |
| T-14-SC | Tampering | npm installs | accept | No new packages (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cd apps/api &amp;&amp; npx vitest run src/graph/nodes/facilitation-agent.test.ts src/graph/nodes/analytics-agent.test.ts` passes
- `grep -rn "roleInvocationCounts" apps/api/src/graph` returns matches in state.ts + both nodes
- `cd apps/api &amp;&amp; npx tsc --noEmit` compiles clean
</verification>

<success_criteria>
- Per-role, per-branch invocation counter persisted in PostgresSaver; re-anchor every 15 invocations, prompt-only (PERSONA-04)
- Both Role nodes' adapter.stream() wrapped in Generation observations with trigger + tier + usageDetails (COST-03 node half)
- Both Role nodes instruct the model never to emit artifact strings (SPEECH-01)
</success_criteria>

<output>
Create `.planning/phases/14-polish-triggerengine-wiring/14-06-SUMMARY.md` when done
</output>
