---
phase: 14-polish-triggerengine-wiring
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/api/src/lib/trigger-engine.ts
  - apps/api/src/lib/trigger-engine.test.ts
  - apps/api/src/server.ts
  - apps/api/src/lib/auto-freeze.ts
  - apps/api/src/lib/auto-freeze.test.ts
autonomous: true
requirements: [TRIGGER-07, COST-03]

must_haves:
  truths:
    - "A persistent TriggerEngine loop runs on the standalone Node server and survives the full session lifetime"
    - "The silence-fire path is traced in Langfuse with a trigger:silence_gate tag (previously untraced)"
    - "When a Blueprint opts in, a silence fire also weighs phase-readiness (redirect vs suggest-advance)"
    - "SIGTERM/SIGINT stops the loop and trackers cleanly; auto-freeze fires after 5 minutes"
  artifacts:
    - path: "apps/api/src/lib/trigger-engine.ts"
      provides: "generalized TriggerEngine loop (drift-aware, cancellable)"
      contains: "startTriggerEngine"
      min_lines: 60
    - path: "apps/api/src/server.ts"
      provides: "graceful shutdown wiring"
      contains: "SIGTERM"
    - path: "apps/api/src/lib/auto-freeze.ts"
      provides: "5-minute auto-freeze constant (D-05)"
      contains: "300000"
  key_links:
    - from: "apps/api/src/lib/trigger-engine.ts"
      to: "graph.invoke CallbackHandler"
      via: "callbacks array with trigger:silence_gate tag"
      pattern: "trigger:silence_gate"
    - from: "apps/api/src/lib/trigger-engine.ts"
      to: "phaseReadinessSkill.detect"
      via: "blueprint coupling toggle"
      pattern: "phaseReadiness"
    - from: "apps/api/src/server.ts"
      to: "trigger-engine stop fn + clearAllTrackers"
      via: "process.on signal handlers"
      pattern: "process.on"
---

<objective>
Generalize the Phase 11 interim `silence-scan.ts` loop into a persistent, hardened `trigger-engine.ts` (TRIGGER-07): a drift-aware, cancellable `setInterval` scan loop that re-evaluates the silence-window trigger (~1 min, D-02), closes the confirmed Langfuse tracing gap on the proactive path (D-14 inherited fix), adds Blueprint-opt-in phase-readiness coupling to silence fires (D-03/D-04), wires graceful SIGTERM/SIGINT shutdown into `server.ts` (currently absent), and reduces auto-freeze to 5 minutes (D-05) to bound the new proactive spend.

Purpose: TRIGGER-07 is satisfied by 5 of 6 triggers already firing reactively (Phases 12-13) plus this timer-based silence re-evaluation (D-01). This is a rename/generalize/harden of an existing module — a low-risk rename per RESEARCH's Runtime State Inventory, not a new subsystem.
Output: `trigger-engine.ts` (renamed from `silence-scan.ts`), graceful shutdown in `server.ts`, 5-minute auto-freeze constant.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md
@.planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md
@.planning/phases/14-polish-triggerengine-wiring/14-PATTERNS.md

<interfaces>
silence-scan.ts today: startSilenceScanLoop(supabase) → raw setInterval (lines 132-143);
  runSilenceScan → scanSession → scanBranch; scanBranch's graph.invoke (lines 258-275) sets
  config.configurable = { thread_id: `${branchId}:bot`, blueprint, providerName, plaintextKey,
  personality, streamWriter } and passes NO `callbacks`. Cooldown read/write via
  graph.getState()/updateState() against triggerMetadata.silence_gate.
  Constants: SCAN_INTERVAL_MS default '15000' (line ~45), SCAN_INTERVAL_FLOOR_MS 5000.

phaseReadinessSkill (apps/api/src/lib/skills/phase-readiness.ts): exports phaseReadinessSkill.detect(context)
  and buildPromptGuidance(context). Already Blueprint-aware, fails closed. D-03 reads this, never duplicates it.

routeFromStart (graph.ts): triggerType === 'silence_gate' routes DIRECTLY to 'facilitation',
  bypassing TriggerGateNode — so do NOT pass botOverrides for TriggerGateNode's gate (Pitfall 6, dead key).

CallbackHandler tag template (ai.ts:364-369): copy verbatim, add trigger:silence_gate.

server.ts (lines 88-110): serve(...) callback calls setupLangfuseOtel(), startAutoFreezeTracker(),
  startSilenceScanLoop(). clearAllTrackers() (auto-freeze.ts:181) exported but ZERO callers today.

auto-freeze.ts: FREEZE_AFTER_MS = parseInt(process.env.AUTO_FREEZE_AFTER_MS ?? '900000', 10) (line 33);
  production-minimum warning at lines 42-47 (threshold 900_000); GRACE_MS unchanged (D-05).

Blueprint toggle (from Plan 01): blueprint.silence_phase_readiness_coupling_enabled (default false).

node:timers/promises async-iterator setInterval + AbortController — RESEARCH Pattern 1 (no in-repo analog).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename/generalize silence-scan.ts into a drift-aware, traced, cancellable trigger-engine.ts</name>
  <files>apps/api/src/lib/trigger-engine.ts, apps/api/src/lib/trigger-engine.test.ts</files>
  <read_first>
    - apps/api/src/lib/silence-scan.ts (the full module — this is the direct base to rename in place; import block lines 25-37, setInterval loop 132-143, scanBranch graph.invoke 258-275, cooldown read/write 394-440, empty-text skip 277-280)
    - apps/api/src/lib/silence-scan.test.ts (rename to trigger-engine.test.ts, update imports/names, keep coverage)
    - apps/api/src/routes/ai.ts (CallbackHandler tag template lines 364-369; and the supabase/serviceClient/branchId config keys ai.ts sets that scanBranch lacks — RESEARCH Pitfall 5)
    - apps/api/src/graph/graph.ts (routeFromStart — confirm silence_gate bypasses TriggerGateNode; Pitfall 6)
    - .planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md (Pattern 1 async loop, Pattern 2 shutdown, Pitfall 5 config keys, D-14 tracing gap)
  </read_first>
  <action>
    Rename `silence-scan.ts` → `trigger-engine.ts` (and `silence-scan.test.ts` → `trigger-engine.test.ts`), keeping the entire import block and the `checkSilenceGate → runArbitration → checkBotBudget` per-branch pipeline unchanged (D-01: the silence path continues to use these exactly as today). Make these specific changes:
    1. Rename the exported entrypoint `startSilenceScanLoop` → `startTriggerEngine`. Change its return type to `Promise<() => void>` returning a stop function.
    2. Replace the raw callback `setInterval` (lines 132-143) with the `node:timers/promises` async-iterator `setInterval` form driven by an `AbortController`, keeping the same per-tick `try/catch` isolation (one bad tick must never kill the loop) and the `[trigger-engine]` log prefix (renamed from `[silence-scan]`). The returned stop function calls `controller.abort()`. Follow RESEARCH Pattern 1.
    3. Change the `SCAN_INTERVAL_MS` default from `'15000'` to `'60000'` (~1 minute, D-02), keeping the existing `SCAN_INTERVAL_FLOOR_MS` (5000) floor-warning idiom.
    4. Close the Langfuse tracing gap (D-14 inherited fix): construct a per-request `CallbackHandler` inside `scanBranch()` and pass it in the `graph.invoke()` `callbacks` array (this path currently passes NO callbacks). Copy the tag template from `ai.ts:364-369` and add `trigger:silence_gate` — i.e. tags `session:${...}`, `branch:${branch.id}`, `trigger:silence_gate`. Per-request construction, never module-level.
    5. Add the reachability config keys `ai.ts` sets but `scanBranch` lacks (Pitfall 5): add `supabase`, `serviceClient`, and `branchId` to the `graph.invoke()` `config.configurable` object so Coach participant-profile personalization can reach the proactive path (Phase 13 D-12/D-13 dependency named in 14-CONTEXT canonical_refs). Do NOT add `botOverrides` — `routeFromStart` sends silence_gate straight to facilitation, bypassing TriggerGateNode's gate, so it is a dead key here (Pitfall 6). Do NOT resolve/set `participantId` in this task — that decision is flagged separately for the developer (see plan note); omitting it degrades gracefully to a generic Coach summary and never throws.
    6. Preserve the empty-text skip idiom (lines 277-280) and the cooldown read/write against `triggerMetadata.silence_gate` unchanged.
    Update `trigger-engine.test.ts` to assert `startTriggerEngine` returns a callable stop function and that calling it aborts the loop (a subsequent tick does not run). Confirm zero remaining references to the old filename via grep.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; grep -rn "silence-scan" src/ ; npx vitest run src/lib/trigger-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/lib/trigger-engine.ts` exists and exports `startTriggerEngine` returning a stop function; `apps/api/src/lib/silence-scan.ts` no longer exists
    - `grep -rn "silence-scan" apps/api/src` returns zero source references (imports updated everywhere)
    - `grep -n "node:timers/promises" apps/api/src/lib/trigger-engine.ts` present; raw callback `setInterval(` for the scan loop removed
    - `grep -n "trigger:silence_gate" apps/api/src/lib/trigger-engine.ts` present inside a CallbackHandler tags array passed to graph.invoke's `callbacks`
    - `grep -n "supabase\|serviceClient\|branchId" apps/api/src/lib/trigger-engine.ts` shows these added to `config.configurable`; `botOverrides` NOT added to this call site
    - `grep -n "'60000'" apps/api/src/lib/trigger-engine.ts` shows the new SCAN_INTERVAL_MS default
    - `cd apps/api &amp;&amp; npx vitest run src/lib/trigger-engine.test.ts` passes
  </acceptance_criteria>
  <done>Generalized, drift-aware, cancellable, Langfuse-traced TriggerEngine with 1-minute interval and reachability config keys; old filename fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add Blueprint-opt-in phase-readiness coupling to silence fires (D-03/D-04)</name>
  <files>apps/api/src/lib/trigger-engine.ts, apps/api/src/lib/trigger-engine.test.ts</files>
  <read_first>
    - apps/api/src/lib/skills/phase-readiness.ts (phaseReadinessSkill.detect(context) + buildPromptGuidance(context); this Skill sets only an ADVISORY signal, never advances phase autonomously — HUMAN-02/T-13-11 invariant)
    - apps/api/src/lib/trigger-engine.ts (the scanBranch silence-fire path from Task 1)
    - packages/types/src/blueprint.ts (the `silence_phase_readiness_coupling_enabled` toggle from Plan 01)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-03: silence can mean "redirect" (existing Phase 11 D-13/D-14 content-aware question) OR "suggest advancing"; D-04: Blueprint-configurable, opt-in)
  </read_first>
  <action>
    In the silence-fire path, when and only when the Blueprint's `silence_phase_readiness_coupling_enabled` toggle is true (default false → existing behavior unchanged for all current Blueprints), also invoke `phaseReadinessSkill.detect(context)` and make its advisory signal available to the Coach so it can weigh two options: "redirect the conversation" (the existing Phase 11 content-aware silence question, D-13/D-14 — reused as-is) versus "suggest advancing to the next phase" (e.g. GROW coaching's Options stage). Reuse the Skill's existing `detect()` and `buildPromptGuidance()` — do NOT duplicate its N/M gate or coverage-judgment logic (RESEARCH Don't-Hand-Roll). Preserve the invariant that phase-readiness is advisory only: the Coach may suggest advancing in its message, but nothing here auto-advances the phase (HUMAN-02/T-13-11). When the toggle is false, `detect()` must not be called at all (no added cost for non-opted-in Blueprints). Add a test: toggle off → `phaseReadinessSkill.detect` is not called on a silence fire; toggle on → it is called and its guidance reaches the fired prompt path.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; npx vitest run src/lib/trigger-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "phaseReadiness\|silence_phase_readiness_coupling_enabled" apps/api/src/lib/trigger-engine.ts` shows the toggle-gated call
    - Test asserts `detect()` is NOT called when the toggle is false and IS called when true
    - No auto-advance of phase occurs (advisory-only invariant preserved)
    - `cd apps/api &amp;&amp; npx vitest run src/lib/trigger-engine.test.ts` passes
  </acceptance_criteria>
  <done>Silence fires weigh phase-readiness only for opted-in Blueprints, reusing the existing Skill, advisory-only.</done>
</task>

<task type="auto">
  <name>Task 3: Graceful shutdown wiring + 5-minute auto-freeze (D-05)</name>
  <files>apps/api/src/server.ts, apps/api/src/lib/auto-freeze.ts, apps/api/src/lib/auto-freeze.test.ts</files>
  <read_first>
    - apps/api/src/server.ts (the full 33-line file; serve() callback lines 88-110 calling startSilenceScanLoop; the [panelito/api] log prefix convention)
    - apps/api/src/lib/auto-freeze.ts (FREEZE_AFTER_MS at line 33; production-minimum warning lines 42-47; GRACE_MS lines 36-41; clearAllTrackers export at line 181)
    - apps/api/src/lib/auto-freeze.test.ts (existing coverage to keep green after the constant change)
    - .planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md (Pattern 2 shutdown; D-05 rationale — bound proactive spend to ~5 fires before freeze)
  </read_first>
  <action>
    In `server.ts`: rename the `startSilenceScanLoop` import/call to `startTriggerEngine`, capture its returned stop function, and register `process.on('SIGTERM', ...)` and `process.on('SIGINT', ...)` handlers that call the stop function AND `clearAllTrackers()` (already exported from `auto-freeze.ts:181`, zero callers today) before `process.exit(0)`, using the existing `[panelito/api]` log prefix. Follow RESEARCH Pattern 2. In `auto-freeze.ts`: change the `FREEZE_AFTER_MS` default from `'900000'` to `'300000'` (5 minutes, D-05 — since the TriggerEngine now fires billable proactive messages on a ~1-minute cadence, an absent creator should accrue ~5 fires' worth of spend, not ~20), and lower the production-minimum warning threshold (lines 42-47) from `900_000` to `300_000` so the new intended default does not spuriously warn. Do NOT change `GRACE_MS` or its floor (D-05: the 30-second grace period is unchanged). No other changes to `auto-freeze.ts`.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; grep -n "SIGTERM\|SIGINT\|startTriggerEngine\|clearAllTrackers" src/server.ts ; grep -n "300000" src/lib/auto-freeze.ts ; npx vitest run src/lib/auto-freeze.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "process.on('SIGTERM'\|process.on(\"SIGTERM\"" apps/api/src/server.ts` present, and the handler calls the trigger-engine stop fn + `clearAllTrackers()`
    - `server.ts` imports/calls `startTriggerEngine` (not `startSilenceScanLoop`)
    - `grep -n "'300000'" apps/api/src/lib/auto-freeze.ts` shows the new FREEZE_AFTER_MS default; production-minimum warning threshold lowered to 300_000
    - `GRACE_MS` unchanged (`grep -n "GRACE" apps/api/src/lib/auto-freeze.ts` shows no numeric change)
    - `cd apps/api &amp;&amp; npx vitest run src/lib/auto-freeze.test.ts` passes
  </acceptance_criteria>
  <done>SIGTERM/SIGINT cleanly stop the loop + trackers; auto-freeze fires after 5 minutes; grace period unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| timer loop → creator BYOK key spend | Proactive fires bill the creator; frequency must stay bounded |
| proactive graph.invoke → Langfuse | Trace tags must be non-sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04a | Denial of Service | TriggerEngine proactive fires (BYOK cost) | mitigate | Preserve the existing budget circuit breaker (checkBotBudget/BOT-01), arbitration lock (BOT-02), two-signal silence gate (BOT-03) unchanged; D-05 lowers auto-freeze to 5 min to bound the new proactive surface; do not introduce any path that bypasses these gates |
| T-14-04b | Denial of Service | scan loop tick overlap / runaway | mitigate | Async-iterator setInterval awaits each tick before the next (no overlap); per-tick try/catch isolates failures; SCAN_INTERVAL_FLOOR_MS warning guards against misconfigured sub-5s intervals |
| T-14-04c | Information Disclosure | proactive CallbackHandler tags | mitigate | Only session/branch/trigger:silence_gate enum strings in tags; no key material (T-06-05) |
| T-14-04d | Denial of Service | graceful shutdown | mitigate | SIGTERM/SIGINT stop the loop + clearAllTrackers before exit, preventing orphaned timers accumulating spend across restarts |
| T-14-SC | Tampering | npm installs | accept | No new packages; node:timers/promises is a Node 22 built-in (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `grep -rn "silence-scan" apps/api/src` returns zero
- `cd apps/api &amp;&amp; npx vitest run src/lib/trigger-engine.test.ts src/lib/auto-freeze.test.ts` passes
- `grep -n "SIGTERM" apps/api/src/server.ts` and `grep -n "300000" apps/api/src/lib/auto-freeze.ts` both return matches
- `cd apps/api &amp;&amp; npx tsc --noEmit` compiles clean
</verification>

<success_criteria>
- Persistent, cancellable TriggerEngine loop on the standalone server, 1-minute silence re-evaluation, traced in Langfuse (TRIGGER-07 + COST-03 proactive gap closed)
- Blueprint-opt-in phase-readiness coupling on silence fires (D-03/D-04)
- SIGTERM/SIGINT graceful shutdown; 5-minute auto-freeze (D-05)
</success_criteria>

<developer_flag>
`participantId` resolution for silence-fires is intentionally NOT implemented here (see plan note / planner return). This plan wires `supabase`/`serviceClient`/`branchId` (required per 14-CONTEXT canonical_refs) so Coach personalization can reach the proactive path, but does not resolve a specific `participantId` from the last human author. This is flagged for developer decision — Coach personalization degrades gracefully to a generic summary without it and never throws.
</developer_flag>

<output>
Create `.planning/phases/14-polish-triggerengine-wiring/14-04-SUMMARY.md` when done
</output>
