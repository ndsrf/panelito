---
phase: 14-polish-triggerengine-wiring
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/api/src/lib/langfuse-generation.ts
  - apps/api/src/lib/langfuse-generation.test.ts
  - apps/api/src/lib/langfuse-otel.ts
  - apps/api/src/lib/adapters/anthropic.ts
  - apps/api/src/lib/adapters/openai.ts
  - apps/api/src/lib/adapters/gemini.ts
  - apps/api/src/lib/adapters/openai.test.ts
  - apps/api/src/lib/adapters/gemini.test.ts
autonomous: true
requirements: [COST-03]

must_haves:
  truths:
    - "Each provider adapter emits a real `usage` event with provider-reported token counts before `done`"
    - "A reusable helper constructs a Langfuse Generation observation with usageDetails so per-generation cost populates in the dashboard"
    - "Langfuse traces carry a dev/production environment set at the processor level (D-13: native environment field, not a free-text tag)"
  artifacts:
    - path: "apps/api/src/lib/langfuse-generation.ts"
      provides: "manual Generation observation helper wrapping an adapter stream"
      exports: ["*"]
      min_lines: 30
    - path: "apps/api/src/lib/adapters/anthropic.ts"
      provides: "usage AIStreamEvent emission"
      contains: "type: 'usage'"
  key_links:
    - from: "apps/api/src/lib/langfuse-generation.ts"
      to: "@langfuse/tracing"
      via: "manual generation observation"
      pattern: "@langfuse/tracing"
    - from: "apps/api/src/lib/adapters/anthropic.ts"
      to: "AIStreamEvent usage member"
      via: "enqueue usage event"
      pattern: "type: 'usage'"
---

<objective>
Build the cost-observability infrastructure COST-03 requires but which cannot "just work" via tags alone (confirmed by RESEARCH: this project routes every LLM call through custom `adapter.stream()` generators, not LangChain `BaseChatModel`, so Langfuse's CallbackHandler never sees model name or token usage). Three pieces: (1) a verified `@langfuse/tracing` helper that constructs a manual Generation observation with real `usageDetails`, (2) extend all three provider adapters to emit a `usage` AIStreamEvent carrying provider-reported token counts, and (3) set the Langfuse `environment` field at the processor level (D-13).

Purpose: Without adapter-surfaced usage + manual Generation observations, the Langfuse dashboard shows $0.00 cost per generation (Pitfall 1). Plan 06 consumes this helper + usage events inside the Role nodes.
Output: New `langfuse-generation.ts` helper + test, usage-event emission in all three adapters, environment field in `langfuse-otel.ts`.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md
@.planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md
@.planning/phases/14-polish-triggerengine-wiring/14-PATTERNS.md

<interfaces>
AIStreamEvent usage member (from Plan 01, packages/types/src/ai.ts):
  { type: 'usage'; inputTokens: number; outputTokens: number }

Anthropic adapter today (anthropic.ts:90-165): queue-bridge with enqueue()/waitForItem(),
  apiStream.on('text',...)/on('contentBlock',...), donePromise = apiStream.done(),
  outer try { ... } finally { yield { type: 'done' } }. Already calls finalMessage() for donePromise.

OpenAI adapter today (openai.ts:46-128): for await (const chunk of stream) loop; tool_use emitted
  only after finish_reason === 'tool_calls'. Doc-comment line 9: NEVER call finalMessage() (latency).
  Needs stream_options: { include_usage: true } on the client.chat.completions.stream({...}) call (line ~73).

Langfuse null-safe convention (langfuse-otel.ts flushLangfuse, lines 83-91): never let a Langfuse
  call throw; wrap in try/catch with '[langfuse-otel] ... (non-fatal)' warn.

LangfuseSpanProcessor construction (langfuse-otel.ts:66-71): new LangfuseSpanProcessor() —
  environment is a constructor option here (or LANGFUSE_TRACING_ENVIRONMENT env fallback), NOT per-request.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify @langfuse/tracing API shape and build the Generation helper</name>
  <files>apps/api/src/lib/langfuse-generation.ts, apps/api/src/lib/langfuse-generation.test.ts</files>
  <read_first>
    - apps/api/node_modules/@langfuse/tracing/dist/index.d.ts (confirm the EXACT exported symbol for creating a manual observation — `startObservation` vs `startActiveObservation` — and the shape of `{ asType: 'generation' }`, `.update({ usageDetails, model, metadata })`, `.end()`; RESEARCH Assumption A1 flags this is unexercised in-repo)
    - apps/api/src/lib/langfuse-otel.ts (copy the `flushLangfuse()` null-safe/never-throws wrapper style at lines 83-91)
    - apps/api/src/lib/model-config.ts (TASK_MODELS shape — model name comes from TASK_MODELS[provider][taskType])
    - .planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md (Pattern 3 + Pitfall 1; Open Question 3 — start with this API-shape confirmation before wiring node call sites)
  </read_first>
  <action>
    First confirm against the installed `@langfuse/tracing/dist/index.d.ts` the exact export name and call signature for a manual generation observation (the RESEARCH pattern assumes `startObservation(name, { asType: 'generation' }, { model, input })` then `.update({ usageDetails, metadata })` then `.end()`, but 5.9.1 may name it `startActiveObservation` — use whatever the `.d.ts` actually exports; this is Open Question 3 / Assumption A1). Then create `langfuse-generation.ts` exporting a helper that, given a name, a model string, a metadata object `{ trigger, tier }`, and an `AsyncIterable<AIStreamEvent>` (the adapter stream), constructs a generation observation, iterates the stream forwarding `text_delta` to an optional `streamWriter` callback and capturing the `usage` event's `inputTokens`/`outputTokens`, then closes the observation with `usageDetails: { input, output }` (Langfuse ingested usage takes priority over model-based inference) plus the `model` and `metadata`. Every `@langfuse/tracing` call (start/update/end) MUST be wrapped so a Langfuse SDK failure never aborts the LLM turn — copy the `flushLangfuse()` never-throws pattern (`[langfuse-generation] ... (non-fatal)` warn on catch, then continue). The helper must still return/yield the assistant text even when Langfuse is disabled (no keys) so the Role nodes are unaffected. Write `langfuse-generation.test.ts` feeding a fake async iterable that yields two `text_delta` events and one `usage` event, asserting the collected text and that the observation received `usageDetails` with the expected input/output numbers (mock the `@langfuse/tracing` module).
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; npx vitest run src/lib/langfuse-generation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `langfuse-generation.ts` imports from `@langfuse/tracing` using the export name actually present in the installed `.d.ts` (confirmed, not assumed)
    - The helper forwards `text_delta` to the streamWriter and captures `usage` into `usageDetails` `{ input, output }`
    - All Langfuse calls are wrapped so a thrown SDK error is caught and logged non-fatally (`grep -n "non-fatal" apps/api/src/lib/langfuse-generation.ts`)
    - `cd apps/api &amp;&amp; npx vitest run src/lib/langfuse-generation.test.ts` passes
  </acceptance_criteria>
  <done>Generation helper exists, API shape confirmed against installed SDK, usage → usageDetails path tested green, never throws into the caller.</done>
</task>

<task type="auto">
  <name>Task 2: Emit `usage` events from all three provider adapters</name>
  <files>apps/api/src/lib/adapters/anthropic.ts, apps/api/src/lib/adapters/openai.ts, apps/api/src/lib/adapters/gemini.ts, apps/api/src/lib/adapters/openai.test.ts, apps/api/src/lib/adapters/gemini.test.ts</files>
  <read_first>
    - apps/api/src/lib/adapters/anthropic.ts (queue-bridge + on('text')/on('contentBlock') + donePromise = apiStream.done() at lines 90-165; it already awaits finalMessage() for donePromise)
    - apps/api/src/lib/adapters/openai.ts (doc-comment line 9: NEVER finalMessage(); the for-await chunk loop and finish_reason handling lines 46-128; the stream construction ~line 73)
    - apps/api/src/lib/adapters/gemini.ts (same AIProvider.stream() try/finally contract)
    - apps/api/src/lib/adapters/openai.test.ts, gemini.test.ts (existing adapter test patterns to extend)
  </read_first>
  <action>
    Extend each adapter's `stream()` to emit exactly one `{ type: 'usage'; inputTokens; outputTokens }` event (using camelCase field names from Plan 01's union) after the model call resolves and BEFORE the outer `finally { yield { type: 'done' } }` — `done` must remain emitted exactly once, outermost (Pitfall 4). Provider specifics:
    - Anthropic (`anthropic.ts`): the adapter already resolves `apiStream.finalMessage()` for `donePromise`; reuse that same resolved final message and enqueue a `usage` event reading `finalMessage.usage.input_tokens` → `inputTokens` and `finalMessage.usage.output_tokens` → `outputTokens` through the existing `enqueue()` queue bridge (do NOT build a second delivery path; this is free — no extra network round-trip since `finalMessage()` is already awaited).
    - OpenAI (`openai.ts`): add `stream_options: { include_usage: true }` to the `client.chat.completions.stream({...})` call so a terminal chunk carries `chunk.usage`; when a chunk with `usage` arrives inside the existing `for await` loop, `yield { type: 'usage', inputTokens: chunk.usage.prompt_tokens, outputTokens: chunk.usage.completion_tokens }` (confirm exact field names against the installed OpenAI SDK — RESEARCH Assumption A2). Do NOT introduce a `finalMessage()` call — the adapter's doc-comment forbids it for latency reasons; read usage off the streamed terminal chunk only.
    - Gemini (`gemini.ts`): capture `usageMetadata` off the final streamed chunk and emit `{ type: 'usage', inputTokens: usageMetadata.promptTokenCount, outputTokens: usageMetadata.candidatesTokenCount }` (confirm exact field names against the installed Gemini SDK — Assumption A2). Use `anthropic.ts`/`openai.ts` as the two side-by-side reference shapes.
    If a provider does not surface usage on a given response, do not throw and do not emit a `usage` event with fabricated/estimated numbers — omit it (the Generation helper already treats missing usage as `usageDetails: undefined`). Never estimate tokens from character counts (RESEARCH Don't-Hand-Roll). Extend `openai.test.ts` and `gemini.test.ts` to assert a `usage` event with numeric fields is emitted when the mock stream includes usage data.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; npx vitest run src/lib/adapters/</automated>
  </verify>
  <acceptance_criteria>
    - Each of `anthropic.ts`, `openai.ts`, `gemini.ts` contains a `type: 'usage'` emission (`grep -rln "type: 'usage'" apps/api/src/lib/adapters` lists all three)
    - `openai.ts` sets `include_usage: true` (`grep -n "include_usage" apps/api/src/lib/adapters/openai.ts`)
    - `openai.ts` does NOT call `finalMessage()` (`grep -n "finalMessage" apps/api/src/lib/adapters/openai.ts` returns nothing new)
    - `done` remains emitted exactly once per adapter in the outer `finally`
    - `cd apps/api &amp;&amp; npx vitest run src/lib/adapters/` passes
  </acceptance_criteria>
  <done>All three adapters surface real provider-reported token counts as a `usage` event; done-once invariant preserved; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Set the Langfuse environment field at the processor level</name>
  <files>apps/api/src/lib/langfuse-otel.ts</files>
  <read_first>
    - apps/api/src/lib/langfuse-otel.ts (the `new LangfuseSpanProcessor()` construction at lines 66-71; the `_getState()`/globalThis hot-reload guard at lines 40-46 — preserve it)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-13: environment is Langfuse's native first-class field, distinct from free-text tags)
    - .planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md (Anti-Patterns + Assumption A3: environment is a LangfuseSpanProcessor option OR the LANGFUSE_TRACING_ENVIRONMENT env var — confirm which the installed 5.9.1 supports)
  </read_first>
  <action>
    Configure the Langfuse `environment` at the processor level, NOT per-request. Confirm against the installed `@langfuse/otel@5.9.1` whether `LangfuseSpanProcessor`'s constructor accepts an `environment` option; if it does, pass a resolved environment value (e.g. from `process.env.LANGFUSE_TRACING_ENVIRONMENT ?? (process.env.NODE_ENV === 'production' ? 'production' : 'development')`) to the `new LangfuseSpanProcessor(...)` call at lines 66-71. If the installed version does not accept the constructor option (Assumption A3), instead ensure `LANGFUSE_TRACING_ENVIRONMENT` is honored (document that deployment must set it) — do not invent a per-request tag as a substitute. Preserve the existing `_getState()`/globalThis hot-reload guard and the existing graceful no-op-when-keys-absent behavior (setup must not throw when Langfuse keys are unset).
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; grep -n "environment\|LANGFUSE_TRACING_ENVIRONMENT" src/lib/langfuse-otel.ts ; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `langfuse-otel.ts` sets environment at the processor level or via `LANGFUSE_TRACING_ENVIRONMENT` (confirmed by grep), and NOT as a per-request CallbackHandler tag
    - `setupLangfuseOtel()` still does not throw when `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY` are absent (existing behavior preserved)
    - `cd apps/api &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Traces carry a dev/production environment at the processor level; no-key no-op behavior intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider SDK response → adapter event | Token counts read from provider responses; must not be fabricated |
| app → Langfuse Cloud | Trace/tag/metadata must not carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03a | Information Disclosure | Generation metadata/tags | mitigate | trigger/tier/environment are non-sensitive enum strings; no API keys or message PII placed in observation input beyond existing system-prompt handling (T-06-05 convention) |
| T-14-03b | Denial of Service | Langfuse SDK calls in hot LLM path | mitigate | All `@langfuse/tracing` calls wrapped never-throw; a Langfuse outage or thrown error must not abort a Coach/Analyst turn (flushLangfuse precedent) |
| T-14-03c | Tampering | token-count integrity | mitigate | Usage read from provider-reported fields only; never estimated from char counts (RESEARCH Don't-Hand-Roll); missing usage → undefined, not a fabricated number |
| T-14-SC | Tampering | npm installs | accept | No new packages; `@langfuse/tracing@5.9.1` already installed (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cd apps/api &amp;&amp; npx vitest run src/lib/langfuse-generation.test.ts src/lib/adapters/` passes
- `grep -rln "type: 'usage'" apps/api/src/lib/adapters` lists all three adapters
- `cd apps/api &amp;&amp; npx tsc --noEmit` compiles clean
</verification>

<success_criteria>
- Manual Generation helper exists with confirmed SDK API, never throws into callers (COST-03 infra)
- All three adapters emit real provider-reported usage events (COST-03 enabler for Plan 06)
- Langfuse environment set at processor level (D-13)
</success_criteria>

<output>
Create `.planning/phases/14-polish-triggerengine-wiring/14-03-SUMMARY.md` when done
</output>
