---
phase: 14-polish-triggerengine-wiring
plan: 07
type: execute
wave: 4
depends_on: ["14-02", "14-03", "14-04", "14-05", "14-06"]
files_modified:
  - scripts/synthetic-session.ts
autonomous: false
requirements: [PERSONA-04, TRIGGER-07, COST-03, SPEECH-01, SPEECH-02, SPEECH-03]

must_haves:
  truths:
    - "A synthetic-session harness can drive ~100 bot invocations and assert output discipline"
    - "A human confirms the TriggerEngine runs live, discipline holds, no artifact strings appear, and Langfuse shows cost-by-trigger-type"
  artifacts:
    - path: "scripts/synthetic-session.ts"
      provides: "100-turn synthetic session harness"
      min_lines: 40
  key_links:
    - from: "scripts/synthetic-session.ts"
      to: "the compiled graph + Role nodes"
      via: "repeated graph.invoke on a bot thread"
      pattern: "invoke"
---

<objective>
Verify the phase's five ROADMAP success criteria that cannot be proven by unit tests alone: the live TriggerEngine end-to-end fire, the 100-turn synthetic-session persona discipline + re-anchor visibility, the absence of any artifact string across all 6 trigger types, and Langfuse cost attribution by trigger type. Task 1 builds the synthetic-session harness (auto); Task 2 is the human verification checkpoint that runs it, runs a live session, and inspects the Langfuse dashboard.

Purpose: Success Criteria 1-5 are live/observability assertions. Per the Phase 11 precedent (no browser-automation tool in this execution environment), the live checks may be accepted via code-review evidence, but the harness and dashboard inspection are the concrete verification artifacts.
Output: `scripts/synthetic-session.ts` harness; a human-verified confirmation of the phase criteria.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md
@.planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md

<interfaces>
containsSpeechArtifact (Plan 01, @panelito/types) — reuse for the harness assertions.
Compiled graph: createGraph (apps/api/src/graph/graph.ts) + getCheckpointer — the same wiring
  trigger-engine.ts uses to invoke the bot thread `${branchId}:bot`.
ROADMAP §Phase 14 Success Criteria 1-5 — the exact assertions this plan verifies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 100-turn synthetic-session harness</name>
  <files>scripts/synthetic-session.ts</files>
  <read_first>
    - apps/api/src/lib/trigger-engine.ts (how the bot thread is invoked — mirror its graph.invoke config shape)
    - apps/api/src/graph/graph.ts (createGraph + routeFromStart)
    - packages/types/src/speech-artifacts.ts (containsSpeechArtifact for artifact assertions)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (Success Criteria 2/3 wording; the synthetic session is this phase's own re-anchor verification step)
  </read_first>
  <action>
    Create `scripts/synthetic-session.ts` — a runnable harness (via `tsx`) that drives a session's bot thread through approximately 100 Coach/Analyst invocations against a real BYOK provider key (read from env; document that running it consumes real API budget). For each captured bot output it asserts and reports: (a) every Coach output ends with a trailing question mark, (b) every Analyst output cites a specific prior message, (c) no output contains any string for which `containsSpeechArtifact(output)` is true (across all 6 trigger types exercised), and (d) it logs the invocation numbers at which the persona re-anchor fires (15, 30, 45, ...) so the operator can cross-check them against Langfuse traces. The harness prints a pass/fail summary per criterion at the end. It must fail gracefully with a clear message if no provider key is configured (so it can be dry-invoked in CI without a key). Keep it a standalone script — do not import it into production runtime paths.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito &amp;&amp; test -f scripts/synthetic-session.ts &amp;&amp; echo "harness present"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/synthetic-session.ts` exists and is runnable via `tsx scripts/synthetic-session.ts`
    - It imports `containsSpeechArtifact` and asserts (a) Coach trailing `?`, (b) Analyst prior-message citation, (c) no artifact strings, (d) logs re-anchor firing invocation numbers
    - Without a provider key it exits with a clear "no key configured" message rather than throwing an unhandled error
    - It is not imported by any production runtime module (`grep -rn "synthetic-session" apps/api/src apps/web` returns nothing)
  </acceptance_criteria>
  <done>Runnable synthetic-session harness exists, asserting all four discipline/artifact criteria and logging re-anchor cadence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase verification checkpoint (live TriggerEngine + synthetic session + Langfuse dashboard)</name>
  <action>
    Human-verify the five ROADMAP Phase 14 success criteria against the running system using the steps in how-to-verify below. This is a verification-only checkpoint; no production code is changed. If browser/live automation is unavailable in this environment, accept via code-review evidence per the Phase 11 precedent and record the precise unverified gap in the SUMMARY's "Known Issues / Not Verified" section.
  </action>
  <what-built>
    The full Phase 14 stack: persistent TriggerEngine loop on the standalone server (1-min silence re-evaluation, graceful SIGTERM/SIGINT shutdown, 5-min auto-freeze), Langfuse cost attribution (trigger + tier tags, real usageDetails via adapter usage events + manual Generation observations, environment field), persona re-anchor every 15 invocations, and speech-artifact filtering (backend skip-insert + frontend zero-presence). Plus the synthetic-session harness from Task 1.
  </what-built>
  <how-to-verify>
    1. Start the standalone API server (`apps/api` — e.g. `npm run dev` or the server entrypoint) and confirm the log line `[trigger-engine] scan loop started (interval: 60000ms)` appears, and that the loop keeps ticking across a full session lifetime without stopping (Success Criterion 1).
    2. In a live session with a Coach-enabled Blueprint, leave the branch silent past the silence threshold and confirm a proactive Coach message appears in chat end-to-end (Criterion 1). Send SIGINT (Ctrl-C) and confirm `[panelito/api] received SIGINT, shutting down` and a clean exit (no orphaned timer).
    3. Run the synthetic harness: `tsx scripts/synthetic-session.ts` with a provider key set. Confirm the summary reports zero Coach outputs without a trailing `?`, zero Analyst outputs without a prior-message citation, and zero artifact strings across all 6 trigger types (Criteria 2 & 3). Note the re-anchor firing invocation numbers it logs.
    4. In the Langfuse dashboard: confirm (a) per-generation cost is populated (non-zero) for Coach/Analyst generations — not $0.00 (Criterion 5); (b) traces are grouped/filterable by `trigger:*` tag and you can identify which trigger type is most expensive without querying the DB (Criterion 5); (c) the persona re-anchor is observable in traces at the invocation numbers logged in step 3 (Criterion 2); (d) both proactive (`trigger:silence_gate`) and reactive (`trigger:human-reactive`) fires appear tagged.
    5. Defense-in-depth check (Criterion 4): inject a test message whose content contains `[canvas updated]` and confirm the frontend renders no row / empty text (silent drop).
  </how-to-verify>
  <resume-signal>Type "approved" if all five criteria hold (or are accepted via code-review with the gap recorded), or describe the failures to feed a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic harness → creator BYOK key | The harness spends real API budget; must be explicit and opt-in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07a | Denial of Service | synthetic harness API spend | mitigate | Harness requires an explicit provider key and documents that it consumes real budget; exits cleanly without a key (no accidental spend); it is not wired into any automatic/CI runtime path |
| T-14-07b | Information Disclosure | harness output / logs | mitigate | Harness logs discipline results and invocation numbers, never key material (T-06-05) |
| T-14-SC | Tampering | npm installs | accept | No new packages; harness uses existing graph + `@panelito/types` (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `scripts/synthetic-session.ts` exists and runs via tsx; exits cleanly without a key
- Human checkpoint approved (or accepted via code-review with the gap recorded in SUMMARY)
- All five ROADMAP Phase 14 success criteria confirmed or their gaps explicitly documented
</verification>

<success_criteria>
- TriggerEngine runs live and survives the session lifetime; silence fire produces a bot message end-to-end (Criterion 1)
- 100-turn synthetic session: no Coach output without `?`, no Analyst output without citation; re-anchor fires every 15, visible in Langfuse (Criterion 2)
- No artifact string across all 6 trigger types; canvas confirmations only on the canvas (Criterion 3)
- Injected artifact-pattern message silently dropped at the frontend (Criterion 4)
- Langfuse dashboard shows cost attribution by trigger type; most-expensive trigger identifiable without DB queries (Criterion 5)
</success_criteria>

<output>
Create `.planning/phases/14-polish-triggerengine-wiring/14-07-SUMMARY.md` when done
</output>
