---
phase: 14-polish-triggerengine-wiring
plan: 05
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/web/components/workspace/MessageBubble.tsx
  - apps/web/components/workspace/MessageList.tsx
autonomous: true
requirements: [SPEECH-02, SPEECH-03]

must_haves:
  truths:
    - "A message whose content contains a system artifact string renders as empty text (SPEECH-02 defense-in-depth) (D-09: two filtering layers only — prompt discipline + frontend filter, no third backend layer)"
    - "A canvas-only assistant message (content is a system artifact) produces no chat row at all — no bubble, no icon (SPEECH-03/D-11)"
  artifacts:
    - path: "apps/web/components/workspace/MessageBubble.tsx"
      provides: "artifact-string filter on rendered content/streamingText"
      contains: "containsSpeechArtifact"
    - path: "apps/web/components/workspace/MessageList.tsx"
      provides: "zero-row suppression for artifact-only assistant messages"
      contains: "containsSpeechArtifact"
  key_links:
    - from: "apps/web/components/workspace/MessageList.tsx"
      to: "@panelito/types containsSpeechArtifact"
      via: "filter/early-return before render"
      pattern: "containsSpeechArtifact"
---

<objective>
Apply the shared speech-artifact filter (Plan 01) at the two frontend render layers: (1) `MessageBubble.tsx` blocks any content matching an artifact pattern by rendering an empty string as defense-in-depth (SPEECH-02, silent drop), and (2) `MessageList.tsx` suppresses the entire row for canvas-only assistant messages whose content is an artifact string, giving zero chat-stream presence (SPEECH-03/D-11 — no bubble, no icon, no row).

Purpose: Frontend half of SPEECH-02/03. The backend half (never writing the artifact string) is Plan 02; this is the defense-in-depth layer per D-09 (two layers only: prompt discipline + frontend filter).
Output: Filtered content render in `MessageBubble.tsx`, row suppression in `MessageList.tsx`.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md
@.planning/phases/14-polish-triggerengine-wiring/14-PATTERNS.md

<interfaces>
containsSpeechArtifact (from Plan 01): import { containsSpeechArtifact } from '@panelito/types'
  — substring match against SPEECH_ARTIFACT_BLOCKLIST.

MessageBubble.tsx render sites: message.content (line ~246, AI bubble; line ~282 human bubble);
  streamingText (lines ~237-243, with the animated cursor span).

MessageList.tsx: the .map() loop (lines ~244-293); existing isSystemMessage early-return branch
  (lines ~245-249) is the precedent for "this row renders differently based on a content predicate";
  fork-separator wrapper logic (lines ~276-290) keys off msg.id — a suppressed message must not break
  adjacent fork-point lookups.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter artifact strings out of rendered bubble content (SPEECH-02)</name>
  <files>apps/web/components/workspace/MessageBubble.tsx</files>
  <read_first>
    - apps/web/components/workspace/MessageBubble.tsx (the message.content render at line ~246; the streamingText render at lines ~237-243; the human bubble content at line ~282)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-09: frontend defense-in-depth filter; D-11: zero-presence decision lives one level up in MessageList, not here)
  </read_first>
  <action>
    Import `containsSpeechArtifact` from `@panelito/types`. At the render sites for `message.content` (line ~246) and `streamingText` (lines ~237-243), replace the displayed text with an empty string when `containsSpeechArtifact(...)` returns true (SPEECH-02 silent drop — no visible placeholder, no error). This is defense-in-depth: it catches an artifact string that somehow reached the client despite the backend fix (Plan 02). Do NOT implement full-row suppression here — that decision belongs one level up in `MessageList.tsx` (Task 2), matching the existing architectural split where `MessageList` decides which messages reach `MessageBubble` and `MessageBubble` only renders what it is given. Apply the filter to the completed-content path and the streaming path; leave the animated cursor span behavior otherwise intact.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; grep -n "containsSpeechArtifact" components/workspace/MessageBubble.tsx ; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "containsSpeechArtifact" apps/web/components/workspace/MessageBubble.tsx` shows the import and its use guarding both `message.content` and `streamingText`
    - When content contains a blocked pattern, the rendered text is empty (silent drop), not a placeholder
    - `cd apps/web &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Artifact-matching content renders empty at the bubble layer as defense-in-depth.</done>
</task>

<task type="auto">
  <name>Task 2: Suppress the entire row for canvas-only artifact messages (SPEECH-03/D-11)</name>
  <files>apps/web/components/workspace/MessageList.tsx</files>
  <read_first>
    - apps/web/components/workspace/MessageList.tsx (the .map() loop lines ~244-293; the isSystemMessage early-return branch lines ~245-249 to mirror; the fork-separator wrapper lines ~276-290 keyed on msg.id)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-11: canvas-only bot turns get zero chat-stream presence — no bubble, no icon, no row; confirmation lives entirely on the canvas)
  </read_first>
  <action>
    Import `containsSpeechArtifact` from `@panelito/types`. Suppress the entire row for assistant messages whose `content` matches an artifact pattern — either by adding an early `return null` branch inside the existing `.map()` callback (mirroring the `isSystemMessage` short-circuit shape at lines 245-249) for `msg.role === 'assistant' && containsSpeechArtifact(msg.content)`, or by a `.filter((msg) => !(msg.role === 'assistant' && containsSpeechArtifact(msg.content)))` before the `.map()`. Per D-11 this means zero presence: no bubble, no icon, no dimmed placeholder, no row. Choose whichever form integrates cleanly with the existing fork-separator wrapper logic (lines 276-290) so that removing a suppressed message does NOT break the fork-point lookup keyed on `msg.id` for adjacent messages — verify a suppressed message between two normal messages leaves their fork separators correct. Only assistant messages are subject to suppression; human/system messages are never suppressed by this filter.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; grep -n "containsSpeechArtifact" components/workspace/MessageList.tsx ; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "containsSpeechArtifact" apps/web/components/workspace/MessageList.tsx` shows the import and the suppression predicate scoped to `role === 'assistant'`
    - A canvas-only assistant message (content is a blocked artifact) yields no rendered row (null / filtered out)
    - Human and system messages are unaffected by the suppression
    - Fork-separator logic still resolves correctly across a suppressed message
    - `cd apps/web &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Canvas-only artifact assistant messages produce zero chat-stream presence; adjacent fork points intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message content (server/Realtime) → React render | Untrusted content; artifact strings must be filtered client-side regardless of position |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05a | Tampering | frontend artifact filter | mitigate | Substring `.includes()` matching (Plan 01) catches artifacts embedded mid-message; a compromised/malicious LLM response cannot evade via a whole-message-equality gap (RESEARCH Pitfall 3) |
| T-14-05b | Tampering | row suppression / fork lookup | mitigate | Suppression scoped to `role === 'assistant'` only; human/system rows never dropped; fork-point keying verified intact so suppression cannot silently corrupt adjacent message threading |
| T-14-SC | Tampering | npm installs | accept | No new packages; imports existing `@panelito/types` (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `grep -n "containsSpeechArtifact" apps/web/components/workspace/MessageBubble.tsx apps/web/components/workspace/MessageList.tsx` returns matches in both
- `cd apps/web &amp;&amp; npx tsc --noEmit` compiles clean
- Manual/render check (covered by phase verification Plan 07): injecting a message with a blocked pattern renders no row and no visible text
</verification>

<success_criteria>
- Artifact-matching content renders empty at the bubble layer (SPEECH-02)
- Canvas-only artifact assistant messages produce zero chat-stream presence (SPEECH-03/D-11)
</success_criteria>

<output>
Create `.planning/phases/14-polish-triggerengine-wiring/14-05-SUMMARY.md` when done
</output>
