---
phase: 14-polish-triggerengine-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/ai.ts
  - apps/api/src/routes/ai.test.ts
autonomous: true
requirements: [COST-03, SPEECH-01, SPEECH-03]

must_haves:
  truths:
    - "The human /invoke path's Langfuse trace carries a trigger-type tag so reactive cost can be compared against proactive cost (D-12: every LLM call tagged, not just proactive; D-13: trigger type + model tier)"
    - "A canvas-only bot turn (no chat text) writes NO message row — no '[canvas updated]' placeholder ever reaches the DB or chat stream (D-10: resolved by UI redesign, not different fallback text)"
  artifacts:
    - path: "apps/api/src/routes/ai.ts"
      provides: "CallbackHandler trigger tag + skip-insert for empty content"
      contains: "trigger:human-reactive"
  key_links:
    - from: "apps/api/src/routes/ai.ts"
      to: "Langfuse CallbackHandler"
      via: "tags array"
      pattern: "trigger:human-reactive"
---

<objective>
Two targeted edits to the human `/invoke` route: (1) extend the existing (and only) `CallbackHandler` construction with a `trigger:human-reactive` tag so ordinary human-reactive LLM calls appear in the same trigger-attributed Langfuse view as proactive fires (D-12/D-13), and (2) remove the `'[canvas updated]'` fallback-text write for canvas-only bot turns so those turns produce zero chat-stream presence (D-10/D-11, closing the last known SPEECH-01 violation in the codebase).

Purpose: Reactive-side of COST-03's "every LLM call tagged" decision, plus the backend half of SPEECH-03's "never through chat text" (the frontend half is Plan 05).
Output: Extended CallbackHandler tags + widened skip-insert condition in `ai.ts`, plus regression coverage in `ai.test.ts`.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md
@.planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md
@.planning/phases/14-polish-triggerengine-wiring/14-PATTERNS.md

<interfaces>
CallbackHandler construction (ai.ts:364-369) — the ONLY current site:
  new CallbackHandler({ tags: [`session:${sessionId}`, `branch:${activeBranchId ?? 'main'}`] })

Fallback write to remove (ai.ts:466-470):
  if (!accumulatedText.trim() && (finalState as any)?.canvasOps?.length > 0) {
    accumulatedText = '[canvas updated]'  // minimal fallback for messages_content_check constraint
  }
  ...then the existing guard: if (accumulatedText.length > 0) { ...INSERT... }

Existing skip-condition idiom to mirror (silence-scan.ts:277-280):
  if (accumulatedText.trim().length === 0) { ...; return }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trigger-type tag to the human-path CallbackHandler</name>
  <files>apps/api/src/routes/ai.ts</files>
  <read_first>
    - apps/api/src/routes/ai.ts (CallbackHandler construction at lines 364-369; note the per-request, never-module-level convention noted at line 365 / D-16)
    - .planning/phases/14-polish-triggerengine-wiring/14-CONTEXT.md (D-12: every LLM call tagged; D-13: tag = trigger type + model tier; environment goes on the processor, NOT here)
  </read_first>
  <action>
    Extend the `tags` array of the existing `CallbackHandler` at lines 364-369 to append the string `trigger:human-reactive` alongside the existing `session:${sessionId}` and `branch:${activeBranchId ?? 'main'}` tags. Do NOT add an `environment` tag here — the Langfuse `environment` field is configured at the processor level in `langfuse-otel.ts` (Plan 03), not per-request (RESEARCH Anti-Patterns). Do NOT add the model-tier tag here either — tier is per-node (fast for facilitation/haiku, capable for analysis/sonnet) and is applied on the Generation observation inside the Role nodes (Plan 06); the human call site cannot know which node will run. Preserve the per-request construction (do not hoist to module scope). This tags array is also the verbatim template the TriggerEngine's new CallbackHandler (Plan 04) copies, so keep it readable.
  </action>
  <verify>
    <automated>grep -n "trigger:human-reactive" apps/api/src/routes/ai.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "trigger:human-reactive" apps/api/src/routes/ai.ts` returns a line inside the CallbackHandler tags array
    - No `environment` string is added to the CallbackHandler tags (`grep -n "environment" apps/api/src/routes/ai.ts` shows no new per-request environment tag)
    - `cd apps/api &amp;&amp; npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Human-path Langfuse trace carries trigger:human-reactive; construction stays per-request.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the '[canvas updated]' fallback so canvas-only turns write no row</name>
  <files>apps/api/src/routes/ai.ts, apps/api/src/routes/ai.test.ts</files>
  <read_first>
    - apps/api/src/routes/ai.ts (lines 466-470 fallback branch; line 472 the `if (accumulatedText.length > 0)` INSERT guard; line 469 the exact string to delete)
    - apps/api/src/routes/ai.test.ts (existing test structure to extend with the canvas-only-turn case)
    - .planning/phases/14-polish-triggerengine-wiring/14-RESEARCH.md (Pitfall 2 — skip INSERT entirely when accumulatedText is empty regardless of canvasOps; messages_content_check is `length between 1 and 4000`)
  </read_first>
  <action>
    Delete the fallback branch at lines 466-470 that assigns `accumulatedText = '[canvas updated]'` when `accumulatedText` is empty but `canvasOps.length > 0`. Do not replace it with any other placeholder string. The existing `if (accumulatedText.length > 0) { ...INSERT... }` guard at line 472 then naturally skips the entire message INSERT whenever `accumulatedText.trim().length === 0`, regardless of whether `canvasOps` is present — this widens the existing no-canvasOps skip (T-07-08) to also cover the canvasOps-present case (D-10/D-11). Confirm no other consumer downstream of this block depends on a non-empty `messages.content` for these canvas-only rows (verify the SSE `phase_signal` emission at lines 465-482 and any node-count/badge logic still functions when no message row is written — the canvas mutation itself is persisted separately). Add a regression test to `ai.test.ts`: a bot turn that yields canvas ops but zero accumulated text results in NO `messages` INSERT call and no `'[canvas updated]'` string anywhere.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; grep -rn "\[canvas updated\]" src/ ; npx vitest run src/routes/ai.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\[canvas updated\]" apps/api/src` returns zero matches (SPEECH-01 violation removed)
    - `ai.test.ts` has a case asserting a canvas-only (empty-text) turn performs no message INSERT
    - `cd apps/api &amp;&amp; npx vitest run src/routes/ai.test.ts` passes
  </acceptance_criteria>
  <done>Canvas-only bot turns write no message row and no placeholder string; regression test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot LLM output → messages table | Empty/artifact content must not be coerced into a persisted chat row |
| app → Langfuse | Tag strings must be non-sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02a | Information Disclosure | CallbackHandler tags | mitigate | New `trigger:human-reactive` tag is a fixed non-sensitive enum string — no session secrets or key material in tags (T-06-05 convention preserved) |
| T-14-02b | Tampering | messages INSERT | mitigate | Removing the placeholder write eliminates the only path that persisted a forbidden artifact string; skip-insert relies on the existing DB CHECK constraint remaining intact, not on placeholder text |
| T-14-SC | Tampering | npm installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `grep -rn "\[canvas updated\]" apps/api/src` returns zero matches
- `grep -n "trigger:human-reactive" apps/api/src/routes/ai.ts` returns the tag
- `cd apps/api &amp;&amp; npx vitest run src/routes/ai.test.ts` passes
</verification>

<success_criteria>
- Human-reactive LLM calls are trigger-tagged in Langfuse (COST-03)
- No `'[canvas updated]'` placeholder is ever written; canvas-only turns produce no chat row (SPEECH-01, backend half of SPEECH-03)
</success_criteria>

<output>
Create `.planning/phases/14-polish-triggerengine-wiring/14-02-SUMMARY.md` when done
</output>
