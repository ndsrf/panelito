---
phase: 06-graph-construction-checkpointer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0009_sessions_current_phase.sql
  - apps/api/package.json
  - apps/api/src/lib/langgraph-checkpointer.ts
  - apps/api/src/lib/langfuse-otel.ts
autonomous: false
requirements: [BLUE-04, ORCH-05, OBS-01, OBS-02]
user_setup:
  - service: langfuse
    why: "OBS-01/OBS-02 trace export to Langfuse cloud; without keys, setupLangfuseOtel() warns and skips (unit tests still pass)"
    env_vars:
      - name: LANGFUSE_PUBLIC_KEY
        source: "Langfuse Dashboard → Settings → API Keys"
      - name: LANGFUSE_SECRET_KEY
        source: "Langfuse Dashboard → Settings → API Keys"
must_haves:
  truths:
    - "sessions.current_phase column exists in the database after migration 0009 is applied"
    - "getCheckpointer() returns a PostgresSaver bound to the langgraph schema and runs setup() idempotently"
    - "setupLangfuseOtel() registers a LangfuseSpanProcessor when credentials exist, and warns+skips when they do not"
  artifacts:
    - path: "supabase/migrations/0009_sessions_current_phase.sql"
      provides: "sessions.current_phase TEXT NULL column"
      contains: "current_phase"
    - path: "apps/api/src/lib/langgraph-checkpointer.ts"
      provides: "getCheckpointer() singleton (PostgresSaver, langgraph schema)"
      exports: ["getCheckpointer"]
    - path: "apps/api/src/lib/langfuse-otel.ts"
      provides: "setupLangfuseOtel(), getLangfuseSpanProcessor()"
      exports: ["setupLangfuseOtel", "getLangfuseSpanProcessor"]
  key_links:
    - from: "apps/api/src/lib/langgraph-checkpointer.ts"
      to: "env.SUPABASE_DIRECT_URL"
      via: "PostgresSaver.fromConnString with schema langgraph"
      pattern: "schema:\\s*['\"]langgraph['\"]"
    - from: "apps/api/src/lib/langfuse-otel.ts"
      to: "@langfuse/otel LangfuseSpanProcessor"
      via: "NodeTracerProvider span processor registration"
      pattern: "LangfuseSpanProcessor"
---

<objective>
Provision the infrastructure the graph depends on but that lives outside the graph directory: migration 0009 (adds `sessions.current_phase` for BLUE-04), the `@langfuse/otel` install, the `getCheckpointer()` PostgresSaver singleton (ORCH-05), and the `setupLangfuseOtel()` OTel bootstrap (OBS-01/OBS-02). These are all disjoint from the graph node files, so this plan runs in parallel with plan 06-01.

Purpose: Wave 2 graph construction can proceed against MemorySaver without these, but the Wave 3 integration test and Langfuse verification require the checkpointer singleton and OTel provider to exist. Building them in Wave 1 keeps the critical path short.

Output: applied migration 0009, `@langfuse/otel` + peer deps installed, `langgraph-checkpointer.ts` (new), `langfuse-otel.ts` (new).
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-graph-construction-checkpointer/06-CONTEXT.md
@.planning/phases/06-graph-construction-checkpointer/06-RESEARCH.md
@.planning/phases/06-graph-construction-checkpointer/06-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

From apps/api/src/lib/env.ts (lines 22-28):
- `env.SUPABASE_DIRECT_URL` is validated as a postgresql:// connection string (port 5432 direct connection — supports prepared statements; NO "prepare:false" option needed or available)

From apps/api/src/lib/supabase.ts (singleton/factory pattern to mirror):
- `export function createServiceClient()` — used for any Supabase read inside libs

From apps/api/src/lib/auto-freeze.ts + server.ts (startup wiring pattern):
- `startAutoFreezeTracker(createServiceClient())` is invoked in the `serve(...)` callback in server.ts. langfuse-otel setup follows the same startup-call pattern (wiring into server.ts happens in plan 06-04).

From @langchain/langgraph-checkpoint-postgres@1.0.4 (RESEARCH Pattern 3, VERIFIED from dist):
- `PostgresSaver.fromConnString(connString, { schema: 'langgraph' })` — default schema is 'public'; MUST pass 'langgraph' (RESEARCH Pitfall 3)
- `await checkpointer.setup()` — idempotent; creates langgraph.checkpoints/checkpoint_blobs/checkpoint_writes/checkpoint_migrations

From @langfuse/otel@5.9.1 + @langfuse/tracing (RESEARCH Pattern 4):
- `new LangfuseSpanProcessor()` — reads LANGFUSE_PUBLIC_KEY/LANGFUSE_SECRET_KEY from process.env (RESEARCH A4, verify after install)
- `new NodeTracerProvider({ spanProcessors: [...] })` from @opentelemetry/sdk-trace-base; `.register()`
- `setLangfuseTracerProvider(provider)` from @langfuse/tracing
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing yet — this checkpoint gates the `@langfuse/otel@5.9.1` install because it is a new [ASSUMED] package (installed for the first time in this project). RESEARCH.md Package Legitimacy Audit marked it [OK] (same monorepo as @langfuse/langchain, published 2025-08-07), but a package-manager install requires a blocking human verification per security policy.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@langfuse/otel and confirm it is published by the `langfuse` org, version 5.9.1 exists, and it is part of the langfuse/langfuse-js monorepo.
    2. Confirm @langfuse/langchain (already installed in Phase 5) is from the same publisher.
    3. Confirm no postinstall scripts (research reported none).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Apply migration 0009 (sessions.current_phase) and install @langfuse/otel</name>
  <files>supabase/migrations/0009_sessions_current_phase.sql, apps/api/package.json</files>
  <read_first>
    - supabase/migrations/0008_nsai_foundation.sql (migration header comment style, IF NOT EXISTS guards, COMMENT ON COLUMN pattern)
    - apps/api/package.json (current dependency list — confirm @langfuse/langchain present, @langfuse/otel absent)
  </read_first>
  <action>
    Create `supabase/migrations/0009_sessions_current_phase.sql` with a header comment (mirror 0008 style) and: `ALTER TABLE public.sessions ADD COLUMN IF NOT EXISTS current_phase TEXT NULL DEFAULT NULL;` followed by `COMMENT ON COLUMN public.sessions.current_phase IS 'Active Blueprint phase id (references blueprint.phase_sequence[].id). NULL means use blueprint.phase_sequence[0].id as fallback. Updated only by explicit human action (HUMAN-02).';`. Apply it locally with `supabase migration up`. Then install the observability packages (only after the checkpoint above is approved): run `pnpm --filter @panelito/api add @langfuse/otel@5.9.1 @opentelemetry/sdk-trace-base@2.8.0 @opentelemetry/exporter-trace-otlp-http@0.219.0 @opentelemetry/core@2.8.0` — the OTel peer deps are added explicitly because pnpm strict mode does not auto-resolve them (RESEARCH Open Question 2 / A2). NULL default is required so the one existing dev session is not broken (RESEARCH Gap 1). Do NOT add any langgraph.* table DDL to this migration — PostgresSaver.setup() owns those tables (RESEARCH Pitfall 4).
  </action>
  <verify>
    <automated>grep -q "current_phase" supabase/migrations/0009_sessions_current_phase.sql && node -e "const p=require('./apps/api/package.json'); const d={...p.dependencies,...p.devDependencies}; process.exit(d['@langfuse/otel']?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0009_sessions_current_phase.sql` exists and contains `ADD COLUMN IF NOT EXISTS current_phase TEXT`
    - The migration file contains NO `langgraph.` table CREATE statements
    - `psql "$SUPABASE_DIRECT_URL" -c "\d public.sessions"` (or Supabase Studio) shows a `current_phase` column of type text, nullable
    - `apps/api/package.json` dependencies include `@langfuse/otel` at 5.9.1
    - `@opentelemetry/sdk-trace-base`, `@opentelemetry/exporter-trace-otlp-http`, `@opentelemetry/core` present in apps/api dependencies
  </acceptance_criteria>
  <done>current_phase column exists in sessions; @langfuse/otel + OTel peer deps installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create getCheckpointer() PostgresSaver singleton and setupLangfuseOtel() bootstrap</name>
  <files>apps/api/src/lib/langgraph-checkpointer.ts, apps/api/src/lib/langfuse-otel.ts</files>
  <read_first>
    - apps/api/src/lib/supabase.ts (module-level factory/singleton pattern)
    - apps/api/src/lib/env.ts (SUPABASE_DIRECT_URL export — lines 22-28)
    - apps/api/src/lib/auto-freeze.ts (startup function shape that server.ts calls)
    - .planning/phases/06-graph-construction-checkpointer/06-RESEARCH.md (Patterns 3 and 4 — verified API shapes)
  </read_first>
  <action>
    Create `apps/api/src/lib/langgraph-checkpointer.ts`: module-level `let _checkpointer: PostgresSaver | null = null` guard; `export async function getCheckpointer(): Promise<PostgresSaver>` that lazily calls `PostgresSaver.fromConnString(env.SUPABASE_DIRECT_URL, { schema: 'langgraph' })` then `await _checkpointer.setup()` on first call only (idempotent thereafter). The `{ schema: 'langgraph' }` option is MANDATORY — default is 'public' and would create tables in the wrong schema (RESEARCH Pitfall 3). Do NOT pass any `prepare` option — it does not exist in @langchain/langgraph-checkpoint-postgres@1.0.4 (RESEARCH Anti-Pattern). Use log prefix `[langgraph-checkpointer]`.
    Create `apps/api/src/lib/langfuse-otel.ts`: `export function setupLangfuseOtel(): void` that returns early with a `console.warn('[langfuse-otel] credentials not set — tracing disabled')` if `LANGFUSE_PUBLIC_KEY` or `LANGFUSE_SECRET_KEY` is missing from process.env; otherwise constructs `new LangfuseSpanProcessor()`, wraps it in `new NodeTracerProvider({ spanProcessors: [processor] })`, calls `provider.register()`, and `setLangfuseTracerProvider(provider)` (from @langfuse/tracing). Also `export function getLangfuseSpanProcessor()` returning the module-level processor (or null). Do NOT create a module-level Langfuse singleton beyond the span processor bootstrap — the per-request CallbackHandler is created inside graph invocations (banned per REQUIREMENTS.md; created in plan 06-04). Server.ts wiring of setupLangfuseOtel() happens in plan 06-04.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/lib/langgraph-checkpointer.ts` contains `export async function getCheckpointer` and `schema: 'langgraph'`
    - langgraph-checkpointer.ts contains `.setup()` guarded by a module-level null check (setup runs once)
    - langgraph-checkpointer.ts does NOT contain the string `prepare`
    - `apps/api/src/lib/langfuse-otel.ts` exports `setupLangfuseOtel` and `getLangfuseSpanProcessor`
    - langfuse-otel.ts warns and returns early when `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY` are absent
    - `cd apps/api && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>getCheckpointer() and setupLangfuseOtel() compile and follow the verified RESEARCH patterns; no server wiring yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → apps/api node_modules | New package install (@langfuse/otel) crosses supply-chain boundary |
| env.SUPABASE_DIRECT_URL → PostgresSaver Pool | DB credential used to open a direct Postgres connection |
| process.env Langfuse keys → OTel exporter | Secrets read at startup; must never be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | @langfuse/otel install | mitigate | Blocking human-verify checkpoint before install; verify on npmjs.com/package/@langfuse/otel; [OK] per RESEARCH audit; no postinstall scripts |
| T-06-03 | Tampering | crafted thread_id → checkpoint corruption | accept | thread_id = branch_id (UUID) enforced upstream by session auth (Phase 7); checkpointer itself keys by thread_id only |
| T-06-04 | Info Disclosure | SUPABASE_DIRECT_URL in logs | mitigate | env.ts validates format and never echoes; langgraph-checkpointer.ts logs `[langgraph-checkpointer]` messages WITHOUT the connection string |
| T-06-05 | Info Disclosure | Langfuse keys in logs | mitigate | setupLangfuseOtel warns on absence only; never logs key values |
</threat_model>

<verification>
- `psql "$SUPABASE_DIRECT_URL" -c "\d public.sessions"` shows `current_phase text`
- `apps/api/package.json` lists `@langfuse/otel@5.9.1`
- `cd apps/api && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- sessions.current_phase column exists (BLUE-04 unblocked)
- getCheckpointer() PostgresSaver singleton bound to langgraph schema (ORCH-05 infra ready)
- setupLangfuseOtel() bootstraps OTel exporter with graceful skip (OBS-01/OBS-02 infra ready)
</success_criteria>

<output>
Create `.planning/phases/06-graph-construction-checkpointer/06-02-SUMMARY.md` when done
</output>
