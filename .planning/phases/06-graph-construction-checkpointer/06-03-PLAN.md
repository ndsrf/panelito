---
phase: 06-graph-construction-checkpointer
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - apps/api/src/graph/state.ts
  - apps/api/src/graph/nodes/orchestrator.ts
  - apps/api/src/graph/nodes/agent.ts
  - apps/api/src/graph/nodes/mutation-gate.ts
  - apps/api/src/graph/nodes/drift-reply.ts
  - apps/api/src/graph/graph.ts
  - apps/api/src/graph/graph.test.ts
autonomous: true
requirements: [BLUE-03, BLUE-04, ORCH-02, ORCH-03, ORCH-04, HUMAN-03]
must_haves:
  truths:
    - "A unit test drives the StateGraph with MemorySaver and a loaded Debate Blueprint end to end"
    - "OrchestratorNode classifies input as DOMAIN_MATCH / DOMAIN_BRIDGE / DOMAIN_DRIFT"
    - "AgentNode returns ADD_NODE / ADD_EDGE / NO_ACTION with a confidence score constrained to Blueprint vocabulary"
    - "MutationGateNode maps confidence >0.85 to committed, 0.5–0.85 to ghost, <0.5 to no canvas op"
    - "DOMAIN_DRIFT with a passing roll routes to DriftReplyNode (conversational reply, no canvas mutation); a failing roll exits silently"
  artifacts:
    - path: "apps/api/src/graph/state.ts"
      provides: "GraphStateAnnotation (Annotation.Root) + GraphState type"
      contains: "Annotation.Root"
    - path: "apps/api/src/graph/graph.ts"
      provides: "createGraph(checkpointer?) factory + routeAfterOrchestrator"
      exports: ["createGraph"]
    - path: "apps/api/src/graph/nodes/mutation-gate.ts"
      provides: "confidence threshold routing + Blueprint vocabulary post-validation"
      contains: "0.85"
    - path: "apps/api/src/graph/graph.test.ts"
      provides: "unit tests covering all 5 D-11 mock paths"
      contains: "MemorySaver"
  key_links:
    - from: "apps/api/src/graph/graph.ts"
      to: "apps/api/src/graph/nodes/orchestrator.ts"
      via: "addConditionalEdges routeAfterOrchestrator"
      pattern: "addConditionalEdges"
    - from: "apps/api/src/graph/nodes/agent.ts"
      to: "canvasMutationTool"
      via: "adapter.stream tool_use event"
      pattern: "canvas_mutation"
    - from: "apps/api/src/graph/nodes/mutation-gate.ts"
      to: "blueprint.node_types / edge_types"
      via: "vocabulary membership check"
      pattern: "node_types|edge_types"
---

<objective>
Build the full LangGraph StateGraph in `apps/api/src/graph/` — state schema, four nodes (Orchestrator, Agent, MutationGate, DriftReply), the `createGraph(checkpointer?)` factory, and a unit-test suite driven by a mock AIProvider with MemorySaver. This is the core deliverable of Phase 6: the graph is proven in isolation before Wave 3 swaps in PostgresSaver and Langfuse.

Purpose: Satisfies BLUE-03 (Blueprint vocab injection), BLUE-04 (phase-aware prompt), ORCH-02 (classification + drift bypass), ORCH-03 (structured agent output), ORCH-04 (confidence thresholds), HUMAN-03 (drift reply, no refusal). ORCH-05 (PostgresSaver) and OBS-01/02 (Langfuse) are proven in plan 06-04.

Output: state.ts, four node files, graph.ts factory, graph.test.ts (all 5 D-11 mock paths, MemorySaver).
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-graph-construction-checkpointer/06-CONTEXT.md
@.planning/phases/06-graph-construction-checkpointer/06-RESEARCH.md
@.planning/phases/06-graph-construction-checkpointer/06-PATTERNS.md
@.planning/phases/06-graph-construction-checkpointer/06-01-SUMMARY.md

<interfaces>
<!-- From plan 06-01 (Wave 1) + codebase. Executor uses these directly. -->

From @panelito/types (after plan 06-01):
- `canvasMutationTool: ProviderTool` — pass to adapter.stream() as the tools array in AgentNode
- `CanvasOpSchema` (Zod discriminated union on `op`), `CanvasOp` type — MutationGateNode safe-parses AgentNode output
- `Blueprint` type — has `node_types[]`, `edge_types[]`, `phase_sequence[]`, `active_persona_ids`, `drift_reply_probability`
- `ProviderMessage` (`{ role, content }`), `AIProvider`, `AIStreamEvent` types

From @langchain/langgraph@1.4.7 (RESEARCH Patterns 1-2, VERIFIED):
- `Annotation`, `Annotation.Root`, `StateGraph`, `START`, `END`, `MemorySaver`, `BaseCheckpointSaver`
- Do NOT use `MessagesAnnotation` — it requires BaseMessage[], incompatible with ProviderMessage (RESEARCH Pitfall 6). Use a custom concat reducer.

From apps/api/src/lib/adapter-factory.ts:
- `createAdapter(provider: ProviderName, apiKey: string): AIProvider` — the ONLY way graph nodes call an LLM. Never import @anthropic-ai/sdk directly.

Node signature (RESEARCH Pattern, VERIFIED): `(state: GraphState, config?: RunnableConfig) => Promise<Partial<GraphState>>`
- Blueprint read via `config?.configurable?.blueprint as Blueprint` (loaded fresh, NOT checkpointed — D-09)
- provider name + decrypted key read via `config?.configurable` (passed by Phase 7 route / test)

From apps/api/src/routes/ai.ts (lines 247-278 — stream loop + tool_use handling pattern), apps/api/src/lib/cap-guard.ts (lines 97-120 — multi-threshold branching), apps/api/src/lib/ai-provider.test.ts (mock adapter test pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Graph state schema + OrchestratorNode (classification + drift roll)</name>
  <files>apps/api/src/graph/state.ts, apps/api/src/graph/nodes/orchestrator.ts</files>
  <read_first>
    - .planning/phases/06-graph-construction-checkpointer/06-RESEARCH.md (Pattern 1 state.ts, verified LangGraph 1.4.7 Annotation API; Pattern 6 pitfall on MessagesAnnotation)
    - .planning/phases/06-graph-construction-checkpointer/06-PATTERNS.md (state.ts and orchestrator.ts sections)
    - apps/api/src/lib/cap-guard.ts (node-style function reading state + returning result; fail-open logging)
    - apps/api/src/lib/adapter-factory.ts (createAdapter signature)
  </read_first>
  <behavior>
    - GraphStateAnnotation.State includes blueprintId, currentPhaseId, messages (concat reducer, default []), canvasOps (concat reducer, default []), guardrailResult, agentConfidence, driftAction, and an agentOutput field for the CanvasOp emitted by AgentNode
    - orchestratorNode returns guardrailResult = one of DOMAIN_MATCH / DOMAIN_BRIDGE / DOMAIN_DRIFT
    - On DOMAIN_DRIFT: reads blueprint.drift_reply_probability, rolls Math.random(); roll < probability → driftAction 'replied', roll >= probability → driftAction 'ignored'
    - orchestratorNode resolves currentPhaseId to a phase in blueprint.phase_sequence; NULL/missing falls back to phase_sequence[0].id (BLUE-04)
  </behavior>
  <action>
    Create `apps/api/src/graph/state.ts` exactly per RESEARCH Pattern 1: `Annotation.Root({...})` with locked fields blueprintId, currentPhaseId, messages (custom `reducer: (left, right) => left.concat(Array.isArray(right)?right:[right])`, `default: () => []`), canvasOps (same reducer shape), plus Claude-designed fields guardrailResult (`'DOMAIN_MATCH'|'DOMAIN_BRIDGE'|'DOMAIN_DRIFT'|null`, default null), agentConfidence (`number|null`, default null), driftAction (`'replied'|'ignored'|null`, default null), and add `agentOutput` (`CanvasOp|null`, default null) so AgentNode can hand its parsed op to MutationGateNode. Export `type GraphState = typeof GraphStateAnnotation.State`. Do NOT use MessagesAnnotation (RESEARCH Pitfall 6).
    Create `apps/api/src/graph/nodes/orchestrator.ts`: `orchestratorNode(state, config?): Promise<Partial<GraphState>>`. Read `blueprint` from `config.configurable.blueprint`. Resolve the active phase: find `blueprint.phase_sequence.find(p => p.id === state.currentPhaseId)` else fall back to `blueprint.phase_sequence[0]` (BLUE-04). Build a minimal classification system prompt that injects blueprint node/edge type ids and the active phase llm_instructions (BLUE-03), then call the LLM via `createAdapter(providerName, plaintextKey)` (both from config.configurable) to classify the last message as DOMAIN_MATCH / DOMAIN_BRIDGE / DOMAIN_DRIFT. On DOMAIN_DRIFT: `const roll = Math.random()`; `driftAction = roll < (blueprint.drift_reply_probability ?? 0.8) ? 'replied' : 'ignored'` (D-03). Return `{ guardrailResult, driftAction, currentPhaseId: resolvedPhaseId }`. Record drift_probability / drift_roll / drift_action for the Langfuse span (D-04) — for Phase 6, attach them as node output fields / log with `[orchestrator]` prefix; the CallbackHandler span capture is wired in plan 06-04. Use fail-open logging (`[orchestrator]` prefix) per cap-guard.ts convention.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/graph/state.ts` contains `Annotation.Root` and does NOT contain `MessagesAnnotation`
    - state.ts exports `GraphStateAnnotation` and `type GraphState`
    - state.ts declares `messages`, `canvasOps`, `guardrailResult`, `agentConfidence`, `driftAction`, `agentOutput`
    - `apps/api/src/graph/nodes/orchestrator.ts` reads `config?.configurable?.blueprint`, calls `Math.random()`, and references `drift_reply_probability`
    - orchestrator.ts falls back to `phase_sequence[0]` when currentPhaseId does not match (BLUE-04)
    - `cd apps/api && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>State schema compiles with the ProviderMessage-safe reducer; OrchestratorNode classifies and rolls drift per D-03/D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentNode + MutationGateNode + DriftReplyNode</name>
  <files>apps/api/src/graph/nodes/agent.ts, apps/api/src/graph/nodes/mutation-gate.ts, apps/api/src/graph/nodes/drift-reply.ts</files>
  <read_first>
    - apps/api/src/routes/ai.ts (lines 247-278 — adapter.stream loop, tool_use event, safeParse-and-drop-silently pattern)
    - apps/api/src/lib/cap-guard.ts (lines 97-120 — multi-threshold branching logic)
    - .planning/phases/06-graph-construction-checkpointer/06-PATTERNS.md (agent.ts, mutation-gate.ts, drift-reply.ts sections)
    - packages/types/src/canvas.ts (CanvasOpSchema branches + confidence field)
  </read_first>
  <behavior>
    - agentNode calls adapter.stream(messages, [canvasMutationTool], {...}) with a system prompt = base + blueprint vocab + active phase llm_instructions; collects the canvas_mutation tool_use event, safeParses via CanvasOpSchema, sets agentOutput + agentConfidence
    - mutationGateNode: agentOutput NO_ACTION or missing → returns {} (no canvasOps). Invalid node_type_id/edge_type_id vs blueprint arrays → returns {} (silent). confidence >0.85 → status 'committed'; 0.5–0.85 → status 'ghost'; <0.5 → returns {} (silent). Valid op → appends { ...op, status } to canvasOps.
    - driftReplyNode calls adapter.stream([lastMessage], [], {...}) with a minimal "respond naturally, do not refuse" system prompt; collects text_delta only; returns { driftAction: 'replied' } and NO canvas op (HUMAN-03)
  </behavior>
  <action>
    Create `apps/api/src/graph/nodes/agent.ts`: `agentNode(state, config?)`. Read blueprint + provider name + plaintext key from config.configurable. Build system prompt via a `buildAgentSystemPrompt(blueprint, currentPhaseId)` helper: base instructions + injected `blueprint.node_types`/`edge_types` ids (BLUE-03) + the active phase's `llm_instructions` (BLUE-04). Loop `adapter.stream(state.messages, [canvasMutationTool], { model: 'claude-sonnet-4-6', maxTokens: 1024, system })`; on `event.type === 'tool_use' && event.name === 'canvas_mutation'` run `CanvasOpSchema.safeParse(event.input)`; on success set `agentOutput = parsed.data` and `agentConfidence = 'confidence' in parsed.data ? parsed.data.confidence : null`; on failure log `[agent]` + drop (do not throw). Return `{ agentOutput, agentConfidence }`.
    Create `apps/api/src/graph/nodes/mutation-gate.ts`: `mutationGateNode(state, config?)`. Read blueprint from config. If `!state.agentOutput || state.agentOutput.op === 'NO_ACTION'` return `{}`. Post-validate vocabulary (D-07): for ADD_NODE check `blueprint.node_types.some(n => n.id === op.node_type_id)`; for ADD_EDGE check `blueprint.edge_types.some(e => e.id === op.edge_type_id)`; on failure log `[mutation-gate]` + return `{}`. Threshold routing (ORCH-04): `const c = state.agentConfidence ?? 0`; `c > 0.85` → status 'committed'; `c >= 0.5` → status 'ghost'; else return `{}`. Return `{ canvasOps: [{ ...state.agentOutput, status }] }` (reducer appends). Use CanvasNodeStatus values ('committed'|'ghost'|'silent') from canvas.ts.
    Create `apps/api/src/graph/nodes/drift-reply.ts`: `driftReplyNode(state, config?)`. Read provider name + key from config. System prompt: 'Respond naturally and helpfully to this message. Do not refuse or reject it.' (D-01, HUMAN-03). Loop `adapter.stream([lastMessage], [], { model: 'claude-sonnet-4-6', maxTokens: 512, system })` collecting `text_delta` events only (Phase 7 consumes them via callbacks; Phase 6 tests assert on collected text). Return `{ driftAction: 'replied' }` — NEVER emit a canvasOp (HUMAN-03). Use `[drift-reply]` log prefix.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/graph/nodes/agent.ts` references `canvasMutationTool` and `CanvasOpSchema.safeParse`
    - agent.ts injects `blueprint.node_types` / `edge_types` and phase `llm_instructions` into the system prompt (BLUE-03/BLUE-04)
    - `apps/api/src/graph/nodes/mutation-gate.ts` contains the thresholds `0.85` and `0.5` and validates node_type_id/edge_type_id against blueprint arrays
    - mutation-gate.ts returns `{}` (no canvasOps) for NO_ACTION, vocabulary violation, and confidence <0.5
    - `apps/api/src/graph/nodes/drift-reply.ts` passes `[]` as the tools array and returns no canvasOps
    - Every node imports LLM access via `createAdapter` and none import `@anthropic-ai/sdk`
    - `cd apps/api && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Three nodes compile; MutationGate enforces both thresholds and Blueprint vocabulary; DriftReply never mutates canvas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: createGraph factory + unit tests covering all 5 D-11 mock paths</name>
  <files>apps/api/src/graph/graph.ts, apps/api/src/graph/graph.test.ts</files>
  <read_first>
    - apps/api/src/lib/adapter-factory.ts (single-export factory pattern)
    - apps/api/src/lib/ai-provider.test.ts (mock adapter / deterministic async-generator test pattern)
    - .planning/phases/06-graph-construction-checkpointer/06-RESEARCH.md (Pattern 2 graph factory + routeAfterOrchestrator; Pattern 7 mock AIProvider; D-11 five stubs)
    - apps/api/src/graph/state.ts (from Task 1)
  </read_first>
  <behavior>
    - createGraph(checkpointer?) compiles a StateGraph: START→orchestrator; conditional edges from orchestrator to agent | driftReply | END; agent→mutationGate→END; driftReply→END
    - routeAfterOrchestrator: DOMAIN_DRIFT + driftAction 'replied' → 'driftReply'; DOMAIN_DRIFT + 'ignored' → 'end'; otherwise → 'agent'
    - createGraph() with no arg uses MemorySaver
    - Unit tests inject a mock AIProvider (via config.configurable) and cover all 5 D-11 paths: MATCH+ADD_NODE conf 0.9 → committed op; MATCH+ADD_EDGE conf 0.7 → ghost op; MATCH+NO_ACTION conf 0.3 → no op; DRIFT+replied → driftAction 'replied', 0 canvasOps; DRIFT+ignored → silent, 0 canvasOps
  </behavior>
  <action>
    Create `apps/api/src/graph/graph.ts` per RESEARCH Pattern 2: `export function createGraph(checkpointer?: BaseCheckpointSaver)` building `new StateGraph(GraphStateAnnotation)` with the four nodes, `.addEdge(START,'orchestrator')`, `.addConditionalEdges('orchestrator', routeAfterOrchestrator, { agent: 'agent', driftReply: 'driftReply', end: END })`, `.addEdge('agent','mutationGate')`, `.addEdge('mutationGate', END)`, `.addEdge('driftReply', END)`, returning `graph.compile({ checkpointer: checkpointer ?? new MemorySaver() })`. Define `routeAfterOrchestrator(state)`: if `state.guardrailResult === 'DOMAIN_DRIFT'` return `state.driftAction === 'replied' ? 'driftReply' : 'end'`; else `'agent'`. The optional checkpointer param is the test/prod swap (D-66).
    Create `apps/api/src/graph/graph.test.ts`: implement `createMockAdapter(events: AIStreamEvent[]): AIProvider` (RESEARCH Pattern 7) returning a deterministic async generator. Build the 5 D-11 stubs. Because OrchestratorNode also calls the adapter to classify, the mock must return the classification the test needs before the agent tool_use event — design the mock to yield a classification signal the orchestrator prompt expects, OR inject `guardrailResult`/`driftAction` via a test seam (e.g., pass an initial state override or a classifier stub through config.configurable). Drive `createGraph()` (MemorySaver) with `graph.invoke(initialState, { configurable: { thread_id, blueprint: <loaded debate blueprint fixture>, providerName: 'anthropic', plaintextKey: 'test', classifierAdapter/agentAdapter: <mock> } })`. Assert: committed path yields exactly one canvasOp with status 'committed'; ghost path status 'ghost'; NO_ACTION path 0 canvasOps; DRIFT-replied yields driftAction 'replied' and 0 canvasOps; DRIFT-ignored yields 0 canvasOps and no reply. Use a loaded/hand-built Debate Blueprint fixture (node_types incl. 'hypothesis', edge_types incl. 'SUPPORTS') so vocabulary validation passes. Never call getCheckpointer() here (RESEARCH Anti-Pattern) — MemorySaver only.
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/graph/graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/graph/graph.ts` exports `createGraph` and contains `addConditionalEdges` and `MemorySaver`
    - graph.ts `routeAfterOrchestrator` returns 'driftReply' for replied drift, 'end' for ignored drift, 'agent' otherwise
    - `apps/api/src/graph/graph.test.ts` contains `MemorySaver` and does NOT contain `getCheckpointer`
    - graph.test.ts has 5 test cases mapping to the 5 D-11 paths (committed / ghost / no-op / drift-replied / drift-ignored)
    - `cd apps/api && npx vitest run src/graph/graph.test.ts` exits 0 with all 5 paths passing
  </acceptance_criteria>
  <done>The StateGraph runs end to end under MemorySaver; all 5 mock paths pass, proving classification, thresholds, and drift routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user message → OrchestratorNode/AgentNode LLM prompt | Untrusted participant text enters LLM context |
| AgentNode LLM tool output → MutationGateNode | LLM-generated canvas op crosses into committed state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | LLM prompt injection via user message | mitigate | System prompt position (system beats user) in orchestrator + agent; Blueprint vocabulary constraint injected into system prompt (BLUE-03) |
| T-06-07 | Tampering | Canvas mutation outside Blueprint vocabulary | mitigate | MutationGateNode post-validates node_type_id/edge_type_id against blueprint.node_types/edge_types; invalid → dropped silently |
| T-06-08 | Tampering | Malformed AgentNode tool output | mitigate | CanvasOpSchema.safeParse in AgentNode; parse failure logged and dropped (fail-silent, no throw) — V5 Input Validation control |
| T-06-09 | Info Disclosure | DOMAIN_DRIFT refusal leaking to user | mitigate | DriftReplyNode uses minimal "do not refuse" prompt; HUMAN-03 satisfied — no error/rejection surfaced |
| T-06-SC | Tampering | npm installs | accept | No installs in this plan (graph code only); SC gate handled in plan 06-02 |
</threat_model>

<verification>
- `cd apps/api && npx tsc --noEmit` exits 0
- `cd apps/api && npx vitest run src/graph/graph.test.ts` — all 5 D-11 paths pass
- `grep -L "@anthropic-ai/sdk" apps/api/src/graph/nodes/*.ts` (no node imports the SDK directly)
</verification>

<success_criteria>
- StateGraph (Orchestrator → Agent → MutationGate, + DriftReply branch) builds and runs under MemorySaver
- OrchestratorNode classifies DOMAIN_MATCH/BRIDGE/DRIFT; drift roll routes correctly (ORCH-02)
- AgentNode emits vocabulary-constrained ADD_NODE/ADD_EDGE/NO_ACTION + confidence (ORCH-03, BLUE-03)
- MutationGateNode thresholds: >0.85 committed, 0.5–0.85 ghost, <0.5 silent (ORCH-04)
- Phase-aware prompt mutation wired via currentPhaseId (BLUE-04)
- DriftReplyNode replies conversationally with no canvas mutation, no refusal (HUMAN-03)
</success_criteria>

<output>
Create `.planning/phases/06-graph-construction-checkpointer/06-03-SUMMARY.md` when done
</output>
