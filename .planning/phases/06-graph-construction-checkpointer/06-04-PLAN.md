---
phase: 06-graph-construction-checkpointer
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/graph/graph.integration.test.ts
autonomous: false
requirements: [ORCH-05, OBS-01, OBS-02]
must_haves:
  truths:
    - "The PostgresSaver checkpointer stores graph state in langgraph.checkpoints after the first invocation"
    - "A second invocation on the same thread_id resumes from checkpoint (state accumulates, not resets)"
    - "setupLangfuseOtel() is called at server startup so CallbackHandler spans reach the OTel provider"
    - "A test graph run with a CallbackHandler produces at least one complete Langfuse trace after forceFlush"
  artifacts:
    - path: "apps/api/src/graph/graph.integration.test.ts"
      provides: "D-12 PostgresSaver resume test against real Supabase"
      contains: "getCheckpointer"
    - path: "apps/api/src/server.ts"
      provides: "setupLangfuseOtel() startup call"
      contains: "setupLangfuseOtel"
  key_links:
    - from: "apps/api/src/graph/graph.integration.test.ts"
      to: "langgraph.checkpoints"
      via: "checkpointer.get after invoke"
      pattern: "checkpointer.get"
    - from: "apps/api/src/server.ts"
      to: "setupLangfuseOtel"
      via: "startup invocation before serve callback returns"
      pattern: "setupLangfuseOtel\\(\\)"
---

<objective>
Prove the two pieces of the graph that require real infrastructure: PostgresSaver cross-invocation resume (ORCH-05 / D-12) against the real Supabase langgraph schema, and Langfuse trace export (OBS-01 / OBS-02) via the OTel provider wired at server startup plus the per-request CallbackHandler + forceFlush pattern. This is the last plan of Phase 6; after it, the graph is fully proven in isolation and Phase 7 can wire it into /invoke.

Purpose: Success criteria 4 (PostgresSaver resume) and 5 (Langfuse trace with node spans, classification, confidence, token costs) from ROADMAP §Phase 6.

Output: server.ts wired with setupLangfuseOtel(), graph.integration.test.ts (real Supabase), and a human-verified Langfuse dashboard trace.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-graph-construction-checkpointer/06-CONTEXT.md
@.planning/phases/06-graph-construction-checkpointer/06-RESEARCH.md
@.planning/phases/06-graph-construction-checkpointer/06-PATTERNS.md
@.planning/phases/06-graph-construction-checkpointer/06-02-SUMMARY.md
@.planning/phases/06-graph-construction-checkpointer/06-03-SUMMARY.md

<interfaces>
<!-- From plans 06-02 and 06-03. Executor uses these directly. -->

From plan 06-02:
- `getCheckpointer(): Promise<PostgresSaver>` (apps/api/src/lib/langgraph-checkpointer.ts) — bound to langgraph schema, runs setup() once
- `setupLangfuseOtel(): void`, `getLangfuseSpanProcessor()` (apps/api/src/lib/langfuse-otel.ts)

From plan 06-03:
- `createGraph(checkpointer?)` (apps/api/src/graph/graph.ts) — pass the PostgresSaver for integration test

From apps/api/src/lib/blueprint-loader.ts:
- `loadBlueprint('debate-strategy-v1'): Promise<Blueprint>` — seeded by migration 0008

From apps/api/src/server.ts (RESEARCH PATTERNS server startup section):
- `serve({ fetch: app.fetch, port: env.API_PORT }, (info) => { startAutoFreezeTracker(createServiceClient())... })` — add setupLangfuseOtel() alongside

From @langfuse/langchain + @langfuse/tracing (RESEARCH Pattern 5):
- `new CallbackHandler({ sessionId, userId, tags })` per invocation (module-level singleton BANNED — REQUIREMENTS.md)
- `await getLangfuseTracerProvider().forceFlush()` after graph.invoke() — NOT `handler.flushAsync()` (removed in v5, RESEARCH State of the Art)

From apps/api/src/routes/sessions.test.ts (real-Supabase integration test structure: beforeAll/afterAll, createServiceClient).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire setupLangfuseOtel() into server startup</name>
  <files>apps/api/src/server.ts</files>
  <read_first>
    - apps/api/src/server.ts (current serve() call + startAutoFreezeTracker wiring)
    - apps/api/src/lib/langfuse-otel.ts (setupLangfuseOtel signature from plan 06-02)
  </read_first>
  <action>
    In `apps/api/src/server.ts`, import `setupLangfuseOtel` from `./lib/langfuse-otel` and call `setupLangfuseOtel()` at startup — placed so the OTel provider is registered BEFORE any graph invocation can occur. Call it inside the `serve(...)` startup callback alongside `startAutoFreezeTracker(...)`, or immediately before `serve()`. It is safe to call unconditionally: it warns and returns early when LANGFUSE_PUBLIC_KEY/LANGFUSE_SECRET_KEY are absent (plan 06-02 behavior). Do NOT create a module-level CallbackHandler here (banned) — only the span-processor bootstrap is registered at startup; per-request CallbackHandlers are created inside graph invocations (Task 2 test + Phase 7 route).
  </action>
  <verify>
    <automated>grep -q "setupLangfuseOtel()" apps/api/src/server.ts && cd apps/api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/server.ts` imports `setupLangfuseOtel` from `./lib/langfuse-otel`
    - server.ts calls `setupLangfuseOtel()` at startup (before or within the serve callback)
    - server.ts does NOT construct a `new CallbackHandler` or a module-level Langfuse client
    - `cd apps/api && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>OTel provider is registered at server startup with graceful skip when credentials are missing.</done>
</task>

<task type="auto">
  <name>Task 2: PostgresSaver + Langfuse integration test (D-12, OBS-01/02)</name>
  <files>apps/api/src/graph/graph.integration.test.ts</files>
  <read_first>
    - apps/api/src/routes/sessions.test.ts (real-Supabase integration test structure: createServiceClient, beforeAll/afterAll, real network)
    - .planning/phases/06-graph-construction-checkpointer/06-RESEARCH.md (Code Examples: PostgresSaver integration test structure; Pattern 5 CallbackHandler + forceFlush; Pitfall 5 drift trace on silent exit)
    - apps/api/src/lib/langgraph-checkpointer.ts (getCheckpointer from plan 06-02)
    - apps/api/src/graph/graph.ts (createGraph from plan 06-03)
    - apps/api/vitest.config.ts (how .env / SUPABASE_DIRECT_URL is loaded into the test env)
  </read_first>
  <action>
    Create `apps/api/src/graph/graph.integration.test.ts`. Skip the whole suite when `env.SUPABASE_DIRECT_URL` is absent (use `describe.skipIf`) so CI without the DB does not fail. Test A (D-12 resume): `const checkpointer = await getCheckpointer(); const graph = createGraph(checkpointer); const blueprint = await loadBlueprint('debate-strategy-v1'); const threadId = 'test-' + Date.now();` invoke once with `{ blueprintId: 'debate-strategy-v1', currentPhaseId: 'opening', messages: [], canvasOps: [] }` and config `{ configurable: { thread_id: threadId, blueprint, providerName: 'anthropic', plaintextKey: <mock/test> } }`. Assert `await checkpointer.get({ configurable: { thread_id: threadId } })` is not null (state stored in langgraph.checkpoints). Invoke a second time on the same threadId adding a message; assert `result.messages.length` grew (state accumulated, not reset) — proving cross-invocation resume (ORCH-05). Use a mock/deterministic adapter injected via config.configurable (reuse the createMockAdapter helper pattern from graph.test.ts) so the test does not spend real Claude tokens. Test B (OBS smoke, skipIf no LANGFUSE keys): create a per-request `new CallbackHandler({ tags: ['phase6-test'] })`, invoke the graph with `callbacks: [handler]`, then `await getLangfuseTracerProvider().forceFlush()` and assert forceFlush resolves without throwing (full dashboard confirmation is the human-verify checkpoint). Do NOT create a module-level CallbackHandler (banned). Clean up: the test threads are ephemeral (`test-${Date.now()}`) — no afterAll deletion strictly required, but log the threadId for manual inspection.
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/graph/graph.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/graph/graph.integration.test.ts` contains `getCheckpointer` and `checkpointer.get`
    - The suite uses `describe.skipIf` (or equivalent) keyed on SUPABASE_DIRECT_URL / LANGFUSE keys so it does not hard-fail without infra
    - Test A asserts checkpoint is non-null after first invoke and messages accumulate on second invoke (same thread_id)
    - Test B constructs a per-request `new CallbackHandler` and calls `getLangfuseTracerProvider().forceFlush()`; contains NO module-level CallbackHandler
    - `cd apps/api && npx vitest run src/graph/graph.integration.test.ts` exits 0 (or reports skipped when infra absent)
  </acceptance_criteria>
  <done>PostgresSaver stores and resumes thread state across two invocations; CallbackHandler + forceFlush path runs without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    setupLangfuseOtel() is wired at server startup, and the integration test invokes the graph with a per-request CallbackHandler followed by forceFlush. This checkpoint confirms end-to-end trace export to the Langfuse dashboard — the one thing an automated test cannot assert (dashboard state is external).
  </what-built>
  <how-to-verify>
    1. Ensure LANGFUSE_PUBLIC_KEY and LANGFUSE_SECRET_KEY are set in apps/api/.env (Langfuse Dashboard → Settings → API Keys).
    2. Run the integration test: `cd apps/api && npx vitest run src/graph/graph.integration.test.ts` (Test B should execute, not skip).
    3. Open your Langfuse dashboard (cloud.langfuse.com or self-hosted) → Traces.
    4. Confirm at least one trace tagged `phase6-test` appears with: node spans (orchestrator / agent / mutationGate), the guardrail classification result, the agent confidence score, and token costs.
    5. Also confirm a DOMAIN_DRIFT-silent test run still produces a trace (D-04: drift events recorded even on silent exit) — check for a trace whose orchestrator span records drift_action.
  </how-to-verify>
  <resume-signal>Type "approved" if the Langfuse trace shows node spans + classification + confidence + token costs, or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env.SUPABASE_DIRECT_URL → PostgresSaver Pool | Direct DB connection opened during integration test |
| user message content → Langfuse trace | Message text exported to external observability platform |
| crafted thread_id → checkpoint namespace | thread_id keys checkpoint storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | crafted thread_id → checkpoint corruption | accept | thread_id = branch_id (UUID) verified by existing session auth in Phase 7; integration test uses ephemeral `test-${Date.now()}` ids that cannot collide with real branch UUIDs |
| T-06-11 | Info Disclosure | Langfuse traces contain user message PII | accept | Acceptable for BYOK platform-owner observability (RESEARCH Security Domain); traces are owner-scoped |
| T-06-12 | Info Disclosure | SUPABASE_DIRECT_URL echoed in test logs | mitigate | Test logs only the ephemeral threadId, never the connection string; env.ts never echoes the URL |
| T-06-13 | Info Disclosure | Langfuse module-level singleton corrupting concurrent trace context | mitigate | Per-request `new CallbackHandler` inside each invocation; module-level singleton banned and absent from server.ts (Task 1 acceptance criterion) |
| T-06-SC | Tampering | npm installs | accept | No installs in this plan (install done + gated in plan 06-02) |
</threat_model>

<verification>
- `grep -q "setupLangfuseOtel()" apps/api/src/server.ts`
- `cd apps/api && npx vitest run src/graph/graph.integration.test.ts` exits 0 (or skipped without infra)
- Human-verified Langfuse trace shows node spans + classification + confidence + token costs
</verification>

<success_criteria>
- PostgresSaver stores state in langgraph.checkpoints and resumes across two invocations on the same thread_id (ORCH-05, Success Criterion 4)
- setupLangfuseOtel() registered at startup; per-request CallbackHandler + forceFlush path exercised (OBS-01, OBS-02)
- Langfuse dashboard shows a complete trace with node spans, classification, confidence, token costs; drift events traced even on silent exit (Success Criterion 5, D-04)
</success_criteria>

<output>
Create `.planning/phases/06-graph-construction-checkpointer/06-04-SUMMARY.md` when done
</output>
