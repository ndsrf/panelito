---
phase: 09-graph-canvas-frontend
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - apps/web/components/workspace/widgets/graphLayout.ts
  - apps/web/components/workspace/widgets/GraphNode.tsx
  - apps/web/components/workspace/widgets/GraphEdge.tsx
  - apps/web/components/workspace/widgets/GraphCanvas.tsx
  - apps/web/components/workspace/widgets/widget-registry.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, CANVAS-04]
user_setup: []

must_haves:
  truths:
    - "The Graph Canvas renders nodes and edges from sessionStore.canvasNodes/canvasEdges using @xyflow/react"
    - "Node and edge colors come from the active Blueprint's node_types[].color / edge_types[].color, not hardcoded"
    - "Ghost nodes render with dashed border and ~40% dimness; committed nodes render solid; silent nodes are filtered out"
    - "Clicking a ghost node's confirm sets it committed; dismiss sets it silent; both optimistic + PATCH + revert-on-error"
    - "The 'graph' widget type resolves to GraphCanvas in widgetRegistry, loaded via next/dynamic ssr:false"
  artifacts:
    - path: "apps/web/components/workspace/widgets/GraphCanvas.tsx"
      provides: "@xyflow/react canvas reading from sessionStore"
      min_lines: 40
      contains: "@xyflow/react/dist/style.css"
    - path: "apps/web/components/workspace/widgets/GraphNode.tsx"
      provides: "custom node renderer + ghost confirm/dismiss"
      contains: "nodeTypes"
    - path: "apps/web/components/workspace/widgets/GraphEdge.tsx"
      provides: "Blueprint-colored custom edge"
      contains: "edgeTypes"
    - path: "apps/web/components/workspace/widgets/graphLayout.ts"
      provides: "dagre auto-layout fallback for null positions"
      contains: "applyDagreLayout"
  key_links:
    - from: "apps/web/components/workspace/widgets/widget-registry.ts"
      to: "GraphCanvas"
      via: "next/dynamic ssr:false registration under 'graph'"
      pattern: "ssr:\\s*false"
    - from: "apps/web/components/workspace/widgets/GraphNode.tsx"
      to: "/api/canvas_nodes/:id"
      via: "apiFetch PATCH on confirm/dismiss"
      pattern: "canvas_nodes/"
---

<objective>
Build the Graph Canvas renderer: the dagre layout utility, the custom GraphNode (committed/ghost visuals + confirm/dismiss), the custom GraphEdge (Blueprint-colored), the GraphCanvas @xyflow/react wrapper reading from sessionStore, and the widget-registry 'graph' registration loaded ssr:false. This is the visible surface — the interactive node/edge canvas participants see and touch.

Purpose: UI-01 (interactive Blueprint-colored canvas), UI-02 (ghost dashed/dim + confirm/dismiss), UI-03 (reactive re-render from store). Depends on the 'graph' PanelWidget type (09-01) and @dagrejs/dagre install (09-01); the store already exposes canvasNodes/canvasEdges.

Output: graphLayout.ts, GraphNode.tsx, GraphEdge.tsx, GraphCanvas.tsx, widget-registry.ts extended.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-graph-canvas-frontend/09-CONTEXT.md
@.planning/phases/09-graph-canvas-frontend/09-RESEARCH.md
@.planning/phases/09-graph-canvas-frontend/09-PATTERNS.md
@.planning/phases/09-graph-canvas-frontend/09-UI-SPEC.md

<interfaces>
<!-- CanvasNode/CanvasEdge from @panelito/types (see 09-01 interfaces). status: committed|ghost|silent.
     position_x/position_y: number|null. node_type_id references Blueprint.node_types[].id. -->

<!-- Blueprint node/edge color source (packages/types/src/blueprint.ts):
     NodeTypeConfig { id, label, color, description }
     EdgeTypeConfig { id, label, color }
     Color is read at RUNTIME from Blueprint — never hardcode (UI-SPEC §Color). -->

<!-- widget-registry.ts: WidgetComponent = ComponentType<{ data: PanelWidget; isFullscreen?: boolean }>.
     widgetRegistry is a Map<PanelWidget['widget_type'], WidgetComponent>.
     GraphCanvas ignores `data` (reads sessionStore per D-06) but must satisfy the signature — cast `as WidgetComponent`. -->

<!-- sessionStore (apps/web/store/session-store.ts): canvasNodes, canvasEdges selectors.
     Blueprint access on client: the active Blueprint must be reachable to map node_type_id → color.
     Check how the Blueprint is exposed to client components; if not already in a store, derive
     the color from the canvasNode's blueprint via the panelStore/sessionStore, else fall back to a
     neutral color. Prefer reading Blueprint node/edge configs already available client-side. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: graphLayout util + GraphEdge + GraphNode</name>
  <files>apps/web/components/workspace/widgets/graphLayout.ts, apps/web/components/workspace/widgets/GraphEdge.tsx, apps/web/components/workspace/widgets/GraphNode.tsx</files>
  <read_first>
    - .planning/phases/09-graph-canvas-frontend/09-RESEARCH.md §Pattern 2 (dagre), §Pattern 3 (GraphNode), §Pitfall 1 (module-level nodeTypes/edgeTypes)
    - .planning/phases/09-graph-canvas-frontend/09-PATTERNS.md GraphNode.tsx / GraphEdge.tsx / graphLayout.ts sections (full excerpts)
    - .planning/phases/09-graph-canvas-frontend/09-UI-SPEC.md §Ghost Node Visual Contract, §Interaction Contract, §Copywriting Contract (Spanish aria-labels + toast copy), §Blueprint Node/Edge Colors
    - apps/web/components/ui/button.tsx (shadcn Button variant/size props)
    - apps/web/hooks/use-session-channel.ts (toast import pattern from sonner)
    - apps/web/lib/api.ts (apiFetch signature)
    - packages/types/src/canvas.ts + packages/types/src/blueprint.ts (CanvasNode/CanvasEdge, NodeTypeConfig.color, EdgeTypeConfig.color)
  </read_first>
  <action>
    graphLayout.ts: pure util, no project imports. Export `applyDagreLayout(nodes: Node[], edges: Edge[]): Node[]` using `@dagrejs/dagre` with NODE_WIDTH=180, NODE_HEIGHT=60, `rankdir: 'TB', ranksep: 60, nodesep: 40`; return nodes with computed `position` centered (subtract half width/height). Import types `Node, Edge` from '@xyflow/react'.
    GraphEdge.tsx: 'use client'. Custom edge using `BaseEdge` + `getSmoothStepPath`; stroke = `data?.blueprintColor ?? '#6366f1'`, strokeWidth 1.5. Export `edgeTypes = { graphEdge: GraphEdge }` at MODULE LEVEL (Pitfall 1).
    GraphNode.tsx: 'use client'. `NodeProps<Node<{ canvasNode: CanvasNode; blueprintColor: string }>>`. `isGhost = canvasNode.status === 'ghost'`. Local `optimisticStatus` state seeded from canvasNode.status; if 'silent' return null. Ghost style: `opacity: 0.6`, `border: 1.5px dashed ${blueprintColor}`, fill `${blueprintColor}1A`, label color `#a1a1aa` (UI-SPEC ghost contract). Committed style: `1.5px solid ${blueprintColor}`, fill `${blueprintColor}26`, label `--color-foreground`. Node label 12px weight 400. Render source/target `Handle`s (Top target, Bottom source). When ghost, render two shadcn `Button`s with `min-h-[44px] min-w-[44px]` (mobile touch target, hard constraint): confirm (Check icon, aria-label `Confirmar nodo ${label}`) → optimistic set 'committed' then `apiFetch(\`/api/canvas_nodes/${canvasNode.id}\`, { method: 'PATCH', body: JSON.stringify({ status: 'committed' }) })`, on error revert to 'ghost' + `toast.error('No se pudo actualizar el nodo. Inténtalo de nuevo.')`; dismiss (X icon, variant destructive, aria-label `Descartar nodo ${label}`) → optimistic set 'silent' then PATCH `{ status: 'silent' }`, on error revert + same toast. Export `nodeTypes = { graphNode: GraphNode }` at MODULE LEVEL (Pitfall 1). Copy is Spanish per UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && grep -c "applyDagreLayout" apps/web/components/workspace/widgets/graphLayout.ts && grep -c "export const nodeTypes" apps/web/components/workspace/widgets/GraphNode.tsx && grep -c "export const edgeTypes" apps/web/components/workspace/widgets/GraphEdge.tsx && grep -c "min-h-\[44px\]" apps/web/components/workspace/widgets/GraphNode.tsx && pnpm --filter @panelito/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - graphLayout.ts exports applyDagreLayout using @dagrejs/dagre with rankdir 'TB'
    - GraphNode.tsx exports `nodeTypes` at module level (not inside the component — Pitfall 1)
    - GraphEdge.tsx exports `edgeTypes` at module level
    - GraphNode ghost buttons use `min-h-[44px] min-w-[44px]`, Spanish aria-labels (`Confirmar nodo`, `Descartar nodo`), and PATCH to `/api/canvas_nodes/:id` with revert-on-error toast
    - Ghost visual: `opacity: 0.6` + dashed border; committed: solid border; silent: returns null
    - `pnpm --filter @panelito/web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Layout util + both custom renderers exist with correct ghost/committed visuals, module-level type maps, and accessible confirm/dismiss.</done>
</task>

<task type="auto">
  <name>Task 2: GraphCanvas wrapper + widget-registry 'graph' registration</name>
  <files>apps/web/components/workspace/widgets/GraphCanvas.tsx, apps/web/components/workspace/widgets/widget-registry.ts</files>
  <read_first>
    - .planning/phases/09-graph-canvas-frontend/09-RESEARCH.md §Pattern 1 (controlled store), §Pattern 4 (next/dynamic), §Pitfall 2 (CSS import), §Pitfall 7 (fullscreen)
    - .planning/phases/09-graph-canvas-frontend/09-PATTERNS.md GraphCanvas.tsx + widget-registry.ts sections (full excerpts)
    - .planning/phases/09-graph-canvas-frontend/09-UI-SPEC.md §Component Inventory, §Copywriting Contract (empty state "El grafo está vacío" / body; fullscreen label "Vista Completa del Grafo"), §Interaction Contract (pan/zoom/Controls; nodesDraggable false)
    - apps/web/components/workspace/widgets/RadarWidget.tsx (widget component shape, container sizing `w-full h-full min-h-0`)
    - apps/web/components/workspace/widgets/widget-registry.ts (existing Map registration + WidgetComponent type)
    - apps/web/store/session-store.ts (canvasNodes/canvasEdges selectors)
  </read_first>
  <action>
    GraphCanvas.tsx: 'use client'. Import `import '@xyflow/react/dist/style.css'` at top (Pitfall 2 — mandatory, inside this dynamically-loaded module). Read `canvasNodes`/`canvasEdges` from sessionStore. Build rfNodes via useMemo: filter out `status === 'silent'`, map to `{ id, type: 'graphNode', position: { x: position_x ?? 0, y: position_y ?? 0 }, data: { canvasNode, blueprintColor } }` where blueprintColor is resolved from the active Blueprint's `node_types.find(t => t.id === n.node_type_id)?.color` (read Blueprint client-side; fall back to a neutral color if unavailable). Build rfEdges similarly with `type: 'graphEdge'`, source/target = source_node_id/target_node_id, `blueprintColor` from `edge_types.find(...)?.color`. Compute `needsLayout = rfNodes.some(n => n.data.canvasNode.position_x === null)`; if so run `applyDagreLayout`. Keep local `nodes`/`edges` state synced from the memoized values via useEffect; `onNodesChange` via `applyNodeChanges`. Render inside `<div className="w-full h-full min-h-0">` a `<ReactFlow nodes edges nodeTypes edgeTypes onNodesChange nodesDraggable={false} nodesConnectable={false} colorMode="dark" fitView>` with `<Background />` and `<Controls />`. Import `nodeTypes`/`edgeTypes` from the module-level exports in GraphNode/GraphEdge (NOT redefined here — Pitfall 1). When there are zero visible nodes, render the empty state copy "El grafo está vacío" / "El AI añadirá nodos a medida que avance la conversación." (UI-SPEC copy). Export a named `GraphCanvas`.
    widget-registry.ts: add `import dynamic from 'next/dynamic'` and register `const GraphCanvasDynamic = dynamic(() => import('./GraphCanvas').then(m => ({ default: m.GraphCanvas })), { ssr: false, loading: () => <div className="w-full h-full bg-card animate-pulse rounded" /> }) as WidgetComponent`; add `['graph', GraphCanvasDynamic]` to the Map (Pattern 4 — ssr:false mandatory since @xyflow/react needs DOM APIs). GraphCanvas ignores the `data` prop; the `as WidgetComponent` cast satisfies the registry signature (D-06).
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && grep -c "@xyflow/react/dist/style.css" apps/web/components/workspace/widgets/GraphCanvas.tsx && grep -c "ssr: false" apps/web/components/workspace/widgets/widget-registry.ts && grep -c "'graph'" apps/web/components/workspace/widgets/widget-registry.ts && pnpm --filter @panelito/web exec tsc --noEmit && pnpm --filter @panelito/web build</automated>
  </verify>
  <acceptance_criteria>
    - GraphCanvas.tsx imports `@xyflow/react/dist/style.css` (Pitfall 2)
    - GraphCanvas reads canvasNodes/canvasEdges from sessionStore, filters silent, resolves colors from Blueprint node_types/edge_types (no hardcoded node colors)
    - GraphCanvas renders ReactFlow with `nodesDraggable={false}`, Background, Controls, and imports module-level nodeTypes/edgeTypes
    - Empty state renders "El grafo está vacío" when no visible nodes
    - widget-registry.ts registers `'graph'` via `dynamic(... { ssr: false })` cast `as WidgetComponent`
    - `pnpm --filter @panelito/web exec tsc --noEmit` and `pnpm --filter @panelito/web build` both exit 0
  </acceptance_criteria>
  <done>GraphCanvas renders store-driven, Blueprint-colored nodes/edges with dagre fallback; 'graph' is registered ssr:false; web build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /api/canvas_nodes/:id | Ghost confirm/dismiss crosses to the server (server-side membership check lives in 09-01) |
| @xyflow/react rendering | AI-emitted labels rendered as node text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering | node.label rendered in GraphNode | mitigate | Labels rendered as React text children (auto-escaped) — no dangerouslySetInnerHTML |
| T-09-09 | Denial of Service | @xyflow/react SSR crash | mitigate | next/dynamic ssr:false gate + AnalyticsPanelErrorBoundary wraps WidgetZone (crash isolates from chat) |
| T-09-10 | Repudiation | optimistic confirm/dismiss lost on error | accept | Optimistic state reverts on PATCH failure + sonner toast; server broadcast is source of truth |
</threat_model>

<verification>
- `pnpm --filter @panelito/web exec tsc --noEmit` passes
- `pnpm --filter @panelito/web build` passes (SSR gate holds — no @xyflow/react in server bundle)
- nodeTypes/edgeTypes are module-level; CSS imported; ghost/committed visuals per UI-SPEC
</verification>

<success_criteria>
- Graph Canvas renders Blueprint-colored nodes/edges from the store
- Ghost nodes dashed + dim with working confirm/dismiss (optimistic + PATCH + revert)
- 'graph' widget resolves to GraphCanvas via ssr:false dynamic import
</success_criteria>

<output>
Create `.planning/phases/09-graph-canvas-frontend/09-03-SUMMARY.md` when done
</output>
