---
phase: 09-graph-canvas-frontend
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified: []
autonomous: false
requirements: [CANVAS-03, CANVAS-04, UI-01, UI-02, UI-03]
user_setup: []

must_haves:
  truths:
    - "A Debate-Blueprint session renders the Graph Canvas (View A); a chart-mode session renders the Recharts panel (View B); neither crashes"
    - "An AI canvas mutation triggers a live re-render on a second connected client within one second, no reload or manual branch switch"
    - "Ghost nodes render dashed + dim; confirm promotes to committed for all participants; dismiss removes it for all; 60s unacknowledged ghosts disappear"
    - "Switching branches re-renders the canvas to that branch's committed snapshot"
  artifacts: []
  key_links:
    - from: "second browser tab"
      to: "canvas_update broadcast"
      via: "Supabase Realtime"
      pattern: "canvas_update"
---

<objective>
Human verification of the four Phase 9 success criteria against the running application, using two browser sessions. This is the acceptance gate for the entire phase — everything built in Waves 1–3 must demonstrably work end to end for real participants.

Purpose: Automated tests cover units and contracts, but the phase goal is a live, multi-client interactive canvas. Real-time re-render latency, ghost confirm/dismiss propagation, branch-switch reconciliation, and view-mode routing can only be proven with a running app and two clients.

Output: Human sign-off on the four ROADMAP §Phase 9 success criteria.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-graph-canvas-frontend/09-CONTEXT.md
@.planning/phases/09-graph-canvas-frontend/09-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify view-mode routing + live canvas render (SC-1, SC-4)</name>
  <files>none (verification only)</files>
  <what-built>Server-side canvas_view_mode resolution (09-02), the 'graph' widget + GraphCanvas renderer (09-03), and the panel_update('graph') broadcast (09-01). A Debate-Blueprint session should show the Graph Canvas; a chart-mode Blueprint should show the existing Recharts panel.</what-built>
  <action>Human-verify against the running app (autonomous=false): confirm SC-1 and SC-4. Follow the how-to-verify steps to prove that a Debate/Strategy (graph) Blueprint renders the Graph Canvas with Blueprint colors (Hypothesis indigo, Evidence emerald, Counter-Argument red, Action amber), a chart Blueprint renders the existing Recharts panel, neither view crashes, and a branch switch re-renders the committed snapshot for that branch with no cross-branch ghost carryover. Do not mark complete until every step passes.</action>
  <how-to-verify>
    1. Start the app: `pnpm dev` (or the project's standard dev command). Confirm the API and web app are running; ensure migration 0011 was pushed (09-01 Task 5).
    2. Open a session whose active Blueprint has `canvas_view_mode: 'graph'` (Debate/Strategy Blueprint). Send a message that should trigger AI canvas mutations.
    3. Confirm the top panel switches to the Graph Canvas (View A) and renders nodes/edges with Blueprint colors (Hypothesis indigo, Evidence emerald, Counter-Argument red, Action amber). Confirm zoom/pan Controls work; nodes are not draggable.
    4. Open a session whose Blueprint has `canvas_view_mode: 'chart'`. Confirm the Graph Canvas never appears and the existing Recharts widget path renders normally. Neither view crashes.
    5. On a branch switch in the graph session, confirm the canvas re-renders to that branch's committed snapshot (SC-1 / UI-03 branch-switch clause). Ghost nodes from another branch are not carried over.
  </how-to-verify>
  <verify>Manual: graph Blueprint shows Graph Canvas with correct colors; chart Blueprint shows Recharts; branch switch re-renders committed snapshot; no crash.</verify>
  <done>SC-1 and SC-4 confirmed live: view routing works both directions and branch-switch re-renders the committed snapshot.</done>
  <resume-signal>Type "approved" if graph/chart routing and per-branch committed re-render both work, or describe what failed (include which view mode and branch).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify real-time multi-client updates + ghost lifecycle (SC-2, SC-3)</name>
  <files>none (verification only)</files>
  <what-built>Ghost persistence + committed/ghost broadcast (09-01), merge-by-id + reconnect fetch (09-02), and ghost confirm/dismiss + dashed/dim rendering (09-03). Canvas mutations must reach all clients within one second; ghosts confirm/dismiss for everyone and expire after 60s.</what-built>
  <action>Human-verify against the running app with two clients (autonomous=false): confirm SC-2 and SC-3. Follow the how-to-verify steps to prove live sync under one second, ghost dashed/dim rendering, confirm/dismiss propagation to all participants, 60-second pg_cron expiry, multi-turn merge (earlier nodes persist), and reconnect reconciliation to committed-only. Do not mark complete until every step passes.</action>
  <how-to-verify>
    1. Open the same graph-mode session in two browser tabs (Tab A creator, Tab B guest via join link).
    2. In Tab A, send a message that triggers an AI canvas mutation. Confirm the new node(s) appear in BOTH tabs within one second — no page reload, no manual branch switch (SC-3 / UI-03).
    3. Confirm ghost nodes (0.5–0.85 confidence) render with a dashed border and ~40% dimness in both tabs; committed nodes (>0.85) render solid (SC-2 / UI-02).
    4. In Tab B, click a ghost node's confirm (Confirmar / checkmark). Confirm it becomes committed (solid) in BOTH tabs within one second.
    5. Trigger another ghost node; in Tab A click dismiss (Descartar / X). Confirm it disappears in BOTH tabs.
    6. Trigger a ghost node and leave it untouched. After ~60–90 seconds, confirm it disappears (pg_cron expiry) — verify by a fresh reconnect/branch-switch or the next canvas_update.
    7. Multi-turn check: send several messages in sequence; confirm earlier nodes remain on the canvas (merge semantics, not replaced by each new broadcast).
    8. Reconnect check: briefly disable/re-enable network in Tab B; on reconnect confirm the canvas shows the committed-only snapshot (ghosts intentionally dropped per D-03) with no earlier committed nodes lost.
  </how-to-verify>
  <verify>Manual: mutation propagates to both tabs <1s; ghost dashed/dim; confirm/dismiss propagate; 60s expiry; multi-turn merge preserves earlier nodes; reconnect shows committed-only.</verify>
  <done>SC-2 and SC-3 confirmed live across two clients: real-time sync, ghost lifecycle, expiry, merge, and reconnect all behave per spec.</done>
  <resume-signal>Type "approved" if live sync (<1s), ghost dashed/dim rendering, confirm/dismiss propagation, 60s expiry, multi-turn merge, and reconnect reconciliation all work, or describe failures with tab + step number.</resume-signal>
</task>

</tasks>

<verification>
All four ROADMAP §Phase 9 success criteria observed live across two clients:
- SC-1: branch switch / reconnect fetch renders correct committed set; ghosts discarded
- SC-2: Blueprint-colored nodes; ghost dashed + ~40% opacity; confirm/dismiss; 60s expiry
- SC-3: mutation re-renders all clients within one second, no reload
- SC-4: graph (View A) and chart (View B) coexist; neither crashes
</verification>

<success_criteria>
- Human sign-off recorded on both checkpoint tasks
- No console errors that break the chat during any canvas interaction
</success_criteria>

<output>
Create `.planning/phases/09-graph-canvas-frontend/09-04-SUMMARY.md` when done
</output>
