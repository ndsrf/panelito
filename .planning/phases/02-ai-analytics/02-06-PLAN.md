---
phase: 02-ai-analytics
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - apps/api/src/routes/personas.ts
  - apps/api/src/index.ts
  - apps/web/app/(protected)/sessions/new/new-session-form.tsx
  - apps/web/components/workspace/CreatorControls.tsx
autonomous: false
requirements: [PERSONA-01, PERSONA-02, PERSONA-03]
user_setup: []
must_haves:
  truths:
    - "The session creation form has an Analista Científico persona card (ON by default) that sets active_personas at creation (PERSONA-01/02)"
    - "POST /api/sessions/:id/personas toggles a persona in sessions.active_personas and is creator-gated"
    - "The CreatorControls workspace panel has an Analistas drawer with a live Switch to toggle the persona mid-session (PERSONA-02)"
    - "When the persona is toggled OFF, the AI stops responding (the invoke route's no_active_persona gate from Plan 02 takes effect)"
    - "Toggling the persona is optimistic with a toast on failure and rollback (Pitfall 7)"
  artifacts:
    - path: "apps/api/src/routes/personas.ts"
      provides: "POST /personas toggle route"
      contains: "active_personas"
    - path: "apps/web/components/workspace/CreatorControls.tsx"
      provides: "persona management drawer with Switch"
      contains: "analista_cientifico"
    - path: "apps/web/app/(protected)/sessions/new/new-session-form.tsx"
      provides: "persona picker card"
      contains: "active_personas"
  key_links:
    - from: "apps/web/components/workspace/CreatorControls.tsx"
      to: "POST /api/sessions/:id/personas"
      via: "apiFetch"
      pattern: "/personas"
    - from: "apps/api/src/routes/personas.ts"
      to: "sessions.active_personas"
      via: "supabase update"
      pattern: "active_personas"
---

<objective>
Complete the persona system: a personas toggle route, the session-creation persona picker, and the CreatorControls in-session persona drawer with a live Switch. This makes PERSONA-02 ("toggle before or during a session") real end to end — and the OFF state stops the AI (enforced by the no_active_persona gate already in the Plan 02 invoke route). The persona library UI pattern is built to extend to Devil's Advocate in v2 without new UI work (D-13).

Purpose: The persona badge (Plan 03) already labels AI messages; this plan delivers the management controls that gate the AI on/off.
Output: A personas route, a creation-form picker, and a workspace persona drawer.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ai-analytics/02-UI-SPEC.md
@.planning/phases/02-ai-analytics/02-PATTERNS.md
@.planning/phases/02-ai-analytics/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01/02 and existing code. -->

From @panelito/types (Plan 01):
- PERSONA_LIBRARY: PersonaConfig[] (one entry: analista_cientifico)
- PERSONA_IDS = ['analista_cientifico'] as const

From Plan 01 migration (live): sessions.active_personas text[] NOT NULL DEFAULT '{analista_cientifico}'

From Plan 02 invoke route: returns 409 no_active_persona when the session's active_personas is empty (the OFF state that stops the AI). This plan flips that array.

From apps/api/src/routes/messages.ts (analog for personas.ts): Hono + AuthVariables + requireAuth; createServiceClient; Zod body parse → 400; creator ownership check (creator_id === user.id else 403).

From apps/api/src/index.ts: route registration block style (lines 42-69).

From apps/web/app/(protected)/sessions/new/new-session-form.tsx (EXISTING — extend):
- useForm + zodResolver(SessionCreateInputSchema); mode card selector pattern (copy for persona card); submit handler
- Add active_personas to the submitted payload (default ['analista_cientifico'])

From apps/web/components/workspace/CreatorControls.tsx (EXISTING — extend):
- FreezeButton/UnfreezeButton/CloseButton; desktop float + mobile Sheet (side bottom); apiFetch + ApiError handler pattern (handleFreeze analog)
- toast utility available (used elsewhere) — confirm the import path while reading the file

shadcn components to add (official registry only): switch, scroll-area — `npx shadcn@latest add switch scroll-area`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Personas toggle route + registration</name>
  <files>apps/api/src/routes/personas.ts, apps/api/src/index.ts</files>
  <read_first>
    - apps/api/src/routes/messages.ts (auth + creator ownership 403 + Zod body parse + service-client update + error handling)
    - apps/api/src/routes/sessions.ts (how a session row is updated and ownership verified)
    - apps/api/src/index.ts (route registration block style)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (section "apps/api/src/index.ts" — personas route registration)
  </read_first>
  <action>
    Create apps/api/src/routes/personas.ts: Hono router typed with AuthVariables, requireAuth on '*'. Define PostPersonaBodySchema = z.object({ personaId: z.enum(PERSONA_IDS), active: z.boolean() }). Add POST '/' handler: read sessionId from param, user from c.get('user'); fetch the session (id, creator_id, active_personas); 404 if missing; 403 if creator_id !== user.id (PERSONA-02 is creator-only). Parse body (400 on failure). Compute the new active_personas array: if active true, add personaId if absent; if active false, remove it. Update sessions.active_personas via createServiceClient (500 on error). Return c.json({ active_personas: newArray }, 200). The session_status / active_personas change should reach clients — if the sessions table is already in the realtime publication, the update broadcasts automatically; otherwise rely on the client optimistic update (the OFF gate is enforced server-side at invoke time regardless).
    Register in apps/api/src/index.ts: import personasRouter and app.route('/api/sessions/:id/personas', personasRouter) with a comment block matching existing style (PERSONA-02).
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit && grep -q "personasRouter" src/index.ts && grep -q "active_personas" src/routes/personas.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - personas.ts contains a creator ownership check returning 403 for non-creators
    - personas.ts adds/removes personaId from active_personas and updates the sessions row
    - personas.ts validates body with z.enum(PERSONA_IDS) + boolean active
    - index.ts contains app.route('/api/sessions/:id/personas', personasRouter)
    - pnpm --filter @panelito/api typecheck exits 0
  </acceptance_criteria>
  <done>The persona toggle persists to sessions.active_personas under creator authorization; the empty-array OFF state stops the AI via the Plan 02 invoke gate.</done>
</task>

<task type="auto">
  <name>Task 2: Session-creation persona picker</name>
  <files>apps/web/app/(protected)/sessions/new/new-session-form.tsx</files>
  <read_first>
    - apps/web/app/(protected)/sessions/new/new-session-form.tsx (full — useForm, mode card selector pattern lines ~112-136, submit handler)
    - apps/web/lib/utils.ts (cn helper)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (section "new-session-form.tsx" — persona picker section markup + form schema extension)
    - .planning/phases/02-ai-analytics/02-UI-SPEC.md (Surface 4 — persona card anatomy, selected/off states, 44px touch target, copy, default ON)
  </read_first>
  <action>
    Add a persona section to the session creation form AFTER the mode selector, before the submit button. Section label "Analista IA" (13px muted). Render one persona card (button type=button, full width, bg-card, border, rounded 8px, p-4, hover bg-muted, flex row 12px gap, min 44px touch target) for the Analista Científico entry from PERSONA_LIBRARY: a 40px Indigo-tinted avatar (FlaskConical 20px Indigo 400, aria-hidden), name "Analista Científico" (15px), description "Analiza datos, detecta falacias y estructura la información cuantitativa." (13px muted, line-clamp-2), and a Switch (shadcn — add it in this task via npx shadcn@latest add switch scroll-area) on the far right. Selected/active state: ring-2 ring-primary + 2px Indigo 500 border + Indigo 400 avatar border. Off state: plain border, reduced-opacity avatar. Footer note "Puedes activar o desactivar los analistas durante la sesión." (13px muted, 8px below). Add active_personas to the form values (default ['analista_cientifico']); toggling the card adds/removes 'analista_cientifico'. Include active_personas in the create-session submit payload so the new session row starts with the chosen personas. Default is ON.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit && grep -q "active_personas" "app/(protected)/sessions/new/new-session-form.tsx" && grep -q "Analista" "app/(protected)/sessions/new/new-session-form.tsx" && test -f components/ui/switch.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The form renders an "Analista IA" section with the Analista Científico card and a Switch, ON by default
    - active_personas is included in the create-session submit payload (default ['analista_cientifico'])
    - Persona card meets the 44px touch-target minimum and shows the selected ring when active
    - components/ui/switch.tsx exists (shadcn switch added from official registry)
    - pnpm --filter @panelito/web typecheck exits 0
  </acceptance_criteria>
  <done>A creator can pre-select the Analista persona at session creation (PERSONA-01/02 before-session path).</done>
</task>

<task type="auto">
  <name>Task 3: CreatorControls persona management drawer with live toggle</name>
  <files>apps/web/components/workspace/CreatorControls.tsx</files>
  <read_first>
    - apps/web/components/workspace/CreatorControls.tsx (full — FreezeButton/CloseButton, desktop float + mobile Sheet, handleFreeze apiFetch+ApiError pattern, toast import path)
    - apps/web/components/ui/sheet.tsx + apps/web/components/ui/switch.tsx (Sheet side variants; Switch props)
    - apps/web/store/session-store.ts (current active_personas source if the session object carries it — read how session state is held)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (section "CreatorControls.tsx" — Analistas button, drawer Sheet, handlePersonaToggle with toast)
    - .planning/phases/02-ai-analytics/02-UI-SPEC.md (Surface 5 — drawer mechanism desktop Sheet side=right 320px / mobile inline section, persona card with Switch, copy, aria-labels)
  </read_first>
  <action>
    Add persona management to CreatorControls. Add an "Analistas" button (outline, h-9, Lucide Users icon + "Analistas") alongside Freeze/Close. Desktop (>=768px): open a Sheet side="right" width 320px titled "Analistas activos"; mobile (<768px): render the persona section inline inside the existing bottom Sheet (no nested sheet). In the drawer, render the Analista Científico persona card (same anatomy as Surface 4) with a live Switch reflecting whether 'analista_cientifico' is in the session's active_personas. handlePersonaToggle(active): set the switch optimistically, await apiFetch POST /api/sessions/${session.id}/personas { personaId: 'analista_cientifico', active }; on ApiError revert the switch AND show toast.error("No se pudo cambiar el analista. Inténtalo de nuevo.") (Pitfall 7 — this IS a toasted error, unlike reaction revert). Switch aria-label "Desactivar Analista Científico" when on / "Activar Analista Científico" when off. Disable the switch while a toggle is in flight. Footer note "Los cambios se aplican de inmediato a los mensajes siguientes." When the persona is toggled off and active_personas becomes empty, the AI stops responding (server gate from Plan 02) — no extra client logic needed.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit && grep -q "/personas" components/workspace/CreatorControls.tsx && grep -q "analista_cientifico" components/workspace/CreatorControls.tsx && grep -q "No se pudo cambiar el analista" components/workspace/CreatorControls.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CreatorControls has an "Analistas" button opening a Sheet (desktop side=right 320px / mobile inline section)
    - The drawer shows the Analista Científico card with a live Switch bound to active_personas membership
    - handlePersonaToggle POSTs to /personas, optimistically updates, and on failure reverts + toasts "No se pudo cambiar el analista. Inténtalo de nuevo."
    - Switch aria-labels match the on/off copy from the UI spec
    - pnpm --filter @panelito/web typecheck exits 0
  </acceptance_criteria>
  <done>The creator can toggle the Analista on/off mid-session; OFF stops AI responses (PERSONA-02 during-session path).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — persona picker + mid-session toggle + OFF stops AI</name>
  <what-built>Persona management end to end: creation-form picker, in-session Analistas drawer with a live Switch, and the OFF state that halts AI responses.</what-built>
  <how-to-verify>
    1. Run pnpm dev with a verified key. Go to /sessions/new — confirm the "Analista IA" section shows the Analista Científico card ON by default; create the session.
    2. In the workspace, send "@analista, hola" — confirm the AI responds (persona active).
    3. Open CreatorControls → "Analistas" — confirm the drawer opens (Sheet on desktop, inline on mobile) with a Switch in the ON position.
    4. Toggle the Switch OFF. Send "@analista, ¿sigues ahí?" — confirm the AI does NOT respond (active_personas now empty; server no_active_persona gate).
    5. Toggle the Switch back ON, send @analista again — confirm the AI responds.
    6. Force a failure (stop the API briefly) and toggle — confirm the switch reverts and a toast "No se pudo cambiar el analista. Inténtalo de nuevo." appears (Pitfall 7).
    7. Confirm each AI bubble still shows the "Analista Científico" persona badge (PERSONA-03 from Plan 03).
  </how-to-verify>
  <resume-signal>Type "approved" if the picker, mid-session toggle, OFF-stops-AI behavior, and failure toast all work, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client toggle → /personas | Untrusted persona-toggle request crosses into the session config that gates the AI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Elevation of Privilege | guest toggling personas | mitigate | personas route 403s non-creators (creator_id === user.id); only the creator can change active_personas (PERSONA-02) |
| T-02-19 | Tampering | invalid persona id in toggle body | mitigate | Zod z.enum(PERSONA_IDS) rejects unknown persona ids at the route boundary |
| T-02-20 | Spoofing | optimistic toggle desync hides AI-off state | mitigate | Server enforces no_active_persona at invoke time (Plan 02) regardless of client state; failed toggle reverts + toasts (Pitfall 7) |
| T-02-SC | Tampering | shadcn switch/scroll-area install | mitigate | Added from official shadcn registry only (ui.shadcn.com/r) per RESEARCH Registry Safety; no third-party registries; not a package-manager install of an unaudited package |
</threat_model>

<verification>
- pnpm --filter @panelito/api typecheck exits 0
- pnpm --filter @panelito/web typecheck exits 0
- Human checkpoint confirms picker, toggle, OFF-stops-AI, failure toast
</verification>

<success_criteria>
- Analista Científico available in all sessions, selectable at creation and toggleable mid-session (PERSONA-01/02)
- OFF state stops AI responses (server-gated)
- AI bubbles persona-labeled (PERSONA-03, delivered in Plan 03, verified here)
</success_criteria>

<output>
Create `.planning/phases/02-ai-analytics/02-06-SUMMARY.md` when done.
</output>
