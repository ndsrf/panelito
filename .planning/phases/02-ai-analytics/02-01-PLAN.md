---
phase: 02-ai-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0004_reactions_personas.sql
  - packages/types/src/panel.ts
  - packages/types/src/reaction.ts
  - packages/types/src/persona.ts
  - packages/types/src/index.ts
  - apps/api/src/lib/ai-provider.ts
  - apps/api/src/lib/anthropic.ts
autonomous: false
requirements: [AI-04, AI-05, AI-08, PANEL-01, PANEL-04, REACT-01, REACT-02, REACT-03, REACT-04, PERSONA-01]
must_haves:
  truths:
    - "The reactions table exists in the live local Supabase DB with a UNIQUE(message_id, author_id, emoji) constraint and a CHECK on emoji values"
    - "The sessions table has an active_personas text[] column defaulting to {analista_cientifico}"
    - "The messages table has a role column (user|assistant) so AI context assembly can distinguish persona turns"
    - "PanelWidgetSchema validates all 4 widget types via a discriminated union and rejects malformed payloads without throwing"
    - "An AIProvider interface exists wrapping streaming text + tool use behind a provider-agnostic contract"
    - "compressHistory() produces a short summary string from older messages for the AI-08 sliding window"
  artifacts:
    - path: "supabase/migrations/0004_reactions_personas.sql"
      provides: "reactions table, sessions.active_personas, messages.role"
      contains: "create table public.reactions"
    - path: "packages/types/src/panel.ts"
      provides: "PanelWidgetSchema discriminated union + PanelWidget + AIStreamEvent types"
      exports: ["PanelWidgetSchema", "PanelWidget", "AIStreamEvent"]
    - path: "packages/types/src/reaction.ts"
      provides: "Reaction + ReactionCount schemas/types"
      exports: ["ReactionSchema", "Reaction", "ReactionCountSchema", "ReactionCount"]
    - path: "packages/types/src/persona.ts"
      provides: "PersonaConfig + PERSONA_LIBRARY"
      exports: ["PersonaConfig", "PERSONA_LIBRARY", "PERSONA_IDS", "PersonaId"]
    - path: "apps/api/src/lib/ai-provider.ts"
      provides: "AIProvider interface + AnthropicAdapter + renderPanelTool"
      exports: ["AIProvider", "AnthropicAdapter", "renderPanelTool"]
  key_links:
    - from: "packages/types/src/index.ts"
      to: "panel.ts / reaction.ts / persona.ts"
      via: "barrel re-export"
      pattern: "export .*from './panel'"
    - from: "apps/api/src/lib/ai-provider.ts"
      to: "@anthropic-ai/sdk messages.stream"
      via: "AnthropicAdapter.stream"
      pattern: "client.messages.stream"
---

<objective>
Establish the Phase 2 contracts-first foundation: the database schema (reactions table, active_personas column, messages.role column), the shared Zod types every downstream plan imports (PanelWidget, Reaction, PersonaConfig), the provider-agnostic AIProvider streaming interface (D-03), and the AI-08 history compression helper.

Purpose: Downstream plans (streaming backend, widgets, reactions, personas) all consume these contracts. Defining them first eliminates the scavenger-hunt anti-pattern and lets Waves 2-6 build against fixed signatures.
Output: One migration (pushed to live DB), three new shared type modules, one new AIProvider module, and an extended anthropic.ts.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-analytics/02-RESEARCH.md
@.planning/phases/02-ai-analytics/02-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor builds against. Do not re-derive. -->

From apps/api/src/lib/anthropic.ts (EXTEND, do not replace):
- export interface Message { role: 'user' | 'assistant'; content: string }
- export interface AssembleOptions { systemPrompt; personaInstructions; historicalSummary; recentMessages: Message[]; userMessage }
- export function assemblePromptArray(opts: AssembleOptions): MessageParam[]   // body unchanged
- export async function verifyApiKey(key: string): Promise<VerifyResult>       // model 'claude-haiku-4-5-20251001'

From supabase/migrations/0001_initial_schema.sql:
- public.messages columns: id, session_id, author_id, display_name, parent_id, path_id (default 'main'), content, canvas_snapshot_state jsonb, created_at
- public.messages has NO role column today (the ai.ts stub queries .select('role, content') which currently returns null for role — this plan adds the column)
- index messages_session_path_created_idx on (session_id, path_id, created_at) already exists
- RLS pattern: SELECT for session participants; INSERT guarded by auth.uid() = author_id

From packages/types/src/index.ts: barrel re-exports each module (e.g. export * from './message')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared Zod types — panel, reaction, persona</name>
  <files>packages/types/src/panel.ts, packages/types/src/reaction.ts, packages/types/src/persona.ts, packages/types/src/index.ts, packages/types/src/panel.test.ts</files>
  <read_first>
    - packages/types/src/message.ts (Zod object + z.infer export pattern, analog for panel/reaction)
    - packages/types/src/session.ts (enum + object pattern, analog for persona)
    - packages/types/src/index.ts (barrel export style)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (sections "packages/types/src/panel.ts", "reaction.ts", "persona.ts" — exact recommended schemas)
    - .planning/phases/02-ai-analytics/02-RESEARCH.md (Code Examples — PanelWidgetSchema shape)
  </read_first>
  <behavior>
    - PanelWidgetSchema.safeParse a radar payload with 3 axes succeeds
    - PanelWidgetSchema.safeParse a radar payload with 1 axis fails (min 3 axes)
    - PanelWidgetSchema.safeParse a bento payload with 1 card succeeds
    - PanelWidgetSchema.safeParse an unknown widget_type fails (discriminated union rejects)
    - PanelWidgetSchema.safeParse a pie payload with 1 segment fails (min 2 segments)
    - PanelWidgetSchema.safeParse(null) returns success false and does not throw
    - ReactionSchema rejects an emoji not in the 4 power emojis
    - PERSONA_LIBRARY has exactly one entry with id 'analista_cientifico'
  </behavior>
  <action>
    Create packages/types/src/panel.ts exporting PanelWidgetSchema as a z.discriminatedUnion('widget_type', [...]) with 4 variants: bento (cards array of {category max 60, concept max 120, relevance_score number 0-100 optional}, min 1 max 6), radar (axes array of {axis max 60, value 0-100}, min 3 max 8), scatter (points array of {concept max 80, consensus 0-100, impact 0-100}, min 1 max 20), pie (segments array of {label max 60, value greater-or-equal 0}, min 2 max 8). Each variant has optional title. Export type PanelWidget = z.infer of the schema. Export type AIStreamEvent = union of {type:'text_delta';text:string} | {type:'panel_update';payload:PanelWidget} | {type:'done'}.
    Create packages/types/src/reaction.ts exporting ReactionSchema ({id uuid, message_id uuid, session_id uuid, author_id uuid, emoji enum of the 4 power emojis, created_at string}) and ReactionCountSchema ({emoji, count nonnegative int, isOwn boolean}) plus z.infer types Reaction and ReactionCount.
    Create packages/types/src/persona.ts exporting PERSONA_IDS = ['analista_cientifico'] as const, type PersonaId, PersonaConfigSchema ({id, displayName, description, systemPromptAddition, icon, active}), type PersonaConfig, and PERSONA_LIBRARY array with the single Analista Científico entry (displayName "Analista Científico", description "Analiza datos, detecta falacias y estructura la información cuantitativa.", icon "FlaskConical", systemPromptAddition a clinical/neutral analyst instruction satisfying PERSONA-01, active true).
    Add three re-export lines to packages/types/src/index.ts: export * from './panel'; export * from './reaction'; export * from './persona'.
    Create packages/types/src/panel.test.ts (vitest) asserting every behavior case above.
  </action>
  <verify>
    <automated>cd packages/types && pnpm exec vitest run src/panel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - panel.ts contains the literal z.discriminatedUnion('widget_type'
    - panel.ts exports PanelWidgetSchema, PanelWidget, AIStreamEvent
    - reaction.ts exports ReactionSchema, ReactionCountSchema, Reaction, ReactionCount
    - persona.ts exports PERSONA_LIBRARY with one entry whose id === 'analista_cientifico'
    - index.ts contains export * from './panel'
    - pnpm --filter @panelito/types typecheck exits 0
    - panel.test.ts assertions pass (all behavior cases green)
  </acceptance_criteria>
  <done>All four widget variants validate; malformed/unknown payloads are rejected without throwing; barrel exports compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AIProvider interface + AnthropicAdapter + render_panel tool + compressHistory</name>
  <files>apps/api/src/lib/ai-provider.ts, apps/api/src/lib/ai-provider.test.ts, apps/api/src/lib/anthropic.ts</files>
  <read_first>
    - apps/api/src/lib/anthropic.ts (verifyApiKey wraps new Anthropic({ apiKey }); assemblePromptArray; model 'claude-haiku-4-5-20251001')
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (section "apps/api/src/lib/ai-provider.ts"; section "anthropic.ts (extend)" — compressHistory)
    - .planning/phases/02-ai-analytics/02-RESEARCH.md (Pattern 1 streamSSE; "render_panel tool definition"; Don't Hand-Roll — use contentBlock event, never parse input_json_delta chunks)
  </read_first>
  <behavior>
    - AnthropicAdapter.stream() yields text_delta events as on('text') fires, then a tool_use event when a render_panel tool_use block completes, then a done event
    - Tool input JSON is only emitted after the contentBlock event fires (fully accumulated), never from partial input_json_delta chunks
    - compressHistory([]) returns the empty string
    - compressHistory(messages) calls model 'claude-haiku-4-5-20251001' and returns the text content of the response
  </behavior>
  <action>
    Create apps/api/src/lib/ai-provider.ts. Define and export: TextDeltaEvent {type:'text_delta';text:string}, ToolUseEvent {type:'tool_use';name:string;input:unknown}, DoneEvent {type:'done'}, union type AIStreamEvent, and interface AIProvider with method stream(messages: MessageParam[], tools: Anthropic.Tool[], options: { model: string; maxTokens: number; system?: string }) returning AsyncIterable of AIStreamEvent. Export const renderPanelTool with name 'render_panel', description "Renders a visual analytics widget in the analytics panel based on the conversation.", and input_schema: object with properties widget_type (string enum ['bento','radar','scatter','pie']), title (string), data (object), required ['widget_type','data']. Export class AnthropicAdapter implements AIProvider with constructor(apiKey: string). In stream(), construct new Anthropic({ apiKey }), call client.messages.stream({ model, max_tokens, tools, messages, system }), register on('text', ...) to enqueue a TextDeltaEvent and on('contentBlock', ...) to enqueue a ToolUseEvent when block.type === 'tool_use'. Bridge the event-callback API to AsyncIterable using an internal queue plus a resolver promise so events yield in arrival order; await apiStream.done(), then enqueue DoneEvent and close the iterator. Model is taken from options, never hardcoded in the adapter.
    Extend apps/api/src/lib/anthropic.ts: add and export async function compressHistory(client: Anthropic, messages: Message[]): Promise<string> that returns '' when messages is empty, else calls client.messages.create with model 'claude-haiku-4-5-20251001', max_tokens 512, one user message requesting a 3-5 sentence summary of the serialized messages, and returns the response text (or '' if no text block). Do NOT modify assemblePromptArray body or AssembleOptions — path filtering happens at the DB query in Plan 02.
    Create apps/api/src/lib/ai-provider.test.ts: mock @anthropic-ai/sdk messages.stream to emit a text event, then a tool_use contentBlock, then resolve done(); assert the async iterator yields text_delta, tool_use, done in that order. Add a compressHistory test asserting empty input returns ''.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec vitest run src/lib/ai-provider.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - ai-provider.ts contains client.messages.stream
    - ai-provider.ts exports AIProvider, AnthropicAdapter, renderPanelTool
    - renderPanelTool.input_schema.properties.widget_type.enum equals ['bento','radar','scatter','pie']
    - anthropic.ts contains export async function compressHistory
    - compressHistory uses model 'claude-haiku-4-5-20251001'
    - ai-provider.test.ts asserts iterator order text_delta then tool_use then done and passes
    - pnpm --filter @panelito/api typecheck exits 0
  </acceptance_criteria>
  <done>The adapter yields a provider-agnostic AsyncIterable of stream events; the render_panel tool is defined; compressHistory returns a summary string. No Hono route logic references the SDK directly.</done>
</task>

<task type="auto">
  <name>Task 3: Migration — reactions table, active_personas, messages.role</name>
  <files>supabase/migrations/0004_reactions_personas.sql</files>
  <read_first>
    - supabase/migrations/0001_initial_schema.sql (header comment format; messages table; RLS SELECT for participants + INSERT auth.uid()=author_id; index style; supabase_realtime publication usage)
    - supabase/migrations/0003_ai_count_helpers.sql (most recent migration — column-add + grant style)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (section "supabase/migrations/0004_reactions_personas.sql" — table + RLS pattern)
    - .planning/phases/02-ai-analytics/02-RESEARCH.md (New DB Migration Requirements; Open Question 2 — Postgres Realtime INSERT subscription)
  </read_first>
  <action>
    Create supabase/migrations/0004_reactions_personas.sql using the 0001 header comment format (Migration: 0004_reactions_personas; Creates: reactions table, sessions.active_personas, messages.role).
    (1) CREATE TABLE public.reactions: id uuid PK default gen_random_uuid(); message_id uuid NOT NULL REFERENCES public.messages(id) ON DELETE CASCADE; session_id uuid NOT NULL REFERENCES public.sessions(id) ON DELETE CASCADE; author_id uuid NOT NULL; emoji text NOT NULL CHECK (emoji IN the 4 power emojis brain/fire/pin/target); created_at timestamptz NOT NULL DEFAULT now(); UNIQUE (message_id, author_id, emoji).
    (2) CREATE INDEX reactions_message_idx ON public.reactions (message_id).
    (3) ALTER TABLE public.reactions ENABLE ROW LEVEL SECURITY. Add a SELECT policy allowing session participants to read (mirror the messages SELECT policy from 0001) and an INSERT policy WITH CHECK (auth.uid() = author_id) (mirror messages INSERT). Then run alter publication supabase_realtime add table public.reactions so INSERT events broadcast to participants (Open Question 2 recommendation).
    (4) ALTER TABLE public.sessions ADD COLUMN active_personas text[] NOT NULL DEFAULT '{analista_cientifico}'.
    (5) ALTER TABLE public.messages ADD COLUMN role text NOT NULL DEFAULT 'user' CHECK (role IN ('user','assistant')). This lets AI rows be distinguished from human rows so AI-06/AI-08 context assembly maps the correct Anthropic role. Existing rows default to 'user'.
  </action>
  <verify>
    <automated>grep -qi "create table public.reactions" supabase/migrations/0004_reactions_personas.sql && grep -qi "active_personas text" supabase/migrations/0004_reactions_personas.sql && grep -qi "add column role text" supabase/migrations/0004_reactions_personas.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains create table public.reactions
    - The reactions table has unique (message_id, author_id, emoji)
    - The reactions emoji column has a CHECK constraint listing the 4 power emojis
    - File contains add column active_personas text[] with default '{analista_cientifico}'
    - File contains add column role text with CHECK (role in ('user','assistant'))
    - File contains alter publication supabase_realtime add table public.reactions
  </acceptance_criteria>
  <done>The migration file exists with the reactions table, RLS policies, Realtime publication, active_personas column, and messages.role column. Push to live DB is the BLOCKING task below.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Push schema to local Supabase DB</name>
  <what-built>Migration 0004_reactions_personas.sql (reactions table, sessions.active_personas, messages.role). The executor runs the schema push to apply it to the running local Supabase instance. Type checks and builds pass WITHOUT this push because Supabase types are config-derived, not live-DB-derived — so this push is mandatory to avoid a false-positive verification state.</what-built>
  <how-to-verify>
    1. Ensure local Supabase is running: supabase status (start with supabase start if needed).
    2. Apply the migration: supabase migration up (local). If pushing to a linked remote, set SUPABASE_ACCESS_TOKEN and run supabase db push. If interactive prompts block in non-TTY, run manually.
    3. Confirm the schema landed: supabase db diff shows no pending diff for 0004, OR via psql: \d public.reactions lists the table; \d public.sessions shows active_personas; \d public.messages shows role.
    4. Confirm reactions is in the supabase_realtime publication: select * from pg_publication_tables where tablename = 'reactions'; returns a row.
  </how-to-verify>
  <resume-signal>Type "approved" once the reactions table, active_personas, and messages.role exist in the live local DB, or describe the push error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to reactions table | Guest/creator-supplied reaction INSERTs cross into Postgres |
| AI tool output to PanelWidgetSchema | Untrusted model JSON crosses into client React state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | reactions INSERT | mitigate | RLS INSERT policy WITH CHECK (auth.uid() = author_id) — mirrors messages pattern; a user cannot insert reactions as another author |
| T-02-02 | Tampering | reaction emoji value | mitigate | CHECK constraint restricts emoji to the 4 power emojis; arbitrary strings rejected at DB level |
| T-02-03 | Denial of Service | duplicate reaction spam | mitigate | UNIQUE(message_id, author_id, emoji) prevents row inflation per user per emoji |
| T-02-04 | Tampering | AI panel payload corruption | mitigate | PanelWidgetSchema.safeParse() (Plans 03/04) rejects malformed payloads without throwing (AI-05) — schema authored here |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (all 6 core packages already in monorepo, slopcheck OK per RESEARCH Package Legitimacy Audit). shadcn switch/scroll-area added in Plan 06 from official registry only |
</threat_model>

<verification>
- pnpm --filter @panelito/types typecheck exits 0
- pnpm --filter @panelito/api typecheck exits 0
- pnpm --filter @panelito/types test passes (panel schema cases)
- pnpm --filter @panelito/api test runs ai-provider.test.ts green
- Migration applied to live local DB; reactions table, active_personas, and messages.role all present
</verification>

<success_criteria>
- All four PanelWidget variants validate; malformed payloads rejected without throwing (AI-05 foundation)
- AIProvider interface + AnthropicAdapter compile and yield events in order (AI-04 / D-03 foundation)
- compressHistory returns a summary string (AI-08 foundation)
- Live DB has reactions table (REACT-01..04), active_personas (PERSONA-01), messages.role
</success_criteria>

<output>
Create `.planning/phases/02-ai-analytics/02-01-SUMMARY.md` when done.
</output>
