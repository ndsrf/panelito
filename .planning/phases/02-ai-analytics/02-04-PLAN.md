---
phase: 02-ai-analytics
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - apps/web/components/workspace/widgets/widget-registry.ts
  - apps/web/components/workspace/widgets/BentoGrid.tsx
  - apps/web/components/workspace/widgets/RadarWidget.tsx
  - apps/web/components/workspace/widgets/ScatterWidget.tsx
  - apps/web/components/workspace/widgets/PieWidget.tsx
  - apps/web/components/workspace/AnalyticsPanel.tsx
autonomous: false
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04]
must_haves:
  truths:
    - "The analytics panel renders the correct Recharts widget for the active panelStore widgetType (bento/radar/scatter/pie)"
    - "A widget registry Map<widgetType, ReactComponent> resolves each of the 4 widget types and is extensible without touching the panel container (D-06)"
    - "Widget transitions cross-fade/morph via Framer Motion AnimatePresence keyed on widgetType-branchId (D-08)"
    - "The panel header strip shows a colored branch badge (Main) and an Analizando... pulse while AI is streaming (PANEL-03)"
    - "The widget zone lives inside the existing error boundary so a Recharts crash shows the fallback card and never breaks the chat (LAYOUT-07 preserved)"
  artifacts:
    - path: "apps/web/components/workspace/widgets/widget-registry.ts"
      provides: "extensible widget Map"
      exports: ["widgetRegistry"]
    - path: "apps/web/components/workspace/widgets/RadarWidget.tsx"
      provides: "Recharts radar widget"
      exports: ["RadarWidget"]
    - path: "apps/web/components/workspace/AnalyticsPanel.tsx"
      provides: "dynamic widget zone + header strip"
      contains: "AnimatePresence"
  key_links:
    - from: "apps/web/components/workspace/AnalyticsPanel.tsx"
      to: "usePanelStore"
      via: "widgetType/widgetData read"
      pattern: "usePanelStore"
    - from: "apps/web/components/workspace/AnalyticsPanel.tsx"
      to: "widgetRegistry"
      via: "registry.get(widgetType)"
      pattern: "widgetRegistry.get"
---

<objective>
Render the analytics panel widgets that make Phase 2 feel like "Project Multiverse": four Recharts-based widget components (bento grid, radar, scatter, pie), an extensible registry Map, and the AnalyticsPanel dynamic widget zone with a branch-badge header strip and Framer Motion morph transitions — all inside the existing LAYOUT-07 error boundary.

Purpose: The panel is the product's core differentiator. After this plan, when the AI emits a render_panel tool call, the correct widget visibly morphs into the top 40% of the screen.
Output: Four widget components, a registry, and an extended AnalyticsPanel.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ai-analytics/02-UI-SPEC.md
@.planning/phases/02-ai-analytics/02-PATTERNS.md
@.planning/phases/02-ai-analytics/02-03-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01/03 and existing code. -->

From @panelito/types (Plan 01):
- type PanelWidget = discriminated union on widget_type: 'bento' (cards), 'radar' (axes), 'scatter' (points), 'pie' (segments), each with optional title
- Extract<PanelWidget, { widget_type: 'radar' }> narrows to the radar variant, etc.

From apps/web/store/panel-store.ts (Plan 03):
- usePanelStore() → { widgetType, widgetData, branchId, snapshotState, setWidget, clearWidget, setBranchId, hydrateFromSnapshot }

From apps/web/components/workspace/AnalyticsPanel.tsx (EXISTING — preserve):
- AnalyticsPanelErrorBoundary class component (LAYOUT-07) — wraps content; do not touch its logic
- StateNoKey / StateKeySet empty-state components
- Props interface { hasApiKey: boolean } — Plan 03 added isStreaming?: boolean

Recharts 3.x (installed): import type { TooltipContentProps } from 'recharts' (NOT TooltipProps — renamed in 3.x). All widget files need 'use client'.

UI-SPEC color palette (widget-scoped inline props, not CSS vars): Indigo 500 #6366f1, Violet 400 #a78bfa, Sky 400 #38bdf8, Emerald 400 #34d399, Amber 400 #fbbf24; grid stroke --border #3f3f46; card surface --card #18181b; muted --muted #27272a.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four widget components + registry</name>
  <files>apps/web/components/workspace/widgets/BentoGrid.tsx, apps/web/components/workspace/widgets/RadarWidget.tsx, apps/web/components/workspace/widgets/ScatterWidget.tsx, apps/web/components/workspace/widgets/PieWidget.tsx, apps/web/components/workspace/widgets/widget-registry.ts, apps/web/components/workspace/widgets/widget-registry.test.tsx</files>
  <read_first>
    - apps/web/components/workspace/ChatStream.tsx (existing component structure, 'use client', cn usage)
    - apps/web/components/ui/card.tsx (shadcn Card for bento)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (widget sections — BentoGrid full pattern; RadarWidget Recharts 3.x tooltip; Scatter/Pie specs; widget-registry Map)
    - .planning/phases/02-ai-analytics/02-UI-SPEC.md (Widget 1-4 exact specs: grid cols, radii, donut radii, axis domains, palette, legend)
    - .planning/phases/02-ai-analytics/02-RESEARCH.md (Pattern 5 TooltipContentProps; Pitfall 2 Recharts SSR — 'use client' + dynamic ssr:false fallback)
  </read_first>
  <behavior>
    - widgetRegistry.get('bento'), .get('radar'), .get('scatter'), .get('pie') each return a component (not undefined)
    - widgetRegistry.get('unknown') returns undefined
    - BentoGrid renders one card element per data.cards entry
    - Each widget component is typed to its narrowed PanelWidget variant via Extract
  </behavior>
  <action>
    Create the four widget components, each 'use client', each accepting props { data } narrowed to its Extract<PanelWidget, {widget_type}> variant.
    BentoGrid: pure CSS grid (grid-cols-2 mobile, md:grid-cols-3), gap 8px, overflow-y auto; each card is a shadcn Card (bg-muted, border, rounded 8px, p-3) with category label (13px muted), concept (20px semibold, line-clamp-2), and optional relevance_score pill (right-aligned, bg-card).
    RadarWidget: Recharts ResponsiveContainer 100%x100% → RadarChart with PolarGrid (stroke #3f3f46), PolarAngleAxis (tick 11px #a1a1aa), Radar (stroke #6366f1, fill #6366f1 fillOpacity 0.2, strokeWidth 2), custom Tooltip typed with TooltipContentProps<number,string>, no PolarRadiusAxis, no Legend. Map data.axes to {subject, value}.
    ScatterWidget: ResponsiveContainer → ScatterChart with CartesianGrid (stroke #3f3f46 40%, dasharray "3 3"), XAxis "Consenso" domain [0,100], YAxis "Impacto" domain [0,100] (both 11px muted), Scatter fill #6366f1 radius 6, custom Tooltip showing "Consenso: X | Impacto: Y | concept". Map data.points to {x: consensus, y: impact, concept}.
    PieWidget: ResponsiveContainer → PieChart with Pie innerRadius 40% outerRadius 70% paddingAngle 3; Cells cycling the 5-color palette (Indigo/Violet/Sky/Emerald/Amber, repeat for >5); segment stroke #09090b width 2; Legend below (13px muted, max 6); custom Tooltip "label: X%". Map data.segments to {name: label, value}.
    Create widget-registry.ts ('use client'): export type WidgetComponent = ComponentType<{ data: PanelWidget }>; export const widgetRegistry = new Map keyed by the 4 widget_type literals to the corresponding components (cast each component to WidgetComponent). D-06: future types register here only — the panel container does not change. If Recharts SSR crashes occur, wrap radar/scatter/pie imports with next/dynamic ssr:false (Pitfall 2).
    Create widget-registry.test.tsx asserting all 4 keys resolve and an unknown key returns undefined, plus a render smoke test of BentoGrid with two cards.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run components/workspace/widgets/widget-registry.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - widget-registry.ts exports widgetRegistry resolving 'bento','radar','scatter','pie' to components and undefined for unknown
    - RadarWidget/ScatterWidget/PieWidget import type { TooltipContentProps } from 'recharts' (not TooltipProps)
    - all four widget files begin with 'use client'
    - BentoGrid renders one card per cards entry (test asserts count)
    - pnpm --filter @panelito/web typecheck exits 0
    - widget-registry.test.tsx passes
  </acceptance_criteria>
  <done>Each PanelWidget variant has a dedicated renderer; the registry is the single extension point.</done>
</task>

<task type="auto">
  <name>Task 2: AnalyticsPanel dynamic widget zone — header strip + AnimatePresence morph</name>
  <files>apps/web/components/workspace/AnalyticsPanel.tsx</files>
  <read_first>
    - apps/web/components/workspace/AnalyticsPanel.tsx (full file — preserve AnalyticsPanelErrorBoundary, StateNoKey, StateKeySet, props interface incl. isStreaming from Plan 03)
    - apps/web/components/ui/badge.tsx (shadcn Badge for the branch badge)
    - apps/web/store/panel-store.ts (usePanelStore selectors)
    - apps/web/components/workspace/widgets/widget-registry.ts (registry.get)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (section "AnalyticsPanel.tsx" — header strip markup + widget zone AnimatePresence)
    - .planning/phases/02-ai-analytics/02-UI-SPEC.md (Surface 1 — header strip 36px, branch badge colors, widget zone height calc, animation spec)
  </read_first>
  <action>
    Extend AnalyticsPanel. Inside AnalyticsPanelErrorBoundary, ABOVE the widget zone, add a 36px flex-shrink-0 header strip (PANEL-03): a branch Badge pill (13px) with bg active-branch-color at 20% opacity, 1px border in branch color, text in light variant (Indigo 300 for Main), a 6px left dot in branch color, content "Main"; on the right, when isStreaming is true, a muted "Analizando..." label with an Indigo 500 animate-pulse dot (hidden at rest). Below the header, add the widget zone: read { widgetType, widgetData, branchId } from usePanelStore; resolve WidgetComponent = widgetType ? widgetRegistry.get(widgetType) : null. Wrap in AnimatePresence mode="wait". When WidgetComponent && widgetData, render a motion.div keyed `${widgetType}-${branchId}` with the D-08 animation spec (initial opacity 0 scale 0.97 blur(4px); animate opacity 1 scale 1 blur(0px); exit opacity 0 scale 1.02 blur(4px); duration 0.28 ease [0.25,0.46,0.45,0.94]) containing <WidgetComponent data={widgetData} />. Otherwise render a motion.div keyed "empty" containing the existing StateKeySet (hasApiKey) or StateNoKey. Widget zone height is calc(100% - 36px), overflow hidden, padding 16px. Keep the entire dynamic zone INSIDE the error boundary so a Recharts render crash shows the LAYOUT-07 fallback and the chat below stays operational. Do not alter the error boundary or empty-state component internals.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit && grep -q "AnimatePresence" components/workspace/AnalyticsPanel.tsx && grep -q "widgetRegistry.get" components/workspace/AnalyticsPanel.tsx && grep -q "usePanelStore" components/workspace/AnalyticsPanel.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - AnalyticsPanel.tsx reads usePanelStore and resolves widgetRegistry.get(widgetType)
    - The widget zone is wrapped in AnimatePresence mode="wait" with a motion.div keyed `${widgetType}-${branchId}` using the D-08 spec values
    - A 36px header strip renders a "Main" branch Badge and a conditional "Analizando..." pulse label driven by isStreaming
    - The dynamic widget zone remains inside AnalyticsPanelErrorBoundary (error boundary and empty states unchanged)
    - pnpm --filter @panelito/web typecheck exits 0
  </acceptance_criteria>
  <done>The panel renders the live widget with a morph transition and a branch-badge header, fault-isolated from the chat stream.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — widgets render and morph in the panel</name>
  <what-built>The analytics panel now renders the AI-selected Recharts widget with a branch-badge header and a Framer Motion morph transition, inside the LAYOUT-07 error boundary.</what-built>
  <how-to-verify>
    1. Run pnpm dev with a verified Anthropic key. Create a session, open the workspace.
    2. Send a message that invites quantitative structure, e.g. "@analista, compara estas opciones en varios ejes" — confirm a radar (or other) widget morphs into the top panel with the cross-fade/scale/blur effect.
    3. Send another @analista message that suggests a different breakdown (e.g. "@analista, desglosa esto en proporciones") — confirm the widget transitions to a new type with the morph animation.
    4. Confirm the panel header shows the "Main" branch badge with the indigo dot, and "Analizando..." appears with a pulse dot only while streaming.
    5. (AI-05 sanity) This is verified automatically in Plan 03; here just confirm no crash and the previous widget persists if a malformed payload ever arrives.
    6. Confirm the chat stream below remains fully operational throughout (LAYOUT-07).
  </how-to-verify>
  <resume-signal>Type "approved" if widgets render correctly, the morph transition plays, and the branch badge + streaming label behave, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panelStore data → Recharts | Schema-valid-but-unexpected data could still crash a chart component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Denial of Service | Recharts crash on schema-valid edge data takes down the panel | mitigate | Widget zone stays inside AnalyticsPanelErrorBoundary (LAYOUT-07); a render throw shows the fallback card and the chat stream below remains operational |
| T-02-14 | Tampering | oversized/looping widget data degrades UI | accept | PanelWidgetSchema array bounds (min/max per variant) from Plan 01 cap element counts; remaining visual edge cases are low-value and non-crashing inside the boundary |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | recharts + framer-motion already installed and slopcheck-approved (RESEARCH); no new installs |
</threat_model>

<verification>
- pnpm --filter @panelito/web typecheck exits 0
- widget-registry.test.tsx passes
- Human checkpoint confirms widget render + morph + branch badge + fault isolation
</verification>

<success_criteria>
- Panel renders the correct widget type for the AI tool call (PANEL-01)
- Widget zone morphs on widget/branch change (D-08) — re-render contract ready for PANEL-02/branch switch
- Branch badge visible on the panel (PANEL-03)
- canvas_snapshot_state persisted in Plan 02 feeds future re-render (PANEL-04 wired end to end)
</success_criteria>

<output>
Create `.planning/phases/02-ai-analytics/02-04-SUMMARY.md` when done.
</output>
