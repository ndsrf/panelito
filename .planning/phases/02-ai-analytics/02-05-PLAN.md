---
phase: 02-ai-analytics
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - apps/web/components/workspace/QuickReactionPopover.tsx
  - apps/web/components/workspace/MessageBubble.tsx
  - apps/web/hooks/use-reactions.ts
  - apps/web/components/workspace/MessageList.tsx
autonomous: false
requirements: [REACT-01, REACT-02, REACT-03, REACT-04, REACT-05]
must_haves:
  truths:
    - "Double-tapping a message and choosing an emoji POSTs to /reactions and shows the badge optimistically before server confirmation (REACT-05)"
    - "Reaction badges render below the message bubble with emoji + count, own-reaction highlighted, zero-count hidden"
    - "When the server reports triggersAI for fire/pin/target, the client opens the invoke SSE stream so the AI follows up (REACT-02/03/04)"
    - "A failed reaction POST silently reverts the optimistic badge (no toast) (REACT-05 / Pitfall 6)"
    - "Reaction counts stay in sync across participants via the reactions Realtime INSERT subscription"
  artifacts:
    - path: "apps/web/hooks/use-reactions.ts"
      provides: "reaction state + optimistic apply/revert + Realtime sync"
      exports: ["useReactions"]
    - path: "apps/web/components/workspace/QuickReactionPopover.tsx"
      provides: "real POST + optimistic + AI trigger"
      contains: "/reactions"
    - path: "apps/web/components/workspace/MessageBubble.tsx"
      provides: "reaction badge rendering"
      contains: "ReactionCount"
  key_links:
    - from: "apps/web/components/workspace/QuickReactionPopover.tsx"
      to: "POST /api/sessions/:id/reactions"
      via: "apiFetch"
      pattern: "/reactions"
    - from: "apps/web/hooks/use-reactions.ts"
      to: "reactions Realtime channel"
      via: "supabase postgres_changes INSERT"
      pattern: "reactions"
---

<objective>
Wire the power-reaction system end to end: replace the QuickReactionPopover console.log with a real optimistic POST, render reaction count badges on message bubbles, sync counts across participants via Realtime, and open the AI invoke stream when fire/pin/target reactions trigger a follow-up. After this plan, the four power reactions (🧠🔥📌🎯) are fully functional.

Purpose: Reactions are the second AI-activation path (besides @analista) and the optimistic UI is a mobile-responsiveness requirement (REACT-05).
Output: A working reaction popover, badge rendering, reaction sync hook, and reaction-triggered AI.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ai-analytics/02-UI-SPEC.md
@.planning/phases/02-ai-analytics/02-PATTERNS.md
@.planning/phases/02-ai-analytics/02-02-SUMMARY.md
@.planning/phases/02-ai-analytics/02-03-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01/02/03 and existing code. -->

From @panelito/types (Plan 01):
- type ReactionCount = { emoji: '🧠'|'🔥'|'📌'|'🎯'; count: number; isOwn: boolean }
- type Reaction = { id, message_id, session_id, author_id, emoji, created_at }

From Plan 02 reactions route:
- POST /api/sessions/:id/reactions body { messageId, emoji } → 201 { ...row, triggersAI }
- triggersAI is true for 🔥/📌/🎯, false for 🧠
- reactions table is in the supabase_realtime publication (INSERT events broadcast)

From Plan 03:
- useAIStream(sessionId) → { isAIStreaming, streamingText, openAIStream } — call openAIStream when a reaction triggersAI
- MessageBubble already has isAI/isStreaming/streamingText props (Plan 03)

From apps/web/components/workspace/QuickReactionPopover.tsx (EXISTING — extend):
- REACTION_EMOJIS constant; popover shell; handleReact body currently console.log — replace it
- Props { messageId, open, onOpenChange } — add sessionId, onOptimisticReaction, onRevertReaction

From apps/web/components/workspace/MessageBubble.tsx (Plan 03 extended):
- Add reactions?: ReactionCount[] prop and render badges below the bubble (Surface 3 spec)

From apps/web/lib/api.ts: apiFetch + ApiError (use for the reaction POST — it returns JSON, unlike the SSE stream).
From apps/web/hooks/use-session-channel.ts: existing Supabase Realtime channel subscription pattern (postgres_changes) to mirror for reactions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useReactions hook — optimistic state, Realtime sync, AI trigger relay</name>
  <files>apps/web/hooks/use-reactions.ts, apps/web/hooks/use-reactions.test.ts</files>
  <read_first>
    - apps/web/hooks/use-session-channel.ts (Supabase Realtime postgres_changes subscription pattern)
    - apps/web/lib/api.ts (apiFetch, ApiError)
    - apps/web/store/session-store.ts (Zustand/hook patterns and the current user id source)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (Shared Patterns — Realtime broadcast; API Fetch + optimistic revert; QuickReactionPopover handleReact)
    - .planning/phases/02-ai-analytics/02-RESEARCH.md (Pitfall 6 optimistic revert; Open Question 2 reactions Realtime INSERT)
    - .planning/phases/02-ai-analytics/02-UI-SPEC.md (Surface 3 own-reaction state, zero-count hidden)
  </read_first>
  <behavior>
    - applyOptimistic(messageId, emoji) increments that emoji's count for the message and marks isOwn true immediately
    - revert(messageId, emoji) restores the pre-apply snapshot for that message (count + isOwn)
    - getReactionCounts(messageId) returns only entries with count > 0 (zero-count hidden)
    - a Realtime INSERT for a reaction by another user increments the count without setting isOwn
    - a Realtime INSERT echoing the current user's own optimistic reaction does not double-count
  </behavior>
  <action>
    Create apps/web/hooks/use-reactions.ts exporting useReactions(sessionId, currentUserId). Maintain an in-memory map keyed by message_id to an array of ReactionCount. Expose: getReactionCounts(messageId) returning entries with count > 0 only (REACT: zero-count hidden); applyOptimistic(messageId, emoji) that snapshots current state then increments/creates the emoji entry and sets isOwn true; revert(messageId, emoji) restoring the snapshot (Pitfall 6); ingest(reactionRow) that applies a Realtime INSERT (increment count; set isOwn only if author_id === currentUserId; dedupe so an echo of the local optimistic apply does not double-count — match on message_id+emoji+author and skip if already counted as own). Subscribe to a Supabase Realtime channel for postgres_changes INSERT on public.reactions filtered by session_id (mirror use-session-channel pattern), routing each INSERT through ingest. Also fetch initial reaction counts on mount (GET existing reactions for the session's messages — if no GET endpoint exists, aggregate from the messages already loaded; document the choice). Provide a postReaction(messageId, emoji) helper that applyOptimistic, then apiFetch POST /api/sessions/${sessionId}/reactions { messageId, emoji }; on success, if response.triggersAI is true, return a signal so the caller opens the AI stream (REACT-02/03/04); on ApiError, call revert silently (no toast, Pitfall 6).
    Create apps/web/hooks/use-reactions.test.ts covering applyOptimistic, revert, zero-count hiding, foreign-user ingest, and own-echo dedupe.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run hooks/use-reactions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - use-reactions.ts exports useReactions with getReactionCounts, applyOptimistic, revert, postReaction
    - getReactionCounts hides zero-count entries
    - revert restores the pre-apply snapshot (test asserts)
    - foreign-user Realtime INSERT increments count without isOwn; own echo does not double-count (tests assert)
    - postReaction reverts silently on ApiError (no toast)
    - pnpm --filter @panelito/web typecheck exits 0
    - use-reactions.test.ts passes
  </acceptance_criteria>
  <done>Reaction state is optimistic, Realtime-synced, deduped, and signals AI follow-up for the three trigger emojis.</done>
</task>

<task type="auto">
  <name>Task 2: QuickReactionPopover POST + MessageBubble badges + list wiring</name>
  <files>apps/web/components/workspace/QuickReactionPopover.tsx, apps/web/components/workspace/MessageBubble.tsx, apps/web/components/workspace/MessageList.tsx</files>
  <read_first>
    - apps/web/components/workspace/QuickReactionPopover.tsx (full — preserve REACTION_EMOJIS + popover shell; replace handleReact)
    - apps/web/components/workspace/MessageBubble.tsx (Plan 03 state — add reactions prop + badge block per Surface 3)
    - apps/web/components/workspace/MessageList.tsx (how bubbles are mapped — pass reaction counts + wire popover callbacks)
    - .planning/phases/02-ai-analytics/02-PATTERNS.md (QuickReactionPopover new props; MessageBubble reaction badge markup)
    - .planning/phases/02-ai-analytics/02-UI-SPEC.md (Surface 3 reaction badge anatomy, own-reaction colors, aria-label English)
  </read_first>
  <action>
    Extend QuickReactionPopover: add props sessionId, onOptimisticReaction(emoji), onRevertReaction(emoji). Replace the console.log handleReact with: close the popover, call onOptimisticReaction(emoji), then await the reaction POST via the useReactions postReaction (or via the callbacks passed from MessageList so the hook owns state); on failure call onRevertReaction(emoji) silently. If the POST signals triggersAI, the parent (MessageList/workspace) opens the AI stream — pass that signal up via a callback.
    Extend MessageBubble: add reactions?: ReactionCount[] prop and render the badge row BELOW the bubble content (Surface 3): horizontal flex, 4px gap, wrap; each badge a 24px pill (px 6, rounded full) with emoji (14px) + count (13px); own-reaction state uses Indigo 500 border + Indigo 500 at 10% bg + Indigo 300 text; non-own uses muted bg + border; aria-label in English "{emoji} reaction, {count} times". Hidden when no reactions.
    Wire MessageList: instantiate/consume useReactions(sessionId, currentUserId); pass getReactionCounts(message.id) to each MessageBubble; pass sessionId + onOptimisticReaction/onRevertReaction (delegating to the hook's applyOptimistic/revert) into the per-message QuickReactionPopover; when postReaction returns a triggersAI signal, call the workspace's openAIStream (thread the callback from workspace.tsx through to MessageList). AI bubbles are also reactable (Surface 3) — do not gate reactions to human messages.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit && grep -q "/reactions" components/workspace/QuickReactionPopover.tsx && grep -q "ReactionCount" components/workspace/MessageBubble.tsx && grep -q "useReactions" components/workspace/MessageList.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - QuickReactionPopover.tsx POSTs to /reactions and calls onOptimisticReaction before the await, onRevertReaction on failure
    - MessageBubble.tsx renders reaction badges below the bubble per Surface 3 (own-reaction highlighted, zero-count hidden, English aria-label)
    - MessageList.tsx consumes useReactions and passes counts + popover callbacks per message
    - A triggersAI response opens the AI invoke stream (reaction-triggered AI)
    - pnpm --filter @panelito/web typecheck exits 0
  </acceptance_criteria>
  <done>Double-tap → emoji applies an optimistic badge, persists, syncs to all participants, and (for fire/pin/target) triggers an AI follow-up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — power reactions and reaction-triggered AI</name>
  <what-built>The four power reactions are functional: optimistic badges, cross-participant sync, and AI follow-up for fire/pin/target.</what-built>
  <how-to-verify>
    1. Run pnpm dev with a verified key. Open a session in two windows (creator + guest).
    2. Double-tap a message, pick 🧠 Insight — confirm the badge appears instantly (optimistic) and the AI does NOT respond (REACT-01).
    3. Confirm the badge count appears in the OTHER window within ~1s (Realtime sync).
    4. Double-tap an idea message, pick 🔥 Intensify — confirm the AI streams a critical follow-up (REACT-02) and the input soft-locks during it.
    5. Pick 📌 Pin on a message — confirm the AI converts it into a panel card/widget (REACT-03).
    6. Pick 🎯 Simplify on an AI message — confirm the AI returns 3-5 bullet points (REACT-04).
    7. Simulate a server error (e.g. stop the API briefly) and react — confirm the badge silently reverts with no toast (REACT-05 / Pitfall 6).
  </how-to-verify>
  <resume-signal>Type "approved" if all four reactions behave correctly, sync works, and optimistic revert is silent, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client reaction tap → /reactions | Untrusted reaction payload + AI-trigger request |
| reactions Realtime → all clients | Reaction INSERTs broadcast to every participant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Denial of Service | reaction-spam to drive AI cost | mitigate | Server reactionRateLimit 60/min/user (Plan 02) + checkCap before each AI invoke + UNIQUE constraint (Plan 01) prevents duplicate-row inflation |
| T-02-16 | Spoofing | applying reactions as another user | mitigate | RLS WITH CHECK (auth.uid() = author_id) (Plan 01); server sets author_id from the authed user, not the client body |
| T-02-17 | Tampering | optimistic count drift from server truth | mitigate | revert restores snapshot on POST failure; Realtime INSERT reconciles counts with deduped ingest (own-echo skip) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; all dependencies installed + slopcheck-approved (RESEARCH) |
</threat_model>

<verification>
- pnpm --filter @panelito/web typecheck exits 0
- use-reactions.test.ts passes (optimistic, revert, dedupe, zero-count)
- Human checkpoint confirms all four reactions + sync + silent revert
</verification>

<success_criteria>
- 🧠 marks key concept, no AI (REACT-01); 🔥 intensify, 📌 pin, 🎯 simplify trigger correct AI behavior (REACT-02/03/04)
- Reactions apply optimistically before server confirmation (REACT-05)
- Counts sync across participants; failed POST reverts silently
</success_criteria>

<output>
Create `.planning/phases/02-ai-analytics/02-05-SUMMARY.md` when done.
</output>
