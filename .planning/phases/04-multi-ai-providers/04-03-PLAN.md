---
phase: 04-multi-ai-providers
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - supabase/migrations/0006_multi_provider_keys.sql
  - packages/types/src/api-key.ts
  - packages/types/src/index.ts
  - apps/api/src/routes/keys.ts
  - apps/api/src/routes/settings.ts
autonomous: false
requirements: [AI-01, AI-02, AI-10]
user_setup: []
must_haves:
  truths:
    - "creator_settings has anthropic_api_key (renamed from encrypted_api_key), openai_api_key, gemini_api_key, and active_provider columns after migration push"
    - "authenticated PostgREST role cannot SELECT any of the three key columns"
    - "POST /api/keys/verify accepts a provider field, verifies the right provider, and persists to the matching column"
    - "GET /api/keys/status returns MultiProviderStatus (per-provider has_key/last4/is_active + active_provider)"
    - "PUT /api/keys/active-provider updates active_provider; PUT /api/settings accepts active_provider"
  artifacts:
    - path: "supabase/migrations/0006_multi_provider_keys.sql"
      provides: "Column rename + 3 new columns + column-level REVOKE/GRANT"
      contains: "active_provider"
    - path: "packages/types/src/api-key.ts"
      provides: "Provider-aware ApiKeyVerifyRequestSchema + MultiProviderStatusSchema"
      contains: "MultiProviderStatusSchema"
    - path: "apps/api/src/routes/keys.ts"
      provides: "Multi-provider verify/status + active-provider route"
      contains: "active-provider"
  key_links:
    - from: "apps/api/src/routes/keys.ts"
      to: "creator_settings.{provider}_api_key"
      via: "encrypt + upsert to provider-specific column"
      pattern: "_api_key"
    - from: "apps/api/src/routes/keys.ts"
      to: "apps/api/src/lib/verify-key.ts"
      via: "verifyOpenAIKey / verifyGeminiKey dispatch"
      pattern: "verify(OpenAI|Gemini)Key"
---

<objective>
Migrate the `creator_settings` schema for multi-provider key storage (D-11, D-13), push the migration, then refactor the keys and settings routes to verify/store keys per provider and persist the active provider. Extend the shared api-key types for multi-provider.

Purpose: D-10/D-11/D-13 — three encrypted key columns + active_provider, upfront per-provider verification, column-level security for the new columns (V4 access control). This unblocks the route wiring in Plan 04.
Output: Migration 0006 (pushed), extended api-key types, refactored keys.ts + settings.ts.

NOTE: autonomous=false — Task 2 ([BLOCKING] schema push) may require interactive Supabase auth.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-multi-ai-providers/04-RESEARCH.md
@.planning/phases/04-multi-ai-providers/04-PATTERNS.md

<interfaces>
<!-- Current state extracted from codebase. -->

creator_settings today (migration 0001 + 0002): columns include user_id, encrypted_api_key,
api_response_cap, updated_at. Migration 0002 REVOKEs SELECT(encrypted_api_key) from authenticated
and GRANTs SELECT(user_id, api_response_cap, updated_at).

From @panelito/types (Plan 01): `ProviderSchema = z.enum(['anthropic','openai','gemini'])`,
`type ProviderName`. Import these in api-key.ts.

From Plan 02 (verify-key.ts): `verifyOpenAIKey(key)`, `verifyGeminiKey(key)` → VerifyResult.
NOTE: Plan 02 is a sibling in Wave 2. If verify-key.ts is not yet present when this plan's keys.ts
refactor runs, the import will fail typecheck — but execute-phase runs Wave 2 plans before Wave 3
consumes them. keys.ts only needs the verify functions at runtime in Plan 04's integration; here
the import + dispatch wiring must compile. Coordinate: this plan imports from '../lib/verify-key'.

Current keys.ts patterns (apps/api/src/routes/keys.ts):
  - verifyRateLimit (lines 27-31): 5/min/user
  - POST /verify (lines 50-104): validate → verifyApiKey → encrypt → cap-preserving upsert
  - GET /status (lines 112-140): decrypt-for-last4, returns { has_api_key, last4 }
  - DELETE / (lines 148-163): nulls encrypted_api_key
  Uses: encryptKey/decryptKey from '../lib/crypto', verifyApiKey from '../lib/anthropic'

Current settings.ts: GET/PUT select 'user_id, encrypted_api_key, api_response_cap, updated_at',
strip encrypted_api_key before responding (lines 49, 69, 128, 136). UpdateSettingsSchema (lines 20-27)
only allows api_response_cap.

@apps/api/src/routes/keys.ts
@apps/api/src/routes/settings.ts
@packages/types/src/api-key.ts
@supabase/migrations/0002_creator_settings_grants.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0006 + extend api-key types for multi-provider</name>
  <read_first>
    - supabase/migrations/0002_creator_settings_grants.sql (REVOKE/GRANT column-level pattern to replicate)
    - packages/types/src/api-key.ts (current ApiKeyVerifyRequestSchema + CreatorSettingsSchema to extend)
    - packages/types/src/index.ts (api-key re-export block lines 21-26)
    - .planning/phases/04-multi-ai-providers/04-RESEARCH.md (Pattern 6 migration SQL, Code Examples data shapes)
  </read_first>
  <action>
    Create supabase/migrations/0006_multi_provider_keys.sql. First statement: `ALTER TABLE public.creator_settings RENAME COLUMN encrypted_api_key TO anthropic_api_key;` (data-preserving). Then `ALTER TABLE public.creator_settings ADD COLUMN IF NOT EXISTS openai_api_key text, ADD COLUMN IF NOT EXISTS gemini_api_key text, ADD COLUMN IF NOT EXISTS active_provider text DEFAULT 'anthropic' CHECK (active_provider IN ('anthropic','openai','gemini'));`. Then column-level lockdown (V4 / Pitfall 8): `REVOKE SELECT (anthropic_api_key, openai_api_key, gemini_api_key) ON public.creator_settings FROM authenticated;` followed by `GRANT SELECT (user_id, api_response_cap, active_provider, updated_at) ON public.creator_settings TO authenticated;`. The rename means the old REVOKE on encrypted_api_key no longer covers it — re-revoking by the new name is mandatory. In packages/types/src/api-key.ts: import `{ ProviderSchema }` and `type { ProviderName }` from './ai'. Replace `ApiKeyVerifyRequestSchema` with `z.object({ provider: ProviderSchema, key: z.string().min(10) })` (per-provider prefix validation happens in the route, not here — keys differ: sk-ant-/sk-/AI...). Add `ProviderKeyStatusSchema = z.object({ provider: ProviderSchema, has_key: z.boolean(), last4: z.string().nullable(), is_active: z.boolean() })` and `MultiProviderStatusSchema = z.object({ active_provider: ProviderSchema, providers: z.array(ProviderKeyStatusSchema) })` with inferred type `MultiProviderStatus`. Extend `CreatorSettingsSchema` to add `active_provider: ProviderSchema`. Update packages/types/src/index.ts to export the new schemas/types (ProviderKeyStatusSchema, MultiProviderStatusSchema, MultiProviderStatus) alongside the existing api-key exports.
  </action>
  <verify>
    <automated>grep -q "RENAME COLUMN encrypted_api_key TO anthropic_api_key" supabase/migrations/0006_multi_provider_keys.sql && grep -q "active_provider" supabase/migrations/0006_multi_provider_keys.sql && grep -q "REVOKE SELECT (anthropic_api_key, openai_api_key, gemini_api_key)" supabase/migrations/0006_multi_provider_keys.sql && grep -q "MultiProviderStatusSchema" packages/types/src/api-key.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/0006_multi_provider_keys.sql renames encrypted_api_key → anthropic_api_key
    - migration adds openai_api_key, gemini_api_key (text) and active_provider (text, DEFAULT 'anthropic', CHECK in the 3-provider set)
    - migration REVOKEs SELECT on all three key columns from `authenticated` and GRANTs SELECT only on (user_id, api_response_cap, active_provider, updated_at)
    - packages/types/src/api-key.ts ApiKeyVerifyRequestSchema includes a `provider` field of ProviderSchema
    - packages/types/src/api-key.ts exports MultiProviderStatusSchema and CreatorSettingsSchema includes active_provider
    - packages/types/src/index.ts re-exports MultiProviderStatusSchema and MultiProviderStatus
  </acceptance_criteria>
  <done>Migration file and extended shared types define the multi-provider storage contract with column-level security; no runtime changes applied yet.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Push migration 0006 to the database</name>
  <what-built>Migration 0006 (column rename + 3 new columns + REVOKE/GRANT) is written and ready to apply.</what-built>
  <action>
    Apply the migration BEFORE any route code referencing the new column names runs. Run `supabase db push` from the repo root. Non-TTY/CI workaround: set `SUPABASE_ACCESS_TOKEN` env var first, or for local dev use `supabase db push --local`. This task is [BLOCKING] — the `encrypted_api_key` → `anthropic_api_key` rename breaks every existing query referencing the old name until the route code in Task 3 lands, AND route code referencing openai_api_key/gemini_api_key/active_provider fails at runtime with "column does not exist" until this push completes (RESEARCH Pitfall 3).
  </action>
  <how-to-verify>
    1. Run `supabase db push` (or `--local`) and confirm it reports migration 0006 applied with no error.
    2. Verify columns exist: query `select column_name from information_schema.columns where table_name='creator_settings';` — expect anthropic_api_key, openai_api_key, gemini_api_key, active_provider; expect NO encrypted_api_key.
    3. If push requires interactive login that cannot be suppressed, authenticate, then re-run.
  </how-to-verify>
  <verify>
    <human-check>Migration 0006 applied; information_schema shows the 4 columns and no encrypted_api_key</human-check>
  </verify>
  <resume-signal>Type "pushed" once the migration is applied and columns are confirmed, or describe the error.</resume-signal>
  <done>creator_settings has anthropic_api_key/openai_api_key/gemini_api_key/active_provider in the live DB; encrypted_api_key no longer exists.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor keys.ts + settings.ts for multi-provider</name>
  <read_first>
    - apps/api/src/routes/keys.ts (full file — verify/status/delete handlers + rate limit + cap-preserving upsert)
    - apps/api/src/routes/settings.ts (full file — GET/PUT + UpdateSettingsSchema + encrypted_api_key strip)
    - apps/api/src/lib/verify-key.ts (Plan 02 — verifyOpenAIKey/verifyGeminiKey)
    - apps/api/src/lib/anthropic.ts (verifyApiKey for the anthropic branch)
    - packages/types/src/api-key.ts (extended schemas from Task 1)
    - .planning/phases/04-multi-ai-providers/04-PATTERNS.md (keys.ts + settings.ts sections lines 549-642)
  </read_first>
  <action>
    In apps/api/src/routes/keys.ts: import `verifyOpenAIKey, verifyGeminiKey` from '../lib/verify-key' and keep `verifyApiKey` from '../lib/anthropic'; import `ProviderSchema` from '@panelito/types'. In POST /verify, after parsing the now-provider-aware ApiKeyVerifyRequestSchema, dispatch verification by `provider`: anthropic→verifyApiKey, openai→verifyOpenAIKey, gemini→verifyGeminiKey. Add per-provider prefix validation before verification: anthropic key must start 'sk-ant-', openai 'sk-', gemini 'AI' (return 400 invalid_request on mismatch — Tampering mitigation). On verify success, encrypt and upsert to the column named `${provider}_api_key` (build the update/insert object with a computed key `{ [`${provider}_api_key`]: encrypted }`); preserve the cap-preserving upsert logic (insert default cap 150 when no row). Rewrite GET /status to select `anthropic_api_key, openai_api_key, gemini_api_key, active_provider`, decrypt each present key for its last4 (try/catch → treat tamper as no key per T-06-04), and return a `MultiProviderStatus` { active_provider, providers: [{provider, has_key, last4, is_active}] for each of the 3 }. Update DELETE / to accept a `?provider=` query param (default 'anthropic') and null only the matching `${provider}_api_key` column (other providers' keys persist silently — D-12). Add `PUT /active-provider`: validate body `{ provider }` with ProviderSchema, update `active_provider` column for the user (upsert pattern). All handlers keep `requireAuth` and the existing error-code-only logging. In apps/api/src/routes/settings.ts: extend UpdateSettingsSchema to add `active_provider: ProviderSchema.optional()`; change both `.select(...)` strings from `encrypted_api_key` to `anthropic_api_key` and add `active_provider` to the selected columns; change the strip destructure from `{ encrypted_api_key: _omit, ...rest }` to strip all three key columns `{ anthropic_api_key: _a, openai_api_key: _o, gemini_api_key: _g, ...rest }` and compute `has_api_key: _a !== null`.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | grep -E "routes/keys|routes/settings" | head; grep -q "verifyOpenAIKey" src/routes/keys.ts && grep -q "active-provider" src/routes/keys.ts && ! grep -q "encrypted_api_key" src/routes/keys.ts && ! grep -q "encrypted_api_key" src/routes/settings.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/routes/keys.ts dispatches verification by provider (grep shows verifyOpenAIKey and verifyGeminiKey)
    - keys.ts upserts to a computed `${provider}_api_key` column and contains a `PUT /active-provider` route (grep `active-provider`)
    - keys.ts GET /status returns active_provider plus a providers array (MultiProviderStatus shape)
    - keys.ts contains no reference to `encrypted_api_key` (grep returns nothing)
    - settings.ts UpdateSettingsSchema includes `active_provider`; selects use `anthropic_api_key` and `active_provider`; no `encrypted_api_key` reference remains
    - `npx tsc --noEmit` reports no errors in routes/keys.ts or routes/settings.ts
  </acceptance_criteria>
  <done>Keys and settings routes manage three provider keys + active_provider, verify per provider with prefix guards, enforce column security, and never reference the old column name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → keys/settings route | Authenticated creator submits provider + key (untrusted input) |
| PostgREST authenticated role → creator_settings | Browser-side reads must never see key columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | New openai/gemini/anthropic key columns | mitigate | Migration 0006 REVOKEs SELECT on all three from `authenticated`; only service-role Hono client reads them |
| T-04-08 | Tampering | Key for wrong provider stored in wrong column | mitigate | Per-provider prefix validation (sk-ant- / sk- / AI) in POST /verify before encrypt + write |
| T-04-09 | Tampering | active_provider tampered in transit | mitigate | active_provider updated only via authenticated PUT; RLS enforces auth.uid()=user_id (policy from migration 0001) |
| T-04-10 | Denial of Service | Verify endpoint abuse | mitigate | Existing verifyRateLimit (5/min/user) applies to all provider verify calls — no change needed |
| T-04-11 | Information Disclosure | status route leaks key blob | mitigate | GET /status returns only last4 (decrypt-for-last4); never the encrypted blob or plaintext (AI-02 / T-06-02) |
</threat_model>

<verification>
- Migration 0006 applied; information_schema shows the 4 columns, no encrypted_api_key
- `npx tsc --noEmit` passes for apps/api and packages/types
- No `encrypted_api_key` reference remains in keys.ts or settings.ts
- GET /api/keys/status returns MultiProviderStatus; PUT /api/keys/active-provider updates active_provider
</verification>

<success_criteria>
creator_settings stores three encrypted provider keys plus active_provider with column-level security (D-11, D-13, V4); keys.ts verifies per provider with prefix guards and persists to the right column (D-10); status returns per-provider state; active provider is updatable.
</success_criteria>

<output>
Create `.planning/phases/04-multi-ai-providers/04-03-SUMMARY.md` when done
</output>
