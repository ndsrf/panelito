---
phase: 04-multi-ai-providers
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/api/src/lib/adapters/openai.ts
  - apps/api/src/lib/adapters/gemini.ts
  - apps/api/src/lib/adapter-factory.ts
  - apps/api/src/lib/model-config.ts
  - apps/api/src/lib/verify-key.ts
  - apps/api/src/lib/adapters/openai.test.ts
  - apps/api/src/lib/adapters/gemini.test.ts
autonomous: true
requirements: [AI-03, AI-04, AI-05, AI-08]
must_haves:
  truths:
    - "OpenAIAdapter streams text_delta events and emits a single tool_use event only after finish_reason === 'tool_calls'"
    - "GeminiAdapter streams text_delta events and emits tool_use from chunk.functionCalls accumulated across chunks"
    - "Both adapters always yield exactly one done event (try/finally) even on mid-stream error"
    - "createAdapter('openai'|'gemini'|'anthropic', key) returns the matching adapter instance"
    - "TASK_MODELS maps every provider to analysis/compression/categorization model IDs validated in AI-SPEC"
    - "verifyOpenAIKey and verifyGeminiKey return the shared VerifyResult shape"
  artifacts:
    - path: "apps/api/src/lib/adapters/openai.ts"
      provides: "OpenAIAdapter — index-keyed tool call accumulation, system as messages[0]"
      contains: "class OpenAIAdapter implements AIProvider"
    - path: "apps/api/src/lib/adapters/gemini.ts"
      provides: "GeminiAdapter — @google/genai v2, role mapping, systemInstruction"
      contains: "class GeminiAdapter implements AIProvider"
    - path: "apps/api/src/lib/adapter-factory.ts"
      provides: "createAdapter(provider, apiKey) → AIProvider"
      contains: "export function createAdapter"
    - path: "apps/api/src/lib/model-config.ts"
      provides: "TASK_MODELS task-to-model mapping per provider (D-04)"
      contains: "export const TASK_MODELS"
    - path: "apps/api/src/lib/verify-key.ts"
      provides: "verifyOpenAIKey + verifyGeminiKey"
      contains: "verifyOpenAIKey"
  key_links:
    - from: "apps/api/src/lib/adapter-factory.ts"
      to: "apps/api/src/lib/adapters/openai.ts"
      via: "import { OpenAIAdapter }"
      pattern: "import.*OpenAIAdapter.*adapters/openai"
    - from: "apps/api/src/lib/adapters/gemini.ts"
      to: "@google/genai"
      via: "import { GoogleGenAI }"
      pattern: "import.*GoogleGenAI.*@google/genai"
---

<objective>
Implement the OpenAI and Gemini adapters behind the `AIProvider` interface from Plan 01, plus the adapter factory, task-to-model config, and per-provider key verification helpers. Each adapter is a thin SDK conversion layer; all business logic stays in the route.

Purpose: D-14 requires full parity — all 3 providers produce valid `render_panel` tool calls during streaming. D-04 requires task-to-model mapping. D-10 requires per-provider key verification.
Output: Two new adapters with unit tests, the factory, model-config, and verify-key helpers.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-multi-ai-providers/04-RESEARCH.md
@.planning/phases/04-multi-ai-providers/04-PATTERNS.md
@.planning/phases/04-multi-ai-providers/04-AI-SPEC.md

<interfaces>
<!-- From Plan 01 (04-01-SUMMARY.md). Executor uses these directly. -->

From @panelito/types (created in Plan 01):
  interface ProviderMessage { role: 'user'|'assistant'|'system'; content: string }
  interface ProviderTool { name: string; description: string; parameters: Record<string, unknown> }
  interface ProviderCapabilities { streaming; toolUse; contextCaching; semanticCaching; imageInput; voiceInput; compression }  // all booleans
  type AIStreamEvent = { type:'text_delta'; text:string } | { type:'tool_use'; name:string; input:unknown } | { type:'done' }
  interface AIProvider { capabilities(): ProviderCapabilities; stream(messages, tools, options): AsyncIterable<AIStreamEvent> }
  type ProviderName = 'anthropic' | 'openai' | 'gemini'

Existing VerifyResult (apps/api/src/lib/anthropic.ts lines 24-26 — IMPORT, do not redefine):
  export type VerifyResult = { ok: true } | { ok: false; error: 'invalid_key' | 'rate_limited' | 'network_error' }

Structural twin for both adapters: apps/api/src/lib/adapters/anthropic.ts (from Plan 01) — same
interface, different SDK. Reference its capabilities() + try/finally done-guard pattern.

IMPORTANT: the shared types package is `@panelito/types` (AI-SPEC.md erroneously wrote
`@multiverse/types` — ignore that; use `@panelito/types`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenAIAdapter with index-keyed tool call accumulation</name>
  <read_first>
    - apps/api/src/lib/adapters/anthropic.ts (Plan 01 — capabilities() + try/finally done structure to mirror)
    - .planning/phases/04-multi-ai-providers/04-PATTERNS.md (OpenAIAdapter section lines 285-371 — exact accumulation pattern)
    - .planning/phases/04-multi-ai-providers/04-AI-SPEC.md (Section 3 OpenAI entry point + Common Pitfalls 1, 3)
  </read_first>
  <behavior>
    - Given chunks with delta.content 'Hel' then 'lo', adapter yields two text_delta events ('Hel','lo')
    - Given tool_call deltas for index 0 ('{"widget_' then 'type":"pie","segments":[]}') with NO finish_reason, adapter yields NO tool_use event
    - When a chunk arrives with finish_reason === 'tool_calls', adapter yields one tool_use event with parsed input object { widget_type:'pie', segments:[] }
    - Two concurrent tool calls at index 0 and index 1 produce two separate tool_use events with independent parsed args
    - Malformed accumulated JSON (unparseable) is skipped (continue), no throw
    - done is always the final event (try/finally), even if iteration throws
  </behavior>
  <action>
    Create apps/api/src/lib/adapters/openai.ts. Import `OpenAI from 'openai'` and `type { AIProvider, AIStreamEvent, ProviderMessage, ProviderTool, ProviderCapabilities } from '@panelito/types'`. Implement `class OpenAIAdapter implements AIProvider` with constructor `(private readonly apiKey: string)`. `capabilities()` returns streaming:true, toolUse:true, all 5 others false. `stream()` wraps its full body in try/finally yielding `{ type: 'done' }` in finally. Build `oaiMessages`: push `{ role:'system', content: options.system }` FIRST if options.system set (Pattern 2 — system as messages[0], NOT a top-level param), then map each ProviderMessage skipping role==='system' to `{ role: m.role as 'user'|'assistant', content: m.content }`. Convert tools to `{ type:'function', function: { name, description, parameters: t.parameters } }`. Call `client.chat.completions.stream({ model: options.model, max_tokens: options.maxTokens, messages: oaiMessages, tools: oaiTools })`. Do NOT call `stream.finalMessage()` (kills first-token latency — anti-pattern). Maintain `toolCallAccumulator: Record<number, { id; name; arguments }>` keyed by `tc.index`. For each chunk: yield text_delta on delta.content; for each `tc` in `delta.tool_calls ?? []` init accumulator[idx] on first sight (id + name) then append `tc.function?.arguments ?? ''`; when `chunk.choices[0]?.finish_reason === 'tool_calls'`, iterate accumulator values, `JSON.parse(tc.arguments)` inside try/catch (continue on SyntaxError), yield `{ type:'tool_use', name: tc.name, input: parsed }`. Create apps/api/src/lib/adapters/openai.test.ts mocking the `openai` module to feed a synthetic async-iterable of ChatCompletionChunk-shaped objects covering all behavior cases above.
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/lib/adapters/openai.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/lib/adapters/openai.ts contains `class OpenAIAdapter implements AIProvider`
    - openai.ts accumulates tool calls keyed by `tc.index` (grep shows `tc.index` or `delta.tool_calls`)
    - openai.ts contains no `finalMessage(` call (grep returns nothing)
    - openai.ts stream() body wrapped in `try { ... } finally { yield { type: 'done' } }`
    - openai.ts pushes system prompt as `{ role: 'system'` into the messages array (not a top-level param)
    - `npx vitest run src/lib/adapters/openai.test.ts` exits 0; tests assert no tool_use before finish_reason and exactly one done event
  </acceptance_criteria>
  <done>OpenAIAdapter conforms to AIProvider, accumulates tool calls by index, emits tool_use only after finish_reason, and always closes with done; covered by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GeminiAdapter with @google/genai v2 + role mapping</name>
  <read_first>
    - apps/api/src/lib/adapters/anthropic.ts (Plan 01 — structure to mirror)
    - .planning/phases/04-multi-ai-providers/04-PATTERNS.md (GeminiAdapter section lines 375-454)
    - .planning/phases/04-multi-ai-providers/04-AI-SPEC.md (Section 3 Gemini entry point + Common Pitfalls 2, 4, 5)
  </read_first>
  <behavior>
    - Given chunks with chunk.text 'Hi' then ' there', adapter yields two text_delta events
    - A ProviderMessage with role 'system' is filtered OUT of contents; options.system goes to config.systemInstruction
    - A ProviderMessage with role 'assistant' maps to contents role 'model' (NOT 'assistant')
    - chunk.functionCalls returns complete objects; adapter accumulates across chunks and emits tool_use AFTER the loop ends (one per FunctionCall) with input = fc.args object (no JSON.parse)
    - done is always the final event (try/finally)
  </behavior>
  <action>
    Create apps/api/src/lib/adapters/gemini.ts. Begin the import block with the mandatory comment `// @google/genai v2 — do NOT use @google/generative-ai (legacy v1 SDK)` (D-15) then `import { GoogleGenAI } from '@google/genai'` and `type { AIProvider, AIStreamEvent, ProviderMessage, ProviderTool, ProviderCapabilities } from '@panelito/types'`. Implement `class GeminiAdapter implements AIProvider`, constructor `(private readonly apiKey: string)`, `capabilities()` returning streaming:true, toolUse:true, others false. `stream()` body in try/finally with `yield { type:'done' }` in finally. Instantiate `new GoogleGenAI({ apiKey: this.apiKey })`. Build `contents` by filtering out role==='system' then mapping `{ role: m.role === 'assistant' ? 'model' : 'user', parts: [{ text: m.content }] }` (Pattern 3 / Pitfall 5). Build `functionDeclarations` from tools mapping `{ name, description, parametersJsonSchema: t.parameters }` (AI-SPEC §4 uses `parametersJsonSchema` for Gemini). Call `await ai.models.generateContentStream({ model: options.model, contents, config: { maxOutputTokens: options.maxTokens, ...(options.system ? { systemInstruction: options.system } : {}), tools: functionDeclarations.length ? [{ functionDeclarations }] : undefined } })`. Accumulate `accumulatedFunctionCalls: Array<{ name; args }>`: in the for-await loop yield text_delta on `chunk.text`, and for each fc in `chunk.functionCalls ?? []` push `{ name: fc.name ?? '', args: fc.args ?? {} }` (complete objects — do NOT JSON.parse, Pitfall 4). AFTER the loop, yield one `{ type:'tool_use', name: fc.name, input: fc.args }` per accumulated call. Create apps/api/src/lib/adapters/gemini.test.ts mocking `@google/genai` with a synthetic stream covering text deltas, role filtering (assert no 'system'/'assistant' role leaks into contents passed to the mock), and functionCalls accumulation.
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/lib/adapters/gemini.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/lib/adapters/gemini.ts contains `class GeminiAdapter implements AIProvider`
    - gemini.ts import block contains the comment string `do NOT use @google/generative-ai`
    - gemini.ts imports `GoogleGenAI` from `@google/genai` (grep shows no `@google/generative-ai` import)
    - gemini.ts maps `role === 'assistant'` to `'model'` and filters `role !== 'system'` before building contents
    - gemini.ts passes system via `systemInstruction` in config (grep shows `systemInstruction`)
    - gemini.ts stream() body wrapped in try/finally yielding done; tool_use emitted after the for-await loop
    - `npx vitest run src/lib/adapters/gemini.test.ts` exits 0
  </acceptance_criteria>
  <done>GeminiAdapter conforms to AIProvider using @google/genai v2 with correct role mapping, systemInstruction placement, and post-loop function-call emission; covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Adapter factory, TASK_MODELS config, and per-provider key verification</name>
  <read_first>
    - apps/api/src/lib/adapters/anthropic.ts, openai.ts, gemini.ts (the three adapter classes the factory wires)
    - apps/api/src/lib/anthropic.ts (existing verifyApiKey lines 39-70 + VerifyResult type lines 24-26 — reuse the pattern + type)
    - apps/api/src/lib/rate-limit.ts (factory function export pattern analog)
    - .planning/phases/04-multi-ai-providers/04-RESEARCH.md (Pattern 4 factory, Pattern 5 verify, Code Examples TASK_MODELS)
  </read_first>
  <action>
    Create apps/api/src/lib/adapter-factory.ts: import `type { AIProvider, ProviderName } from '@panelito/types'` and the three adapter classes; export `function createAdapter(provider: ProviderName, apiKey: string): AIProvider` with a switch returning `new AnthropicAdapter(apiKey)` / `new OpenAIAdapter(apiKey)` / `new GeminiAdapter(apiKey)`, default branch throwing `Unknown provider: ${provider satisfies never}`. Create apps/api/src/lib/model-config.ts: import `type { ProviderName } from '@panelito/types'`; export `type TaskType = 'analysis' | 'compression' | 'categorization'`; export `const TASK_MODELS: Record<ProviderName, Record<TaskType, string>>` with EXACTLY these validated IDs (AI-SPEC §4): anthropic { analysis:'claude-sonnet-4-6', compression:'claude-haiku-4-5-20251001', categorization:'claude-haiku-4-5-20251001' }, openai { analysis:'gpt-5.4', compression:'gpt-5.4-mini', categorization:'gpt-5.4-mini' }, gemini { analysis:'gemini-2.5-flash', compression:'gemini-2.5-flash', categorization:'gemini-2.5-flash' } — `as const`. Do NOT use gpt-4o/gpt-4o-mini (deprecated Feb 2026) or gemini-2.0-flash (shut down June 2026). Create apps/api/src/lib/verify-key.ts: import `OpenAI from 'openai'`, `{ GoogleGenAI } from '@google/genai'`, and `type { VerifyResult } from './anthropic'`. Export `async function verifyOpenAIKey(key): Promise<VerifyResult>` calling `await new OpenAI({ apiKey: key }).models.list()` — return { ok:true } on success; in catch return invalid_key for `OpenAI.AuthenticationError`/`OpenAI.PermissionDeniedError`, rate_limited for `OpenAI.RateLimitError`, { ok:true } for any other `OpenAI.APIError` (reached API), else network_error. Export `async function verifyGeminiKey(key): Promise<VerifyResult>` calling `await new GoogleGenAI({ apiKey: key }).models.list()`; on error inspect `(err as Error).message`: invalid_key if it includes 'API_KEY_INVALID' or 'PERMISSION_DENIED', rate_limited if 'RESOURCE_EXHAUSTED', else network_error. Add a `// [ASSUMED] Gemini error shape — verify against live API; see RESEARCH Open Question 1` comment above verifyGeminiKey. Log only the error class name on failure, never the key (T-06-03 pattern from anthropic.ts line 50).
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | grep -E "adapter-factory|model-config|verify-key" | head; grep -q "export function createAdapter" src/lib/adapter-factory.ts && grep -q "gpt-5.4" src/lib/model-config.ts && ! grep -q "gpt-4o" src/lib/model-config.ts && grep -q "verifyOpenAIKey" src/lib/verify-key.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/lib/adapter-factory.ts contains `export function createAdapter` with a switch over 'anthropic'/'openai'/'gemini'
    - apps/api/src/lib/model-config.ts contains `export const TASK_MODELS` with `gpt-5.4` and `gemini-2.5-flash`; contains no `gpt-4o` and no `gemini-2.0-flash` (grep returns nothing for those)
    - apps/api/src/lib/verify-key.ts exports `verifyOpenAIKey` and `verifyGeminiKey` both returning `VerifyResult`
    - verify-key.ts contains the `[ASSUMED]` comment referencing the Gemini error shape open question
    - `npx tsc --noEmit` reports no errors in adapter-factory.ts, model-config.ts, verify-key.ts
  </acceptance_criteria>
  <done>Factory selects the correct adapter by ProviderName; TASK_MODELS pins validated 2026 model IDs per provider/task; verifyOpenAIKey/verifyGeminiKey return the shared VerifyResult shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| creator → provider API | Key verification call sends the plaintext key to the provider over TLS |
| adapter → provider SDK | Decrypted key passed to SDK client constructor in-memory only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | verify-key.ts error logging | mitigate | Log only error class name, never the key (T-06-03 pattern); generic invalid_key returned to caller |
| T-04-04 | Information Disclosure | Provider enumeration via error detail | mitigate | verify functions return only the VerifyResult enum (invalid_key/rate_limited/network_error) — no provider-specific leak |
| T-04-05 | Denial of Service | Adapter never yields done on mid-stream throw | mitigate | Both new adapters wrap stream() in try/finally; unit tests assert done is the final event on error |
| T-04-06 | Tampering | Wrong/deprecated model ID causes runtime 404 | mitigate | TASK_MODELS pins AI-SPEC-validated IDs; acceptance test greps out gpt-4o / gemini-2.0-flash |
</threat_model>

<verification>
- `npx vitest run src/lib/adapters/openai.test.ts src/lib/adapters/gemini.test.ts` passes
- `npx tsc --noEmit` passes for apps/api
- TASK_MODELS contains no deprecated model IDs
- Gemini adapter imports only `@google/genai`, never the v1 package
</verification>

<success_criteria>
OpenAI and Gemini adapters implement the AIProvider contract with correct accumulation, system-prompt placement, role mapping, and try/finally done guards (D-14 parity); the factory resolves any provider; TASK_MODELS pins validated model IDs (D-04); verify helpers return the shared VerifyResult shape (D-10).
</success_criteria>

<output>
Create `.planning/phases/04-multi-ai-providers/04-02-SUMMARY.md` when done
</output>
