---
phase: 08-human-control-canvas-sync
plan: 05
type: execute
wave: 3
depends_on: [08-01]
files_modified:
  - apps/web/store/session-store.ts
  - apps/web/hooks/use-ai-stream.ts
  - apps/web/hooks/use-session-channel.ts
  - apps/web/components/workspace/CreatorControls.tsx
  - apps/web/components/workspace/InputBox.tsx
autonomous: true
requirements:
  - HUMAN-01
  - HUMAN-02
  - CANVAS-02

must_haves:
  truths:
    - "session-store.ts exposes micLocked: boolean, currentPhase: string | null, canvasNodes: CanvasNode[], canvasEdges: CanvasEdge[] state with setMicLocked, setCurrentPhase, setCanvasData setters (D-08, D-09, D-14)"
    - "use-ai-stream.ts handles phase_signal SSE event inside the for-block loop before done event; parses { current_phase_id, blueprint_id } and stores phaseSignal + pendingPhaseId in component state (D-10)"
    - "use-ai-stream.ts returns mic_locked as a new AIStreamStatus variant when the /invoke response is HTTP 409 with { error: 'mic_locked' } body (D-05, D-02)"
    - "use-session-channel.ts handles mic_acquired, mic_released, canvas_update, and phase_advanced broadcasts; mic events filter by payload.branch_id === activeBranchId before mutating store (D-04, D-12, D-16)"
    - "CreatorControls.tsx renders an Advance Phase button (disabled outline when no signal, enabled indigo primary when phase_signal received) and a Badge showing currentPhase; PATCH /sessions/:id/phase is called on click with pendingPhaseId; creator-only gate enforced in component (D-11, D-12, D-13)"
    - "InputBox.tsx reads micLocked from session-store — no visual change in Phase 8 beyond the existing streaming dots; micLocked store field is set reactively by use-session-channel broadcast handlers (D-04)"
  artifacts:
    - path: apps/web/store/session-store.ts
      provides: "Phase 8 state: micLocked, currentPhase, canvasNodes, canvasEdges with typed setters"
      contains: "micLocked"
    - path: apps/web/hooks/use-ai-stream.ts
      provides: "phase_signal SSE handler + mic_locked 409 handler"
      contains: "phase_signal"
    - path: apps/web/hooks/use-session-channel.ts
      provides: "mic_acquired, mic_released, canvas_update, phase_advanced broadcast handlers"
      contains: "mic_acquired"
    - path: apps/web/components/workspace/CreatorControls.tsx
      provides: "Advance Phase button + phase Badge per UI-SPEC Surface 1"
      contains: "Avanzar fase"
    - path: apps/web/components/workspace/InputBox.tsx
      provides: "micLocked read from store (store integration only, no new visual)"
      contains: "micLocked"
  key_links:
    - from: apps/web/hooks/use-session-channel.ts
      to: apps/web/store/session-store.ts
      via: "useSessionStore.getState().setMicLocked(true|false) on mic_acquired/mic_released"
      pattern: "setMicLocked"
    - from: apps/web/hooks/use-ai-stream.ts
      to: apps/web/components/workspace/CreatorControls.tsx
      via: "phaseSignal + pendingPhaseId returned from useAIStream hook; CreatorControls reads them to enable button"
      pattern: "phaseSignal"
    - from: apps/web/components/workspace/CreatorControls.tsx
      to: "PATCH /sessions/:id/phase"
      via: "apiFetch on button click with { next_phase_id: pendingPhaseId }"
      pattern: "apiFetch.*phase"
---

<objective>
Implement the Phase 8 frontend surface: store extensions, SSE event handling, Realtime broadcast handling, and the creator-only "Advance Phase" affordance.

Purpose: Completes the human-control loop — mic lock state flows from Realtime broadcasts into the store; phase_signal SSE events enable the creator's Advance Phase button; canvas updates are stored for Phase 9 to render.
Output: Five modified files, zero new packages, full UI-SPEC compliance.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-human-control-canvas-sync/08-CONTEXT.md
@.planning/phases/08-human-control-canvas-sync/08-UI-SPEC.md
@.planning/phases/08-human-control-canvas-sync/08-01-PLAN.md
</context>

<interfaces>
<!-- Types available from @panelito/types after Plan 01 completes -->

From packages/types/src/canvas.ts:
  export type CanvasNode = z.infer<typeof CanvasNodeSchema>;
  // fields: id, session_id, branch_id, blueprint_id, node_type_id, label, status, position_x, position_y

  export type CanvasEdge = z.infer<typeof CanvasEdgeSchema>;
  // fields: id, session_id, branch_id, source_node_id, target_node_id, label, status

From packages/types/src/index.ts:
  export type { CanvasNode, CanvasEdge, CanvasOp, CanvasNodeStatus } from "./canvas";

Current AIStreamStatus union (use-ai-stream.ts line 31–37):
  'idle' | 'streaming' | 'done' | 'typing_hold' | 'no_persona' | 'no_api_key' | 'error'

Current session-store state (session-store.ts lines 19–55):
  messages, typingUsers, session, activeBranchId, branches
  setters: addMessage, setMessages, setTypingUsers, setSession, setBranchId, setBranches, addBranch, updateBranch

Existing apiFetch import in CreatorControls.tsx:
  import { apiFetch, ApiError } from '@/lib/api'

Existing toast import in CreatorControls.tsx:
  import { toast } from 'sonner'

Existing Button + Badge imports available in shadcn (no install needed):
  import { Button } from '@/components/ui/button'
  import { Badge } from '@/components/ui/badge'

Available lucide-react icons already imported or available:
  ChevronRight, Loader2 (for spinner during PATCH pending state)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend session-store with Phase 8 state fields and setters</name>
  <files>apps/web/store/session-store.ts</files>
  <read_first>
    - apps/web/store/session-store.ts (read current state interface and create() call to understand insertion points before modifying)
    - packages/types/src/canvas.ts (confirm CanvasNode and CanvasEdge type shapes)
  </read_first>
  <action>
    Add four new state fields to SessionStoreState interface and their implementations in the create() call.

    New fields in the interface (add after the existing `branches: Branch[]` field):
    - `micLocked: boolean` — true when a mic_acquired broadcast has been received for the active branch (HUMAN-01, D-04). Initialize to false.
    - `currentPhase: string | null` — current phase ID from session state or phase_advanced broadcast (HUMAN-02, D-09). Initialize to null.
    - `canvasNodes: CanvasNode[]` — canvas nodes received from canvas_update broadcast (CANVAS-02, D-14). Initialize to empty array.
    - `canvasEdges: CanvasEdge[]` — canvas edges received from canvas_update broadcast (CANVAS-02, D-14). Initialize to empty array.

    New setters in the interface (add after the existing `updateBranch` setter):
    - `setMicLocked: (locked: boolean) => void` — sets micLocked field. Used by use-session-channel.ts on mic_acquired (true) and mic_released (false).
    - `setCurrentPhase: (phase: string) => void` — sets currentPhase field. Used by use-session-channel.ts on phase_advanced broadcast.
    - `setCanvasData: (nodes: CanvasNode[], edges: CanvasEdge[]) => void` — replaces canvasNodes and canvasEdges in full (not merge). Used by use-session-channel.ts on canvas_update broadcast.

    Add import for CanvasNode and CanvasEdge from '@panelito/types' — append to the existing import on line 11: `import type { Message, Session, Branch, CanvasNode, CanvasEdge } from '@panelito/types'`.

    Implement setters in the create() body using the existing pattern (e.g. `setMicLocked: (locked) => set({ micLocked: locked })`).

    JSDoc for each new field: cite the decision ID (e.g. `/** Mic lock state from Realtime broadcast — branch-scoped (HUMAN-01, D-04). */`).
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && pnpm --filter apps/web tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - session-store.ts contains `micLocked: boolean` in the SessionStoreState interface
    - session-store.ts contains `currentPhase: string | null` in the SessionStoreState interface
    - session-store.ts contains `canvasNodes: CanvasNode[]` in the SessionStoreState interface
    - session-store.ts contains `canvasEdges: CanvasEdge[]` in the SessionStoreState interface
    - session-store.ts contains `setMicLocked`, `setCurrentPhase`, `setCanvasData` setter declarations in the interface
    - session-store.ts imports `CanvasNode, CanvasEdge` from '@panelito/types'
    - `pnpm --filter apps/web tsc --noEmit` exits 0 with no TypeScript errors in session-store.ts or any consumer
    - micLocked initializes to false, currentPhase to null, canvasNodes to [], canvasEdges to []
  </acceptance_criteria>
  <done>session-store provides all Phase 8 state fields and setters. TypeScript build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Handle phase_signal SSE event and mic_locked 409 in use-ai-stream.ts</name>
  <files>apps/web/hooks/use-ai-stream.ts</files>
  <read_first>
    - apps/web/hooks/use-ai-stream.ts (read full file — especially the SSE for-block loop at lines 172–207 and the 409 response handling at lines 137–149)
    - .planning/phases/08-human-control-canvas-sync/08-UI-SPEC.md (Surface 2 mic_locked handling; Surface 1 phase_signal flow)
  </read_first>
  <action>
    Make three changes to use-ai-stream.ts:

    1. Add 'mic_locked' to the AIStreamStatus union type (line 32–38). New union member:
       `| 'mic_locked'`  // 409 from /invoke — another graph run in progress on this branch

    2. Update the HTTP 409 handler (lines 138–141). Currently it maps 409 to 'no_persona'. The 409 body must now be parsed to distinguish the two cases:
       - If response body JSON has `{ error: 'mic_locked' }` → setStatus('mic_locked'), show `toast.error('El analista ya está respondiendo en esta rama.')`
       - If response body JSON has `{ error: 'no_persona' }` or any other shape → setStatus('no_persona') (existing behavior preserved)
       Use `response.json().catch(() => ({}))` to parse — treat parse failure as no_persona.

    3. Add two new pieces of component state at the top of useAIStream (after the existing useState declarations):
       - `const [phaseSignal, setPhaseSignal] = useState(false)` — true when phase_signal SSE event received
       - `const [pendingPhaseId, setPendingPhaseId] = useState<string | null>(null)` — the current_phase_id from the phase_signal payload

    4. Add phase_signal SSE event handler inside the for-block loop (after the panel_update handler, before the done handler):
       ```
       } else if (event === 'phase_signal') {
         // D-10: phase_signal arrives before done event; enable Advance Phase button for creator
         try {
           const payload = JSON.parse(data) as { current_phase_id: string; blueprint_id: string }
           setPhaseSignal(true)
           setPendingPhaseId(payload.current_phase_id)
         } catch {
           console.warn('[ai-stream] malformed phase_signal:', data)
         }
       }
       ```
       This follows the same try/catch JSON parse pattern already used for text_delta and panel_update.

    5. Expose phaseSignal and pendingPhaseId in the UseAIStreamReturn interface and in the hook's return object. Add resetStream to also reset phaseSignal to false and pendingPhaseId to null.

    The hook signature and all existing behavior must remain unchanged — this is additive only.

    Do NOT add a toast import if one doesn't exist — import { toast } from 'sonner' only if not already present.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && pnpm --filter apps/web tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - use-ai-stream.ts AIStreamStatus union contains 'mic_locked' as a member
    - use-ai-stream.ts contains a case for event === 'phase_signal' inside the for-block loop
    - use-ai-stream.ts phaseSignal and pendingPhaseId are included in UseAIStreamReturn interface
    - use-ai-stream.ts 409 handler parses response body and routes to 'mic_locked' vs 'no_persona' based on error field
    - toast.error('El analista ya está respondiendo en esta rama.') is called when status is set to 'mic_locked'
    - resetStream also sets phaseSignal to false and pendingPhaseId to null
    - `pnpm --filter apps/web tsc --noEmit` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>useAIStream handles phase_signal SSE events (enabling the Advance Phase button) and differentiates mic_locked 409 from no_persona 409.</done>
</task>

<task type="auto">
  <name>Task 3: Add four Realtime broadcast handlers in use-session-channel.ts</name>
  <files>apps/web/hooks/use-session-channel.ts</files>
  <read_first>
    - apps/web/hooks/use-session-channel.ts (read full file — understand the .on('broadcast', ...) chain pattern before adding new handlers)
    - .planning/phases/08-human-control-canvas-sync/08-UI-SPEC.md (Interaction Contracts section — exact store method calls and branch_id filter logic)
  </read_first>
  <action>
    Add four new .on('broadcast', ...) handlers to the existing Supabase channel subscription, chained after the existing `branch_update` handler and before `.subscribe()`.

    Import addition: add `import { toast } from 'sonner'` if not already present.
    Import addition: add `CanvasNode, CanvasEdge` to the existing `@panelito/types` type imports.

    Handler 1 — mic_acquired (D-04, HUMAN-01):
    - Event name: 'mic_acquired'
    - Logic: only apply if `payload.branch_id === useSessionStore.getState().activeBranchId`
    - Action: `useSessionStore.getState().setMicLocked(true)`
    - Log: `console.log('[useSessionChannel] mic_acquired', payload.branch_id)`

    Handler 2 — mic_released (D-04, HUMAN-01):
    - Event name: 'mic_released'
    - Logic: only apply if `payload.branch_id === useSessionStore.getState().activeBranchId`
    - Action: `useSessionStore.getState().setMicLocked(false)`
    - Log: `console.log('[useSessionChannel] mic_released', payload.branch_id, payload.reason)`

    Handler 3 — canvas_update (D-16, CANVAS-02):
    - Event name: 'canvas_update'
    - No branch filter — canvas is session-wide
    - Action: `useSessionStore.getState().setCanvasData(payload.nodes as CanvasNode[], payload.edges as CanvasEdge[])`
    - Log: `console.log('[canvas] update received', (payload.nodes as CanvasNode[]).length, 'nodes', (payload.edges as CanvasEdge[]).length, 'edges')`

    Handler 4 — phase_advanced (D-12, HUMAN-02):
    - Event name: 'phase_advanced'
    - Action: `useSessionStore.getState().setCurrentPhase(payload.new_phase_id as string)`
    - Toast: `toast.success('La sesion ha avanzado a la siguiente fase.')`
    - Log: `console.log('[useSessionChannel] phase_advanced', payload.new_phase_id)`

    All handlers follow the existing pattern: `({ payload }) => { ... }`.
    The branch_id comparison for mic handlers reads activeBranchId at call time using getState() — this avoids stale closure issues.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && grep -c "mic_acquired\|mic_released\|canvas_update\|phase_advanced" apps/web/hooks/use-session-channel.ts</automated>
  </verify>
  <acceptance_criteria>
    - use-session-channel.ts contains handlers for 'mic_acquired', 'mic_released', 'canvas_update', 'phase_advanced'
    - mic_acquired handler calls setMicLocked(true) only when payload.branch_id === activeBranchId
    - mic_released handler calls setMicLocked(false) only when payload.branch_id === activeBranchId
    - canvas_update handler calls setCanvasData with payload.nodes and payload.edges
    - phase_advanced handler calls setCurrentPhase(payload.new_phase_id) and shows toast.success('La sesion ha avanzado a la siguiente fase.')
    - grep output is 4 (four event names present, each once)
    - `pnpm --filter apps/web tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>use-session-channel handles all four Phase 8 broadcast events. Canvas data stored. Phase advancement toasted for all participants.</done>
</task>

<task type="auto">
  <name>Task 4: Add Advance Phase affordance to CreatorControls.tsx and wire micLocked in InputBox.tsx</name>
  <files>
    apps/web/components/workspace/CreatorControls.tsx
    apps/web/components/workspace/InputBox.tsx
  </files>
  <read_first>
    - apps/web/components/workspace/CreatorControls.tsx (read full file — understand the desktop bar layout at lines 350–373 and the mobile Sheet at lines 406–438 before inserting the new section)
    - apps/web/components/workspace/InputBox.tsx (read full file — understand props interface and the localAIStreaming pattern at lines 37–53)
    - .planning/phases/08-human-control-canvas-sync/08-UI-SPEC.md (Surface 1 full spec: all button states, aria-labels, copy, animation, mobile vs desktop sizing)
  </read_first>
  <action>
    PART A — CreatorControls.tsx:

    The CreatorControls component receives `session: Session` and `shortCode: string` as existing props. The useAIStream hook is NOT imported here — instead, the Advance Phase affordance receives `phaseSignal` and `pendingPhaseId` as new optional props. Caller (workspace page) passes them through. This keeps hook ownership at the workspace level.

    1. Update CreatorControlsProps interface — add:
       - `phaseSignal?: boolean` — true when the LLM has signalled phase readiness (D-10)
       - `pendingPhaseId?: string | null` — the phase ID from the phase_signal payload
       - `onPhaseConsumed?: () => void` — called after PATCH success or error to reset signal in parent

    2. Add new imports at the top:
       - `ChevronRight, Loader2` from 'lucide-react' (append to existing lucide import line)
       - `Badge` from '@/components/ui/badge'

    3. Add AdvancePhaseButton as a new internal function component (similar to FreezeButton pattern):
       - Props: `{ sessionId: string, phaseSignal: boolean, pendingPhaseId: string | null, onConsumed?: () => void }`
       - Local state: `pending: boolean`, `pulsing: boolean`
       - Effect: when phaseSignal transitions from false to true, set pulsing to true (one-shot)
       - On click: set pending true; call `apiFetch('/api/sessions/${sessionId}/phase', { method: 'PATCH', body: JSON.stringify({ next_phase_id: pendingPhaseId }) })`
         - On success: `toast.success('Fase avanzada.')`, call onConsumed?.(), reset pulsing to false
         - On 400 error (ApiError with status 400): `toast.error('No hay una fase siguiente en este Blueprint.')`, call onConsumed?.(), reset pulsing
         - On 403 or other error: log error, call onConsumed?.() to clear signal
         - Finally: set pending false
       - Button visual per UI-SPEC Surface 1 states:
         - Not enabled (phaseSignal false OR pendingPhaseId null): `variant="outline"` disabled, `opacity-60`, label "Siguiente fase", aria-label "Esperando señal del analista"
         - Enabled (phaseSignal true AND pendingPhaseId non-null): `variant="default"` enabled, conditionally add `animate-pulse` class when pulsing is true, label "Avanzar fase", aria-label "Avanzar a la siguiente fase"
         - Pending PATCH: disabled, label "Avanzando...", replace ChevronRight with `Loader2 className="animate-spin"`
       - Desktop size: `size="sm" h-9 gap-2`
       - Mobile size: `h-11` (44px touch target per UI-SPEC mobile constraint)

    4. Add currentPhase Badge: reads `useSessionStore((s) => s.currentPhase)` inside CreatorControls body. Renders `<Badge variant="secondary" className="text-[13px] font-normal">{currentPhase}</Badge>` when currentPhase is non-null.

    5. Desktop bar: add AdvancePhaseButton + Badge after the existing CloseButton (same row, `items-center gap-2` flex row). The Badge renders inline to the left of the button.

    6. Mobile Sheet: add a new `border-t` section after the existing Ramas section with header "Fase actual" (same pattern as "Gestión de Ramas" header at line 428 — `text-[13px] text-muted-foreground font-medium uppercase tracking-wider text-left`). Inside: Badge + AdvancePhaseButton with h-11 sizing.

    PART B — InputBox.tsx:

    InputBox already reads from session-store. Add a single store read for micLocked — this fulfils the contract requirement that InputBox knows about mic lock state even though no visual change occurs in Phase 8. Future phases can use this field.

    In InputBox body, after the existing `const activeBranchId = useSessionStore((s) => s.activeBranchId)` line, add:
    `const micLocked = useSessionStore((s) => s.micLocked)`

    Add a JSDoc comment above it: `// Phase 8 (HUMAN-01, D-04): micLocked from mic_acquired/mic_released broadcasts. No visual change in Phase 8 — streaming dots already communicate AI activity. Used by Phase 9 for additional UX signals.`

    The micLocked variable is intentionally unused visually in Phase 8 — add `// eslint-disable-next-line @typescript-eslint/no-unused-vars` directly above the const if the linter flags it, rather than suppressing it globally.

    Do NOT change any existing behavior in InputBox.
  </action>
  <verify>
    <automated>cd /home/jgm/dev/projects/web-projects/panelito && pnpm --filter apps/web tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - CreatorControls.tsx contains "Avanzar fase" and "Siguiente fase" strings
    - CreatorControls.tsx contains AdvancePhaseButton function component with pending state and pulsing state
    - CreatorControls.tsx imports Badge from '@/components/ui/badge'
    - CreatorControls.tsx imports ChevronRight and Loader2 from 'lucide-react'
    - CreatorControls.tsx desktop bar contains AdvancePhaseButton rendered after CloseButton
    - CreatorControls.tsx mobile Sheet contains a section with header "Fase actual"
    - CreatorControls.tsx button aria-label is "Avanzar a la siguiente fase" when enabled and "Esperando señal del analista" when disabled
    - CreatorControls.tsx calls PATCH /sessions/:id/phase on button click
    - toast.success('Fase avanzada.') is called on PATCH success
    - toast.error('No hay una fase siguiente en este Blueprint.') is called on PATCH 400
    - InputBox.tsx contains `const micLocked = useSessionStore((s) => s.micLocked)` after the activeBranchId line
    - `pnpm --filter apps/web tsc --noEmit` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>Creator sees Advance Phase button enabled (indigo primary, pulse animation) when LLM signals readiness. PATCH /sessions/:id/phase fires on click. Phase Badge shows current phase name. InputBox reads micLocked from store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Realtime broadcast → session-store | mic_acquired/mic_released payloads arrive via Supabase Realtime; branch_id must match active branch before mutating state |
| SSE stream → useAIStream component state | phase_signal payload parsed from SSE data field; malformed JSON is caught and discarded |
| Creator click → PATCH /sessions/:id/phase | HTTP call with next_phase_id; server enforces ownership gate and phase sequence validation (D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-A | Elevation of Privilege | PATCH /sessions/:id/phase button | mitigate | Button only rendered when session.creator_id === user.id in component; server ALSO enforces 403 ownership gate (D-11, D-13) — defense in depth |
| T-08-05-B | Tampering | mic_acquired broadcast — cross-branch state bleed | mitigate | Branch ID filter: `payload.branch_id === activeBranchId` checked before setMicLocked() (D-04); cross-branch mic events are silently ignored |
| T-08-05-C | Tampering | phase_signal SSE payload — malformed current_phase_id | mitigate | try/catch around JSON.parse; pendingPhaseId remains null if parse fails; button stays disabled; server validates next_phase_id against Blueprint.phase_sequence on PATCH (D-11) |
| T-08-05-D | Information Disclosure | canvasNodes/canvasEdges stored in session-store | accept | Canvas data is session-scoped and already visible to all session participants; no PII; Phase 9 renders it from store |
| T-08-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 05; all changes are source code extensions to existing files |
</threat_model>

<verification>
After all four tasks complete:

1. TypeScript clean: `pnpm --filter apps/web tsc --noEmit` exits 0 with no errors in any of the five modified files.

2. Store fields present:
   `grep -c "micLocked\|currentPhase\|canvasNodes\|canvasEdges" apps/web/store/session-store.ts`
   → output >= 8 (each field appears in interface, initializer, and setter)

3. SSE handler present:
   `grep -c "phase_signal" apps/web/hooks/use-ai-stream.ts`
   → output >= 3 (AIStreamStatus + handler branch + useState)

4. Realtime handlers present:
   `grep -c "mic_acquired\|mic_released\|canvas_update\|phase_advanced" apps/web/hooks/use-session-channel.ts`
   → output == 4

5. Copy strings present:
   `grep -c "Avanzar fase\|Siguiente fase\|Avanzando" apps/web/components/workspace/CreatorControls.tsx`
   → output == 3

6. InputBox store read:
   `grep -c "micLocked" apps/web/components/workspace/InputBox.tsx`
   → output >= 1
</verification>

<success_criteria>
- All five files modified without TypeScript errors (pnpm --filter apps/web tsc --noEmit exits 0)
- session-store exposes micLocked, currentPhase, canvasNodes, canvasEdges with typed setters
- phase_signal SSE event received in use-ai-stream enables the Advance Phase button in CreatorControls
- mic_acquired/mic_released Realtime broadcasts update micLocked in store, filtered by activeBranchId (D-02, D-04)
- canvas_update Realtime broadcast stores full node/edge arrays in session-store for Phase 9 (D-16, CANVAS-02)
- phase_advanced Realtime broadcast updates currentPhase + shows toast to all participants (D-12)
- PATCH /sessions/:id/phase called on creator button click; success shows "Fase avanzada." toast; 400 shows "No hay una fase siguiente en este Blueprint." toast (D-11)
- All UI copy is in Spanish per UI-SPEC Copywriting Contract
- No new npm packages added
- No @xyflow/react canvas rendering (Phase 9 boundary respected)
</success_criteria>

<output>
Create `.planning/phases/08-human-control-canvas-sync/08-05-SUMMARY.md` when done
</output>
