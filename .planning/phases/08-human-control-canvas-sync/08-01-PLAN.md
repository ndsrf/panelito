---
phase: 08-human-control-canvas-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/canvas-tool.ts
  - apps/api/src/graph/state.ts
autonomous: true
requirements:
  - HUMAN-01
  - HUMAN-02
  - CANVAS-02

must_haves:
  truths:
    - "canvasMutationTool has a phase_signal boolean property in its parameters.properties block"
    - "GraphStateAnnotation has a phase_signal field with overwrite-style reducer and null default"
    - "AgentNode can return phase_signal as a top-level state key without TypeScript errors"
  artifacts:
    - path: packages/types/src/canvas-tool.ts
      provides: "canvasMutationTool extended with optional phase_signal boolean property"
      contains: "phase_signal"
    - path: apps/api/src/graph/state.ts
      provides: "GraphStateAnnotation with phase_signal: boolean | null field"
      contains: "phase_signal"
  key_links:
    - from: packages/types/src/canvas-tool.ts
      to: apps/api/src/graph/nodes/agent.ts
      via: "LLM tool output includes phase_signal which agent.ts reads from raw event.input"
      pattern: "phase_signal"
    - from: apps/api/src/graph/state.ts
      to: apps/api/src/routes/ai.ts
      via: "finalState.phase_signal read post-graph in route cleanup block"
      pattern: "phase_signal.*Annotation"
---

<objective>
Extend the shared types and graph state to carry the advisory phase_signal field through the LangGraph pipeline — before any graph node or route code touches it.

Purpose: Both the canvasMutationTool (which instructs the LLM) and GraphStateAnnotation (which carries the value through the graph) must be updated before agent.ts or ai.ts can use the field. These two files have no dependency on each other and no dependency on the migration.
Output: Extended canvas-tool.ts and state.ts. No new packages. No migration. These changes unlock Plan 03 (graph nodes) and Plan 04 (route).
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-human-control-canvas-sync/08-CONTEXT.md
@.planning/phases/08-human-control-canvas-sync/08-RESEARCH.md
@.planning/phases/08-human-control-canvas-sync/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phase_signal property to canvasMutationTool</name>
  <files>packages/types/src/canvas-tool.ts</files>
  <read_first>
    - packages/types/src/canvas-tool.ts (full file — understand the current parameters.properties block structure before inserting)
  </read_first>
  <action>
    Add a new optional property `phase_signal` to the `parameters.properties` object of `canvasMutationTool`, after the existing `reason` property (line 68–71) and before the closing `}` of `properties`. Do NOT modify the `required` array — it stays as `['op']`, making `phase_signal` optional.

    The new property object:
    - key: `phase_signal`
    - type: `'boolean'`
    - description: `'Optional. Set to true when the conversation has reached a natural conclusion for the current phase and the group is ready to advance to the next phase. This is advisory only — a human must confirm phase advancement.'`

    This follows the exact same optional-field pattern as the existing `reason` property at lines 68–71 of canvas-tool.ts.

    Source-of-truth: CONTEXT.md D-08 and PATTERNS.md §canvas-tool.ts section.

    Do not add phase_signal to CanvasOpSchema in canvas.ts — the field is deliberately read from the raw tool input BEFORE safeParse in agent.ts (Pitfall 2/4 in RESEARCH.md). Adding it to CanvasOpSchema would conflate the canvas op data with the advisory signal.
  </action>
  <verify>
    <automated>grep -c "phase_signal" /home/jgm/dev/projects/web-projects/panelito/packages/types/src/canvas-tool.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/types/src/canvas-tool.ts contains `phase_signal` in the parameters.properties block
    - The `required` array in canvas-tool.ts still contains only `'op'` (phase_signal is optional)
    - `cd /home/jgm/dev/projects/web-projects/panelito && pnpm --filter @panelito/types build` exits 0 with no TypeScript errors
    - The property description contains the string "advisory only"
  </acceptance_criteria>
  <done>canvasMutationTool instructs the LLM that it may optionally set phase_signal: true when the conversation is ready to advance phase</done>
</task>

<task type="auto">
  <name>Task 2: Add phase_signal field to GraphStateAnnotation</name>
  <files>apps/api/src/graph/state.ts</files>
  <read_first>
    - apps/api/src/graph/state.ts (full file — understand the overwrite-style Annotation pattern before inserting)
  </read_first>
  <action>
    Add a new `phase_signal` field to `GraphStateAnnotation` in `apps/api/src/graph/state.ts`, after the existing `steeringTextEnabled` field (lines 76–79) and before the closing `})` of `Annotation.Root`.

    The new field uses the exact same overwrite-style reducer pattern as `steeringTextEnabled`:
    - type: `boolean | null`
    - reducer: `(_: boolean | null, v: boolean | null) => v`
    - default: `() => null`

    Add a JSDoc comment above the field: `/** phase_signal from AgentNode tool output — advisory only; human must confirm phase advancement (HUMAN-02, D-09). */`

    No new imports are needed — `Annotation` is already imported at line 13.

    Source-of-truth: CONTEXT.md D-09 and PATTERNS.md §state.ts section.
  </action>
  <verify>
    <automated>grep -c "phase_signal" /home/jgm/dev/projects/web-projects/panelito/apps/api/src/graph/state.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/graph/state.ts contains `phase_signal` as a field in `GraphStateAnnotation`
    - The field uses `Annotation<boolean | null>` with `reducer: (_, v) => v` and `default: () => null` (same pattern as steeringTextEnabled)
    - `cd /home/jgm/dev/projects/web-projects/panelito && pnpm --filter apps/api tsc --noEmit` exits 0 with no TypeScript errors related to state.ts
    - `GraphState` (the derived type at the bottom of the file) automatically includes `phase_signal: boolean | null` — no separate type annotation needed
  </acceptance_criteria>
  <done>GraphStateAnnotation carries phase_signal as an overwrite-style boolean | null field, enabling AgentNode to return it and ai.ts to read it from finalState</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → canvasMutationTool schema | LLM emits phase_signal; schema extension guides model; no trusted enforcement here |
| canvasMutationTool → AgentNode safeParse | phase_signal is read from raw event.input BEFORE CanvasOpSchema.safeParse() — by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-A | Tampering | canvasMutationTool phase_signal property | accept | phase_signal is advisory only; it cannot write DB autonomously — PATCH /phase endpoint enforces human gate (D-13); schema extension merely guides the LLM |
| T-08-01-B | Information Disclosure | GraphStateAnnotation phase_signal field | accept | field is boolean | null; no PII; overwrite reducer ensures last-write-wins; no sensitive information exposed |
| T-08-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 01; all changes are source code only |
</threat_model>

<verification>
After both tasks complete:
- `pnpm --filter @panelito/types build` exits 0
- `pnpm --filter apps/api tsc --noEmit` exits 0 (no TypeScript errors in state.ts or any consumer)
- `grep "phase_signal" packages/types/src/canvas-tool.ts` shows the property definition
- `grep "phase_signal" apps/api/src/graph/state.ts` shows the Annotation field
</verification>

<success_criteria>
- canvasMutationTool.parameters.properties.phase_signal exists with type 'boolean' and is NOT in the required array
- GraphStateAnnotation includes phase_signal: Annotation&lt;boolean | null&gt; with overwrite reducer
- TypeScript builds cleanly for @panelito/types and apps/api
</success_criteria>

<output>
Create `.planning/phases/08-human-control-canvas-sync/08-01-SUMMARY.md` when done
</output>
