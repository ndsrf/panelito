---
phase: 08-human-control-canvas-sync
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/api/src/graph/nodes/agent.ts
  - apps/api/src/graph/nodes/mutation-gate.ts
autonomous: true
requirements:
  - HUMAN-02

must_haves:
  truths:
    - "AgentNode reads phase_signal from raw event.input BEFORE CanvasOpSchema.safeParse() so the field is not stripped"
    - "AgentNode returns phase_signal as a top-level field in its return object alongside agentOutput and agentConfidence"
    - "The no-tool-call return path in agent.ts also returns phase_signal: null"
    - "MutationGateNode does not need to be modified — AgentNode returns phase_signal directly to state via GraphStateAnnotation"
  artifacts:
    - path: apps/api/src/graph/nodes/agent.ts
      provides: "AgentNode that extracts phase_signal from raw tool input and returns it as state field"
      contains: "phase_signal"
    - path: apps/api/src/graph/nodes/mutation-gate.ts
      provides: "MutationGateNode confirmed NOT resetting phase_signal (returns {} or partial state without overwriting)"
      contains: "canvasOps"
  key_links:
    - from: apps/api/src/graph/nodes/agent.ts
      to: apps/api/src/graph/state.ts
      via: "AgentNode return { agentOutput, agentConfidence, phase_signal } — phase_signal field set in GraphStateAnnotation"
      pattern: "return.*phase_signal"
    - from: apps/api/src/graph/nodes/mutation-gate.ts
      to: apps/api/src/graph/state.ts
      via: "MutationGateNode return does NOT include phase_signal — overwrite reducer preserves prior value"
      pattern: "canvasOps"
---

<objective>
Update AgentNode to extract the phase_signal field from raw LLM tool output before CanvasOpSchema.safeParse() strips it, and return it as a top-level state field. Verify MutationGateNode does not accidentally reset it.

Purpose: The Zod discriminated union in CanvasOpSchema uses strict parsing — unknown fields (including phase_signal) are dropped silently. To preserve the field, AgentNode must read it from the raw event.input object first, then run safeParse on the rest. This is the correct threading pattern documented in RESEARCH.md Pitfall 4.
Output: agent.ts modified to extract and return phase_signal. mutation-gate.ts confirmed (and left unchanged) since it does not need to propagate phase_signal — GraphStateAnnotation's overwrite reducer preserves the value set by AgentNode.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-human-control-canvas-sync/08-CONTEXT.md
@.planning/phases/08-human-control-canvas-sync/08-RESEARCH.md
@.planning/phases/08-human-control-canvas-sync/08-PATTERNS.md
@.planning/phases/08-human-control-canvas-sync/08-01-SUMMARY.md
</context>

<interfaces>
<!-- From apps/api/src/graph/state.ts (after Plan 01 changes):
export const GraphStateAnnotation = Annotation.Root({
  // ... existing fields ...
  steeringTextEnabled: Annotation<boolean | null>({ reducer: (_, v) => v, default: () => null }),
  /** phase_signal from AgentNode tool output — advisory only; human must confirm (HUMAN-02, D-09). */
  phase_signal: Annotation<boolean | null>({ reducer: (_, v) => v, default: () => null }),
})
export type GraphState = typeof GraphStateAnnotation.State
-->

<!-- From apps/api/src/graph/nodes/agent.ts (current tool_use handler — lines 148–172):
} else if (event.type === 'tool_use' && event.name === 'canvas_mutation') {
  const parsed = CanvasOpSchema.safeParse(event.input)
  if (parsed.success) {
    agentOutput = parsed.data
    agentConfidence = 'confidence' in parsed.data ? (parsed.data.confidence ?? null) : null
  } else {
    console.error('[agent] CanvasOpSchema.safeParse failed — dropping malformed tool output', ...)
  }
  break
}
// ...
return { agentOutput, agentConfidence }
-->

<!-- From apps/api/src/graph/nodes/mutation-gate.ts (current return paths):
// return { canvasOps: [gatedOp] }  OR  return {}  — phase_signal NOT included
// The overwrite reducer (_, v) => v means returning {} leaves phase_signal unchanged in state.
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract phase_signal in AgentNode before safeParse</name>
  <files>apps/api/src/graph/nodes/agent.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/agent.ts (full file — understand the complete tool_use event handler block at lines 148–172 and the final return statement)
    - apps/api/src/graph/state.ts (confirm phase_signal: Annotation&lt;boolean | null&gt; now exists after Plan 01)
    - apps/api/src/graph/nodes/mutation-gate.ts (read the return statement to understand what MutationGateNode currently returns — confirms it does NOT need modification)
  </read_first>
  <action>
    Modify the `tool_use` event handler block in `apps/api/src/graph/nodes/agent.ts`. The block currently starts at approximately line 148 with `} else if (event.type === 'tool_use' && event.name === 'canvas_mutation') {`.

    Make two changes:

    CHANGE 1 — Inside the tool_use block, BEFORE the `CanvasOpSchema.safeParse(event.input)` call, add:
    ```
    const rawInput = event.input as Record<string, unknown>
    const rawPhaseSignal = typeof rawInput.phase_signal === 'boolean' ? rawInput.phase_signal : null
    ```
    Then keep the existing safeParse call and its success/failure branches unchanged.
    Then replace `break` with `return { agentOutput, agentConfidence, phase_signal: rawPhaseSignal }`.

    CHANGE 2 — Find the final `return { agentOutput, agentConfidence }` statement (currently at approximately line 172, the return at the END of the agentNode function, outside all event handlers). Add `phase_signal: null` to this return object so it becomes `return { agentOutput, agentConfidence, phase_signal: null }`.

    This second change is critical: every code path that exits agentNode must include `phase_signal` in its return value. If agentNode does NOT call the canvas_mutation tool (e.g., DOMAIN_DRIFT path or streaming-only response), phase_signal defaults to null.

    Source-of-truth: CONTEXT.md D-08, RESEARCH.md Pitfall 2 and Pitfall 4, PATTERNS.md §agent.ts section.

    Do NOT change CanvasOpSchema, do NOT add phase_signal to any Zod schema. The extraction must happen from `event.input` before safeParse runs.
  </action>
  <verify>
    <automated>grep -c "rawPhaseSignal\|phase_signal" /home/jgm/dev/projects/web-projects/panelito/apps/api/src/graph/nodes/agent.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/graph/nodes/agent.ts contains `rawPhaseSignal` variable assigned before `CanvasOpSchema.safeParse`
    - The tool_use event handler returns `{ agentOutput, agentConfidence, phase_signal: rawPhaseSignal }` (not `break`)
    - The fallback return at the end of the function returns `{ agentOutput, agentConfidence, phase_signal: null }`
    - `cd /home/jgm/dev/projects/web-projects/panelito && pnpm --filter apps/api tsc --noEmit` exits 0 with no TypeScript errors in agent.ts
    - Existing unit tests in `apps/api/src/graph/graph.test.ts` still pass: `pnpm --filter apps/api test -- --run graph.test` exits 0
  </acceptance_criteria>
  <done>AgentNode extracts phase_signal from raw LLM tool output before safeParse, returns it as a top-level state field on all code paths</done>
</task>

<task type="auto">
  <name>Task 2: Verify MutationGateNode does not reset phase_signal</name>
  <files>apps/api/src/graph/nodes/mutation-gate.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/mutation-gate.ts (full file — understand all return paths and confirm phase_signal does not appear and does not need to be added)
    - apps/api/src/graph/state.ts (confirm the overwrite reducer for phase_signal: reducer: (_, v) => v means returning {} from mutation-gate leaves phase_signal unchanged)
  </read_first>
  <action>
    Read mutation-gate.ts and confirm:
    1. All return statements in the function return objects that do NOT include `phase_signal`
    2. Given the overwrite-style reducer `reducer: (_, v) => v`, returning `{}` or `{ canvasOps: [...] }` from MutationGateNode will NOT reset `phase_signal` to null because LangGraph only calls the reducer for fields that are present in the returned object
    3. If any return path does accidentally include `phase_signal: null` (which would reset it), remove that field

    If mutation-gate.ts is confirmed correct as-is (does not include phase_signal in any return), add a code comment at the top of the function body:
    ```
    // D-08 (Phase 8): phase_signal is NOT in the return value by design.
    // AgentNode sets it in state before MutationGateNode runs.
    // The overwrite reducer preserves it — returning {} here does not reset it.
    ```

    This task is verification-first — the expected outcome is NO code change to mutation-gate.ts beyond the comment. If the file is already correct, the comment is the only change.

    Source-of-truth: RESEARCH.md Pitfall 4 and PATTERNS.md §mutation-gate.ts section.
  </action>
  <verify>
    <automated>grep -c "canvasOps\|phase_signal" /home/jgm/dev/projects/web-projects/panelito/apps/api/src/graph/nodes/mutation-gate.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/graph/nodes/mutation-gate.ts does NOT set `phase_signal` in any return value (preserving the field via reducer)
    - File contains the D-08 comment explaining why phase_signal is intentionally absent from return values
    - `pnpm --filter apps/api tsc --noEmit` exits 0 (no new TypeScript errors in mutation-gate.ts)
    - Existing graph tests pass: `pnpm --filter apps/api test -- --run graph.test` exits 0
  </acceptance_criteria>
  <done>MutationGateNode confirmed to not reset phase_signal; the overwrite reducer in GraphStateAnnotation preserves AgentNode's value through MutationGateNode execution</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool output → rawInput.phase_signal | Untrusted LLM boolean; read before Zod validation |
| AgentNode return → GraphStateAnnotation | phase_signal stored as boolean | null in graph state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-A | Elevation of Privilege | AgentNode phase_signal extraction — LLM setting phase_signal: true | mitigate | D-13: phase_signal is advisory only; it triggers an SSE event to the frontend; the human must make a separate HTTP PATCH /sessions/:id/phase call; AgentNode has no path to write sessions.current_phase |
| T-08-03-B | Tampering | CanvasOpSchema.safeParse strips phase_signal from agentOutput | accept | Intended behavior — phase_signal is NOT a canvas op field; reading it from rawInput before safeParse is the correct design (Pitfall 2/4) |
| T-08-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 03; source-only changes |
</threat_model>

<verification>
After both tasks complete:
- `pnpm --filter apps/api tsc --noEmit` exits 0
- `pnpm --filter apps/api test -- --run graph.test` passes
- `grep "rawPhaseSignal" apps/api/src/graph/nodes/agent.ts` shows the extraction before safeParse
- `grep "phase_signal" apps/api/src/graph/nodes/agent.ts` appears in both the tool_use return and the fallback return
- mutation-gate.ts does not contain `phase_signal` in any return statement
</verification>

<success_criteria>
- AgentNode extracts phase_signal from raw event.input before CanvasOpSchema.safeParse
- AgentNode returns phase_signal on all code paths (tool_use path: rawPhaseSignal, fallback path: null)
- MutationGateNode confirmed to not include phase_signal in return values — overwrite reducer preserves AgentNode's value
- All existing graph tests pass
</success_criteria>

<output>
Create `.planning/phases/08-human-control-canvas-sync/08-03-SUMMARY.md` when done
</output>
