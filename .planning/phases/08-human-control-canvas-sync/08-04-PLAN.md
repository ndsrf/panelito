---
phase: 08-human-control-canvas-sync
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - apps/api/src/routes/ai.ts
  - apps/api/src/routes/sessions.ts
autonomous: true
requirements:
  - HUMAN-01
  - HUMAN-02
  - CANVAS-02

must_haves:
  truths:
    - "POST /invoke acquires the mic lock as the FIRST DB operation (before cap check, before streamSSE) (D-01)"
    - "If mic lock is held and not expired, /invoke returns 409 { error: 'mic_locked', branch_id }"
    - "On mic acquisition, mic_acquired is broadcast on channel(session:sessionId) with { branch_id }"
    - "A finally block always releases the mic via release_mic RPC and broadcasts mic_released; try_acquire_mic is called with p_expiry_seconds: 30 so expired locks (>30s) are stolen on next acquire (D-06, D-07)"
    - "If finalState.phase_signal is true, route emits a phase_signal SSE event BEFORE the done event"
    - "Committed canvas ops are upserted to canvas_nodes/canvas_edges AFTER the SSE done event (outside streamSSE or via waitUntil) — canvas writes do not block SSE stream completion (D-18)"
    - "canvas_update is broadcast with full DB rows { nodes, edges } after upsert completes"
    - "ADD_NODE ops generate server-side UUIDs before DB upsert so the same UUID appears in the Realtime broadcast payload; ADD_EDGE ops run after (D-15)"
    - "ADD_EDGE ops drop silently if source or target node is not in DB (D-17)"
    - "PATCH /sessions/:id/phase returns 403 if caller is not session creator"
    - "PATCH /sessions/:id/phase returns 400 if next_phase_id is not in Blueprint.phase_sequence"
    - "PATCH /sessions/:id/phase writes current_phase to DB and broadcasts phase_advanced"
  artifacts:
    - path: apps/api/src/routes/ai.ts
      provides: "Mic acquire/release + phase_signal SSE event + canvas upserts integrated into /invoke route"
      contains: "try_acquire_mic"
    - path: apps/api/src/routes/sessions.ts
      provides: "PATCH /:id/phase endpoint with ownership gate + blueprint sequence validation + broadcast"
      contains: "patch.*phase\|PATCH.*phase"
  key_links:
    - from: apps/api/src/routes/ai.ts
      to: supabase/migrations/0010_mic_lock.sql
      via: "supabase.rpc('try_acquire_mic', { p_branch_id, p_holder_id, p_expiry_seconds: 30 })"
      pattern: "try_acquire_mic"
    - from: apps/api/src/routes/ai.ts
      to: apps/api/src/graph/state.ts
      via: "finalState.phase_signal read after Promise.all([runGraph, drainQueue])"
      pattern: "finalState.*phase_signal\|phase_signal.*finalState"
    - from: apps/api/src/routes/sessions.ts
      to: apps/api/src/lib/blueprint-loader.ts
      via: "loadBlueprint(session.blueprint_id).phase_sequence.some()"
      pattern: "loadBlueprint.*phase_sequence"
---

<objective>
Integrate the mic lock, phase_signal SSE event, and canvas upserts into the /invoke route, and add the PATCH /sessions/:id/phase endpoint. This is the primary Phase 8 backend modification — it wires together the DB foundations from Plan 02 and the graph node changes from Plan 03.

Purpose: These two route modifications complete the server-side half of all three Phase 8 requirements. The /invoke route now enforces concurrency control (HUMAN-01) and emits the phase readiness signal (HUMAN-02) and writes committed canvas mutations to the DB (CANVAS-02). The PATCH endpoint enforces the invariant that only humans can advance current_phase (HUMAN-02 D-13).
Output: Modified ai.ts and sessions.ts implementing D-01 through D-18 from CONTEXT.md. All operations follow the post-stream cleanup sequence defined in PATTERNS.md.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-human-control-canvas-sync/08-CONTEXT.md
@.planning/phases/08-human-control-canvas-sync/08-RESEARCH.md
@.planning/phases/08-human-control-canvas-sync/08-PATTERNS.md
@.planning/phases/08-human-control-canvas-sync/08-01-SUMMARY.md
@.planning/phases/08-human-control-canvas-sync/08-02-SUMMARY.md
@.planning/phases/08-human-control-canvas-sync/08-03-SUMMARY.md
</context>

<interfaces>
<!-- From apps/api/src/routes/ai.ts — the post-stream cleanup sequence (read before modifying):

Current sequence after Promise.all([runGraph(), drainQueue()]) at line 363:
  1. Message INSERT (lines 365–397)
  2. new_message broadcast (lines 394–397)
  3. incrementCount (line 400)
  4. Langfuse forceFlush (lines 407–409)
  5. stream.writeSSE({ event: 'done', data: '{}' }) (line 411)

Phase 8 inserts these steps in the correct order (per PATTERNS.md §Post-Stream Cleanup Sequence):
  1. Message INSERT + new_message broadcast  [existing]
  2. phase_signal SSE event (if finalState.phase_signal === true)  [NEW — BEFORE done]
  3. incrementCount  [existing]
  4. Langfuse forceFlush  [existing]
  5. stream.writeSSE({ event: 'done', data: '{}' })  [existing]
  6. Canvas upserts (ADD_NODE first → UUIDs, then ADD_EDGE)  [NEW — AFTER done]
  7. canvas_update broadcast  [NEW — fire-and-forget after upserts]

finally block (wraps the outer try containing streamSSE — D-06):
  8. release_mic RPC  [NEW]
  9. mic_released broadcast  [NEW — fire-and-forget]

Mic acquire block (Step 8.5 — BEFORE streamSSE() call, after loadBlueprint at line 265):
  - try_acquire_mic(activeBranchId ?? sessionMainBranchId, sessionId, 30)
  - if !acquired → return 409 { error: 'mic_locked', branch_id: activeBranchId }
  - if acquired → httpSend mic_acquired fire-and-forget

Note on activeBranchId null path (Pitfall 1 in RESEARCH.md):
  The route currently sets activeBranchId = body.branchId || null.
  Before the mic acquire, resolve to a real UUID:
  if (!activeBranchId) {
    const { data: mainBranch } = await supabase
      .from('branches')
      .select('id')
      .eq('session_id', sessionId)
      .eq('is_main', true)
      .single()
    if (!mainBranch) return c.json({ error: 'branch_not_found' }, 404)
    activeBranchId = mainBranch.id  // now a real UUID
  }
  (Use activeBranchId (non-null) for ALL subsequent operations including graph thread_id)
-->

<!-- From apps/api/src/routes/sessions.ts — the freeze endpoint pattern (read before modifying):

sessionsRouter.post('/:id/freeze', requireAuth, async (c) => {
  // 1. parse params
  const { id } = c.req.param()
  const user = c.get('user')
  // 2. fetch session with creator_id
  const { data: session } = await supabase.from('sessions').select('id, creator_id, status').eq('id', id).single()
  if (fetchError || !session) return c.json({ error: 'not_found' }, 404)
  // 3. ownership gate
  if (session.creator_id !== user.id) return c.json({ error: 'forbidden' }, 403)
  // 4. action
  await freezeSession(supabase, id, 'creator_freeze')
  // 5. return
  return c.json({ status: 'frozen' }, 200)
})
-->

<!-- From apps/api/src/lib/blueprint-loader.ts — loadBlueprint signature (already imported in ai.ts):
async function loadBlueprint(blueprintId: string): Promise<Blueprint>
Blueprint.phase_sequence: Array<{ id: string; name: string; instructions: string; ... }>
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add mic lock, phase_signal SSE, and canvas upserts to /invoke route</name>
  <files>apps/api/src/routes/ai.ts</files>
  <read_first>
    - apps/api/src/routes/ai.ts (FULL FILE — understand exact line positions for all insertion points before modifying; specifically: where loadBlueprint ends (~line 265), the streamSSE() call (~line 280), the post-stream cleanup block (~lines 362–419), and the existing try/catch structure)
    - apps/api/src/routes/messages.ts (lines 130–145 only — the httpSend fire-and-forget broadcast pattern; copy verbatim)
    - apps/api/src/lib/cap-guard.ts (the supabase.rpc call pattern — copy for try_acquire_mic and release_mic)
    - apps/api/src/graph/state.ts (confirm phase_signal field exists after Plan 01)
    - supabase/migrations/0010_mic_lock.sql (confirm function signatures before calling RPCs)
  </read_first>
  <action>
    Make three targeted insertions and one addition to `apps/api/src/routes/ai.ts`:

    INSERTION A — Main branch UUID resolution + mic acquire (after loadBlueprint block, before `return streamSSE(c, async (stream) => {`):

    Step A1: Resolve activeBranchId to a real UUID. After the existing `activeBranchId` assignment block, add a check: if `activeBranchId` is null, query branches for the session's main branch (`is_main: true`) and assign the UUID. If no main branch found, return 404 `{ error: 'branch_not_found' }`.

    Step A2: Immediately after that resolution, call `try_acquire_mic` RPC:
    - Call: `supabase.rpc('try_acquire_mic', { p_branch_id: activeBranchId, p_holder_id: sessionId, p_expiry_seconds: 30 })`
    - On micError or empty result: return `c.json({ error: 'mic_lock_error' }, 500)`
    - On `!micResult[0].acquired`: return `c.json({ error: 'mic_locked', branch_id: activeBranchId }, 409)`
    - On success: fire-and-forget `httpSend('mic_acquired', { branch_id: activeBranchId })` using the messages.ts pattern (no await, .catch only)

    INSERTION B — phase_signal SSE event (inside the `streamSSE` callback, BEFORE the existing `stream.writeSSE({ event: 'done', ... })` call):

    Add:
    ```
    if ((finalState as any)?.phase_signal === true) {
      await stream.writeSSE({
        event: 'phase_signal',
        data: JSON.stringify({
          current_phase_id: session.current_phase ?? blueprint.phase_sequence[0]?.id ?? '',
          blueprint_id: session.blueprint_id,
        }),
      })
    }
    ```
    This block must be BEFORE the `done` event and AFTER message INSERT and incrementCount (per PATTERNS.md cleanup sequence step 2).

    INSERTION C — Canvas upserts (inside the `streamSSE` callback, AFTER the `stream.writeSSE({ event: 'done', ... })` call):

    Add the canvas upsert block that:
    1. Filters `finalState.canvasOps` for `op.status === 'committed'` ops only (D-14)
    2. Processes ADD_NODE ops first: `crypto.randomUUID()` for each, upsert to `canvas_nodes` with `{ id, session_id: sessionId, branch_id: activeBranchId, blueprint_id: session.blueprint_id, node_type_id: op.node_type_id, label: op.label, status: 'committed', position_x: null, position_y: null }`, `onConflict: 'id'`, `.select().single()`; build `nodeIdMap: Map<string, string>` from `op.label → nodeId` for same-invocation edge resolution; collect successful rows in `nodeRows[]`; log and skip on error (fail-silent)
    3. Processes ADD_EDGE ops: resolve `sourceId` via `nodeIdMap.get(op.source_node_id) ?? op.source_node_id` (handles same-invocation label refs), then verify both nodes exist in `canvas_nodes` table filtered by `session_id`; if either check fails, log warning and skip (D-17); upsert to `canvas_edges` with `{ id: crypto.randomUUID(), session_id, branch_id, blueprint_id, source_node_id: sourceId, target_node_id: targetId, edge_type_id: op.edge_type_id, status: 'committed' }`, `onConflict: 'id'`; collect successful rows in `edgeRows[]`
    4. If any rows collected: fire-and-forget `httpSend('canvas_update', { nodes: nodeRows, edges: edgeRows })` (no await, .catch only) — D-16
    
    All canvas upsert errors are fail-silent: `console.warn('[ai] canvas_* upsert failed — skipping', err?.message)` then continue.

    INSERTION D — Finally block wrapping the existing outer try/catch:

    The current structure is `try { return streamSSE(...) } catch (err) { ... }`. Wrap this with a `finally` block that:
    1. Awaits `supabase.rpc('release_mic', { p_branch_id: activeBranchId })`
    2. Fire-and-forget `httpSend('mic_released', { branch_id: activeBranchId, reason: 'completed' })` (no await, .catch only)

    Critical ordering invariant (per RESEARCH.md Pitfall 6 and PATTERNS.md §Post-Stream Cleanup Sequence):
    - phase_signal SSE event: BEFORE done
    - canvas upserts: AFTER done
    - mic release: in finally (always runs, even on AbortError)

    No new imports are needed — all utilities (supabase, createServiceClient, stream.writeSSE, incrementCount) are already imported. `crypto.randomUUID()` is a Node.js global.
  </action>
  <verify>
    <automated>grep -c "try_acquire_mic\|release_mic\|phase_signal\|canvas_update\|mic_acquired\|mic_released" /home/jgm/dev/projects/web-projects/panelito/apps/api/src/routes/ai.ts</automated>
  </verify>
  <acceptance_criteria>
    - ai.ts contains `try_acquire_mic` RPC call BEFORE the `return streamSSE(` line
    - ai.ts contains a 409 return for `!micResult[0].acquired` before streamSSE
    - ai.ts contains `release_mic` RPC in a `finally` block
    - ai.ts contains `httpSend('mic_acquired', ...)` and `httpSend('mic_released', ...)` (both fire-and-forget, no await)
    - ai.ts contains `phase_signal` SSE event emission block BEFORE `writeSSE({ event: 'done' })`
    - ai.ts contains canvas upsert block AFTER `writeSSE({ event: 'done' })`
    - ai.ts contains `httpSend('canvas_update', ...)` fire-and-forget after upserts
    - `pnpm --filter apps/api tsc --noEmit` exits 0 with no TypeScript errors
    - `pnpm --filter apps/api test -- --run ai.test` exits 0 (existing tests pass — new route behavior may need new tests in Plan 05 scope, but existing tests must not break)
    - `grep -c "crypto.randomUUID" /home/jgm/dev/projects/web-projects/panelito/apps/api/src/routes/ai.ts` returns >= 1 (canvas node UUID generation present)
  </acceptance_criteria>
  <done>/invoke route now: resolves branch UUID → acquires mic lock (409 if held) → streams graph → emits phase_signal SSE if signalled → emits done → upserts committed canvas ops → broadcasts canvas_update → always releases mic in finally</done>
</task>

<task type="auto">
  <name>Task 2: Add PATCH /:id/phase endpoint to sessions router</name>
  <files>apps/api/src/routes/sessions.ts</files>
  <read_first>
    - apps/api/src/routes/sessions.ts (full file — understand the sessionsRouter structure; find the freeze endpoint at ~line 244 as the exact analog to copy; confirm loadBlueprint import or add it; confirm requireAuth middleware pattern)
    - apps/api/src/lib/blueprint-loader.ts (confirm loadBlueprint function signature and that it's importable from sessions.ts)
    - apps/api/src/routes/messages.ts (lines 130–145 — httpSend pattern for phase_advanced broadcast)
  </read_first>
  <action>
    Add a new `sessionsRouter.patch('/:id/phase', requireAuth, async (c) => { ... })` handler to `apps/api/src/routes/sessions.ts`. Place it after the existing `/unfreeze` endpoint (~line 390).

    The handler must implement this exact sequence (per CONTEXT.md D-11, D-12, D-13 and PATTERNS.md §sessions.ts section):

    1. Extract `{ id }` from `c.req.param()` and `user` from `c.get('user')`. Create `supabase = createServiceClient()`.

    2. Parse and validate request body with inline Zod schema:
       `const PatchPhaseBodySchema = z.object({ next_phase_id: z.string().min(1) })`
       Use `safeParse` on `await c.req.json().catch(() => ({}))`. On failure: `return c.json({ error: 'invalid_request', message: 'next_phase_id is required' }, 400)`.

    3. Fetch session: `.select('id, creator_id, blueprint_id, current_phase')`. On error/missing: return 404 `{ error: 'not_found' }`.

    4. Ownership gate (D-11): `if (session.creator_id !== user.id) return c.json({ error: 'forbidden' }, 403)`. This is the same pattern as the freeze endpoint at ~line 262.

    5. Blueprint phase sequence validation (D-11):
       - Call `await loadBlueprint(session.blueprint_id)` — import `loadBlueprint` from `'../lib/blueprint-loader'` if not already imported in sessions.ts
       - Check `blueprint.phase_sequence.some((p: { id: string }) => p.id === parsed.data.next_phase_id)`
       - On false: return `c.json({ error: 'invalid_phase', message: 'next_phase_id not in Blueprint phase_sequence' }, 400)`

    6. Write to DB: `supabase.from('sessions').update({ current_phase: parsed.data.next_phase_id }).eq('id', id)`. On updateErr: return 500 `{ error: 'update_failed' }`.

    7. Broadcast phase_advanced (D-12): Fire-and-forget `httpSend('phase_advanced', { new_phase_id: parsed.data.next_phase_id, blueprint_id: session.blueprint_id })` on `channel(\`session:${id}\`)`.

    8. Return `c.json({ current_phase: parsed.data.next_phase_id }, 200)`.

    Important: `loadBlueprint` may not be currently imported in sessions.ts. Add the import from `'../lib/blueprint-loader'` if needed. Do NOT duplicate the blueprint loading logic — use the existing cached loader.

    D-13 invariant: this endpoint is the ONLY way to write `sessions.current_phase`. The LLM has no path here. The ownership gate (`creator_id === user.id`) ensures only the session creator can call it.
  </action>
  <verify>
    <automated>grep -c "patch.*phase\|\.patch(" /home/jgm/dev/projects/web-projects/panelito/apps/api/src/routes/sessions.ts</automated>
  </verify>
  <acceptance_criteria>
    - sessions.ts contains `sessionsRouter.patch('/:id/phase', requireAuth, ...)` handler
    - Handler validates `next_phase_id` via Zod (returns 400 on missing/empty)
    - Handler has ownership gate `session.creator_id !== user.id → 403`
    - Handler validates `next_phase_id` against `blueprint.phase_sequence` (returns 400 if not found)
    - Handler writes `current_phase` to sessions table
    - Handler broadcasts `phase_advanced` via fire-and-forget httpSend with `{ new_phase_id, blueprint_id }`
    - Handler returns 200 `{ current_phase: next_phase_id }` on success
    - `pnpm --filter apps/api tsc --noEmit` exits 0 with no TypeScript errors in sessions.ts
    - `pnpm --filter apps/api test -- --run sessions.test` exits 0 (existing session tests pass)
    - No code path in sessions.ts bypasses the `creator_id` ownership check before writing `current_phase`
  </acceptance_criteria>
  <done>PATCH /sessions/:id/phase endpoint enforces creator ownership, validates Blueprint phase sequence, writes current_phase to DB, and broadcasts phase_advanced — the LLM has no path to this endpoint</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /invoke | Authenticated user request; mic lock acquired before any AI call |
| POST /invoke → try_acquire_mic RPC | Service role DB call; atomic CAS prevents concurrent graph runs on same branch |
| POST /invoke → canvas_nodes / canvas_edges | Service role upsert; only committed ops (status='committed') written (D-14) |
| Client → PATCH /sessions/:id/phase | Authenticated user request; ownership gate enforced before any write |
| PATCH /sessions/:id/phase → sessions.current_phase | Service role write; blueprint.phase_sequence validation prevents arbitrary phase IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-A | Denial of Service | Concurrent /invoke flooding same branch | mitigate | try_acquire_mic atomic CAS; concurrent callers get 409 mic_locked immediately; 30s expiry prevents permanent lock |
| T-08-04-B | Elevation of Privilege | LLM autonomously advancing current_phase | mitigate | D-13: LLM has no tool or code path to PATCH /sessions/:id/phase; phase_signal only emits an SSE event; human HTTP call required; ownership gate enforces creator-only |
| T-08-04-C | Tampering | Forged next_phase_id not in Blueprint phase_sequence | mitigate | blueprint.phase_sequence.some() validation before DB write (D-11); returns 400 invalid_phase if not found |
| T-08-04-D | Tampering | Non-creator calling PATCH /sessions/:id/phase | mitigate | creator_id ownership gate: session.creator_id !== user.id → 403 forbidden |
| T-08-04-E | Tampering | Canvas node injection with wrong Blueprint vocabulary | accept | MutationGateNode vocabulary validation already in place (D-07, Phase 6); Phase 8 only writes ops that passed the gate (status='committed'); vocabulary is already enforced upstream |
| T-08-04-F | Availability | Mic lock not released on SSE stream crash / AbortError | mitigate | finally block always calls release_mic RPC; 30s expiry on try_acquire_mic provides secondary safety net (D-06/D-07) |
| T-08-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 04; source code modifications only |
</threat_model>

<verification>
After both tasks complete:
- `pnpm --filter apps/api tsc --noEmit` exits 0 for both ai.ts and sessions.ts
- `pnpm --filter apps/api test -- --run ai.test` passes
- `pnpm --filter apps/api test -- --run sessions.test` passes
- `grep "try_acquire_mic" apps/api/src/routes/ai.ts` shows the RPC call before streamSSE
- `grep "release_mic" apps/api/src/routes/ai.ts` shows the finally block
- `grep "phase_signal" apps/api/src/routes/ai.ts` shows both the SSE event emission and the canvas filter check
- `grep "canvas_nodes\|canvas_edges" apps/api/src/routes/ai.ts` shows the upsert block
- `grep "\.patch(" apps/api/src/routes/sessions.ts` shows the new PATCH handler
- Curl test: `curl -X PATCH /api/sessions/test/phase -H "auth" -d '{"next_phase_id":"fake"}' → 400 invalid_phase`
</verification>

<success_criteria>
- POST /invoke: mic acquired as first DB op; 409 returned if lock held; finally always releases mic
- POST /invoke: phase_signal SSE event emitted before done if finalState.phase_signal === true
- POST /invoke: committed canvas ops upserted after done; canvas_update broadcast with full DB rows
- PATCH /sessions/:id/phase: creator-only; Blueprint sequence validated; DB written; phase_advanced broadcast
- TypeScript clean; all existing tests pass
</success_criteria>

<output>
Create `.planning/phases/08-human-control-canvas-sync/08-04-SUMMARY.md` when done
</output>
