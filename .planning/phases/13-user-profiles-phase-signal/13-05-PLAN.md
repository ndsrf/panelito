---
phase: 13-user-profiles-phase-signal
plan: 05
type: execute
wave: 4
depends_on: [13-03, 13-04]
files_modified:
  - apps/api/src/routes/ai.ts
  - apps/api/src/graph/nodes/analytics-agent.ts
  - apps/api/src/graph/nodes/facilitation-agent.ts
  - apps/api/src/graph/graph.integration.test.ts
autonomous: true
requirements: [PROFILE-02, TRIGGER-02]
must_haves:
  truths:
    - "ai.ts threads supabase, branchId, participantId, and botOverrides into graphConfig.configurable so profileBuilder/phase-readiness/moderation are reachable in production"
    - "analyticsAgentNode sets phase_signal=true only when state.firingSkillId === 'phase-readiness'; the existing SSE plumbing then emits the phase_signal event"
    - "when a firing Skill targets a specific participant (skillMeta.participantId), the Role node appends that participant's profile summary to its prompt guidance (D-11)"
    - "an integration test proves phase-readiness firing produces a phase_signal in finalState and does NOT fire on an unrelated fact-check"
  artifacts:
    - path: "apps/api/src/routes/ai.ts"
      provides: "graphConfig.configurable supabase/branchId/participantId/botOverrides (F2/D-12/D-13)"
      contains: "branchId"
    - path: "apps/api/src/graph/nodes/analytics-agent.ts"
      provides: "phase_signal derivation (F3) + D-11 analyst personalization"
      contains: "phase-readiness"
    - path: "apps/api/src/graph/nodes/facilitation-agent.ts"
      provides: "D-11 coach personalization via summarizeParticipant"
      contains: "summarizeParticipant"
  key_links:
    - from: "apps/api/src/graph/nodes/analytics-agent.ts"
      to: "GraphState.phase_signal"
      via: "state.firingSkillId === 'phase-readiness'"
      pattern: "phase-readiness"
    - from: "apps/api/src/routes/ai.ts"
      to: "graph config"
      via: "graphConfig.configurable.branchId/supabase/participantId/botOverrides"
      pattern: "branchId"
---

<objective>
Close the production-reachability loop and deliver Coach personalization + phase-signal emission. Thread `supabase`/`branchId`/`participantId`/`botOverrides` into `ai.ts`'s `graphConfig.configurable` (RESEARCH CRITICAL Finding 2 / D-12 / D-13 — this also retroactively fixes Phase 12's silently-broken moderation escalation and the dead "Analistas activos" toggle). Derive `phase_signal` in `analyticsAgentNode` strictly from `state.firingSkillId === 'phase-readiness'` (Finding 3), reusing the untouched SSE → CreatorControls → PATCH plumbing. Wire D-11 personalization: when a firing Skill carries a `skillMeta.participantId`, the Role node fetches that profile and appends `summarizeParticipant()` to its guidance. Prove it all with an integration test.

Purpose: without ai.ts wiring, profileBuilder/phase-readiness/moderation are unreachable no-ops in a real session; without the analyticsAgentNode derivation, the phase-readiness Skill can never reach the existing phase_signal plumbing.
Output: 3 modified runtime files + 1 integration test.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-user-profiles-phase-signal/13-CONTEXT.md
@.planning/phases/13-user-profiles-phase-signal/13-RESEARCH.md
@.planning/phases/13-user-profiles-phase-signal/13-PATTERNS.md

<interfaces>
From apps/api/src/routes/ai.ts (existing graphConfig to extend — verified current source):
```
const graphConfig = { configurable: { thread_id: `${activeBranchId ?? sessionId}:human`, blueprint, providerName, plaintextKey, activePersonas, streamWriter }, callbacks: [callbackHandler], signal: c.req.raw.signal }
// activeBranchId and supabase (createServiceClient) are already in scope here.
// Only the session creator can invoke (session.creator_id === user.id enforced earlier) → the human-path participant is user.id.
// Existing phase_signal SSE block (UNCHANGED) reads finalState.phase_signal === true and emits { next_phase_id, blueprint_id }.
```
From apps/api/src/graph/nodes/analytics-agent.ts:
```
const factCheckFraming = config?.configurable?.factCheckFraming === true || state.firingSkillId === 'fact-check'  // line ~134 — the precedent to mirror
const firingAnalystSkill = ANALYST_SKILLS.find(s => s.id === state.firingSkillId)  // ~155
const skillGuidance = firingAnalystSkill ? firingAnalystSkill.buildPromptGuidance({ state, blueprint, config }) : undefined
// return currently: { agentOutput, agentConfidence, triggerMetadata }  (NO phase_signal)
```
From apps/api/src/graph/nodes/facilitation-agent.ts: symmetric `firingCoachSkill.buildPromptGuidance(...)` splice (~127-132).
From apps/api/src/lib/participant-profile.ts (Plan 02): getParticipantProfile(supabase, branchId, participantId).
From apps/api/src/lib/bot-context.ts (Plan 03): summarizeParticipant(profile).
GraphState.phase_signal is `Annotation<boolean | null>` — set `true` or `null` (never undefined).
skillMeta: the moderation Skill already sets `meta: { participantId, ... }`; fact-check sets meta with a claim reference. Only Skills with a resolvable `skillMeta.participantId` trigger personalization.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ai.ts — thread supabase/branchId/participantId/botOverrides into graphConfig.configurable (F2/D-12/D-13)</name>
  <files>apps/api/src/routes/ai.ts</files>
  <read_first>
    - apps/api/src/routes/ai.ts (the graphConfig object at ~lines 345-356; activeBranchId resolution at ~281-295; the ownership check confirming only the creator invokes; the botOverrides source — grep for where the "Analistas activos" toggle persists, e.g. a sessions column or the bots route)
    - apps/api/src/graph/nodes/trigger-gate.ts (how botOverrides is consumed: `config.configurable.botOverrides as Record<string, boolean>`, role-gate `?? blueprint.bot_defaults`)
    - apps/api/src/lib/skills/moderation.ts (reads config.configurable.supabase/branchId/participantId — the keys that must now be supplied)
    - .planning/phases/13-user-profiles-phase-signal/13-RESEARCH.md (Finding 2, Pitfall 3, Open Question 2 — botOverrides gap; variables already in scope)
  </read_first>
  <action>
    Extend `graphConfig.configurable` in ai.ts with: `supabase` (the `createServiceClient()` instance already in scope as `supabase`) — also alias as `serviceClient` if downstream code reads that key (orphan-edge/profile-builder read `serviceClient ?? createServiceClient()`; moderation reads `supabase`; supply BOTH keys pointing at the same client to satisfy every consumer), `branchId: activeBranchId`, `participantId: user.id` (the human-path invoker is always the session creator per the ownership gate), and `botOverrides` read from wherever the creator's Coach/Analyst on-off toggle persists (D-13 — resolve the same source the "Analistas activos" UI writes; if it lives on the session row, SELECT it with the existing session fetch; if via a bots table/route, fetch it before constructing graphConfig). Do NOT change the existing keys (thread_id, blueprint, providerName, plaintextKey, activePersonas, streamWriter), the callbacks, or the signal. Do NOT modify the existing phase_signal SSE emission block — it already reads finalState.phase_signal generically. Add a brief comment tagging F2/D-12/D-13.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit && grep -q "branchId: activeBranchId\|branchId:activeBranchId" src/routes/ai.ts && grep -q "participantId" src/routes/ai.ts && grep -q "botOverrides" src/routes/ai.ts && pnpm test -- ai 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `graphConfig.configurable` in ai.ts sets `branchId` (= activeBranchId), `participantId` (= user.id), a Supabase client under both `supabase` and `serviceClient` keys, and `botOverrides` (grep confirms all four tokens).
    - The botOverrides value is read from the same persistence the "Analistas activos" toggle writes (documented in the task summary; not hardcoded).
    - The existing configurable keys and the phase_signal SSE block are unchanged (grep confirms thread_id/streamWriter still present; the SSE block still reads finalState.phase_signal).
    - `pnpm test -- ai` and `pnpm exec tsc --noEmit` pass.
  </acceptance_criteria>
  <done>The human /invoke path supplies supabase/branchId/participantId/botOverrides; profileBuilder, phase-readiness, and moderation are now reachable in production.</done>
</task>

<task type="auto">
  <name>Task 2: analyticsAgentNode phase_signal derivation (F3) + D-11 personalization in both Role nodes</name>
  <files>apps/api/src/graph/nodes/analytics-agent.ts, apps/api/src/graph/nodes/facilitation-agent.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/analytics-agent.ts (factCheckFraming derivation line ~134; firingAnalystSkill splice ~155-164; the Partial<GraphState> return ~210)
    - apps/api/src/graph/nodes/facilitation-agent.ts (firingCoachSkill splice ~127-132; its Partial<GraphState> return)
    - apps/api/src/lib/participant-profile.ts (getParticipantProfile), apps/api/src/lib/bot-context.ts (summarizeParticipant)
    - .planning/phases/13-user-profiles-phase-signal/13-RESEARCH.md (Finding 3 — the exact `phaseReadinessFired` derivation returning phase_signal:true|null; Pitfall 5 — gate strictly on === 'phase-readiness'; Pattern 3 — personalization only for Skills with a single target participant)
    - .planning/phases/13-user-profiles-phase-signal/13-PATTERNS.md (analytics-agent + facilitation-agent sections)
  </read_first>
  <action>
    In `analytics-agent.ts`: derive `const phaseReadinessFired = state.firingSkillId === 'phase-readiness'` (strict equality — Pitfall 5, never a broad "any analyst skill" check) and add `phase_signal: phaseReadinessFired ? true : null` to the node's Partial<GraphState> return (use `null`, not `undefined`, matching the Annotation<boolean|null> shape). Both Role nodes: after computing `skillGuidance`, if the firing Skill carries a resolvable target participant (`const targetId = state.skillMeta?.participantId as string | undefined`), await `getParticipantProfile(supabase, branchId, targetId)` using the same `config.configurable.supabase`/`branchId` seam the other nodes use, and append `summarizeParticipant(profile)` to the guidance block passed into the system-prompt builder (append after skillGuidance, before Personality voice — the existing step-3.5 slot). When `targetId` is absent (e.g. phase-readiness fired — no single target), append nothing (graceful). Fetch failures must fail-open (summarizeParticipant(null) already returns a safe string). Do not restructure the system-prompt builders; only extend the guidance string they already receive. Keep all other analytics/facilitation behavior unchanged.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit && grep -q "phase-readiness" src/graph/nodes/analytics-agent.ts && grep -q "phase_signal" src/graph/nodes/analytics-agent.ts && grep -q "summarizeParticipant" src/graph/nodes/facilitation-agent.ts && grep -q "summarizeParticipant" src/graph/nodes/analytics-agent.ts && pnpm test -- analytics-agent 2>&1 | tail -20 && pnpm test -- facilitation-agent 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - analyticsAgentNode returns `phase_signal: true` ONLY when `state.firingSkillId === 'phase-readiness'` and `null` otherwise (a test with firingSkillId==='fact-check' asserts phase_signal is null — Pitfall 5).
    - Both Role nodes call `summarizeParticipant` and only fetch a profile when `state.skillMeta?.participantId` is present (grep confirms the guarded call; a test with no participantId asserts no profile fetch and unchanged guidance).
    - Profile-fetch failure does not throw (fail-open — summarizeParticipant(null) path).
    - `pnpm test -- analytics-agent` and `pnpm test -- facilitation-agent` pass; tsc passes.
  </acceptance_criteria>
  <done>phase_signal is set strictly for phase-readiness firings, and personalized profile context is appended when a firing Skill targets a specific participant.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end integration test — phase_signal emission + profile population + misattribution guard</name>
  <files>apps/api/src/graph/graph.integration.test.ts</files>
  <read_first>
    - apps/api/src/graph/graph.ts (final topology after Plan 03 — human path agent→mutationGate→argGraphBuilder→profileBuilder→triggerGate→analysis→mutationGate→END)
    - existing integration/graph tests in apps/api/src/graph/ (the createGraph + injected-adapter + MemorySaver/serviceClient injection harness to reuse)
    - .planning/phases/13-user-profiles-phase-signal/13-RESEARCH.md (Security Domain — the four threat assertions; System Architecture Diagram for the expected finalState)
  </read_first>
  <action>
    Add an integration test (reuse the existing createGraph + injected-adapter harness; MemorySaver checkpointer; injected `serviceClient` mock for Supabase). Cover: (1) a human-path invocation where the injected phase-readiness coverage-judgment adapter returns `sufficient:true` after the N/M gate is satisfied → assert `finalState.phase_signal === true` and `finalState.firingSkillId === 'phase-readiness'`. (2) a human-path invocation where a fact-check Skill fires instead → assert `finalState.phase_signal !== true` (null) — misattribution guard (Pitfall 5 / T-13). (3) assert the coverage-judgment adapter was invoked with `TASK_MODELS[provider].analysis` (capable tier — COST-01 / T-13-10). (4) assert profileBuilder path ran on the human message (argGraph populated → upsertParticipantProfile called on the injected client) — reachability proof (Finding 1). Use deterministic injected adapters/clients (no live network, no live DB).
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- graph.integration 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 asserts finalState.phase_signal === true when phase-readiness fires on the human path.
    - Test 2 asserts finalState.phase_signal is null when a non-phase-readiness Analyst Skill fires (misattribution blocked).
    - Test 3 asserts the coverage-judgment call used the `.analysis` tier for the tested provider(s).
    - Test 4 asserts the profile upsert was invoked on the human path (Finding 1 reachability).
    - `pnpm test -- graph.integration` passes with fully injected adapters/clients (no live network/DB).
  </acceptance_criteria>
  <done>An integration test proves TRIGGER-02 emission, PROFILE-01 reachability, the cost-tier guard, and the misattribution guard end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firing Skill id → phase_signal | Only 'phase-readiness' may enable the user-facing "Advance Phase" affordance |
| config.configurable → graph nodes | ai.ts is the sole supplier of supabase/branchId/participantId/botOverrides on the human path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Tampering | phase_signal spoofed by a non-phase-readiness Skill firing | mitigate | Strict `state.firingSkillId === 'phase-readiness'` equality in analyticsAgentNode; integration Test 2 asserts fact-check firing yields phase_signal=null (Pitfall 5). |
| T-13-13 | Elevation of Privilege | LLM writing current_phase directly | mitigate | analyticsAgentNode only sets an advisory boolean; current_phase write stays the human-only PATCH /:id/phase (HUMAN-02 invariant; that route untouched). |
| T-13-14 | Information Disclosure | Coach prompt leaking another participant's profile | mitigate | Server-side profile reads use the service-role client (intended) and personalization only targets `skillMeta.participantId` (the participant the firing Skill is already responding to); no cross-participant enumeration. |
| T-13-15 | Tampering (cost) | Coverage-judgment resolving the wrong tier once reachable in production | mitigate | Integration Test 3 asserts `.analysis` tier resolution end-to-end (COST-01). |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `pnpm test -- ai`, `-- analytics-agent`, `-- facilitation-agent`, `-- graph.integration` all pass.
- `cd apps/api && pnpm exec tsc --noEmit` passes.
- Grep confirms branchId/participantId/botOverrides/supabase in ai.ts graphConfig, and strict `phase-readiness` gating in analytics-agent.ts.
</verification>

<success_criteria>
- The human /invoke path makes profileBuilder, phase-readiness, and moderation reachable (F2/D-12/D-13).
- phase-readiness firing produces a phase_signal that flows through the existing SSE → CreatorControls → PATCH plumbing; the human still clicks to advance.
- Coach/Analyst prompts include a targeted participant's profile summary when a Skill targets one (PROFILE-02).
- Integration test proves emission, reachability, cost-tier, and misattribution guards.
</success_criteria>

<output>
Create `.planning/phases/13-user-profiles-phase-signal/13-05-SUMMARY.md` when done.
</output>
