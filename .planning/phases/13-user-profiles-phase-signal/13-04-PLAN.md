---
phase: 13-user-profiles-phase-signal
plan: 04
type: tdd
wave: 3
depends_on: [13-02]
files_modified:
  - apps/api/src/lib/skills/phase-readiness.ts
  - apps/api/src/lib/skills.ts
  - apps/api/src/graph/nodes/trigger-gate.ts
  - apps/api/src/graph/nodes/agent.ts
autonomous: true
requirements: [TRIGGER-02]
must_haves:
  truths:
    - "phase-readiness is a registered Analyst Skill following the {id, role, detect, buildPromptGuidance} contract"
    - "detect() enforces the sequential N-then-M gate: committed canvas nodes >= min_nodes must accumulate first, THEN min_messages_after human messages, before the coverage-judgment LLM call fires"
    - "coverage is judged via LLM judgment against the phase's existing llm_instructions field, not a new static required_topics field (D-07)"
    - "the coverage-judgment call resolves TASK_MODELS[provider].analysis (capable tier), never .classification"
    - "the ad-hoc phase_signal emission in agent.ts is removed in favor of the phase-readiness Skill-driven path inside TriggerGateNode, reusing the existing phase_signal SSE/UI/PATCH plumbing as-is (D-08)"
  artifacts:
    - path: "apps/api/src/lib/skills/phase-readiness.ts"
      provides: "phaseReadinessSkill (N/M gate + coverage judgment)"
      exports: ["phaseReadinessSkill"]
      min_lines: 60
    - path: "apps/api/src/lib/skills.ts"
      provides: "phase-readiness registered in ANALYST_SKILLS"
      contains: "phaseReadinessSkill"
    - path: "apps/api/src/graph/nodes/trigger-gate.ts"
      provides: "phaseGateProgress threaded from skill meta into GraphState"
      contains: "phaseGateProgress"
  key_links:
    - from: "apps/api/src/lib/skills.ts"
      to: "phaseReadinessSkill"
      via: "ANALYST_SKILLS array"
      pattern: "ANALYST_SKILLS.*phaseReadinessSkill|phaseReadinessSkill.*ANALYST_SKILLS"
    - from: "apps/api/src/lib/skills/phase-readiness.ts"
      to: "TASK_MODELS[provider].analysis"
      via: "coverage-judgment adapter.stream model resolution"
      pattern: "TASK_MODELS\\[.*\\]\\.analysis"
---

<objective>
Build the phase-readiness detection half of TRIGGER-02. Add the `phase-readiness` Analyst Skill (sequential N-then-M gate per D-09, coverage judgment against the phase's `llm_instructions` + argGraph summary + last M human messages per D-07/D-10, capable-tier per COST-01), register it in `ANALYST_SKILLS`, thread its `phaseGateProgress` counter through `TriggerGateNode` into GraphState, and remove the deprecated ad-hoc `phase_signal` emission from `agent.ts` (D-08).

Purpose: the LLM signals phase readiness holistically — decoupled from whether it happened to propose a canvas mutation that turn — while the human still clicks to advance (HUMAN-02 invariant unchanged).
Output: 1 new Skill, 3 modified files.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-user-profiles-phase-signal/13-CONTEXT.md
@.planning/phases/13-user-profiles-phase-signal/13-RESEARCH.md
@.planning/phases/13-user-profiles-phase-signal/13-PATTERNS.md

<interfaces>
From apps/api/src/lib/skills.ts:
```
export interface Skill { id: string; role: 'coach' | 'analyst'; detect(ctx: SkillContext): Promise<SkillDetectionResult>; buildPromptGuidance(ctx: SkillContext): string }
export interface SkillContext { state: GraphState; blueprint: Blueprint; config?: any }
export const ANALYST_SKILLS: Skill[] = [factCheckSkill, orphanEdgeSkill]
```
From packages/types/src/skill.ts: `SkillDetectionResult = { fires: boolean; confidence: number(0..1); meta?: Record<string,unknown> | null }`.
From apps/api/src/graph/state.ts (Plan 01): `phaseGateProgress: { phaseId, nodeCountAtGateOpen, messagesSinceGateOpen } | null`; also existing `currentPhaseId`, `firingSkillId`, `firingSkillRole`, `skillMeta`, `argGraph`, `messages`.
From apps/api/src/graph/nodes/trigger-gate.ts: returns `{ firingSkillId, firingSkillRole, skillMeta: result.meta ?? null, triggerGateComplete: true }` on every path; role-gate reads botOverrides ?? bot_defaults.
From @panelito/types (Plan 01): `phaseReadinessJudgmentTool` (name 'judge_phase_readiness', params sufficient:boolean, confidence:number).
Analogs: fact-check.ts (tiered gate → capable classifier → SkillDetectionResult; the safeParse + bounded-retry-then-fail-closed loop); orphan-edge.ts (committed-node COUNT via `.from('canvas_nodes').eq('status','committed')`, serviceClient seam, branchId guard); summarizeArgGraph (bot-context.ts).
From apps/api/src/lib/model-config.ts: TASK_MODELS[provider].analysis (capable), .classification (light).
agent.ts current ad-hoc emission: lines ~150-181 extract phase_signal from the canvas_mutation tool input and return it in Partial<GraphState>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase-readiness Skill — sequential N/M gate + coverage judgment</name>
  <files>apps/api/src/lib/skills/phase-readiness.ts</files>
  <read_first>
    - apps/api/src/lib/skills/fact-check.ts (tiered detect() shape; classifyTier2 safeParse + MAX_ATTEMPTS bounded retry → fail-closed; buildPromptGuidance splice)
    - apps/api/src/lib/skills/orphan-edge.ts (committed-node COUNT query `status='committed'`; serviceClient ?? createServiceClient() seam; branchId guard)
    - apps/api/src/lib/bot-context.ts (summarizeArgGraph, escapeUntrustedText)
    - .planning/phases/13-user-profiles-phase-signal/13-RESEARCH.md (Pattern 2 skeleton — the exact N-then-M sequencing, phaseGateProgress reset-on-phase-change, "committed nodes means canvas_nodes.status='committed' NOT argGraph.nodes.length")
    - .planning/phases/13-user-profiles-phase-signal/13-PATTERNS.md (phase-readiness section)
  </read_first>
  <behavior>
    - detect() resets progress to a fresh object when state.phaseGateProgress is null OR its phaseId !== state.currentPhaseId.
    - When committed canvas_nodes count < gateConfig.min_nodes: returns fires=false, confidence=0, meta.phaseGateProgress reflects the un-opened gate (no LLM call made).
    - Once committed count >= min_nodes but messagesSinceGateOpen+1 < min_messages_after: returns fires=false and increments messagesSinceGateOpen in meta (no LLM call made).
    - Once both thresholds cross: the coverage-judgment LLM call fires exactly once; a `sufficient:true` result yields fires=true and resets phaseGateProgress to null; `sufficient:false` yields fires=false and retains progress.
    - The coverage-judgment adapter.stream is invoked with `TASK_MODELS[provider].analysis` (asserted per provider).
    - Malformed/absent tool output fails closed (fires=false) after MAX_ATTEMPTS.
  </behavior>
  <action>
    Create `apps/api/src/lib/skills/phase-readiness.ts` exporting `phaseReadinessSkill: Skill = { id: 'phase-readiness', role: 'analyst', detect, buildPromptGuidance }` plus internals (`detect`, `buildPromptGuidance`, the coverage output schema) for testing. `detect(context)`: read `branchId` + `serviceClient ?? createServiceClient()` (orphan-edge seam, branchId guard → return fires=false); resolve `gateConfig` = the current phase's `phase_readiness_gate` from `blueprint.phase_sequence.find(p => p.id === state.currentPhaseId)` (falls back to the Zod default {min_nodes:3,min_messages_after:5}). Reset `progress` to `{ phaseId: state.currentPhaseId, nodeCountAtGateOpen: 0, messagesSinceGateOpen: 0 }` when `state.phaseGateProgress` is null or its `phaseId !== state.currentPhaseId`. Query committed node count via `.from('canvas_nodes').select('id', { count: 'exact', head: true }).eq('branch_id', branchId).eq('status', 'committed')` (NOT `state.argGraph.nodes.length` — ArgNode has no committed status). If `committedCount < gateConfig.min_nodes` → return `{ fires: false, confidence: 0, meta: { phaseGateProgress: progress } }`. Else increment `messagesSinceGateOpen` by 1; if `< gateConfig.min_messages_after` → return `{ fires: false, confidence: 0, meta: { phaseGateProgress: { ...progress, messagesSinceGateOpen } } }`. Else run the coverage-judgment call: build messages from the active phase's `llm_instructions`, `summarizeArgGraph(state.argGraph)`, and the last `min_messages_after` human (`role==='user'`) messages from `state.messages`, escaped via `escapeUntrustedText`; stream with `[phaseReadinessJudgmentTool]` at `TASK_MODELS[providerName ?? 'anthropic'].analysis` (capable tier — COST-01), inside a `MAX_ATTEMPTS` loop that `.safeParse()`s the tool output against a local `z.object({ sufficient: z.boolean(), confidence: z.number().min(0).max(1) })`, `continue` on miss, fail-closed (`return { fires: false, confidence: 0 }`) after exhaustion. On success: `sufficient` → `{ fires: true, confidence, meta: { phaseGateProgress: null } }`, else `{ fires: false, confidence, meta: { phaseGateProgress: { ...progress, messagesSinceGateOpen } } }`. `buildPromptGuidance` returns guidance instructing the Coach to ask the group whether they are ready to advance the phase, referencing the coverage rationale — do NOT call summarizeParticipant() (no single target participant, Pattern 3 note). Fail-open on any thrown error (`return { fires: false, confidence: 0 }`).
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit && pnpm test -- phase-readiness 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `phaseReadinessSkill` exports `{ id: 'phase-readiness', role: 'analyst', detect, buildPromptGuidance }`.
    - A test with committed count below min_nodes asserts NO adapter.stream call was made (the gate short-circuits before the LLM).
    - A test with committed count at/above min_nodes but message count below min_messages_after asserts NO adapter.stream call and an incremented messagesSinceGateOpen in meta.
    - A test with both thresholds crossed asserts adapter.stream was called with a model equal to `TASK_MODELS[provider].analysis` for at least anthropic and one other provider (BYOK tier-regression guard — T-13 threat).
    - Committed count is read from `canvas_nodes.status='committed'` (grep confirms), NOT from state.argGraph.
    - Malformed tool output over MAX_ATTEMPTS yields fires=false (fail-closed test).
  </acceptance_criteria>
  <done>phase-readiness Skill enforces the sequential N/M gate, calls the capable tier only after both thresholds, and fails closed on bad output.</done>
</task>

<task type="auto">
  <name>Task 2: Register phase-readiness in ANALYST_SKILLS + thread phaseGateProgress through TriggerGateNode</name>
  <files>apps/api/src/lib/skills.ts, apps/api/src/graph/nodes/trigger-gate.ts</files>
  <read_first>
    - apps/api/src/lib/skills.ts (ANALYST_SKILLS array + imports; registration-order-is-priority-order comment)
    - apps/api/src/graph/nodes/trigger-gate.ts (the return statements — every path returns `{ firingSkillId, firingSkillRole, skillMeta, triggerGateComplete }`; where to also surface phaseGateProgress)
    - .planning/phases/13-user-profiles-phase-signal/13-RESEARCH.md (Pattern 2 note — option (a): TriggerGateNode copies a reserved `meta.phaseGateProgress` key into a real GraphState.phaseGateProgress field on every return, keeping TriggerGateNode the single place routing/metadata fields are set)
  </read_first>
  <action>
    In `skills.ts`, import `phaseReadinessSkill` and add it to `ANALYST_SKILLS` (order = priority; place per intended priority relative to factCheckSkill/orphanEdgeSkill — put phase-readiness where a phase-advance prompt should win over orphan-edge but document the chosen order). In `trigger-gate.ts`, extend every return path to also set `phaseGateProgress`: when a Skill fires, read `result.meta?.phaseGateProgress` (if present) and include it as a top-level `phaseGateProgress` field in the returned Partial<GraphState>; on the no-fire path, surface the phaseGateProgress from whichever candidate result carried it (the phase-readiness detect() result's meta) so the incremented counter persists across turns even when the gate has not yet fired. Preserve the existing `triggerGateComplete: true` on every path and the Promise.allSettled fail-isolation. Do not change the role-gate or priority-order semantics beyond adding the new Skill.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit && grep -q "phaseReadinessSkill" src/lib/skills.ts && grep -q "phaseGateProgress" src/graph/nodes/trigger-gate.ts && pnpm test -- trigger-gate 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `ANALYST_SKILLS` in skills.ts includes `phaseReadinessSkill` (grep confirms).
    - `trigger-gate.ts` sets a top-level `phaseGateProgress` in its returned Partial<GraphState> when a candidate result's meta carries it (grep confirms `phaseGateProgress`).
    - A trigger-gate test asserts that when phase-readiness detect returns meta.phaseGateProgress without firing, the node's return surfaces phaseGateProgress (counter persists) while triggerGateComplete stays true.
    - Existing trigger-gate tests (fail-isolation, role-gate) still pass.
  </acceptance_criteria>
  <done>phase-readiness is a registered Analyst Skill and its N/M counter persists across invocations via TriggerGateNode → GraphState.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the deprecated ad-hoc phase_signal emission from agent.ts (D-08)</name>
  <files>apps/api/src/graph/nodes/agent.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/agent.ts (lines ~150-181 — the current phase_signal extraction from the canvas_mutation tool input and its inclusion in the Partial<GraphState> return)
    - .planning/phases/13-user-profiles-phase-signal/13-RESEARCH.md (State of the Art / D-08 — the ONLY consumers of phase_signal are ai.ts's SSE block and GraphState; agentNode is the only setter today; safe to remove once the Skill-driven path is wired. Leave canvas-tool.ts's phase_signal boolean field alone (Claude's Discretion — removing it is a larger fixture-touching change))
  </read_first>
  <action>
    Remove the ad-hoc `phase_signal` extraction and its inclusion in `agentNode`'s returned Partial<GraphState> (lines ~150-181). agentNode must no longer set `phase_signal` at all — the field will now be set exclusively by analyticsAgentNode when the phase-readiness Skill fires (Plan 05, Finding 3). Do NOT remove `phase_signal` from the `canvas_mutation` tool schema (`packages/types/src/canvas-tool.ts`) — leave that field in place (Claude's Discretion; removing it is a larger fixture-touching change). Confirm via grep that no OTHER production consumer reads a phase_signal set by agentNode before removing. Keep all other agentNode behavior (agentOutput, agentConfidence, canvas mutation extraction) unchanged. Update any agent.ts test that asserted phase_signal emission to reflect that agentNode no longer sets it.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit && ! grep -n "phase_signal" src/graph/nodes/agent.ts && pnpm test -- agent 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "phase_signal" apps/api/src/graph/nodes/agent.ts` returns nothing (the ad-hoc emission is fully removed).
    - agentNode still returns agentOutput/agentConfidence and canvas-mutation extraction is unchanged (agent tests pass).
    - `packages/types/src/canvas-tool.ts` still declares the phase_signal field (not removed).
    - `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <done>agent.ts no longer emits phase_signal; the field is now sourced only through the Skill-driven path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model coverage-judgment output → GraphState | The judge_phase_readiness tool output crosses into typed state and drives a user-facing "Advance Phase" affordance |
| provider tier resolution → BYOK billing | Choosing the wrong TASK_MODELS tier silently changes cost on the creator's key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Tampering | Coverage-judgment output smuggling an out-of-range confidence | mitigate | `.safeParse()` against `{ sufficient: boolean, confidence: number.min(0).max(1) }` with bounded retry → fail-closed (mirrors fact-check classifyTier2). |
| T-13-10 | Tampering (cost) | phase-readiness resolving the wrong TASK_MODELS tier | mitigate | Explicit per-provider unit assertion that the coverage-judgment call uses `TASK_MODELS[provider].analysis`; never `.classification` (COST-01, mirrors fact-check.test.ts tier guard). |
| T-13-11 | Elevation of Privilege | LLM advancing the phase autonomously | mitigate | phase-readiness only sets an advisory signal; actual `current_phase` write remains the human-only PATCH /:id/phase (HUMAN-02, untouched this phase). |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `pnpm test -- phase-readiness`, `pnpm test -- trigger-gate`, `pnpm test -- agent` pass.
- `grep -n "phase_signal" apps/api/src/graph/nodes/agent.ts` is empty.
- `cd apps/api && pnpm exec tsc --noEmit` passes.
</verification>

<success_criteria>
- phase-readiness fires only after the sequential N-then-M gate, using the capable tier.
- The N/M counter survives across invocations via GraphState.phaseGateProgress.
- The ad-hoc agent.ts phase_signal path is gone; readiness is Skill-driven.
</success_criteria>

<output>
Create `.planning/phases/13-user-profiles-phase-signal/13-04-SUMMARY.md` when done.
</output>
