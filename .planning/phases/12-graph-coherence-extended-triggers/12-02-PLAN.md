---
phase: 12-graph-coherence-extended-triggers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/lib/embeddings.ts
  - apps/api/src/lib/embeddings.test.ts
  - apps/api/src/lib/moderation-count.ts
  - apps/api/src/lib/moderation-count.test.ts
  - supabase/migrations/0015_graph_coherence_triggers.sql
autonomous: true
requirements: [TRIGGER-03, GRAPH-03, TRIGGER-06, COST-01]
must_haves:
  truths:
    - "The ONNX all-MiniLM-L6-v2 model loads once as a cached in-flight Promise; concurrent embed() callers never double-load the model"
    - "cosineSimilarity() returns a plain dot product of unit-normalized vectors"
    - "A Blueprint domain centroid is computed once per blueprint.id and cached in memory"
    - "moderation_count is readable/writable per participant per branch from Postgres, fail-closed to gentlest tier (0) on error"
    - "drift_detection_enabled=true and a moderation_counts table exist in the live database after schema push"
  artifacts:
    - path: "apps/api/src/lib/embeddings.ts"
      provides: "getExtractor singleton, embed(), cosineSimilarity(), getDomainCentroid()"
      contains: "_extractorPromise"
    - path: "apps/api/src/lib/moderation-count.ts"
      provides: "getModerationCount + incrementModerationCount, fail-closed"
      contains: "moderation_count"
    - path: "supabase/migrations/0015_graph_coherence_triggers.sql"
      provides: "drift_detection_enabled jsonb update + moderation_counts table + RLS"
      contains: "moderation_counts"
  key_links:
    - from: "apps/api/src/lib/embeddings.ts"
      to: "@huggingface/transformers"
      via: "pipeline('feature-extraction', 'Xenova/all-MiniLM-L6-v2')"
      pattern: "feature-extraction"
    - from: "apps/api/src/lib/moderation-count.ts"
      to: "Supabase moderation_counts"
      via: "service-role client query/upsert"
      pattern: "moderation_counts"
---

<objective>
Build the zero-LLM-cost local infrastructure the Skills depend on: the `@huggingface/transformers` ONNX embedding pipeline (drift-redirect + orphan-edge ghost fallback), the Postgres-backed `moderation_count` escalation counter (moderation tone), and migration `0015` that adds `drift_detection_enabled` to the debate Blueprint and a `moderation_counts` table — then pushes the schema.

Purpose: These are shared, provider-agnostic, $0-cost primitives; embeddings and moderation-count are consumed by Wave 2 Skills. The [BLOCKING] schema push is mandatory — types compile without it, producing a false-positive verification state.
Output: `@huggingface/transformers` installed; new `embeddings.ts`, `moderation-count.ts`; new migration `0015`; schema pushed to the live database.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md
@.planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md

<interfaces>
<!-- Pre-verified implementation-ready blocks from RESEARCH.md Patterns 2/3 and PATTERNS.md. -->

Singleton idiom (copy from apps/api/src/lib/langgraph-checkpointer.ts getCheckpointer):
  let _extractorPromise: Promise<FeatureExtractionPipeline> | null = null
  — assign the PROMISE synchronously before any await (prevents the Promise.allSettled race)

RESEARCH.md Pattern 2 (embed + cosineSimilarity) and Pattern 3 (getDomainCentroid cached
by blueprint.id) are pre-verified against the real @huggingface/transformers 4.2.0 API —
use them directly, do not re-derive.

Fail-closed counter idiom (copy from apps/api/src/lib/bot-budget.ts checkBotBudget):
  const FAIL_CLOSED = { ... lowest-privilege values ... }
  on (error || !data) → console.error('[moderation-count] ...') and return FAIL_CLOSED

Package Legitimacy Audit (12-RESEARCH.md): @huggingface/transformers 4.2.0 — slopcheck [OK],
official Hugging Face repo, not deprecated. Disposition: Approved. No [ASSUMED]/[SUS] — no
human legitimacy checkpoint required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @huggingface/transformers + build embeddings.ts ONNX singleton</name>
  <files>apps/api/package.json, apps/api/src/lib/embeddings.ts, apps/api/src/lib/embeddings.test.ts</files>
  <read_first>
    - apps/api/src/lib/langgraph-checkpointer.ts (the ONE existing lazy-singleton-Promise idiom — copy verbatim structure: assign Promise synchronously before await)
    - apps/api/package.json (confirm @huggingface/transformers absent; note pinned versions convention)
    - .planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md (Standard Stack install command; Pattern 2 embed/cosineSimilarity; Pattern 3 getDomainCentroid; Package Legitimacy Audit; Pitfall 2 singleton race)
    - .planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md (embeddings.ts section)
  </read_first>
  <action>
    Run `npm install --workspace apps/api @huggingface/transformers` (registry-verified 4.2.0, slopcheck [OK] Approved per RESEARCH.md — no human legitimacy checkpoint needed). Create `apps/api/src/lib/embeddings.ts` with: a module-level `let _extractorPromise: Promise<FeatureExtractionPipeline> | null = null`; `getExtractor()` that assigns `_extractorPromise = pipeline('feature-extraction', 'Xenova/all-MiniLM-L6-v2')` synchronously (before any await) and returns it — copy the langgraph-checkpointer.ts idiom exactly to avoid the double-load race (Pitfall 2); `embed(text)` awaiting the extractor with `{ pooling: 'mean', normalize: true }` returning `output.data as Float32Array`; `cosineSimilarity(a, b)` as a plain dot-product loop (vectors already unit-normalized, no magnitude division); and `getDomainCentroid(blueprint)` caching by `blueprint.id` in a `Map<string, Promise<Float32Array>>`, embedding `[blueprint.name, ...node_types.map(n=>n.label), ...edge_types.map(e=>e.label)].join('. ')` (D-08). Use `[embeddings]` console-log prefix. In `embeddings.test.ts`, mock `@huggingface/transformers` `pipeline` (do NOT trigger a real ~90MB download in CI — see RESEARCH.md Environment Availability): assert `getExtractor()` calls `pipeline` at most once across two concurrent callers, assert `cosineSimilarity` of two identical unit vectors ≈ 1 and orthogonal ≈ 0, and assert `getDomainCentroid` embeds once per distinct blueprint.id.
  </action>
  <verify>
    <automated>pnpm --filter api test -- embeddings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@huggingface/transformers" apps/api/package.json`
    - `grep -q "_extractorPromise" apps/api/src/lib/embeddings.ts` and the assignment is `= pipeline(` (not `= await pipeline(`)
    - `grep -q "Xenova/all-MiniLM-L6-v2" apps/api/src/lib/embeddings.ts`
    - `grep -q "getDomainCentroid" apps/api/src/lib/embeddings.ts` with a Map cache keyed by blueprint.id
    - `pnpm --filter api test -- embeddings` passes, including the "pipeline called at most once under concurrency" assertion, with `pipeline` mocked
  </acceptance_criteria>
  <done>@huggingface/transformers installed; embeddings.ts exposes a race-safe singleton + cosine + cached domain centroid; tests green with the model mocked.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0015 (drift_detection_enabled + moderation_counts) and moderation-count.ts</name>
  <files>supabase/migrations/0015_graph_coherence_triggers.sql, apps/api/src/lib/moderation-count.ts, apps/api/src/lib/moderation-count.test.ts</files>
  <read_first>
    - supabase/migrations/0014_personalities.sql (most recent migration — copy header-comment block, the `UPDATE ... SET definition = definition || '{...}'::jsonb` idiom, and RLS ENABLE + policy shape)
    - supabase/migrations/0001_initial_schema.sql (messages.author_id has no FK to auth.users — guests use anon tokens; informs participant_id key stability, RESEARCH Open Question 4)
    - apps/api/src/lib/bot-budget.ts (FAIL_CLOSED + explicit-field-extraction posture to copy for moderation-count.ts)
    - apps/api/src/lib/bot-arbitrator.test.ts (mocked SupabaseClient test convention)
    - .planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md (moderation-count.ts + migration 0015 sections; RESEARCH Open Question 3 recommended table shape)
  </read_first>
  <action>
    Create `supabase/migrations/0015_graph_coherence_triggers.sql`: (a) a header comment block referencing D-09 and D-16; (b) `UPDATE public.domain_blueprints SET definition = definition || '{"drift_detection_enabled": true}'::jsonb WHERE id = 'debate-strategy-v1';` (D-09, jsonb-additive idiom from 0014); (c) a new table `public.moderation_counts (branch_id uuid, participant_id text, count int not null default 0, updated_at timestamptz not null default now(), primary key (branch_id, participant_id))` (RESEARCH Open Question 3 recommended shape — a standalone table so Phase 13's PROFILE-01 can absorb it via INSERT...SELECT without touching sessions/branches); (d) `ALTER TABLE public.moderation_counts ENABLE ROW LEVEL SECURITY;` plus a policy scoped to session participants — do NOT copy 0014's "any authenticated user can SELECT"; follow the tighter branch/session-participant visibility policy used on `messages`/`canvas_nodes` (verify that pattern at implementation time). Create `apps/api/src/lib/moderation-count.ts` exporting `getModerationCount(supabase, branchId, participantId): Promise<number>` and `incrementModerationCount(supabase, branchId, participantId): Promise<number>` — either a direct upsert or an atomic RPC (Claude's discretion; prefer an atomic upsert-returning-count if concurrent increments could race). On any Supabase error, `console.error('[moderation-count] ...')` and return `0` (the gentlest tier — least-privilege), never throw, mirroring bot-budget.ts's FAIL_CLOSED. Add `moderation-count.test.ts` with a mocked SupabaseClient asserting: happy-path read returns the stored count; increment returns count+1; a query error returns 0 and does not throw.
  </action>
  <verify>
    <automated>pnpm --filter api test -- moderation-count</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "moderation_counts" supabase/migrations/0015_graph_coherence_triggers.sql` with `ENABLE ROW LEVEL SECURITY`
    - `grep -q "drift_detection_enabled" supabase/migrations/0015_graph_coherence_triggers.sql` via jsonb `||` update on debate-strategy-v1
    - The moderation_counts RLS policy is NOT "any authenticated user" — it references branch/session participant membership
    - `grep -q "getModerationCount" apps/api/src/lib/moderation-count.ts && grep -q "incrementModerationCount" apps/api/src/lib/moderation-count.ts`
    - moderation-count.ts returns 0 (never throws) on Supabase error
    - `pnpm --filter api test -- moderation-count` passes all three cases
  </acceptance_criteria>
  <done>Migration 0015 written (Blueprint field + RLS-scoped moderation_counts table); moderation-count.ts read/increment fail-closed to 0; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema to the live database</name>
  <files>supabase/migrations/0015_graph_coherence_triggers.sql</files>
  <read_first>
    - supabase/migrations/0015_graph_coherence_triggers.sql (the migration written in Task 2 — confirm it is complete before pushing)
    - .planning/STATE.md (Supabase push precedent: prior phases pushed migrations 0006/0010/0011/0012/0014)
  </read_first>
  <action>
    [BLOCKING] After Task 2's migration file is complete, run `supabase db push` to apply migration `0015_graph_coherence_triggers.sql` to the live database. In a non-TTY environment set `SUPABASE_ACCESS_TOKEN` first. This task is mandatory — build and type checks pass WITHOUT the push (types come from config, not the live DB), so skipping it creates a false-positive verification state. If the push requires an interactive prompt that cannot be suppressed, stop and flag for manual intervention rather than marking complete.
  </action>
  <verify>
    <automated>supabase migration list 2>/dev/null | grep -q "0015" && echo "0015 applied"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes without error (or is flagged for manual intervention if a non-suppressible prompt blocks it)
    - `moderation_counts` table is queryable in the live database and `debate-strategy-v1`'s definition contains `drift_detection_enabled: true`
    - `supabase migration list` shows 0015 as applied
  </acceptance_criteria>
  <done>Migration 0015 is applied to the live database; moderation_counts exists and the Blueprint carries drift_detection_enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → apps/api node_modules | New third-party package (@huggingface/transformers) enters the build |
| Hugging Face Hub → local process | ONNX model weights downloaded lazily on first pipeline() call |
| Supabase moderation_counts → API | Per-participant counter read/written; RLS governs who can read it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | @huggingface/transformers install | mitigate | RESEARCH.md Package Legitimacy Audit: slopcheck [OK], official repo, not deprecated, 4.2.0 pinned per STATE.md locked decision — Approved; install is autonomous (no [ASSUMED]/[SUS]) |
| T-12-03 | Information Disclosure | moderation_counts RLS | mitigate | RLS enabled + policy scoped to session participants (not "any authenticated user") — moderation data is more sensitive than voice/tone config |
| T-12-04 | Denial of Service | ONNX model load per invocation | mitigate | Promise-cached singleton (Pitfall 2) — model loads once, not per message; concurrent callers await the same in-flight Promise |
| T-12-05 | Tampering | moderation_count fail-open | mitigate | On DB error, fail-closed to count 0 (gentlest tone) — a false negative is a gentle nudge, never a wrongful escalation |
</threat_model>

<verification>
- `pnpm --filter api test -- embeddings moderation-count` green
- `supabase migration list` shows 0015 applied
- `pnpm --filter api exec tsc --noEmit` clean
</verification>

<success_criteria>
Embeddings singleton + cosine + cached centroid, the fail-closed moderation counter, and the pushed migration all exist and are verified; @huggingface/transformers is installed and clean.
</success_criteria>

<output>
Create `.planning/phases/12-graph-coherence-extended-triggers/12-02-SUMMARY.md` when done
</output>
