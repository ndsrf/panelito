---
phase: 12-graph-coherence-extended-triggers
plan: 06
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03, 12-04, 12-05]
files_modified:
  - apps/api/src/graph/nodes/trigger-gate.ts
  - apps/api/src/graph/nodes/trigger-gate.test.ts
  - apps/api/src/graph/graph.ts
  - apps/api/src/graph/graph.test.ts
  - apps/api/src/graph/graph.integration.test.ts
  - apps/api/src/lib/bot-registration.ts
  - apps/api/src/lib/bot-registration.test.ts
autonomous: true
requirements: [GRAPH-03, TRIGGER-03, TRIGGER-04, TRIGGER-05, TRIGGER-06, COST-01]
must_haves:
  truths:
    - "TriggerGateNode is reachable from the primary human-message path (after mutationGate) AND the proactive analysis_request path, per the locked human-path reachability decision"
    - "TriggerGateNode role-gates before evaluating any Skill (bot_overrides ?? bot_defaults ?? false); a disabled Role's Skills are never detect()-ed"
    - "TriggerGateNode uses Promise.allSettled; one throwing Skill never blocks siblings; it never throws"
    - "the graph terminates on both human and proactive paths with no infinite loop (triggerGateComplete guard prevents re-entry)"
    - "the argGraphBuilder fixed edge is replaced by a conditional edge (no fixed+conditional fan-out)"
    - "analystScorer scores based on which Analyst Skill's context is present (no longer hardcoded 0)"
  artifacts:
    - path: "apps/api/src/graph/nodes/trigger-gate.ts"
      provides: "detection-only TriggerGateNode (D-05/D-06/D-07)"
      contains: "Promise.allSettled"
    - path: "apps/api/src/graph/graph.ts"
      provides: "TriggerGateNode + routeAfterTriggerGate + human-path + argGraphBuilder conditional edge"
      contains: "routeAfterTriggerGate"
    - path: "apps/api/src/lib/bot-registration.ts"
      provides: "analystScorer extended per firing Skill (D-06)"
      contains: "analystScorer"
  key_links:
    - from: "apps/api/src/graph/graph.ts"
      to: "apps/api/src/graph/nodes/trigger-gate.ts"
      via: "addNode('triggerGate', triggerGateNode) + addConditionalEdges"
      pattern: "triggerGate"
    - from: "apps/api/src/graph/graph.ts"
      to: "state.triggerGateComplete"
      via: "routeAfterMutationGate loop guard (triggerGate first pass, END second)"
      pattern: "triggerGateComplete"
    - from: "apps/api/src/graph/nodes/trigger-gate.ts"
      to: "apps/api/src/lib/skills.ts"
      via: "COACH_SKILLS/ANALYST_SKILLS candidate enumeration"
      pattern: "COACH_SKILLS"
---

<objective>
Wire the detection gate into the LangGraph graph — the highest-risk topology change of the phase. Build the detection-only `TriggerGateNode` (D-05/D-06/D-07), insert it into BOTH the primary human-message path AND the proactive `analysis_request` path (per the LOCKED human-path reachability decision), replace `argGraphBuilder`'s fixed edge with a conditional edge, add a `triggerGateComplete` loop guard so the graph terminates, extend `analystScorer` to score by firing Skill (D-06), and prove routing + termination with integration tests.

Purpose: This is where the four new Skills become live — the gate evaluates them on every human message (and proactive scan tick) and routes to the existing Coach/Analyst Role nodes (wired in Plan 05).
Output: New `trigger-gate.ts`; modified `graph.ts` + tests; extended `bot-registration.ts`.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md
@.planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md
@.planning/phases/12-graph-coherence-extended-triggers/12-AI-SPEC.md
@.planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md

<locked_decisions>
LOCKED (from planning context — do NOT re-open as open questions):

1. TriggerGateNode MUST be reachable from the primary human-message path, not just the
   proactive analysis_request path. Insert triggerGate after mutationGate on the human path:
   agent → mutationGate → triggerGate → conditional(facilitation|analysis|end) → END, in
   addition to the proactive path (argGraphBuilder → triggerGate). All 4 new Skills evaluate
   on every human message. Accepted latency tradeoff: one node hop + a Promise.allSettled
   Skill fan-out per human turn.

2. GRAPH-03 uses the TWO-STEP orphan-edge mechanism (a CanvasNode commits without an edge via
   the existing single-op CanvasOpSchema/mutation-gate.ts path UNCHANGED; a LATER TriggerGateNode
   evaluation detects the orphan and fires a follow-up AnalyticsAgentNode ADD_EDGE, or the
   ghost-edge cosine fallback constructs it). Do NOT redesign CanvasOpSchema/agentNode/
   analyticsAgentNode/mutation-gate.ts into a batch/multi-op schema — out of scope.
</locked_decisions>

<interfaces>
graph.ts current topology (verified):
  START → routeFromStart → facilitation | argGraphBuilder | orchestrator
  orchestrator → routeAfterOrchestrator → agent | driftReply | END
  agent → mutationGate → END        (.addEdge('mutationGate', END), line 135)
  argGraphBuilder → analysis        (.addEdge('argGraphBuilder','analysis'), line 139 — FIXED, must become conditional)
  analysis → mutationGate           (line 146)
  facilitation → END
routeAfterOrchestrator / routeFromStart are the conditional-edge templates to mirror.
trigger-gate skeleton is pre-verified in AI-SPEC Section 3 / RESEARCH Pattern 1 — implementation-ready.
skills.ts (Plan 05): populated COACH_SKILLS/ANALYST_SKILLS
state.ts (Plan 01): firingSkillId, firingSkillRole, skillMeta, triggerGateComplete
bot-registration.ts: analystScorer currently hardcoded to 0 (its own comment anticipates Phase 12)
config.configurable.botOverrides carries session bot_overrides (thread it in like blueprint/providerName)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TriggerGateNode (detection-only, role-gated, fail-isolated) + analystScorer extension</name>
  <files>apps/api/src/graph/nodes/trigger-gate.ts, apps/api/src/graph/nodes/trigger-gate.test.ts, apps/api/src/lib/bot-registration.ts, apps/api/src/lib/bot-registration.test.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/orchestrator.ts (config-read + fail-open node shape; classifierAdapter seam)
    - apps/api/src/lib/silence-scan.ts (lines ~190-191: bot_overrides ?? bot_defaults ?? false role-activation gate, D-07)
    - apps/api/src/lib/bot-arbitrator.ts (runArbitration scorer isolation; analystScorer registration)
    - apps/api/src/lib/bot-registration.ts (analystScorer hardcoded 0 — the extension point its own comment anticipates)
    - apps/api/src/lib/skills.ts (COACH_SKILLS/ANALYST_SKILLS — Plan 05)
    - apps/api/src/graph/state.ts (firingSkillId/firingSkillRole/skillMeta — Plan 01)
    - .planning/phases/12-graph-coherence-extended-triggers/12-AI-SPEC.md (Section 3 triggerGateNode skeleton — implementation-ready)
    - .planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md (Pattern 1; Pitfalls 5 fail-isolation)
  </read_first>
  <behavior>
    - a session with Coach disabled and Analyst disabled evaluates zero Skills and returns { firingSkillId: null, firingSkillRole: null }
    - a disabled Role's Skills never get detect() called (asserted via spies) — role-gate before candidate list assembly (D-07)
    - the first firing Skill (array/priority order) wins; firingSkillId/firingSkillRole/skillMeta are set from it
    - a throwing Skill.detect() does not block sibling Skills and does not make TriggerGateNode throw (Promise.allSettled)
    - a missing blueprint fails open: console.error + { firingSkillId: null, firingSkillRole: null }, never throws
    - analystScorer returns a positive affinity when an Analyst Skill's context is present (firingSkillRole==='analyst'/firingSkillId is orphan-edge or fact-check), 0 otherwise
  </behavior>
  <action>
    Create `apps/api/src/graph/nodes/trigger-gate.ts` exporting `triggerGateNode(state, config?)` following the AI-SPEC Section 3 skeleton exactly: read `config.configurable.blueprint` (fail open with `[trigger-gate]` console.error + `{ firingSkillId: null, firingSkillRole: null }` if missing); compute `coachEnabled`/`analystEnabled` via `config.configurable.botOverrides?.[role] ?? blueprint.bot_defaults?.[role] ?? false` (D-07) ONCE before assembling `candidateSkills = [...(coachEnabled?COACH_SKILLS:[]), ...(analystEnabled?ANALYST_SKILLS:[])]`; run `Promise.allSettled(candidateSkills.map(s => ({ skill: s, result: await s.detect({state,blueprint,config}) })))`; iterate results, skip `rejected` (console.warn), and on the first `fires` return `{ firingSkillId, firingSkillRole, skillMeta }`; else return all-null. Detection-only — never streams, never writes messages, never calls a Role. Extend `bot-registration.ts`'s `analystScorer` (currently returns 0) to return a positive affinity when the arbitration context indicates a firing Analyst Skill (orphan-edge or fact-check present), 0 otherwise (D-06). Add trigger-gate.test.ts (role-gate zero-eval, priority-order first-fires-wins, throwing-skill isolation, missing-blueprint fail-open) and bot-registration.test.ts assertion for the new analystScorer behavior.
  </action>
  <verify>
    <automated>pnpm --filter api test -- trigger-gate bot-registration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Promise.allSettled" apps/api/src/graph/nodes/trigger-gate.ts`
    - the role-gate (`bot_defaults` / `botOverrides` `?? false`) runs before candidateSkills is built (grep confirms order)
    - a disabled Role's Skills receive zero detect() calls (asserted with spies in trigger-gate.test.ts)
    - a throwing Skill leaves TriggerGateNode returning a valid (possibly null) result without throwing
    - `grep -vq "return 0" ` is NOT required, but analystScorer returns a positive value for an Analyst-Skill context (asserted in bot-registration.test.ts)
    - `pnpm --filter api test -- trigger-gate bot-registration` passes
  </acceptance_criteria>
  <done>TriggerGateNode is a role-gated, fail-isolated, detection-only node; analystScorer scores by firing Skill; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: graph.ts topology — human-path + proactive-path triggerGate wiring with loop guard</name>
  <files>apps/api/src/graph/graph.ts</files>
  <read_first>
    - apps/api/src/graph/graph.ts (full: routeFromStart, routeAfterOrchestrator, addNode/addEdge/addConditionalEdges wiring lines ~110-146; header comment style for documenting edge-replacement hazards)
    - apps/api/src/graph/nodes/trigger-gate.ts (Task 1)
    - apps/api/src/graph/state.ts (triggerGateComplete loop-guard field — Plan 01)
    - .planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md (Pitfall 3 fixed+conditional fan-out; Pitfall 4 pathsMap exhaustiveness; "Open architecture question" — RESOLVED by the locked human-path decision)
    - .planning/phases/12-graph-coherence-extended-triggers/12-AI-SPEC.md (Section 3 routeAfterTriggerGate + conditional-edge snippet)
  </read_first>
  <action>
    Modify `graph.ts` to insert the TriggerGateNode into both paths and guarantee termination:
    (a) `.addNode('triggerGate', triggerGateNode)`.
    (b) Add `routeAfterTriggerGate(state): 'facilitation' | 'analysis' | 'end'` — 'coach'→'facilitation', 'analyst'→'analysis', else 'end' (mirrors routeAfterOrchestrator); it sets no state, only reads firingSkillRole. TriggerGateNode itself sets `triggerGateComplete: true` in its return.
    (c) HUMAN PATH (locked decision 1): replace the fixed `.addEdge('mutationGate', END)` with a conditional `routeAfterMutationGate(state): 'triggerGate' | 'end'` that returns 'triggerGate' when `state.triggerGateComplete !== true` else 'end' — so the first pass (after agent commits a CanvasNode) reaches triggerGate, and the second pass (analysis→mutationGate after a Skill fired) terminates at END. Wire `.addConditionalEdges('mutationGate', routeAfterMutationGate, { triggerGate: 'triggerGate', end: END })`.
    (d) PROACTIVE PATH: replace the fixed `.addEdge('argGraphBuilder', 'analysis')` with `.addConditionalEdges('argGraphBuilder', routeAfterArgGraphBuilder, { analysis: 'analysis', triggerGate: 'triggerGate' })` where `routeAfterArgGraphBuilder` returns 'analysis' when `state.triggerType === 'analysis_request'` (Phase 11 behavior preserved) else 'triggerGate' (Pitfall 3 — replace, do not add alongside the fixed edge).
    (e) `.addConditionalEdges('triggerGate', routeAfterTriggerGate, { facilitation: 'facilitation', analysis: 'analysis', end: END })` — all three router return values MUST be pathsMap keys, 'end'→END (Pitfall 4).
    Keep `analysis → mutationGate` (line 146) — after a Skill routes to analysis, its ADD_EDGE CanvasOp commits via mutationGate, which now returns 'end' because triggerGateComplete is true (no infinite loop). Document each replacement with a header comment mirroring the existing Phase 11 START-edge replacement comment style. Update the graph.ts header topology diagram to reflect the new edges.
  </action>
  <verify>
    <automated>pnpm --filter api exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "routeAfterTriggerGate" apps/api/src/graph/graph.ts && grep -q "routeAfterMutationGate" apps/api/src/graph/graph.ts && grep -q "routeAfterArgGraphBuilder" apps/api/src/graph/graph.ts`
    - the fixed `.addEdge('mutationGate', END)` and `.addEdge('argGraphBuilder', 'analysis')` are REMOVED (grep -v confirms neither literal remains) and replaced by conditional edges
    - `routeAfterTriggerGate`'s three return values (facilitation/analysis/end) all appear as pathsMap keys
    - `routeAfterMutationGate` gates on `state.triggerGateComplete` to prevent re-entry
    - `pnpm --filter api exec tsc --noEmit` clean
  </acceptance_criteria>
  <done>TriggerGateNode reachable from human + proactive paths; fixed edges replaced with conditionals; loop guard in place; compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Graph integration tests — routing correctness + termination (no infinite loop)</name>
  <files>apps/api/src/graph/graph.test.ts, apps/api/src/graph/graph.integration.test.ts</files>
  <read_first>
    - apps/api/src/graph/graph.test.ts (existing describe-block convention — this codebase extends this file rather than per-node test files; the SPIKE block documenting LangGraph 1.4.7 pathsMap-unmapped rejection)
    - apps/api/src/graph/graph.integration.test.ts (existing integration harness, MemorySaver + injected adapters)
    - apps/api/src/graph/nodes/trigger-gate.ts, apps/api/src/graph/graph.ts (Tasks 1-2)
    - .planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md (Validation Architecture test map; Pitfall 4 warning signs)
  </read_first>
  <action>
    Extend `graph.test.ts` and `graph.integration.test.ts` (per this codebase's "extend the integration file, no per-node test files" convention) with new describe blocks asserting: (a) a human-message run where a Coach Skill fires routes agent→mutationGate→triggerGate→facilitation→END and TERMINATES (no recursion-limit rejection); (b) a human-message run where an Analyst Skill fires reaches triggerGate→analysis→mutationGate→END and terminates (loop guard verified — mutationGate second pass returns END); (c) a human-message run where NO Skill fires routes triggerGate→END silently, invoking no Role node; (d) the proactive analysis_request path still reaches analysis (Phase 11 behavior preserved) and terminates; (e) firingSkillRole matches the routing outcome (coach→facilitation, analyst→analysis); (f) a pathsMap-exhaustiveness guard (Pitfall 4) — any router return value is mapped. Use MemorySaver + injected fake Skills (via config.configurable) that deterministically fire/not-fire. Assert no graph.invoke() promise rejection and no recursion-limit error on any path.
  </action>
  <verify>
    <automated>pnpm --filter api test -- graph</automated>
  </verify>
  <acceptance_criteria>
    - integration tests assert termination (no recursion-limit / no rejected promise) for: Coach-fires, Analyst-fires, no-fire, and proactive analysis_request paths
    - a no-fire human turn invokes zero Role nodes (asserted via node-invocation spy)
    - firingSkillRole 'coach'→facilitation and 'analyst'→analysis routing asserted
    - `pnpm --filter api test -- graph` passes; full `pnpm --filter api test` is green
  </acceptance_criteria>
  <done>Integration tests prove correct routing and guaranteed termination on all four paths; full API suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human/proactive invocation → TriggerGateNode | Every message now passes an additional detection gate that fans out untrusted-content Skills |
| Skill detect() exceptions → graph execution | A throwing Skill must not abort the graph or block siblings |
| Router return value → LangGraph pathsMap | An unmapped return silently rejects graph.invoke() with no clear trace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Denial of Service (infinite loop) | mutationGate↔triggerGate↔analysis cycle | mitigate | triggerGateComplete loop guard in routeAfterMutationGate; integration test asserts termination (no recursion-limit) on every path |
| T-12-17 | Denial of Service (edge fan-out) | fixed + conditional edge from same source | mitigate | Fixed edges removed and replaced with conditionals (Pitfall 3); grep asserts the fixed literals are gone |
| T-12-18 | Tampering (silent routing failure) | pathsMap exhaustiveness | mitigate | routeAfterTriggerGate's three returns all mapped; test guard per Pitfall 4 |
| T-12-19 | Denial of Service (Skill exception) | TriggerGateNode fan-out | mitigate | Promise.allSettled + per-result rejection handling — one throwing Skill never blocks the gate (D-05, CR-02 precedent) |
| T-12-20 | Elevation (disabled Role fires) | role-activation gate | mitigate | D-07 bot_overrides ?? bot_defaults ?? false checked once before candidate assembly; test asserts zero detect() on disabled Role |
| T-12-SC | Tampering | package installs | accept | No installs in this plan |
</threat_model>

<verification>
- `pnpm --filter api test -- trigger-gate bot-registration graph` green
- Full `pnpm --filter api test` green
- `pnpm --filter api exec tsc --noEmit` clean
- No infinite loop / recursion-limit on any path (integration-asserted)
</verification>

<success_criteria>
The detection gate is live on both the human and proactive paths, role-gated and fail-isolated, with a proven-terminating topology; analystScorer scores by firing Skill; all four new Skills route to the correct Role node. Full API suite green.
</success_criteria>

<output>
Create `.planning/phases/12-graph-coherence-extended-triggers/12-06-SUMMARY.md` when done
</output>
