---
phase: 12-graph-coherence-extended-triggers
plan: 04
type: tdd
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - apps/api/src/lib/skills/orphan-edge.ts
  - apps/api/src/lib/skills/orphan-edge.test.ts
  - apps/api/src/lib/skills/fact-check.ts
  - apps/api/src/lib/skills/fact-check.test.ts
autonomous: true
requirements: [GRAPH-03, TRIGGER-04, TRIGGER-05, COST-01, COST-02]
must_haves:
  truths:
    - "orphan-edge detects a committed CanvasNode with zero edges only after the first 3 committed nodes exist; it queries canvas_nodes/canvas_edges (NOT state.argGraph)"
    - "orphan-edge's ghost-edge fallback targets only status='committed' nodes, ranked by cosine similarity of node.label"
    - "fact-check never reaches an LLM without passing the tier-1 heuristic pre-filter first (three-tier escalation)"
    - "fact-check tier-2 resolves TASK_MODELS[provider].classification; tier-3 resolves TASK_MODELS[provider].analysis — for anthropic, openai, and gemini"
    - "detection tier-1 makes zero adapter calls"
    - "fact-check's buildPromptGuidance() cites the specific claim via summarizeArgGraph and asks for a source, staying in uncertainty framing and never emitting a confident counter-assertion (D-15)"
  artifacts:
    - path: "apps/api/src/lib/skills/orphan-edge.ts"
      provides: "Analyst orphan-edge Skill + ghost-edge cosine fallback (D-13/D-14)"
      contains: "committed"
    - path: "apps/api/src/lib/skills/fact-check.ts"
      provides: "Analyst fact-check Skill, three-tier escalation gate (D-12/COST-02)"
      contains: "classification"
  key_links:
    - from: "apps/api/src/lib/skills/orphan-edge.ts"
      to: "Supabase canvas_nodes/canvas_edges"
      via: "committed-status query via config.configurable service client"
      pattern: "canvas_nodes"
    - from: "apps/api/src/lib/skills/fact-check.ts"
      to: "apps/api/src/lib/model-config.ts"
      via: "TASK_MODELS[provider].classification (tier-2) / .analysis (tier-3)"
      pattern: "TASK_MODELS"
---

<objective>
Implement the two Analyst Skills: `orphan-edge` (detects committed CanvasNodes with no edges after the first 3 nodes and proposes a typed edge, falling back to a ghost edge to the most cosine-similar committed node by label — GRAPH-03/TRIGGER-04, D-13/D-14) and `fact-check` (three-tier escalation gate: heuristic → light classifier → capable analysis — TRIGGER-05/COST-01/COST-02). Includes the deterministic cost-tier routing assertion test that guards the silent-BYOK-billing-regression failure mode (Pitfall 7).

Purpose: These are the Analyst's capabilities the TriggerGateNode (Plan 06) evaluates; both have defined I/O, so this is a TDD plan.
Output: Two Skill modules + tests, including a cross-provider TASK_MODELS routing assertion.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md
@.planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md
@.planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md

<interfaces>
Skill / SkillContext (apps/api/src/lib/skills.ts, Plan 01)
SkillDetectionResult (@panelito/types): { fires; confidence; meta? }
factCheckClassificationTool (@panelito/types, Plan 01): name 'classify_fact_check_need', params needs_fact_check + confidence
embeddings.ts (Plan 02): embed, cosineSimilarity (ghost-edge label ranking, D-13)
TASK_MODELS (apps/api/src/lib/model-config.ts): Record<ProviderName, Record<TaskType, string>>;
  tiers exist already — classification (light), analysis (capable) — for anthropic/openai/gemini
CanvasNodeSchema/CanvasEdgeSchema (packages/types/src/canvas.ts) HAVE a status field (committed|ghost|silent).
ArgNodeSchema/ArgEdgeSchema (packages/types/src/bot.ts) do NOT — do not use argGraph for committed detection.
orchestrator.ts adapter.stream classification-call shape (maxTokens small, tool-use) is the tier-2 analog.
analytics-agent.ts factCheckFraming seam (line ~121) is the tier-3 wiring target (set by Plan 06).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: orphan-edge Skill (committed-orphan detection + ghost-edge cosine fallback, D-13/D-14)</name>
  <files>apps/api/src/lib/skills/orphan-edge.ts, apps/api/src/lib/skills/orphan-edge.test.ts</files>
  <read_first>
    - packages/types/src/canvas.ts (CanvasNodeSchema/CanvasEdgeSchema HAVE status; CanvasNodeStatusSchema = committed|ghost|silent)
    - packages/types/src/bot.ts (ArgNodeSchema/ArgEdgeSchema have NO status — Pitfall 1: do NOT use state.argGraph for committed detection)
    - apps/api/src/lib/silence-gate.ts (Supabase-query + fail-safe posture to copy: on error, fail closed, never throw)
    - apps/api/src/lib/embeddings.ts (embed + cosineSimilarity for the ghost-edge label ranking, Plan 02)
    - apps/api/src/lib/bot-context.ts (escapeUntrustedText + <<<...DATA>>> WR-06 for node-label interpolation)
    - .planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md (D-13 label-only embedding; D-14 committed-only targets; GRAPH-03 two-step mechanism)
    - .planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md (CRITICAL Architecture Finding: two-step orphan-edge, node commits then a LATER invocation detects the orphan)
  </read_first>
  <behavior>
    - detect() does NOT fire when fewer than 3 committed nodes exist on the branch
    - detect() fires when a committed CanvasNode with zero edges exists after the first 3 committed nodes
    - detect() queries canvas_nodes/canvas_edges (committed status) via the injected service client — never reads state.argGraph
    - the ghost-edge fallback ranks only status='committed' candidate targets by cosine similarity of node.label and picks the top match; a ghost/silent node is never a target
    - on any Supabase error detect() returns { fires: false, confidence: 0 } and does not throw
    - buildPromptGuidance() WR-06-escapes any interpolated node label
  </behavior>
  <action>
    Create `orphan-edge.ts` exporting `orphanEdgeSkill: Skill` with `id: 'orphan-edge'`, `role: 'analyst'`. `detect()` uses the injected Supabase service client (from SkillContext.config) to count committed `canvas_nodes` for the branch; if < 3, returns `{ fires: false }`. Otherwise it finds committed nodes with zero rows in `canvas_edges` (as source or target); if an orphan exists, fires with `meta: { orphanNodeId, orphanLabel, ghostTargetNodeId? }`. Implement the D-13 ghost-edge fallback as a helper that embeds the orphan node's `label` and each candidate COMMITTED node's `label` (D-14 — exclude ghost/silent), ranks by `cosineSimilarity`, returns the top match — this is the fallback the Analyst uses when its own LLM edge proposal (Plan 06 tier-3) finds no strong connection. Query `canvas_nodes`/`canvas_edges` ONLY — never `state.argGraph` (Pitfall 1). Fail-closed on Supabase error (`console.error('[orphan-edge] ...')`, return `{ fires: false, confidence: 0 }`). `buildPromptGuidance()` instructs the Analyst to propose a typed Blueprint-vocabulary edge citing the orphan node, WR-06-escaping the label. In orphan-edge.test.ts (mock Supabase + embeddings): assert no-fire under 3 nodes; fire on a committed orphan past 3; ghost fallback picks the top committed cosine match and never a ghost node; a Supabase error yields no-fire without throwing.
  </action>
  <verify>
    <automated>pnpm --filter api test -- orphan-edge</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "canvas_nodes" apps/api/src/lib/skills/orphan-edge.ts` and `grep -Lq "state.argGraph" apps/api/src/lib/skills/orphan-edge.ts` (does NOT read argGraph for committed detection)
    - the ghost-edge fallback filters candidate targets to status='committed' (grep confirms `committed`)
    - detect() returns no-fire below 3 committed nodes and on Supabase error (asserted in tests)
    - buildPromptGuidance node-label interpolation produces the `<<<` WR-06 delimiter
    - `pnpm --filter api test -- orphan-edge` passes all behavior cases
  </acceptance_criteria>
  <done>orphan-edge detects committed orphans past the 3-node threshold against canvas tables, ghost-fallback ranks committed-only targets; tests green.</done>
</task>

<task type="tdd">
  <name>Task 2: fact-check Skill (three-tier escalation gate + cross-provider routing assertion, D-12/COST-01/COST-02)</name>
  <files>apps/api/src/lib/skills/fact-check.ts, apps/api/src/lib/skills/fact-check.test.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/orchestrator.ts (adapter.stream classification call shape, lines ~102-135: small maxTokens, model=TASK_MODELS[provider].classification)
    - apps/api/src/lib/model-config.ts (TASK_MODELS full shape: classification=light, analysis=capable, all 3 providers — no new TaskType needed)
    - apps/api/src/graph/nodes/analytics-agent.ts (factCheckFraming seam line ~121 — tier-3 is analyticsAgentNode, wired live in Plan 06; detect() does NOT call tier-3 itself, D-06)
    - apps/api/src/graph/nodes/arg-graph-builder.ts (safeParse + bounded-retry-with-correction idiom for tool output; use 1 retry for this cheap binary classifier)
    - packages/types/src/fact-check-tool.ts (factCheckClassificationTool, Plan 01)
    - apps/api/src/lib/bot-context.ts (summarizeArgGraph for claim citation context, GRAPH-04; escapeUntrustedText WR-06)
    - .planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md (D-12 claim-shaped tier-1 patterns; D-15 uncertainty framing, cite claim + ask for source)
    - .planning/phases/12-graph-coherence-extended-triggers/12-AI-SPEC.md (Section 4 Pattern 4 three-tier sequencing; Pitfall 7 silent cost regression; Dimension 5 routing test)
  </read_first>
  <behavior>
    - tier-1 looksLikeCheckableClaim() is a pure function (numbers+units, dates/years, absolute qualifiers "siempre/nunca/todos saben que", capitalized entity-like phrases); returns false → detect() returns { fires: false } with ZERO adapter calls
    - tier-2 runs only when tier-1 matched; resolves model = TASK_MODELS[provider].classification; on needs_fact_check=false → { fires: false }
    - tier-2 tool output is safeParsed; a parse failure retries once then fails CLOSED ({ fires: false, confidence: 0 })
    - tier-3 is NOT called inside detect() (D-06) — detect() sets meta { claimMessageId } and fires; tier-3 (analyticsAgentNode) is wired by Plan 06
    - routing assertion: for provider in {anthropic, openai, gemini}, the tier-2 resolved model === TASK_MODELS[provider].classification and the tier-3 target task === analysis (never reversed)
    - buildPromptGuidance() uses uncertainty framing (cites the specific claim via summarizeArgGraph, asks for a source; never a confident counter-assertion) and WR-06-escapes the cited claim
  </behavior>
  <action>
    Create `fact-check.ts` exporting `factCheckSkill: Skill` with `id: 'fact-check'`, `role: 'analyst'`, plus a pure exported `looksLikeCheckableClaim(text: string): boolean` (tier-1, D-12 — zero I/O). `detect()`: tier-1 runs `looksLikeCheckableClaim` on the last message; if false, return `{ fires: false, confidence: 0 }` with no adapter call. If matched, tier-2 calls the injected classifier adapter's `stream()` with `factCheckClassificationTool`, `model: TASK_MODELS[providerName].classification`, `maxTokens: 64`, safeParsing the tool output (`ClassifyOutputSchema`) with ONE retry then fail-closed. If tier-2 `needs_fact_check` is true, return `{ fires: true, confidence, meta: { claimMessageId } }` — do NOT call tier-3 here (D-06: detection-only; tier-3 = analyticsAgentNode with factCheckFraming, wired in Plan 06). `buildPromptGuidance()` returns Spanish uncertainty-framed guidance that cites the specific claim (via summarizeArgGraph context, GRAPH-04) and asks for a source (D-15), WR-06-escaping the cited claim. Fail-silent throughout. In fact-check.test.ts: (a) a non-checkable opinion returns no-fire with zero adapter calls; (b) a checkable claim escalates to tier-2 with the classification-tier model; (c) tier-2 parse failure retries once then fails closed; (d) a DETERMINISTIC routing assertion looping over anthropic/openai/gemini that tier-2 resolves `TASK_MODELS[provider].classification` and tier-3's task type is `analysis` (Pitfall 7 — no type/runtime signal otherwise); (e) buildPromptGuidance never emits a confident-counter-assertion phrase and WR-06-escapes the claim.
  </action>
  <verify>
    <automated>pnpm --filter api test -- fact-check model-config</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "looksLikeCheckableClaim" apps/api/src/lib/skills/fact-check.ts` and it is pure (no adapter/supabase use inside it)
    - tier-1 non-match path makes zero adapter.stream calls (asserted via a spy in tests)
    - `grep -q "TASK_MODELS" apps/api/src/lib/skills/fact-check.ts` resolving `.classification` for tier-2
    - the cross-provider routing test asserts classification (tier-2) and analysis (tier-3) mapping for all three of anthropic/openai/gemini and fails if reversed
    - detect() does not invoke tier-3/analyticsAgentNode (grep confirms no analytics call inside detect)
    - `pnpm --filter api test -- fact-check` passes all five behavior groups
  </acceptance_criteria>
  <done>fact-check enforces heuristic→classification→analysis escalation, never skips tier-1, routes tiers correctly across all providers, uncertainty-frames its guidance; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Participant claim text → fact-check tiers | Untrusted content drives escalation; only checkable claims reach a paid LLM |
| Participant node label → orphan-edge guidance / ghost edge | Untrusted label embedded and interpolated into a system prompt |
| TASK_MODELS tier resolution → creator BYOK bill | Wrong tier silently inflates the creator's own invoice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering (cost/billing) | fact-check TASK_MODELS tier resolution | mitigate | Deterministic per-provider unit assertion that tier-2=classification, tier-3=analysis, never reversed (Pitfall 7, Dimension 5) — no type/runtime signal otherwise; fail-closed on mismatch |
| T-12-10 | Tampering (prompt injection) | fact-check claim citation / orphan-edge label in buildPromptGuidance | mitigate | WR-06 escapeUntrustedText() + <<<...DATA>>> for all interpolated participant content (Dimension 7, Critical) |
| T-12-11 | Elevation / graph corruption | ghost-edge target status | mitigate | D-14 committed-only target filter; Mutation Gate (Plan 06) also rejects non-committed edge targets |
| T-12-12 | Spoofing (bad classifier output) | tier-2 tool output | mitigate | safeParse + 1 retry + fail-closed (fires:false) — a hallucinated positive never spams an unwarranted Analyst challenge |
| T-12-SC | Tampering | package installs | accept | No installs in this plan |
</threat_model>

<verification>
- `pnpm --filter api test -- orphan-edge fact-check model-config` green
- Tier-1 paths (orphan-edge under-3-nodes, fact-check non-claim) make zero adapter calls
- Cross-provider routing assertion passes for anthropic/openai/gemini
</verification>

<success_criteria>
orphan-edge detects committed orphans and ghost-falls-back to committed-only cosine matches; fact-check enforces the three-tier gate with correct per-provider tier routing; all detection tier-1 is $0. Tests green.
</success_criteria>

<output>
Create `.planning/phases/12-graph-coherence-extended-triggers/12-04-SUMMARY.md` when done
</output>
