---
phase: 12-graph-coherence-extended-triggers
plan: 05
type: execute
wave: 3
depends_on: [12-01, 12-03, 12-04]
files_modified:
  - apps/api/src/lib/skills.ts
  - apps/api/src/graph/nodes/facilitation-agent.ts
  - apps/api/src/graph/nodes/facilitation-agent.test.ts
  - apps/api/src/graph/nodes/analytics-agent.ts
  - apps/api/src/graph/nodes/analytics-agent.test.ts
autonomous: true
requirements: [TRIGGER-03, TRIGGER-04, TRIGGER-05, TRIGGER-06]
must_haves:
  truths:
    - "COACH_SKILLS and ANALYST_SKILLS are populated with the five Phase 12 Skills in priority order"
    - "when a Coach Skill fired, FacilitationAgentNode splices that Skill's buildPromptGuidance() output into the system prompt AFTER argGraph context and BEFORE the Personality voice (D-03 order preserved)"
    - "when a Analyst Skill fired, AnalyticsAgentNode splices the guidance in the same slot; fact-check firing sets factCheckFraming live"
    - "the Role behavioral contract (step 1) always dominates — Skill guidance never precedes or overrides it"
  artifacts:
    - path: "apps/api/src/lib/skills.ts"
      provides: "populated COACH_SKILLS/ANALYST_SKILLS registries"
      contains: "orphanEdgeSkill"
    - path: "apps/api/src/graph/nodes/facilitation-agent.ts"
      provides: "Coach Skill buildPromptGuidance injection slot"
      contains: "buildPromptGuidance"
    - path: "apps/api/src/graph/nodes/analytics-agent.ts"
      provides: "Analyst Skill injection slot + live factCheckFraming"
      contains: "firingSkillId"
  key_links:
    - from: "apps/api/src/lib/skills.ts"
      to: "apps/api/src/lib/skills/*.ts"
      via: "import all five Skill objects into the registries"
      pattern: "COACH_SKILLS"
    - from: "apps/api/src/graph/nodes/analytics-agent.ts"
      to: "state.firingSkillId"
      via: "factCheckFraming enabled when firingSkillId==='fact-check'"
      pattern: "fact-check"
---

<objective>
Make the five Skills usable by the Role nodes: populate the `COACH_SKILLS`/`ANALYST_SKILLS` registries (priority order) and add the `buildPromptGuidance()` injection slot to `FacilitationAgentNode` (Coach) and `AnalyticsAgentNode` (Analyst), preserving the fixed D-03 composition order (Role rules → Blueprint → argGraph → Skill guidance → Personality). Wire fact-check's tier-3 live by enabling `factCheckFraming` when the firing Skill is `fact-check`.

Purpose: The TriggerGateNode (Plan 06) sets `state.firingSkillId`; the Role nodes must read it and splice the corresponding guidance. Registries must exist before the gate can enumerate candidate Skills. This is the bridge between the Skills (Plans 03/04) and the graph wiring (Plan 06).
Output: Populated registries; both Role nodes injecting Skill guidance; factCheckFraming live.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md
@.planning/phases/12-graph-coherence-extended-triggers/12-AI-SPEC.md
@.planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md

<interfaces>
skills.ts (Plan 01): empty COACH_SKILLS/ANALYST_SKILLS arrays + Skill/SkillContext interfaces
Skill objects (Plans 03/04): silenceBreakSkill, driftRedirectSkill, moderationSkill (coach);
  orphanEdgeSkill, factCheckSkill (analyst)
GraphState (Plan 01): firingSkillId, firingSkillRole, skillMeta, triggerGateComplete
facilitation-agent.ts buildCoachSystemPrompt: composition order Role rules → Blueprint → argGraph → Personality
analytics-agent.ts: factCheckFraming read at line ~121 (config.configurable.factCheckFraming === true);
  the conditional roleRulesLines.push block (lines ~69-80) is the existing precedent to mirror
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate COACH_SKILLS / ANALYST_SKILLS registries in priority order</name>
  <files>apps/api/src/lib/skills.ts</files>
  <read_first>
    - apps/api/src/lib/skills.ts (empty registries + Skill interface from Plan 01)
    - apps/api/src/lib/skills/silence-break.ts, drift-redirect.ts, moderation.ts (Coach Skill exports — Plan 03)
    - apps/api/src/lib/skills/orphan-edge.ts, fact-check.ts (Analyst Skill exports — Plan 04)
    - .planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md (D-02 Skill roster per Role)
    - .planning/phases/12-graph-coherence-extended-triggers/12-AI-SPEC.md (Section 4: registration order = priority order; first firing Skill wins)
  </read_first>
  <action>
    In `apps/api/src/lib/skills.ts` import the five Skill objects and populate the registries in priority order (first-firing-Skill-wins in TriggerGateNode). Set `COACH_SKILLS = [silenceBreakSkill, moderationSkill, driftRedirectSkill]` and `ANALYST_SKILLS = [factCheckSkill, orphanEdgeSkill]` (D-02 roster; order documents priority — a comment noting order = priority per AI-SPEC Section 4). Do not add a mutable Map registry — a plain exported const array is sufficient per PATTERNS.md.
  </action>
  <verify>
    <automated>pnpm --filter api exec tsc --noEmit && pnpm --filter api test -- skills</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "silenceBreakSkill" apps/api/src/lib/skills.ts && grep -q "moderationSkill" apps/api/src/lib/skills.ts && grep -q "driftRedirectSkill" apps/api/src/lib/skills.ts`
    - `grep -q "factCheckSkill" apps/api/src/lib/skills.ts && grep -q "orphanEdgeSkill" apps/api/src/lib/skills.ts`
    - COACH_SKILLS has 3 entries, ANALYST_SKILLS has 2 entries, both in a documented priority order
    - `pnpm --filter api exec tsc --noEmit` clean
  </acceptance_criteria>
  <done>Both registries are populated with all five Skills in priority order; compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FacilitationAgentNode (Coach) Skill-guidance injection slot</name>
  <files>apps/api/src/graph/nodes/facilitation-agent.ts, apps/api/src/graph/nodes/facilitation-agent.test.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/facilitation-agent.ts (buildCoachSystemPrompt composition order lines ~44-87 — insert AFTER argGraph context, BEFORE Personality voice; D-03 order NEVER reordered)
    - apps/api/src/lib/skills.ts (COACH_SKILLS after Task 1)
    - apps/api/src/graph/state.ts (firingSkillId/skillMeta — Plan 01)
    - .planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md (facilitation-agent.ts / analytics-agent.ts MODIFIED section — buildPromptGuidance injection slot)
  </read_first>
  <behavior>
    - when state.firingSkillId matches a COACH_SKILLS entry, the constructed system prompt contains that Skill's buildPromptGuidance() output
    - the guidance block appears AFTER the argGraph context section and BEFORE the Personality voice section (D-03 order)
    - when no Coach Skill fired (firingSkillId null or not a Coach Skill), no guidance block is added and existing behavior is unchanged
  </behavior>
  <action>
    In `facilitation-agent.ts`, after the argGraph context is composed and before the Personality voice is appended, look up the firing Coach Skill via `COACH_SKILLS.find(s => s.id === state.firingSkillId)` and, if found, splice its `buildPromptGuidance({ state, blueprint, config })` output as its own labeled section (e.g. an "Active trigger guidance" block). Do NOT let it precede or override the Role behavioral contract (step 1) — position it in the same relative slot analytics-agent.ts uses for its factCheckFraming block. Preserve the existing `Partial<GraphState>` return and triggerMetadata mechanism unchanged. Add facilitation-agent.test.ts cases (extend existing file) for the three behaviors, using an injected fake Coach Skill or the real drift-redirect Skill with a stub buildPromptGuidance.
  </action>
  <verify>
    <automated>pnpm --filter api test -- facilitation-agent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "buildPromptGuidance" apps/api/src/graph/nodes/facilitation-agent.ts` and the lookup uses `state.firingSkillId`
    - the guidance block is positioned after argGraph context and before Personality voice (verified by a test asserting substring order in the constructed prompt)
    - a non-firing turn produces a prompt identical to pre-change behavior (regression assertion)
    - `pnpm --filter api test -- facilitation-agent` passes
  </acceptance_criteria>
  <done>Coach node splices firing-Skill guidance in the correct D-03 slot; non-firing turns unchanged; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AnalyticsAgentNode (Analyst) injection slot + live factCheckFraming</name>
  <files>apps/api/src/graph/nodes/analytics-agent.ts, apps/api/src/graph/nodes/analytics-agent.test.ts</files>
  <read_first>
    - apps/api/src/graph/nodes/analytics-agent.ts (factCheckFraming seam line ~121; conditional roleRulesLines.push block lines ~69-80 — the precedent to mirror; composition order lines ~51-109)
    - apps/api/src/lib/skills.ts (ANALYST_SKILLS after Task 1)
    - apps/api/src/graph/state.ts (firingSkillId — Plan 01)
    - .planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md (D-06 tier-3 = analyticsAgentNode with factCheckFraming; D-15 uncertainty framing)
    - .planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md (fact-check tier-3 wiring; facilitation/analytics MODIFIED section)
  </read_first>
  <behavior>
    - when state.firingSkillId matches an ANALYST_SKILLS entry, the system prompt contains that Skill's buildPromptGuidance() output in the correct slot
    - when state.firingSkillId === 'fact-check', factCheckFraming is enabled live (the existing factCheckFraming block activates) even if config.configurable.factCheckFraming is not separately set
    - when no Analyst Skill fired, existing behavior is unchanged
  </behavior>
  <action>
    In `analytics-agent.ts`, derive `factCheckFraming` as `config?.configurable?.factCheckFraming === true || state.firingSkillId === 'fact-check'` so a firing fact-check Skill activates the existing uncertainty-framing block live (realizing the Phase 11 "a future trigger (Phase 12) sets it" seam, D-06/D-15). Additionally, look up the firing Analyst Skill via `ANALYST_SKILLS.find(s => s.id === state.firingSkillId)` and splice its `buildPromptGuidance({ state, blueprint, config })` output as its own labeled section in the same slot the factCheckFraming block occupies (after Role rules, alongside the existing conditional block) — never overriding the Role contract. Preserve the existing return shape and triggerMetadata. Extend analytics-agent.test.ts: firing 'fact-check' enables the factCheckFraming block; firing 'orphan-edge' splices its guidance; a non-firing turn is unchanged.
  </action>
  <verify>
    <automated>pnpm --filter api test -- analytics-agent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "firingSkillId === 'fact-check'" apps/api/src/graph/nodes/analytics-agent.ts` (or equivalent) enabling factCheckFraming live
    - `grep -q "ANALYST_SKILLS" apps/api/src/graph/nodes/analytics-agent.ts` with a lookup by state.firingSkillId
    - a fact-check firing turn's prompt contains the uncertainty-framing block; an orphan-edge firing turn contains the orphan guidance
    - a non-firing turn produces the pre-change prompt (regression assertion)
    - `pnpm --filter api test -- analytics-agent` passes
  </acceptance_criteria>
  <done>Analyst node splices firing-Skill guidance and activates factCheckFraming live on fact-check firing; non-firing turns unchanged; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.firingSkillId → Role node prompt | Gate-set metadata drives which untrusted-content guidance is spliced |
| Skill guidance → system prompt slot | Guidance must never outrank the Role behavioral contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Tampering (prompt-order attack) | Skill guidance composition slot | mitigate | Guidance spliced AFTER Role rules and BEFORE Personality (D-03 fixed order) — Role contract structurally dominant; regression test asserts substring order |
| T-12-14 | Tampering (prompt injection) | interpolated participant content in guidance | mitigate | WR-06 escaping is enforced inside each Skill's buildPromptGuidance (Plans 03/04), unchanged here; this plan only positions the block |
| T-12-15 | Tampering (cost/behavior) | factCheckFraming live activation | mitigate | Enabled only when firingSkillId==='fact-check' (gate-controlled, three-tier-guarded upstream in Plan 04); no unconditional activation |
| T-12-SC | Tampering | package installs | accept | No installs in this plan |
</threat_model>

<verification>
- `pnpm --filter api test -- skills facilitation-agent analytics-agent` green
- `pnpm --filter api exec tsc --noEmit` clean
- Non-firing turns produce byte-identical prompts to pre-change (regression assertions)
</verification>

<success_criteria>
Registries populated; both Role nodes splice firing-Skill guidance in the correct D-03 slot; fact-check activates factCheckFraming live. Non-firing behavior unchanged. Tests green.
</success_criteria>

<output>
Create `.planning/phases/12-graph-coherence-extended-triggers/12-05-SUMMARY.md` when done
</output>
