---
phase: 12-graph-coherence-extended-triggers
plan: 03
type: tdd
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - apps/api/src/lib/skills/silence-break.ts
  - apps/api/src/lib/skills/silence-break.test.ts
  - apps/api/src/lib/skills/drift-redirect.ts
  - apps/api/src/lib/skills/drift-redirect.test.ts
  - apps/api/src/lib/skills/moderation.ts
  - apps/api/src/lib/skills/moderation.test.ts
  - apps/api/src/lib/bot-context.ts
autonomous: true
requirements: [TRIGGER-03, TRIGGER-06]
must_haves:
  truths:
    - "silence-break Skill's detect() wraps checkSilenceGate() unchanged; its delivery mechanism (silence-scan loop) is not altered"
    - "drift-redirect fires only on a sustained 3-message trailing window whose cosine similarity to the Blueprint domain centroid drops below threshold, and never when drift_detection_enabled=false"
    - "moderation heuristic pre-filter fires with zero LLM/adapter calls; tone escalates by moderation_count"
    - "every buildPromptGuidance() that echoes participant content wraps it via escapeUntrustedText() + <<<...DATA>>> framing (WR-06)"
  artifacts:
    - path: "apps/api/src/lib/skills/drift-redirect.ts"
      provides: "Coach drift-redirect Skill (ONNX cosine vs domain centroid, D-08/D-09)"
      contains: "drift_detection_enabled"
    - path: "apps/api/src/lib/skills/moderation.ts"
      provides: "Coach moderation Skill (heuristic pre-filter + escalation tone, D-11/D-16)"
      contains: "checkModerationHeuristic"
    - path: "apps/api/src/lib/skills/silence-break.ts"
      provides: "Coach silence-break Skill retrofit (D-03)"
      contains: "checkSilenceGate"
  key_links:
    - from: "apps/api/src/lib/skills/drift-redirect.ts"
      to: "apps/api/src/lib/embeddings.ts"
      via: "getDomainCentroid + embed + cosineSimilarity"
      pattern: "cosineSimilarity"
    - from: "apps/api/src/lib/skills/moderation.ts"
      to: "apps/api/src/lib/moderation-count.ts"
      via: "getModerationCount for tone escalation"
      pattern: "getModerationCount"
---

<objective>
Implement the three Coach Skills against the `Skill` contract: `silence-break` (retrofit of Phase 11's `checkSilenceGate()`, D-03), `drift-redirect` (ONNX cosine-similarity of the last 3 messages vs the static Blueprint domain centroid, D-08/D-09, TRIGGER-03), and `moderation` (deterministic Spanish heuristic pre-filter with `moderation_count`-keyed tone escalation, D-11/D-16, TRIGGER-06). All three are `$0`-detection Skills — no LLM call during detection.

Purpose: These are the Coach's capabilities the TriggerGateNode (Plan 06) evaluates. Each is independently testable with defined inputs/outputs, so this is a TDD plan.
Output: Three new Skill modules + tests; a `CONTEXT_WINDOWS.driftCheck = 3` constant added to bot-context.ts.
</objective>

<execution_context>
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/workflows/execute-plan.md
@/home/jgm/dev/projects/web-projects/panelito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md
@.planning/phases/12-graph-coherence-extended-triggers/12-RESEARCH.md
@.planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md

<interfaces>
Skill / SkillContext (apps/api/src/lib/skills.ts, Plan 01):
  interface Skill { id; role: 'coach'|'analyst'; detect(ctx): Promise<SkillDetectionResult>; buildPromptGuidance(ctx): string }
  interface SkillContext { state: GraphState; blueprint: Blueprint; config?: any }
SkillDetectionResult (@panelito/types): { fires: boolean; confidence: number; meta?: Record<string,unknown> | null }

embeddings.ts (Plan 02): embed(text), cosineSimilarity(a,b), getDomainCentroid(blueprint)
moderation-count.ts (Plan 02): getModerationCount(supabase, branchId, participantId), incrementModerationCount(...)
silence-gate.ts (existing): checkSilenceGate(args): Promise<{ passed; reason?; presence_fallback }>
bot-context.ts (existing): CONTEXT_WINDOWS, escapeUntrustedText + <<<...DATA>>> framing (WR-06)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: silence-break Skill (retrofit checkSilenceGate onto Skill contract, D-03)</name>
  <files>apps/api/src/lib/skills/silence-break.ts, apps/api/src/lib/skills/silence-break.test.ts</files>
  <read_first>
    - apps/api/src/lib/silence-gate.ts (checkSilenceGate signature + SilenceGateResult — wrap verbatim, do NOT reimplement)
    - apps/api/src/graph/nodes/facilitation-agent.ts (buildCoachSystemPrompt content-aware question logic — reuse, do not duplicate the Spanish BAD/GOOD few-shot block)
    - apps/api/src/lib/skills.ts (Skill interface from Plan 01)
    - .planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md (D-03: delivery mechanism unchanged — silence-scan loop + direct-DB-insert still owns firing)
    - .planning/phases/12-graph-coherence-extended-triggers/12-PATTERNS.md (silence-break.ts section)
  </read_first>
  <behavior>
    - detect() returns { fires: true } when the wrapped checkSilenceGate() reports passed:true
    - detect() returns { fires: false } when checkSilenceGate() reports passed:false (typing or too_soon)
    - buildPromptGuidance() returns the Coach content-aware facilitation-question guidance string
    - the Skill's id is 'silence-break' and role is 'coach'
  </behavior>
  <action>
    Create `silence-break.ts` exporting a `silenceBreakSkill: Skill` with `id: 'silence-break'`, `role: 'coach'`. Its `detect()` calls the existing `checkSilenceGate()` (from the injected SkillContext.config seam) and maps `{ passed }` to `{ fires: passed, confidence: passed ? 1 : 0 }`. Its `buildPromptGuidance()` returns the existing Coach content-aware question guidance — reuse/extract from facilitation-agent.ts's `buildCoachSystemPrompt`, do not duplicate the Spanish few-shot block. Per D-03 do NOT change silence's actual delivery mechanism (silence-scan.ts's setInterval + direct-DB-insert stays as-is); this Skill is only the detect+guidance contract adapter. Write silence-break.test.ts covering the four behaviors with checkSilenceGate injected/mocked.
  </action>
  <verify>
    <automated>pnpm --filter api test -- silence-break</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "id: 'silence-break'" apps/api/src/lib/skills/silence-break.ts && grep -q "role: 'coach'" apps/api/src/lib/skills/silence-break.ts`
    - detect() delegates to checkSilenceGate (grep confirms the call), not a reimplementation
    - `pnpm --filter api test -- silence-break` passes all four behavior cases (RED→GREEN committed)
  </acceptance_criteria>
  <done>silence-break Skill wraps checkSilenceGate + Coach question guidance; delivery mechanism untouched; tests green.</done>
</task>

<task type="tdd">
  <name>Task 2: drift-redirect Skill (ONNX cosine vs domain centroid, D-08/D-09)</name>
  <files>apps/api/src/lib/skills/drift-redirect.ts, apps/api/src/lib/skills/drift-redirect.test.ts, apps/api/src/lib/bot-context.ts</files>
  <read_first>
    - apps/api/src/lib/embeddings.ts (embed, cosineSimilarity, getDomainCentroid — Plan 02)
    - apps/api/src/lib/silence-gate.ts (detect-result shape to mirror; fail-safe posture)
    - apps/api/src/lib/bot-context.ts (CONTEXT_WINDOWS constants; escapeUntrustedText + <<<...DATA>>> WR-06 framing at lines ~36-38, 88-95)
    - .planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md (D-08 static per-Blueprint centroid; D-09 drift_detection_enabled opt-out)
    - .planning/phases/12-graph-coherence-extended-triggers/12-AI-SPEC.md (Section 4b Context Window: driftCheck window = last 3 messages; Dimension 3 rubric — invitation not command)
  </read_first>
  <behavior>
    - detect() short-circuits { fires: false, confidence: 0 } immediately when blueprint.drift_detection_enabled === false (no embed() call)
    - detect() fires (fires:true) only when the last-3-message trailing window cosine similarity to the domain centroid is below threshold (default 0.6, Blueprint-configurable)
    - detect() does NOT fire on a single tangential message (needs the sustained trailing window)
    - buildPromptGuidance() frames redirect as an invitation ("¿cómo se conecta esto con...?") and, if it interpolates message content, wraps it via escapeUntrustedText() + <<<...DATA>>>
  </behavior>
  <action>
    Add `CONTEXT_WINDOWS.driftCheck = 3` to bot-context.ts (do not hardcode 3 inline). Create `drift-redirect.ts` exporting `driftRedirectSkill: Skill` with `id: 'drift-redirect'`, `role: 'coach'`. `detect()` checks `blueprint.drift_detection_enabled` FIRST and short-circuits `{ fires: false, confidence: 0 }` if false (avoid wasted ONNX work, D-09); otherwise embeds the concatenated last `CONTEXT_WINDOWS.driftCheck` messages, computes `cosineSimilarity` against `getDomainCentroid(blueprint)`, and fires when similarity < threshold (default 0.6; read from Blueprint config if present, TRIGGER-03). `confidence` scales with how far below threshold. `buildPromptGuidance()` returns Spanish invitation-framed redirect guidance; any interpolated message text MUST use escapeUntrustedText() + <<<...DATA>>> (WR-06, Dimension 7). Fail-silent: on any embed error, `console.error('[drift-redirect] ...')` and return `{ fires: false, confidence: 0 }`. In drift-redirect.test.ts mock embeddings.ts: assert opt-out short-circuits with zero embed calls; a below-threshold window fires; a single tangential message does not; buildPromptGuidance output contains the WR-06 delimiter when given adversarial input.
  </action>
  <verify>
    <automated>pnpm --filter api test -- drift-redirect</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "drift_detection_enabled" apps/api/src/lib/skills/drift-redirect.ts` checked BEFORE any embed() call
    - `grep -q "driftCheck" apps/api/src/lib/bot-context.ts` (constant added, 3)
    - drift-redirect.ts uses getDomainCentroid + cosineSimilarity from embeddings.ts (grep confirms)
    - buildPromptGuidance interpolating message content produces the `<<<` delimiter given input containing "IGNORE PREVIOUS INSTRUCTIONS"
    - `pnpm --filter api test -- drift-redirect` passes: opt-out zero-embed, below-threshold fires, single-message no-fire, WR-06 escaping
  </acceptance_criteria>
  <done>drift-redirect fires only on sustained sub-threshold drift, respects the opt-out, frames as invitation, WR-06-escapes interpolated content; tests green.</done>
</task>

<task type="tdd">
  <name>Task 3: moderation Skill (Spanish heuristic pre-filter + escalation tone, D-11/D-16)</name>
  <files>apps/api/src/lib/skills/moderation.ts, apps/api/src/lib/skills/moderation.test.ts</files>
  <read_first>
    - apps/api/src/lib/moderation-count.ts (getModerationCount / incrementModerationCount — Plan 02)
    - apps/api/src/lib/silence-gate.ts (detect() function-signature shape to mirror)
    - apps/api/src/lib/bot-context.ts (escapeUntrustedText + <<<...DATA>>> WR-06 framing)
    - .planning/phases/12-graph-coherence-extended-triggers/12-CONTEXT.md (D-11 Spanish keyword list + structural signals; D-16 escalation-after-N; Claude's Discretion on exact list/threshold)
    - .planning/phases/12-graph-coherence-extended-triggers/12-AI-SPEC.md (Dimension 2 rubric — first-offense gentle, escalate only after N; Pitfall 6 regional-slang false positives)
  </read_first>
  <behavior>
    - checkModerationHeuristic(text) flags genuinely rude/insulting content (curated Spanish list) and structural signals (ALL-CAPS ratio, excessive/repeated !/?) with ZERO adapter/LLM calls
    - checkModerationHeuristic does NOT flag an innocent emphatic-but-clean message
    - detect() returns { fires: true } only when the heuristic flags; sets meta with the escalation tier derived from moderation_count
    - buildPromptGuidance() returns a GENTLE non-accusatory redirect when moderation_count === 0; a more direct (still neutral) redirect after N occurrences (D-16); any echoed flagged content is WR-06-escaped
  </behavior>
  <action>
    Create `moderation.ts` exporting a pure `checkModerationHeuristic(messageContent: string): { flagged: boolean; signals: string[] }` (curated Spanish insult/profanity list + ALL-CAPS ratio + repeated-punctuation signals, D-11 — zero I/O, zero LLM) and a `moderationSkill: Skill` with `id: 'moderation'`, `role: 'coach'`. `detect()` runs `checkModerationHeuristic` on the last message; if not flagged returns `{ fires: false, confidence: 0 }` with no DB call; if flagged, reads `getModerationCount` for the participant/branch and returns `{ fires: true, confidence, meta: { escalationTier, participantId } }`. `buildPromptGuidance()` is a DETERMINISTIC lookup keyed by escalation tier (tier 0 = gentle, structurally guaranteed on first offense per the D-16 tone lock) — not an LLM-decided tone; any echoed flagged text uses escapeUntrustedText() + <<<...DATA>>>. Choose a concrete N threshold (Claude's Discretion — document it in the SUMMARY). In moderation.test.ts (mock moderation-count): assert the heuristic makes zero adapter calls; a rude message flags; a clean emphatic message does not; first-offense (count 0) guidance is the gentle tier; count ≥ N guidance is the direct tier; adversarial echoed content is WR-06-escaped.
  </action>
  <verify>
    <automated>pnpm --filter api test -- moderation</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "checkModerationHeuristic" apps/api/src/lib/skills/moderation.ts` and it is a pure function (no adapter/supabase import used inside it)
    - moderation.test.ts asserts zero adapter/LLM invocations during heuristic detection (COST-02 tier-1 $0)
    - first-offense (moderation_count 0) buildPromptGuidance returns the gentle tier; count ≥ N returns the direct tier (deterministic, not LLM-decided)
    - echoed flagged content produces the `<<<` WR-06 delimiter
    - `pnpm --filter api test -- moderation` passes all cases
  </acceptance_criteria>
  <done>moderation fires from a zero-cost heuristic with deterministic count-keyed de-escalating tone; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Participant chat message → Skill.detect() | Untrusted user content evaluated by heuristics / embedded locally |
| Participant content → buildPromptGuidance() → system prompt | Untrusted text spliced into an LLM system prompt (prompt-injection surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering (prompt injection) | drift-redirect/moderation buildPromptGuidance | mitigate | WR-06 escapeUntrustedText() + <<<...DATA>>> framing for any interpolated message content — unit-tested against "IGNORE PREVIOUS INSTRUCTIONS" input (Dimension 7, Critical) |
| T-12-07 | (Interpersonal harm) | moderation false positive | mitigate | D-16 deterministic first-offense-gentle tone lock keyed by moderation_count bounds blast radius; curated Spanish list documented for native-speaker review (Pitfall 6) |
| T-12-08 | Denial of Service | drift-redirect ONNX cost | mitigate | drift_detection_enabled opt-out checked first; cached centroid; fail-silent on embed error |
| T-12-SC | Tampering | package installs | accept | No installs in this plan; @huggingface/transformers audited/installed in Plan 02 |
</threat_model>

<verification>
- `pnpm --filter api test -- silence-break drift-redirect moderation` green
- Each Skill's detect() makes zero LLM/adapter calls (COST-02 tier-1 $0) — asserted in tests
- WR-06 escaping asserted for every guidance that echoes content
</verification>

<success_criteria>
Three Coach Skills implemented and tested: silence-break retrofit (delivery unchanged), drift-redirect (opt-out-aware sustained-window cosine), moderation (zero-cost heuristic + de-escalating tone). All detection is $0.
</success_criteria>

<output>
Create `.planning/phases/12-graph-coherence-extended-triggers/12-03-SUMMARY.md` when done
</output>
